---
phase: 19-general-product-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/productFeedback/types.ts
  - src/productFeedback/schema.ts
  - src/productFeedback/categories.ts
  - src/productFeedback/payload.ts
  - src/productFeedback/submit.ts
  - __tests__/productFeedback-schema.test.ts
  - __tests__/productFeedback-payload.test.ts
  - __tests__/productFeedback-submit.test.ts
autonomous: true
requirements: [PFDBK-03, PFDBK-04, PFDBK-05, TEST-07]

must_haves:
  truths:
    - "The product feedback payload schema accepts a valid {category, message, screen, appVersion, platform} object and rejects invalid enum literals, empty/over-2000 message, and empty/over-20 appVersion"
    - "The payload builder returns exactly the 5 allowed fields and never quiz-answer fields (verb/tense/subject/correctAnswer/selectedAnswer)"
    - "The submit function returns success on 201, validation-error on 400, server-error on any other status, network-error on rejection, and uses a 90s AbortController timeout"
    - "All three new __tests__/productFeedback-*.test.ts suites pass"
    - "src/productFeedback/ is a zero-shared-code structural mirror of src/feedback/ — no shared AbortController/timeout helper is imported from ../feedback (D-07)"
  artifacts:
    - path: "src/productFeedback/types.ts"
      provides: "ProductFeedbackCategory, ProductFeedbackScreen, SCREENS const, ProductFeedbackPayload (z.infer), SubmitResult union"
      contains: "ProductFeedbackPayload"
    - path: "src/productFeedback/schema.ts"
      provides: "productFeedbackPayloadSchema (Zod) with min/max length constraints"
      contains: "productFeedbackPayloadSchema"
    - path: "src/productFeedback/categories.ts"
      provides: "categoryLabels record + CATEGORY_OPTIONS derived array"
      contains: "CATEGORY_OPTIONS"
    - path: "src/productFeedback/payload.ts"
      provides: "buildProductFeedbackPayload() mapping 5 fields, message trimmed"
      exports: ["buildProductFeedbackPayload"]
    - path: "src/productFeedback/submit.ts"
      provides: "submitProductFeedback() POST to /product-feedback with result union"
      exports: ["submitProductFeedback"]
  key_links:
    - from: "src/productFeedback/types.ts"
      to: "src/productFeedback/schema.ts"
      via: "z.infer<typeof productFeedbackPayloadSchema>"
      pattern: "z\\.infer"
    - from: "src/productFeedback/submit.ts"
      to: "https://portuguese-verb-api.onrender.com/product-feedback"
      via: "fetch POST"
      pattern: "product-feedback"
---

<objective>
Build the `src/productFeedback/` domain module — a zero-shared-code structural mirror of `src/feedback/` (D-07) — plus its three Jest unit-test suites. This is the pure-TypeScript, framework-free half of Phase 19: the Zod schema, category label map, payload builder, and submit function, each with full test coverage mirroring the existing `feedback-*.test.ts` suites (TEST-07).

Purpose: Establishes the runtime contract for `POST /product-feedback` (PFDBK-03), its 90s-timeout result-union transport (PFDBK-04), and the structural guarantee that no quiz-answer context is ever sent (PFDBK-05) — all provable by automated tests before any UI is built.
Output: 5 source files under `src/productFeedback/` and 3 test files under `__tests__/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-general-product-feedback/19-CONTEXT.md
@.planning/phases/19-general-product-feedback/19-PATTERNS.md
@.planning/phases/19-general-product-feedback/19-RESEARCH.md
@.planning/phases/19-general-product-feedback/19-VALIDATION.md

<interfaces>
<!-- Exact backend contract for POST /product-feedback (REQUIREMENTS.md PFDBK-03) -->
<!-- category: "bug" | "idea" | "other" -->
<!-- message: string, 1-2000 chars -->
<!-- screen: "setup" | "quiz" | "results" -->
<!-- appVersion: string, 1-20 chars -->
<!-- platform: "ios" | "android" -->
<!-- Success = HTTP 201 (returns persisted row); 400 = validation error; 500/other = server error -->

<!-- SubmitResult union to copy verbatim from src/feedback/types.ts:
  | { status: "success"; data: unknown }
  | { status: "validation-error" }
  | { status: "server-error" }
  | { status: "network-error" }
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contracts, schema, and categories + schema tests</name>
  <files>src/productFeedback/types.ts, src/productFeedback/schema.ts, src/productFeedback/categories.ts, __tests__/productFeedback-schema.test.ts</files>
  <read_first>
    - src/feedback/types.ts (analog — SubmitResult union to copy verbatim, z.infer pattern)
    - src/feedback/schema.ts (analog — z.object/z.enum shape, TENSES/SUBJECTS-as-source-of-truth pattern)
    - src/feedback/reasons.ts (analog for categories.ts — Record + derived {value,label}[] array)
    - src/dataset/types.ts (the TENSES/SUBJECTS `as const` array-as-source-of-truth pattern to replicate for SCREENS)
    - __tests__/feedback-schema.test.ts (analog test structure — validPayload helper, enum matrix, invalid-literal + empty-string rejection loops, label-ordering describe block)
    - .planning/phases/19-general-product-feedback/19-PATTERNS.md (exact adaptation code excerpts for all three source files)
  </read_first>
  <behavior>
    - productFeedbackPayloadSchema.safeParse({category:"bug", message:"x", screen:"setup", appVersion:"1.0.0", platform:"ios"}).success === true
    - Valid across full matrix: every category in [bug,idea,other] x every screen in [setup,quiz,results] x every platform in [ios,android] parses successfully
    - Rejects category:"invalid", screen:"invalid", platform:"invalid"
    - Rejects message:"" (empty) and a 2001-char message; accepts a 2000-char message
    - Rejects appVersion:"" (empty) and a 21-char appVersion; accepts a 20-char appVersion
    - CATEGORY_OPTIONS is [{value:"bug",label:"Bug"},{value:"idea",label:"Idea"},{value:"other",label:"Other"}] in that order
  </behavior>
  <action>
    Create `src/productFeedback/types.ts`: declare `SCREENS = ["setup","quiz","results"] as const`, derive `ProductFeedbackScreen = (typeof SCREENS)[number]`, declare `ProductFeedbackCategory = "bug" | "idea" | "other"`, import `productFeedbackPayloadSchema` from `./schema` and export `ProductFeedbackPayload = z.infer<typeof productFeedbackPayloadSchema>`, and copy the 4-state `SubmitResult` union verbatim from `src/feedback/types.ts`. Use `import type { z } from "zod"` per the analog.

    Create `src/productFeedback/schema.ts`: `productFeedbackPayloadSchema = z.object({ category: z.enum(["bug","idea","other"]), message: z.string().min(1).max(2000), screen: z.enum(SCREENS as unknown as [ProductFeedbackScreen, ...ProductFeedbackScreen[]]), appVersion: z.string().min(1).max(20), platform: z.enum(["ios","android"]) })`. Import `SCREENS` and `type ProductFeedbackScreen` from `./types`. Note the divergence from the analog: `appVersion` here is `.min(1).max(20)` (not unbounded) and `message` is `.min(1).max(2000)`.

    Create `src/productFeedback/categories.ts`: `categoryLabels: Record<ProductFeedbackCategory, string> = { bug: "Bug", idea: "Idea", other: "Other" }` and `CATEGORY_OPTIONS = (["bug","idea","other"] as const).map((value) => ({ value, label: categoryLabels[value] }))`, mirroring `reasons.ts` shape exactly.

    Create `__tests__/productFeedback-schema.test.ts` mirroring `feedback-schema.test.ts`: a `validPayload(overrides)` helper, a nested matrix loop over category/screen/platform asserting `.success`, invalid-literal rejection cases for each enum, empty-string rejection for message/appVersion, PLUS max-length cases not present in the analog — a 2001-char message rejects, a 2000-char message passes, a 21-char appVersion rejects, a 20-char appVersion passes. Also mirror the label-ordering describe block asserting CATEGORY_OPTIONS order and categoryLabels values.

    Named exports only, no default exports, no barrel file (CONVENTIONS.md).
  </action>
  <verify>
    <automated>npx jest __tests__/productFeedback-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/productFeedback-schema.test.ts` passes
    - `npm run typecheck` reports no errors for the three new source files
    - `grep -q "min(1).max(2000)" src/productFeedback/schema.ts` and `grep -q "min(1).max(20)" src/productFeedback/schema.ts` both succeed
    - `grep -q "SCREENS = \[" src/productFeedback/types.ts` succeeds (array-as-source-of-truth)
    - src/productFeedback/schema.ts contains no `verb`/`tense`/`subject`/`correctAnswer`/`selectedAnswer` field
  </acceptance_criteria>
  <done>Schema, types, and categories exist as a mirror with correct length bounds; schema test suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Payload builder + payload tests</name>
  <files>src/productFeedback/payload.ts, __tests__/productFeedback-payload.test.ts</files>
  <read_first>
    - src/feedback/payload.ts (analog — params object shape, trim behavior; NOTE the reason-label composition is NOT replicated here)
    - __tests__/feedback-payload.test.ts (analog test structure — baseParams object, pass-through assertions, schema round-trip)
    - src/productFeedback/types.ts (created in Task 1 — ProductFeedbackCategory, ProductFeedbackScreen, ProductFeedbackPayload)
    - src/productFeedback/schema.ts (created in Task 1 — for the round-trip assertion)
    - .planning/phases/19-general-product-feedback/19-PATTERNS.md (payload divergence: message used verbatim/trimmed, no label prefix)
  </read_first>
  <behavior>
    - buildProductFeedbackPayload({category:"bug", message:"  hi  ", screen:"quiz", appVersion:"1.0.0", platform:"ios"}) returns {category:"bug", message:"hi", screen:"quiz", appVersion:"1.0.0", platform:"ios"}
    - message is trimmed, NOT prefixed with any category label
    - Object.keys(result).sort() === ["appVersion","category","message","platform","screen"] — never verb/tense/subject/correctAnswer/selectedAnswer (PFDBK-05)
    - The returned object passes productFeedbackPayloadSchema.safeParse (round-trip)
  </behavior>
  <action>
    Create `src/productFeedback/payload.ts` exporting `buildProductFeedbackPayload(params: { category: ProductFeedbackCategory; message: string; screen: ProductFeedbackScreen; appVersion: string; platform: "ios" | "android" }): ProductFeedbackPayload`. Return exactly `{ category, message: params.message.trim(), screen, appVersion, platform }`. Do NOT replicate the analog's `"<label>: <freeText>"` message composition — `category` is its own field and `message` is used verbatim (only trimmed). Import types from `./types`.

    Create `__tests__/productFeedback-payload.test.ts` mirroring `feedback-payload.test.ts` structure (a `baseParams` object, `describe("buildProductFeedbackPayload", ...)`), but OMIT the reason-label-composition tests. Include: a trim test, a pass-through test for all 5 fields, a schema round-trip test using `productFeedbackPayloadSchema`, and an explicit PFDBK-05 assertion `expect(Object.keys(payload).sort()).toEqual(["appVersion","category","message","platform","screen"])` proving no quiz-answer field is ever present.

    Named exports only.
  </action>
  <verify>
    <automated>npx jest __tests__/productFeedback-payload.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/productFeedback-payload.test.ts` passes
    - The payload test file contains the literal assertion `["appVersion", "category", "message", "platform", "screen"]`
    - `grep -qE "verb|selectedAnswer|correctAnswer" src/productFeedback/payload.ts` returns nothing (no quiz-answer fields)
    - `npm run typecheck` clean
  </acceptance_criteria>
  <done>Payload builder maps exactly 5 fields with verbatim trimmed message; PFDBK-05 asserted by test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Submit transport + submit tests</name>
  <files>src/productFeedback/submit.ts, __tests__/productFeedback-submit.test.ts</files>
  <read_first>
    - src/feedback/submit.ts (analog — copy structurally verbatim; only endpoint URL + type imports differ)
    - __tests__/feedback-submit.test.ts (analog test — 6 cases: 201/400/500/other-non-201/network-error/90s-timeout; fetch mock + fake timers pattern, afterEach cleanup)
    - src/productFeedback/types.ts (created in Task 1 — ProductFeedbackPayload, SubmitResult)
    - .planning/phases/19-general-product-feedback/19-PATTERNS.md (submit.ts adaptation + D-07 no-shared-helper note)
  </read_first>
  <behavior>
    - fetch resolves 201 -> { status: "success", data } (data from response.json())
    - fetch resolves 400 -> { status: "validation-error" }
    - fetch resolves 500 -> { status: "server-error" }
    - fetch resolves any other non-201 (e.g. 418) -> { status: "server-error" }
    - fetch rejects (network) -> { status: "network-error" }
    - After 90_000ms with no response, controller.abort() fires and result is { status: "network-error" }
    - fetch is called with the /product-feedback endpoint, method POST, Content-Type application/json, JSON.stringify(payload) body, and controller.signal
  </behavior>
  <action>
    Create `src/productFeedback/submit.ts`: copy the structure of `src/feedback/submit.ts` verbatim, renaming the function to `submitProductFeedback`, the constant to `PRODUCT_FEEDBACK_ENDPOINT = "https://portuguese-verb-api.onrender.com/product-feedback"`, keeping `TIMEOUT_MS = 90_000`, and importing `ProductFeedbackPayload`/`SubmitResult` from `./types`. Preserve the exact AbortController + `setTimeout(() => controller.abort(), TIMEOUT_MS)` in try/finally (`clearTimeout` in finally), and the 201/400/else -> success/validation-error/server-error branching with catch -> network-error. Do NOT extract or import a shared timeout helper from `src/feedback/` — intentional duplication per D-07.

    Create `__tests__/productFeedback-submit.test.ts` as a structural mirror of `feedback-submit.test.ts`: same 6 test cases, same `globalThis.fetch` mocking + `jest.useFakeTimers()`/`jest.advanceTimersByTime(90_000)` timeout test, same `afterEach` restoring `originalFetch`, `jest.useRealTimers()`, `jest.clearAllMocks()`. Change only the imported function, the sample payload shape (product feedback fields), and assert the fetch URL ends with `/product-feedback`.

    Named exports only.
  </action>
  <verify>
    <automated>npx jest __tests__/productFeedback-submit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/productFeedback-submit.test.ts` passes with all 6 cases
    - `grep -q "product-feedback" src/productFeedback/submit.ts` succeeds and `grep -q "TIMEOUT_MS = 90_000" src/productFeedback/submit.ts` succeeds
    - `grep -q "from \"../feedback" src/productFeedback/submit.ts` returns nothing (no cross-domain import, D-07)
    - `npm test` full suite green (no regression in existing feedback suites)
  </acceptance_criteria>
  <done>Submit function returns correct result union for 201/400/other/reject with 90s timeout; submit test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile client → backend `POST /product-feedback` | Untrusted user free-text (`message`) and app-supplied metadata cross to the public backend endpoint over HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | `productFeedbackPayloadSchema` (V5 Input Validation) | mitigate | Zod schema enforces category/screen/platform enums, message 1-2000, appVersion 1-20 client-side; backend is authoritative validator returning 400 (mirrors existing `feedback/schema.ts`) |
| T-19-02 | Information Disclosure | `buildProductFeedbackPayload` — quiz-answer leakage | mitigate | Builder returns only the 5 allowed fields; PFDBK-05 asserted by `Object.keys` test — no verb/tense/subject/correctAnswer/selectedAnswer path exists |
| T-19-03 | Denial of Service | unbounded free-text `message` spam to public endpoint | accept | Backend-owned concern (rate limiting is a backend v0.4 responsibility); client only enforces the 1-2000 char contract — identical accepted risk to the shipped `/feedback` endpoint |
| T-19-04 | Information Disclosure | user pastes sensitive text into `message` | accept | Same accepted risk profile as the existing `/feedback` free-text field; no mitigation available or expected at this layer |
| T-19-SC | Tampering | npm installs | n/a | Zero new packages this phase (RESEARCH.md Package Legitimacy Audit: not applicable) — no install task exists |
</threat_model>

<verification>
- `npx jest __tests__/productFeedback-schema.test.ts __tests__/productFeedback-payload.test.ts __tests__/productFeedback-submit.test.ts` all green
- `npm test` full suite green (no regression)
- `npm run typecheck` clean
</verification>

<success_criteria>
- `src/productFeedback/` contains types.ts, schema.ts, categories.ts, payload.ts, submit.ts — all named-export, zero-shared-code mirrors of `src/feedback/`
- Three new test suites cover PFDBK-03 (schema), PFDBK-03/PFDBK-05 (payload), PFDBK-04 (submit) — the TEST-07 coverage matrix
- No quiz-answer field appears anywhere in the domain payload path
</success_criteria>

<output>
Create `.planning/phases/19-general-product-feedback/19-01-SUMMARY.md` when done
</output>
