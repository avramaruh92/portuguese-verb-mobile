---
phase: 19-general-product-feedback
plan: 04
type: execute
wave: 3
depends_on: [19-02]
files_modified:
  - app/quiz.tsx
autonomous: true
requirements: [PFDBK-01, PFDBK-02]

must_haves:
  truths:
    - "The Quiz screen renders 'Report a problem' and 'Help us improve' as two equal-weight, half-width side-by-side buttons in one always-mounted row (D-03)"
    - "'Help us improve' is visible and interactive from question-load (ungated); 'Report a problem' remains gated on lockedChoice via opacity + pointerEvents (D-04)"
    - "'Help us improve' opens ProductFeedbackModal with screen='quiz', reusing the appVersion/platform already computed on this screen, carrying no quiz-answer context"
    - "The existing ReportFeedbackModal wiring is unchanged"
  artifacts:
    - path: "app/quiz.tsx"
      provides: "Two-action feedback row + ProductFeedbackModal wiring (D-03/D-04)"
      contains: "ProductFeedbackModal"
  key_links:
    - from: "app/quiz.tsx"
      to: "src/productFeedback/ProductFeedbackModal.tsx"
      via: "import + render with screen=\"quiz\""
      pattern: "screen=\"quiz\""
    - from: "app/quiz.tsx feedbackRow"
      to: "both trigger Pressables"
      via: "flexDirection row + flex:1 half-width buttons"
      pattern: "feedbackRow"
---

<objective>
Restructure the bottom feedback section of `app/quiz.tsx` into a two-action row (D-03): "Report a problem" and "Help us improve" as equal-weight, half-width, side-by-side buttons in an always-mounted row container. Apply divergent visibility (D-04): "Help us improve" is available from question-load (ungated), "Report a problem" keeps its existing `lockedChoice`-gated opacity/pointer-events behavior. Wire a new `<ProductFeedbackModal screen="quiz" ...>` alongside the untouched `<ReportFeedbackModal>`.

Purpose: Delivers the third PFDBK-01 entry point and the full PFDBK-02 two-action row with the corrected divergent timing (D-04 supersedes ROADMAP's literal "row appears after lock" wording). The Quiz screen already computes appVersion/platform, so they are reused directly.
Output: Modification to `app/quiz.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-general-product-feedback/19-CONTEXT.md
@.planning/phases/19-general-product-feedback/19-PATTERNS.md
@.planning/phases/19-general-product-feedback/19-UI-SPEC.md

<interfaces>
<!-- ProductFeedbackModal (Plan 02) props:
  { visible; screen: "setup"|"quiz"|"results"; appVersion; platform; onClose } -->

<!-- Current app/quiz.tsx feedback block (lines 161-179): a single full-width reportButton
     gated by [styles.reportButton, lockedChoice === null && styles.reportButtonHidden] +
     pointerEvents, followed by <ReportFeedbackModal .../>.
     appVersion/platform already computed at lines 56-57.
     reportButton style (lines 272-277): { minHeight:44, justifyContent:"center",
     alignItems:"center", marginTop: spacing.md } — has NO flex property today. -->

<!-- D-04 divergent visibility:
     - "Help us improve": ungated (no lockedChoice check)
     - "Report a problem": keep lockedChoice===null -> reportButtonHidden(opacity 0) + pointerEvents "none"
     - Row container always mounted so width is stable (no reflow when Report fades in) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-action feedback row + ProductFeedbackModal on Quiz (D-03/D-04)</name>
  <files>app/quiz.tsx</files>
  <read_first>
    - app/quiz.tsx (file being modified — imports lines 1-12, reportVisible state line 27, appVersion/platform lines 56-57, report button JSX lines 161-179, StyleSheet reportButton/reportButtonHidden/reportButtonText lines 272-284)
    - src/productFeedback/ProductFeedbackModal.tsx (Plan 02 — component + prop contract)
    - src/theme/tokens.ts (spacing.md for the row gap/margin)
    - .planning/phases/19-general-product-feedback/19-PATTERNS.md (app/quiz.tsx section — target two-button-row pattern, Pitfall 2 flex:1 requirement)
    - .planning/phases/19-general-product-feedback/19-UI-SPEC.md (Quiz two-button row contract — equal weight, half-width, divergent visibility)
  </read_first>
  <action>
    Modify `app/quiz.tsx`:
    1. Add `import { ProductFeedbackModal } from "../src/productFeedback/ProductFeedbackModal";` alongside the existing `ReportFeedbackModal` import (line 8).
    2. Add `const [productFeedbackVisible, setProductFeedbackVisible] = useState(false);` next to the existing `reportVisible` state (line 27).
    3. Wrap the two trigger buttons in a new always-mounted `<View style={styles.feedbackRow}>` container. Keep the existing "Report a problem" `Pressable` unchanged in behavior — retain `style={[styles.reportButton, lockedChoice === null && styles.reportButtonHidden]}` and `pointerEvents={lockedChoice === null ? "none" : "auto"}` (D-04: Report stays gated). Add a second "Help us improve" `Pressable` with `style={styles.reportButton}` and NO `reportButtonHidden`/`pointerEvents` gating (D-04: ungated from question-load), `onPress` setting `productFeedbackVisible` true, text "Help us improve" in `styles.reportButtonText`.
    4. Add `flex: 1` to both buttons so they split the row into half-widths (Pitfall 2 — `reportButton` currently has no `flex`). Add a new `feedbackRow: { flexDirection: "row", gap: spacing.md, marginTop: spacing.md }` style and REMOVE `marginTop: spacing.md` from `reportButton` (the row now owns that vertical spacing). Add `flex: 1` either to `reportButton` directly or as a shared half-width style applied to both.
    5. Leave the existing `<ReportFeedbackModal .../>` wiring untouched. Add `<ProductFeedbackModal visible={productFeedbackVisible} screen="quiz" appVersion={appVersion} platform={platform} onClose={() => setProductFeedbackVisible(false)} />` after it — reuse the existing `appVersion`/`platform` (lines 56-57), do not recompute. Pass literal `screen="quiz"` (no `usePathname()`).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` clean
    - `grep -q 'screen="quiz"' app/quiz.tsx` succeeds
    - `grep -q 'Help us improve' app/quiz.tsx` succeeds and `grep -q 'Report a problem' app/quiz.tsx` still succeeds
    - `grep -q "feedbackRow" app/quiz.tsx` succeeds and `grep -q "flexDirection" app/quiz.tsx` shows the row style
    - `grep -q "flex: 1" app/quiz.tsx` succeeds (half-width split, Pitfall 2)
    - The "Report a problem" Pressable still references `reportButtonHidden` and `pointerEvents` (Report stays gated, D-04); the "Help us improve" Pressable does not gate on `lockedChoice`
    - `grep -q "usePathname" app/quiz.tsx` returns nothing
    - `npm test` full suite green (no regression)
  </acceptance_criteria>
  <done>Quiz shows an always-mounted half-width two-button row: "Help us improve" available from question-load, "Report a problem" gated on lock; "Help us improve" opens ProductFeedbackModal with screen="quiz".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quiz screen → ProductFeedbackModal | Quiz screen HAS quiz-answer state in scope, but must pass none of it to the product-feedback modal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Information Disclosure | ProductFeedbackModal wiring on the Quiz screen | mitigate | Only `screen`/`appVersion`/`platform` are passed; grep asserts no verb/tense/subject/correctAnswer/selectedAnswer prop is forwarded — the quiz-answer fields stay confined to the untouched ReportFeedbackModal (PFDBK-05) |
| T-19-07 | Spoofing | `screen` field integrity | mitigate | Hardcoded literal `"quiz"`, no runtime pathname derivation (D-08) |
| T-19-SC | Tampering | npm installs | n/a | Zero new packages — no install task |
</threat_model>

<verification>
- `npm run typecheck` clean
- `npm test` full suite green
- Divergent-visibility timing + half-width layout verified on-device in Plan 05 checkpoint
</verification>

<success_criteria>
- Quiz screen renders an equal-weight, half-width, always-mounted two-action row (D-03)
- "Help us improve" is ungated; "Report a problem" stays lockedChoice-gated (D-04)
- ProductFeedbackModal receives screen="quiz" and no quiz-answer context (PFDBK-05)
</success_criteria>

<output>
Create `.planning/phases/19-general-product-feedback/19-04-SUMMARY.md` when done
</output>
