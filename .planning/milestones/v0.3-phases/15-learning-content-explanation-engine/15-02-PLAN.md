---
phase: 15-learning-content-explanation-engine
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/dataset/validate.ts
  - src/dataset/remote.ts
  - src/dataset/source.ts
  - src/components/OfflinePill.tsx
  - src/store/useQuizStore.ts
  - __tests__/dataset-remote.test.ts
  - __tests__/dataset-source.test.ts
  - __tests__/useQuizStore.test.ts
autonomous: true
requirements: [EXPL-01]
must_haves:
  truths:
    - "fetchRemoteVerbs returns { verbs, learning } where learning is parsed from payload.learning via safeParse"
    - "A malformed or absent learning block degrades to learning: undefined without failing the verbs fetch or flipping source to local"
    - "resolveVerbs() and its cached snapshot carry learning alongside verbs and source (single fetch, single cache)"
    - "OfflinePill and useQuizStore still function under the widened snapshot shape"
  artifacts:
    - path: "src/dataset/remote.ts"
      provides: "fetchRemoteVerbs returning { verbs, learning }"
      contains: "LearningContentSchema.safeParse"
    - path: "src/dataset/source.ts"
      provides: "resolveVerbs snapshot with learning field"
      contains: "learning"
    - path: "src/dataset/validate.ts"
      provides: "VerbSchema with optional formIndex"
      contains: "formIndex"
  key_links:
    - from: "src/dataset/remote.ts"
      to: "src/learning/schema.ts"
      via: "LearningContentSchema.safeParse(payload.learning)"
      pattern: "LearningContentSchema\\.safeParse"
    - from: "src/dataset/source.ts"
      to: "src/dataset/remote.ts"
      via: "destructures { verbs, learning } from fetchRemoteVerbs"
      pattern: "learning"
---

<objective>
Thread the backend's optional `learning` block and per-verb `formIndex` through
the existing fetch/snapshot pipeline: validate them with the schemas from Plan
01, change `fetchRemoteVerbs`'s return shape from `Verb[]` to `{ verbs,
learning }`, extend the `resolveVerbs()` per-session snapshot to carry
`learning`, and update every caller and test in the same atomic change.

Purpose: EXPL-01 — parse `learning`/`formIndex`, Zod-validated, without breaking
payloads that omit `learning` (backend fail-closed omission) and without letting
a malformed `learning` block fail the otherwise-valid `verbs` fetch.
Output: updated `src/dataset/{validate,remote,source}.ts`, audited/updated
consumers (`OfflinePill`, `useQuizStore`), and updated `dataset-remote` /
`dataset-source` / `useQuizStore` tests — full suite green.

NOTE: The `fetchRemoteVerbs` return-shape change is BREAKING, not additive.
RESEARCH.md and PATTERNS.md require it to land as one atomic unit with all its
callers/tests — never split across waves or left in an intermediate broken
state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-learning-content-explanation-engine/15-CONTEXT.md
@.planning/phases/15-learning-content-explanation-engine/15-RESEARCH.md
@.planning/phases/15-learning-content-explanation-engine/15-PATTERNS.md
@.planning/phases/15-learning-content-explanation-engine/15-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by Plan 01, consumed here. -->

From src/learning/schema.ts:
  export const LearningContentSchema: ZodType<LearningContent>;
  export const FormMatchSchema: ZodType<FormMatch>;

From src/learning/types.ts:
  export interface LearningContent { version: 1; templates: ...; verbs: ... }
  export type FormMatch = { tense: Tense; subject: Subject };

Current shapes being changed (from this codebase):
  // src/dataset/remote.ts — CURRENT (breaking change target)
  export async function fetchRemoteVerbs(): Promise<Verb[]>
  // src/dataset/source.ts — CURRENT
  let cachedResult: Promise<{ verbs: Verb[]; source: VerbSource }> | null;
  export function resolveVerbs(): Promise<{ verbs: Verb[]; source: VerbSource }>;

Consumers that destructure the snapshot (audit for breakage):
  // src/components/OfflinePill.tsx line ~19: resolveVerbs().then((result) => ...)
  // src/store/useQuizStore.ts line ~47: const { verbs } = await resolveVerbs();
  // __tests__/useQuizStore.test.ts: mockedResolveVerbs.mockResolvedValue({ verbs, source }) — must add learning
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend VerbSchema + fetchRemoteVerbs return shape + resolveVerbs snapshot</name>
  <files>src/dataset/validate.ts, src/dataset/remote.ts, src/dataset/source.ts</files>
  <read_first>
    - src/dataset/validate.ts (VerbSchema and validateDataset current shape)
    - src/dataset/remote.ts (current fetch/timeout/verbs-validation flow, AbortController try/finally)
    - src/dataset/source.ts (current resolve()/cachedResult/prefetch/resolveVerbs)
    - src/learning/schema.ts (LearningContentSchema to call)
    - src/learning/types.ts (LearningContent, FormMatch types)
    - .planning/phases/15-learning-content-explanation-engine/15-RESEARCH.md (Code Examples + Pitfall 3)
    - .planning/phases/15-learning-content-explanation-engine/15-PATTERNS.md (remote.ts / source.ts modify sections)
  </read_first>
  <action>
    In `src/dataset/validate.ts`: add one optional field to `VerbSchema` —
    `formIndex: z.record(z.string(), z.array(FormMatchSchema)).optional()` — importing
    `FormMatchSchema` from `../learning/schema`. Leave `validateDataset` and every other
    field unchanged.

    In `src/dataset/remote.ts`: change `fetchRemoteVerbs`'s return type from
    `Promise<Verb[]>` to `Promise<{ verbs: Verb[]; learning: LearningContent | undefined }>`
    (import `LearningContentSchema` from `../learning/schema`, `LearningContent` from
    `../learning/types`). Keep the existing AbortController/timeout try/finally, the
    `response.ok` throw, the `payload.verbs` array-check throw, and the `validateDataset`
    throw EXACTLY as-is — `verbs` validation failure must still throw. After the existing
    `verbs` validation, add `const learning = LearningContentSchema.safeParse(payload.learning).success ? <parsed data> : undefined;`
    and return `{ verbs: payload.verbs as Verb[], learning }`. CRITICAL (Pitfall 3): a
    `learning` safeParse failure or an absent `payload.learning` must degrade to
    `learning: undefined` — it must NOT throw and must NOT cause the verbs fetch to fall
    back to local.

    In `src/dataset/source.ts`: widen `cachedResult` and `resolveVerbs`'s resolved type
    to `{ verbs: Verb[]; source: VerbSource; learning: LearningContent | undefined }`
    (import `LearningContent` from `../learning/types`). In `resolve()`, destructure
    `const { verbs, learning } = await fetchRemoteVerbs();` and return `{ verbs, source:
    "remote" as const, learning }`; in the catch branch return `{ verbs: localVerbs,
    source: "local" as const, learning: undefined }` (local fallback never carries
    learning, per D-05). Do NOT add a second cache or module-level variable for
    `learning` — extend the single existing `cachedResult` (Pitfall 2). `prefetch()`
    needs no change beyond the type flowing through.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/dataset/validate.ts` `VerbSchema` contains `formIndex` as `z.record(z.string(), z.array(FormMatchSchema)).optional()` (source assertion)
    - `fetchRemoteVerbs` return type is `Promise<{ verbs: Verb[]; learning: LearningContent | undefined }>` and body calls `LearningContentSchema.safeParse(payload.learning)` (source assertion)
    - The `verbs` validation still throws on invalid shape; `learning` parsing never throws (source assertion: no throw in the learning branch)
    - `src/dataset/source.ts` `cachedResult`/`resolveVerbs` resolved type includes `learning`, and the local-fallback branch sets `learning: undefined` (source assertion)
    - Exactly one module-level cache (`cachedResult`) — no second cache variable added (source assertion)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Fetch validates and returns learning; snapshot carries it; malformed/absent learning degrades silently; typecheck clean (tests updated in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Update consumers and tests to the widened snapshot shape</name>
  <files>src/components/OfflinePill.tsx, src/store/useQuizStore.ts, __tests__/dataset-remote.test.ts, __tests__/dataset-source.test.ts, __tests__/useQuizStore.test.ts</files>
  <read_first>
    - src/components/OfflinePill.tsx (how it consumes resolveVerbs())
    - src/store/useQuizStore.ts (the `const { verbs } = await resolveVerbs()` destructure)
    - __tests__/dataset-remote.test.ts (existing fetch-mock assertions, esp. `expect(result).toEqual([sampleVerb])`)
    - __tests__/dataset-source.test.ts (existing `fetchRemoteVerbs.mockResolvedValue([...])` and `expect(result).toEqual({ verbs, source })` assertions)
    - __tests__/useQuizStore.test.ts (existing `mockedResolveVerbs.mockResolvedValue({ verbs, source })` mocks — lines ~66, 115, 215, 222, 228, 232)
    - .planning/phases/15-learning-content-explanation-engine/15-PATTERNS.md (dataset-remote / dataset-source / State-of-the-Art audit notes)
  </read_first>
  <action>
    Audit and update every consumer of the widened snapshot so the whole suite compiles
    and passes:

    `src/components/OfflinePill.tsx` and `src/store/useQuizStore.ts`: both destructure
    named fields (`source` / `verbs`) from `resolveVerbs()`, so their runtime logic is
    unaffected by the extra `learning` key. Confirm they still compile; make no
    behavioral change unless the widened type surfaces a real TypeScript error.

    `__tests__/dataset-remote.test.ts`: update the existing well-shaped-200 assertion
    from `expect(result).toEqual([sampleVerb])` to `expect(result).toEqual({ verbs:
    [sampleVerb], learning: undefined })`, and update any other assertion on
    `fetchRemoteVerbs()`'s resolved value to the new object shape. Add three new `it`
    cases: (a) `payload.learning` present-and-valid resolves `{ verbs, learning: <parsed
    LearningContent> }`; (b) `payload.learning` present-but-malformed resolves `{ verbs,
    learning: undefined }` with verbs unaffected and no throw; (c) `payload.learning`
    absent resolves `{ verbs, learning: undefined }`. Reuse a minimal valid
    LearningContent fixture (version 1, five template strings, one verb entry).

    `__tests__/dataset-source.test.ts`: change every
    `fetchRemoteVerbs.mockResolvedValue([sampleRemoteVerb])` to
    `fetchRemoteVerbs.mockResolvedValue({ verbs: [sampleRemoteVerb], learning: undefined })`,
    and every `expect(result).toEqual({ verbs, source })` to include `learning`. Add one
    new test asserting a non-undefined `learning` value flows through `resolveVerbs()`
    when `fetchRemoteVerbs` resolves one.

    `__tests__/useQuizStore.test.ts`: add `learning: undefined` to every
    `mockedResolveVerbs.mockResolvedValue({ verbs, source })` call so the mock matches the
    widened return type (these tests do not assert on `learning`, but must satisfy the
    type).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` full suite passes (0 failures)
    - `npx tsc --noEmit` exits 0
    - `__tests__/dataset-remote.test.ts` has cases for learning present-valid / present-invalid / absent, each asserting verbs unaffected (source assertion)
    - No `fetchRemoteVerbs.mockResolvedValue([` bare-array mock remains in `__tests__/dataset-source.test.ts` (CLI: `grep -c "mockResolvedValue(\[" __tests__/dataset-source.test.ts` returns 0)
    - Every `mockedResolveVerbs.mockResolvedValue(` call in `__tests__/useQuizStore.test.ts` includes `learning` (source assertion)
  </acceptance_criteria>
  <done>All callers and tests use the { verbs, source, learning } shape; entire suite green; no intermediate broken state left.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend API -> mobile client | `GET /content/verbs` `learning`/`formIndex` fields are untrusted network input parsed here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `payload.learning` / per-verb `formIndex` | mitigate | Parse via `LearningContentSchema.safeParse` / `FormMatchSchema` (through `VerbSchema`) — never `.parse`; a malformed `learning` degrades to `undefined` and never crashes or corrupts the `verbs` snapshot |
| T-15-03 | Denial of Service | malformed `learning` block flipping user to offline fallback | mitigate | `learning` validation failure is isolated from `verbs` validation — it must NOT throw or flip `source` to `"local"` (Pitfall 3), so a bad optional block never falsely triggers the offline pill |
</threat_model>

<verification>
- `npm test` full suite green.
- `npx tsc --noEmit` clean.
- `grep -n "LearningContentSchema.safeParse" src/dataset/remote.ts` present.
- `grep -n "learning" src/dataset/source.ts` shows snapshot field on both remote and local branches.
</verification>

<success_criteria>
- `fetchRemoteVerbs` returns `{ verbs, learning }`; `resolveVerbs()` snapshot carries `learning`.
- Malformed/absent `learning` → `learning: undefined`, never a throw, never a false offline fallback.
- All consumers and tests updated in one atomic change; suite green.
</success_criteria>

<output>
Create `.planning/phases/15-learning-content-explanation-engine/15-02-SUMMARY.md` when done.
</output>
