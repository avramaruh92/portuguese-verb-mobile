---
phase: 01-scaffold
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Running `npx expo start` boots the app on the iOS Simulator to the empty root screen with no red-box or console errors (ROADMAP Phase 1 SC-1, visual confirmation)"
  artifacts: []
  key_links:
    - from: "npx expo start"
      to: "iOS Simulator"
      via: "Metro dev server + placeholder screen render"
      pattern: "Portuguese Verb Quiz"
---

<objective>
Provide human visual confirmation that the scaffolded app boots on the iOS Simulator to the empty placeholder screen with no errors — the one Phase 1 success criterion (SC-1) that cannot be mechanically verified in this environment.

Purpose: `npx expo start` opens a long-running interactive Metro dev server with no exit code and no built-in "render succeeded with zero errors" signal (01-RESEARCH.md Pitfall 3). The bundling proxy (`npx expo export --platform ios`) in Plan 01 proves the app *bundles*, but not that it *renders correctly*. This checkpoint closes that gap with a human glance.

Output: Human sign-off confirming the empty root screen renders. No files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold/01-RESEARCH.md
@.planning/phases/01-scaffold/01-VALIDATION.md
@.planning/phases/01-scaffold/01-UI-SPEC.md
@.planning/phases/01-scaffold/01-01-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm iOS Simulator boots to the empty root screen</name>
  <read_first>
    - .planning/phases/01-scaffold/01-RESEARCH.md (Pitfall 3 — expo start is not scriptable; Open Question 2 — simctl available: iPhone 17 Pro / iOS 26.2; Environment Availability table)
    - .planning/phases/01-scaffold/01-VALIDATION.md (Manual-Only Verifications table — SC-1 test instructions)
    - .planning/phases/01-scaffold/01-UI-SPEC.md (expected placeholder: centered "Portuguese Verb Quiz", #000 on #FFFFFF)
  </read_first>
  <what-built>
    Plan 01 scaffolded a booting Expo Router app: `app/_layout.tsx` (root Stack) + `app/index.tsx` (single route rendering a centered "Portuguese Verb Quiz" placeholder), strict TypeScript, a green jest-expo suite, and an importable Zustand store. `npx expo export --platform ios` already confirmed the app bundles with zero errors. The only unverified item is the actual visual render on the simulator.
  </what-built>
  <how-to-verify>
    1. From the repo root, run `npx expo start` (the interactive Metro dev server — it stays running, this is expected).
    2. Press `i` to launch the iOS Simulator (Xcode's current default device — iPhone 17 Pro / iOS 26.2 is available per RESEARCH.md; D-03 says use whatever ships as default, no legacy pinning), OR run `npx expo run:ios` for a full native build+launch if the dev client is needed.
    3. Wait for the app to load in the simulator.
    4. Confirm ALL of the following:
       - The app launches to a white screen with the centered text "Portuguese Verb Quiz".
       - There is NO red-box error overlay in the simulator.
       - There are NO error logs in the Metro terminal output (warnings are acceptable; errors are not).
       - There is no tabs bar and no second demo screen (D-04: single route only).
    Expected outcome: a blank white placeholder screen with one line of centered text and zero errors.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the iOS Simulator renders the "Portuguese Verb Quiz" placeholder screen (behavior assertion, visual)
    - Human confirms zero red-box errors and zero Metro error logs during boot
    - Human confirms no tabs UI / no extra demo route is present
  </acceptance_criteria>
  <resume-signal>Type "approved" if the empty root screen renders with no errors, or describe what you saw (error text, wrong screen, tabs still present) so the scaffold can be fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan modifies no files, installs no packages, and makes no network calls — it is a human visual verification only. All supply-chain trust boundaries were crossed and mitigated in Plan 01 (see 01-01-PLAN.md threat_model, T-01-SC). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | (inherited from Plan 01) | mitigate | No new packages installed this plan; all installs already verified `[OK]` via slopcheck in 01-RESEARCH.md Package Legitimacy Audit. |

**No high-severity threats.** Verification-only plan — no code, no data, no network, no new ASVS L1 surface.
</threat_model>

<verification>
Manual only — human confirms the simulator boot per the checkpoint's how-to-verify steps. This is the SC-1 visual confirmation that no automated command in this environment can produce (01-VALIDATION.md Manual-Only Verifications).
</verification>

<success_criteria>
- ROADMAP Phase 1 SC-1 (boots on iOS Simulator to empty root screen, no errors) — fully met once the human approves this checkpoint (the bundling portion was already met in Plan 01).
</success_criteria>

<output>
Create `.planning/phases/01-scaffold/01-02-SUMMARY.md` when done, recording the human sign-off outcome.
</output>
