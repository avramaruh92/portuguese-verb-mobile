---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - app.json
  - tsconfig.json
  - .gitignore
  - app/_layout.tsx
  - app/index.tsx
  - src/store/useQuizStore.ts
  - __tests__/smoke.test.ts
  - __tests__/useQuizStore.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running `npx expo export --platform ios` bundles the app with no errors (automated proxy for simulator boot)"
    - "Running `npm test` executes the jest-expo suite and all tests pass"
    - "Running `npx tsc --noEmit` compiles under strict mode with zero errors"
    - "The Zustand store at src/store/useQuizStore.ts imports and exposes readable initial state without runtime error"
    - "The app/ directory contains only _layout.tsx and index.tsx (no tabs demo, single route per D-04)"
    - "D-01: all installs and scripts use npm exclusively (npx create-expo-app, npx expo install) — no yarn/pnpm/bun lockfiles or commands anywhere"
    - "D-02: app/ stays routes-only (_layout.tsx, index.tsx); the Zustand store lives in the sibling src/store/ tree, not inside app/"
    - "D-03: no simulator/device pinning exists in any config — Xcode's current default simulator is used as-is"
  artifacts:
    - path: "app/_layout.tsx"
      provides: "Root Stack layout (headerShown: false)"
      contains: "expo-router"
    - path: "app/index.tsx"
      provides: "Single empty root route rendering 'Portuguese Verb Quiz' placeholder"
      contains: "Portuguese Verb Quiz"
    - path: "src/store/useQuizStore.ts"
      provides: "Zustand store scaffold with status: 'idle'"
      contains: "create"
    - path: "tsconfig.json"
      provides: "Strict TS config extending expo/tsconfig.base"
      contains: "\"strict\": true"
    - path: "package.json"
      provides: "jest-expo preset + test/typecheck scripts"
      contains: "jest-expo"
    - path: "__tests__/smoke.test.ts"
      provides: "Trivial jest-expo smoke test (SC-2)"
    - path: "__tests__/useQuizStore.test.ts"
      provides: "Store import-safety test (SC-4)"
  key_links:
    - from: "app/_layout.tsx"
      to: "app/index.tsx"
      via: "Expo Router file-based Stack"
      pattern: "Stack"
    - from: "__tests__/useQuizStore.test.ts"
      to: "src/store/useQuizStore.ts"
      via: "import + getState()"
      pattern: "useQuizStore"
    - from: "package.json"
      to: "jest-expo"
      via: "jest preset key"
      pattern: "jest-expo"
---

<objective>
Scaffold a working Expo Router + TypeScript + Zustand + jest-expo project skeleton for the Portuguese Verb Conjugation mobile app, stripped to a single empty root route, with strict TypeScript, a green test suite, and an importable Zustand store scaffold.

Purpose: This is the Walking Skeleton (Phase 1 of a new MVP project). It proves the full toolchain works end-to-end before any domain logic (dataset, quiz engine, UI, feedback) is built in Phases 2-5. It establishes the folder-structure, test, and TS-config conventions every later phase inherits.

Output: A booting Expo app (`app/_layout.tsx` + `app/index.tsx`), strict `tsconfig.json`, `package.json` wired for `jest-expo`, a Zustand store scaffold (`src/store/useQuizStore.ts`), and two passing tests. Satisfies ROADMAP Phase 1 success criteria 2, 3, 4 automatically; criterion 1's visual confirmation is handled by Plan 02.
</objective>

<phase_goal>
**As a** developer building this app, **I want to** have a booting Expo Router + strict-TypeScript + Zustand + jest-expo skeleton with a green test suite, **so that** every later phase (dataset, quiz engine, UI, feedback) builds on a proven, conventions-established foundation instead of re-scaffolding.

(Walking Skeleton adaptation: this is an infrastructure-only Phase 1 whose "feature" is the application itself — the user is the developer, per WALKING_SKELETON mode.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold/01-CONTEXT.md
@.planning/phases/01-scaffold/01-RESEARCH.md
@.planning/phases/01-scaffold/01-PATTERNS.md
@.planning/phases/01-scaffold/01-UI-SPEC.md
@.planning/phases/01-scaffold/01-VALIDATION.md
@.planning/phases/01-scaffold/SKELETON.md

<interfaces>
<!-- Reference templates. Source: 01-RESEARCH.md Code Examples / 01-PATTERNS.md (official Expo docs). -->
<!-- Executor should implement these directly — no codebase exploration needed (greenfield repo). -->

app/_layout.tsx — root layout:
  import { Stack } from 'expo-router';
  export default RootLayout() → <Stack screenOptions={{ headerShown: false }} />

app/index.tsx — single route, renders centered "Portuguese Verb Quiz" (Heading token: 20px / weight 600, #000 on #FFFFFF), via View + Text + StyleSheet.

src/store/useQuizStore.ts:
  import { create } from 'zustand';
  interface QuizStoreState { status: 'idle'; }
  export const useQuizStore = create<QuizStoreState>(() => ({ status: 'idle' }));

tsconfig.json:
  { "extends": "expo/tsconfig.base", "compilerOptions": { "strict": true, "noUncheckedIndexedAccess": true, "types": ["jest"] } }

package.json (relevant keys):
  "scripts": { "test": "jest", "typecheck": "tsc --noEmit" }
  "jest": { "preset": "jest-expo" }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Expo Router app and strip to a single root route</name>
  <read_first>
    - .planning/phases/01-scaffold/01-RESEARCH.md (Pattern 1 "Router-ready template, then subtract"; Pitfall 1 tabs-demo; Open Question 1 — run `ls app/` after reset and adapt to actual template output)
    - .planning/phases/01-scaffold/01-CONTEXT.md (D-01 npm; D-02 app/ routes-only + src/ tree; D-03 default simulator; D-04 single index route only)
    - .planning/phases/01-scaffold/01-PATTERNS.md (Template-then-subtract convention; app/_layout.tsx + app/index.tsx reference templates)
    - .planning/phases/01-scaffold/01-UI-SPEC.md (placeholder copy "Portuguese Verb Quiz", Heading token, #FFFFFF/#000000)
  </read_first>
  <action>
    Scaffold into the repo root using npm only (D-01). Run `npx create-expo-app@latest --template default@sdk-57` (produces Expo SDK 57 = expo@57.0.4, expo-router@57.0.4, React Native 0.86, TypeScript 5.x — do NOT let anything pull typescript@7.x). If create-expo-app refuses to init in a non-empty dir, scaffold in a scratch subdir and move generated files into the repo root, preserving the existing `.planning/`, `.git/`, `CLAUDE.md`, `AGENTS.md`, `.mcp.json`.

    Then strip the tabs demo to satisfy D-04: run `npm run reset-project` (the template's own helper). Immediately run `ls app/` and adapt — the acceptance bar is D-04 (single index route), not a hardcoded file list. Delete any leftover demo route group (`app/(tabs)/`), demo `components/`, `hooks/`, `constants/`, `scripts/reset-project.js`, and the moved-aside `app-example/` directory if present, so `app/` contains ONLY `_layout.tsx` and `index.tsx`.

    Write `app/_layout.tsx` as a minimal root Stack (`expo-router` Stack, `headerShown: false`). Write `app/index.tsx` per the 01-PATTERNS.md reference: a centered `View` + `Text` rendering exactly "Portuguese Verb Quiz" using the Heading token (fontSize 20, fontWeight '600', color #000000) on a #FFFFFF background via `StyleSheet.create`.

    Install runtime + dev deps with Expo's resolver (never plain `npm install` for Expo-adjacent pkgs — Pitfall 4): `npx expo install zustand zod` (→ zustand@5.0.14, zod@4.4.3) and `npx expo install -D jest-expo @types/jest` (→ jest-expo@57.0.1, @types/jest matching jest@30.x; jest-expo brings jest transitively — do NOT add a top-level `jest` at a mismatched major).

    Extend `.gitignore` (do not replace the existing one) with `node_modules/`, `.expo/`, and standard Expo/RN ignores if not already covered by the template's generated `.gitignore`.

    Do NOT create `src/dataset/`, `src/quiz-engine/`, or `src/api/` — those are later phases (RESEARCH.md is explicit: no empty placeholder folders). Do NOT install axios, expo-sharing, or @react-native-async-storage/async-storage (STACK.md "What NOT to Use").
  </action>
  <verify>
    <automated>test "$(ls app/ | sort | tr '\n' ' ')" = "_layout.tsx index.tsx " && test ! -d "app/(tabs)" && npx expo export --platform ios</automated>
  </verify>
  <acceptance_criteria>
    - `app/` directory contains exactly two files: `_layout.tsx` and `index.tsx`; no `app/(tabs)/` group exists (source assertion: `ls app/`)
    - `app/index.tsx` renders the literal string "Portuguese Verb Quiz" (source assertion: grep the file)
    - `package.json` lists `expo`, `expo-router`, `zustand`, `zod` as dependencies and `jest-expo`, `@types/jest` as devDependencies; TypeScript is on the 5.x line (NOT 7.x)
    - `npx expo export --platform ios` exits zero (bundling proxy for SC-1 — proves the app bundles with no errors)
    - axios, expo-sharing, and @react-native-async-storage/async-storage are absent from package.json
  </acceptance_criteria>
  <done>Expo Router app scaffolded, tabs demo stripped to a single `app/index.tsx` route rendering the placeholder, all six phase packages installed at the researched versions, and `npx expo export --platform ios` bundles cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire strict TypeScript, jest-expo, the Zustand store scaffold, and passing tests</name>
  <read_first>
    - .planning/phases/01-scaffold/01-RESEARCH.md (Pattern 2 package.json jest key; Pattern 3 strict tsconfig; Pitfall 2 no --watchAll; Code Examples for store, smoke test, tsconfig, package.json wiring)
    - .planning/phases/01-scaffold/01-PATTERNS.md (src/store/useQuizStore.ts, __tests__ conventions, tsconfig.json, package.json reference templates)
    - .planning/phases/01-scaffold/01-VALIDATION.md (Per-Task Verification Map: SC-2 npm test, SC-3 tsc --noEmit, SC-4 store test)
    - package.json, tsconfig.json (as generated by Task 1's scaffold — read before editing)
  </read_first>
  <behavior>
    - Test 1 (smoke, SC-2): a trivial jest-expo assertion (`expect(1 + 1).toBe(2)`) runs and passes, proving the preset transforms/runs.
    - Test 2 (store import-safety, SC-4): importing `useQuizStore` and reading `useQuizStore.getState().status` returns `'idle'` with no runtime error at import time.
  </behavior>
  <action>
    Create `tsconfig.json` (or overwrite the template's) extending `expo/tsconfig.base` with `compilerOptions`: `"strict": true`, `"noUncheckedIndexedAccess": true` (Claude's-discretion strict flag per CONTEXT.md — cheap now, catches missing-conjugation-cell bugs in Phase 2), and `"types": ["jest"]` so Jest globals resolve under strict mode.

    Edit `package.json` (preserve template-generated fields): set `"scripts"."test"` to `"jest"` (plain single run — NEVER `--watchAll`, which never exits under scripted verification per Pitfall 2), add `"scripts"."typecheck"` = `"tsc --noEmit"`, and add the top-level `"jest": { "preset": "jest-expo" }` key. Do not also create a separate `jest.config.js` (having both is the "which config wins" anti-pattern).

    Create `src/store/useQuizStore.ts` (D-02: domain lives in `src/`, not `app/`): a minimal Zustand store — `import { create } from 'zustand'`, an interface `QuizStoreState { status: 'idle' }`, exporting `useQuizStore = create<QuizStoreState>(() => ({ status: 'idle' }))`. No quiz logic — Phases 2-4 own that. This establishes the `src/<domain>/` convention later phases follow.

    Write the two tests in the flat `__tests__/` convention: `__tests__/smoke.test.ts` (the trivial passing assertion) and `__tests__/useQuizStore.test.ts` (imports `useQuizStore` from `../src/store/useQuizStore`, asserts `getState().status` is `'idle'`).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` runs the jest-expo suite and both test files pass (SC-2, SC-4) — command exits zero and terminates (no watch hang)
    - `npx tsc --noEmit` compiles the entire `app/` + `src/` + `__tests__/` tree under `strict: true` with zero errors (SC-3)
    - `tsconfig.json` extends `expo/tsconfig.base` and sets `"strict": true` and `"types": ["jest"]` (source assertion)
    - `package.json` `"jest"."preset"` equals `"jest-expo"` and `"scripts"."test"` is `"jest"` with no `--watchAll` (source assertion)
    - `src/store/useQuizStore.ts` exports `useQuizStore` and `useQuizStore.getState().status === 'idle'` (behavior assertion via the store test)
    - No `jest.config.js` exists alongside the package.json jest key
  </acceptance_criteria>
  <done>Strict TypeScript config, jest-expo preset, the `src/store/useQuizStore.ts` scaffold, and both `__tests__` files are in place; `npm test` and `npx tsc --noEmit` both pass green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dev machine | Package installs (`create-expo-app`, `expo install`) pull third-party code that runs at install/build time. This is the ONLY trust boundary this phase crosses — the app makes zero network calls, has no auth, no sessions, no user input, no crypto, and no database (per 01-RESEARCH.md Security Domain: no applicable ASVS L1 category has an actionable finding). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm package installs (`expo`, `expo-router`, `zustand`, `zod`, `jest-expo`, `@types/jest`) | mitigate | All 6 packages already verified in 01-RESEARCH.md Package Legitimacy Audit: `slopcheck --ecosystem npm` returned `[OK]` for all, `npm view <pkg> scripts.postinstall` empty for all (no postinstall hooks), source repos confirmed (expo/expo, pmndrs/zustand, colinhacks/zod, DefinitelyTyped). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages remain → no blocking legitimacy checkpoint required. If `slopcheck` is re-run before `package.json` exists, force `--ecosystem npm` (auto-detect defaults to pypi → false [SLOP] positives). |
| T-01-02 | Tampering | typescript version pinning | accept | Registry has `typescript@7.0.2` (Go-native rewrite) but Expo/Metro compatibility is unvalidated for SDK 57. Mitigation baked into Task 1: use only Expo-template-installed TS 5.x; never `npm install typescript@7`. Low risk (conservative pin), accepted. |

**No high-severity threats.** This infrastructure-only phase has no auth (V2), sessions (V3), access control (V4), input validation (V5), or cryptography (V6) surface — consistent with the locked "no backend writes, no accounts, offline-only" product constraints. The single actionable security item (supply-chain legitimacy) is already resolved.
</threat_model>

<verification>
Automated (run after each task and at plan completion):
- `npm test` — jest-expo suite green (SC-2, SC-4)
- `npx tsc --noEmit` — strict compile, zero errors (SC-3)
- `npx expo export --platform ios` — bundling proxy, exits zero (SC-1 automated portion)
- `ls app/` — exactly `_layout.tsx` + `index.tsx` (D-04 compliance)

Visual/simulator confirmation of SC-1 is deferred to Plan 02 (`checkpoint:human-verify`) — no automated visual-inspection tooling exists in this environment.
</verification>

<success_criteria>
- ROADMAP Phase 1 SC-2 (test suite executes and passes) — met by `npm test` green
- ROADMAP Phase 1 SC-3 (strict TS compiles, zero errors) — met by `npx tsc --noEmit`
- ROADMAP Phase 1 SC-4 (Zustand store importable, no runtime error) — met by `useQuizStore.test.ts`
- ROADMAP Phase 1 SC-1 (boots to empty screen) — automated bundling portion met by `npx expo export --platform ios`; visual confirmation handled by Plan 02
- D-04 (single index route only) — `app/` contains only `_layout.tsx` + `index.tsx`
</success_criteria>

<output>
Create `.planning/phases/01-scaffold/01-01-SUMMARY.md` when done.
</output>
