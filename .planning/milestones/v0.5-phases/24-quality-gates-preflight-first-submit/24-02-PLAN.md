---
phase: 24-quality-gates-preflight-first-submit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/preflight.ts
  - package.json
autonomous: true
requirements: [SHIP-02]
must_haves:
  truths:
    - "npm run preflight runs a standalone script that checks 4 live backend endpoints by status code"
    - "Against the warm deployed Render backend, all 4 checks pass (200/200/201/201)"
    - "The script prints a per-check pass/fail line and exits non-zero if any check fails"
  artifacts:
    - path: scripts/preflight.ts
      provides: "Reusable live-backend status-code smoke test for /health, /content/verbs, /feedback, /product-feedback"
      contains: "portuguese-verb-api.onrender.com"
    - path: package.json
      provides: "npm run preflight entry"
      contains: "node scripts/preflight.ts"
  key_links:
    - from: scripts/preflight.ts
      to: "https://portuguese-verb-api.onrender.com"
      via: "fetch with AbortController timeout"
      pattern: "AbortController"
    - from: package.json
      to: scripts/preflight.ts
      via: "npm script entry"
      pattern: "preflight.*node scripts/preflight.ts"
---

<objective>
Build a checked-in, reusable preflight script (`scripts/preflight.ts`) that
smoke-tests the live backend by status code only, and wire it to a
`npm run preflight` entry. Run it once against the warm backend and confirm
all four endpoints pass. This same script is what the operator re-runs against
a cold instance in Plan 03 (SHIP-03).

Purpose: Prove the four endpoints the app depends on are live before the first
real TestFlight build (SHIP-02).
Output: `scripts/preflight.ts`, a `preflight` npm script, and a green warm run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-quality-gates-preflight-first-submit/24-CONTEXT.md
@.planning/phases/24-quality-gates-preflight-first-submit/24-PATTERNS.md
@scripts/generate-brand-assets.ts

<interfaces>
Endpoint targets and expected status codes (status-code-only, per D-03):

| Check              | Method | URL                                                         | Expected |
|--------------------|--------|-------------------------------------------------------------|----------|
| /health            | GET    | https://portuguese-verb-api.onrender.com/health             | 200      |
| /content/verbs     | GET    | https://portuguese-verb-api.onrender.com/content/verbs      | 200      |
| /feedback          | POST   | https://portuguese-verb-api.onrender.com/feedback           | 201      |
| /product-feedback  | POST   | https://portuguese-verb-api.onrender.com/product-feedback   | 201      |

These endpoint constants are NOT exported from source (`src/dataset/remote.ts`,
`src/feedback/submit.ts`, `src/productFeedback/submit.ts`) — re-declare the base
URL + path suffixes as file-local SCREAMING_SNAKE_CASE constants in the script.

feedbackPayloadSchema (src/feedback/schema.ts) — valid dummy payload:
  { message, verb, tense, subject, correctAnswer, selectedAnswer, appVersion, platform }
  tense ∈ present_indicative|preterite|imperfect|future
  subject ∈ eu|tu|ele_ela|nos|voces|eles_elas
  platform ∈ ios|android

productFeedbackPayloadSchema (src/productFeedback/schema.ts) — valid dummy payload:
  { category, message, screen, appVersion, platform }
  category ∈ bug|idea|other
  screen ∈ setup|quiz|results
  platform ∈ ios|android
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write scripts/preflight.ts (four status-code checks against the live backend)</name>
  <read_first>
    - scripts/generate-brand-assets.ts (the only standalone-script precedent — copy its structure: top-of-file SCREAMING_SNAKE_CASE consts, small named functions, a single `async function main()` called unconditionally as the last statement, no argv parsing, no `require.main` guard)
    - src/dataset/remote.ts (the AbortController + setTimeout/clearTimeout in try/finally fetch-timeout pattern to mirror; also the CONTENT_ENDPOINT literal)
    - src/feedback/schema.ts and src/feedback/payload.ts (exact feedbackPayloadSchema fields + a real payload shape to mirror for the dummy)
    - src/productFeedback/schema.ts and src/productFeedback/types.ts (productFeedbackPayloadSchema fields incl. the SCREENS enum)
    - .planning/phases/24-quality-gates-preflight-first-submit/24-PATTERNS.md (§scripts/preflight.ts — endpoint table, dummy payloads, exit-code guidance)
  </read_first>
  <action>
    Create `scripts/preflight.ts` as a plain TS script (run via Node native type-stripping, no new dependency). Declare a `BASE_URL` constant `"https://portuguese-verb-api.onrender.com"` and four endpoint constants for `/health`, `/content/verbs`, `/feedback`, `/product-feedback` in SCREAMING_SNAKE_CASE, plus a `TIMEOUT_MS` constant (use 90_000 to match the project's existing fetch timeout convention).
    Write one small async helper per check (or a shared `checkStatus(label, url, expected, init?)` helper) that wraps `fetch` with an `AbortController` + `setTimeout(() => controller.abort(), TIMEOUT_MS)` inside a `try { ... } finally { clearTimeout(...) }`, compares `response.status` to the expected code (status-code only — do NOT call `response.json()` or validate body shape, per D-03), and returns a `{ label, ok, actual, expected }` result. Treat thrown/aborted fetches as a failed check (ok=false), not an uncaught crash.
    GET checks: `/health` expect 200, `/content/verbs` expect 200. POST checks: `/feedback` expect 201 with a schema-valid dummy JSON body (`{ message: "preflight smoke test — ignore", verb: "falar", tense: "present_indicative", subject: "eu", correctAnswer: "falo", selectedAnswer: "falo", appVersion: "preflight", platform: "ios" }`, `Content-Type: application/json`); `/product-feedback` expect 201 with dummy body `{ category: "other", message: "preflight smoke test — ignore", screen: "setup", appVersion: "preflight", platform: "ios" }`.
    In `async function main()`: run the four checks (sequentially is fine), print one line per check like `PASS /health -> 200` or `FAIL /content/verbs -> expected 200 got 503`, print a final summary line, and if any check failed call `process.exit(1)` after printing. Call `main();` unconditionally as the last statement (no argv/CLI-flag parsing).
    Do NOT add env-var config and do NOT add `export` keywords to the production source endpoint constants — re-declare the literals locally in the script.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('scripts/preflight.ts','utf8');const checks=['/health','/content/verbs','/feedback','/product-feedback','AbortController','process.exit(1)','main();'];const missing=checks.filter(c=>!s.includes(c));if(missing.length){console.error('MISSING:',missing);process.exit(1)}console.log('structure ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/preflight.ts` exists and references all four paths: `/health`, `/content/verbs`, `/feedback`, `/product-feedback`.
    - Script uses the `AbortController` + `setTimeout`/`clearTimeout` timeout pattern (contains `AbortController` and `clearTimeout`).
    - Script contains no `response.json(` body-parsing/validation call (status-code-only per D-03).
    - Script ends with an unconditional `main();` call and calls `process.exit(1)` on any failure.
    - Dummy POST payloads include the required schema fields (feedback: `verb`/`tense`/`subject`/`correctAnswer`/`selectedAnswer`/`appVersion`/`platform`/`message`; product-feedback: `category`/`screen`/`appVersion`/`platform`/`message`) with enum values drawn from the allowed sets.
    - `npm run typecheck` exits 0 (script is type-valid, or is excluded consistently with generate-brand-assets.ts if that file is excluded).
  </acceptance_criteria>
  <done>scripts/preflight.ts exists, is structurally complete, and type-checks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the preflight npm script and run it once against the warm backend</name>
  <read_first>
    - package.json (the `scripts` block — add the new entry alongside `generate-assets`)
    - .planning/phases/24-quality-gates-preflight-first-submit/24-PATTERNS.md (§package.json — the exact entry to add)
  </read_first>
  <action>
    Add `"preflight": "node scripts/preflight.ts"` to the `scripts` block in `package.json`, matching the plain `node scripts/<file>.ts` style of the existing `generate-assets` entry (no flags, no separate build step).
    Then run `npm run preflight` once against the currently-warm live backend and confirm the script reports PASS for all four checks and exits 0. The backend is expected to be warm (recently exercised); if the very first call returns a 5xx purely from a cold Render spin-up, re-run once — a warm-path all-pass is the target here (the deliberate >15min-idle cold test is SHIP-03, handled by the operator in Plan 03, not here).
  </action>
  <verify>
    <automated>npm run preflight</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `scripts` block contains `"preflight": "node scripts/preflight.ts"`.
    - `npm run preflight` exits 0 against the warm backend and prints PASS for all four checks (`/health` 200, `/content/verbs` 200, `/feedback` 201, `/product-feedback` 201).
    - The SUMMARY records the actual observed status codes from the warm run.
  </acceptance_criteria>
  <done>`npm run preflight` is wired and passes all four checks warm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script -> live backend | The preflight script sends dummy POST payloads to the production Render backend's `/feedback` and `/product-feedback` endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02 | Tampering/Info | Dummy feedback rows written to prod DB | accept | Payloads are clearly marked "preflight smoke test — ignore"; the backend already accepts arbitrary feedback content by design. Low volume (one row per endpoint per run). No secrets or PII sent. |
| T-24-03 | DoS | Repeated preflight runs against Render | accept | Manually-run dev script, not automated/CI; single-shot four requests per invocation. No amplification. |
</threat_model>

<verification>
- `npm run preflight` exits 0 with all four checks PASS against the warm backend.
- `npm run typecheck` exits 0.
</verification>

<success_criteria>
SHIP-02 satisfied: a reusable, checked-in preflight script confirms
`GET /health`, `GET /content/verbs`, `POST /feedback`, and `POST /product-feedback`
all succeed (by status code) against the deployed Render backend while warm.
</success_criteria>

<output>
Create `.planning/phases/24-quality-gates-preflight-first-submit/24-02-SUMMARY.md` when done.
</output>
