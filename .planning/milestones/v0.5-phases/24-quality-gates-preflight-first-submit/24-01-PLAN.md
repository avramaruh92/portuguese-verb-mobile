---
phase: 24-quality-gates-preflight-first-submit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/feedback/ReportFeedbackModal.tsx
  - src/productFeedback/ProductFeedbackModal.tsx
autonomous: true
requirements: [SHIP-01]
must_haves:
  truths:
    - "npm run lint exits 0 with zero errors (both react-hooks/set-state-in-effect errors gone)"
    - "Both feedback modals still reset reason/category, message, state, and lastStatus every time visible flips false -> true"
    - "The pending success-timer is still cleared on every visible transition and on unmount in both modals (not narrowed to unmount-only)"
  artifacts:
    - path: src/feedback/ReportFeedbackModal.tsx
      provides: "Lint-clean render-time reset for the report-a-problem modal"
      contains: "prevVisibleRef"
    - path: src/productFeedback/ProductFeedbackModal.tsx
      provides: "Lint-clean render-time reset for the product-feedback modal"
      contains: "prevVisibleRef"
  key_links:
    - from: src/feedback/ReportFeedbackModal.tsx
      to: "visible prop transition"
      via: "render-time ref comparison (prevVisibleRef)"
      pattern: "visible && !prevVisibleRef"
    - from: src/productFeedback/ProductFeedbackModal.tsx
      to: "visible prop transition"
      via: "render-time ref comparison (prevVisibleRef)"
      pattern: "visible && !prevVisibleRef"
---

<objective>
Fix the two known `react-hooks/set-state-in-effect` lint errors — one in
`src/feedback/ReportFeedbackModal.tsx:54` and one in
`src/productFeedback/ProductFeedbackModal.tsx:49` — so `npm run lint` passes
with zero errors, without changing any user-visible feedback-modal behavior.

Purpose: Clean lint is a hard gate before the first real EAS build/submit
(SHIP-01, blocking SHIP-04).
Output: Both modals reset form fields via a render-time ref comparison
instead of a `setState`-in-effect, and lint is green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-quality-gates-preflight-first-submit/24-CONTEXT.md
@.planning/phases/24-quality-gates-preflight-first-submit/24-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace effect-based reset with render-time ref reset in ReportFeedbackModal</name>
  <read_first>
    - src/feedback/ReportFeedbackModal.tsx (the file being modified — read the full component to see the useState declarations, the buggy useEffect at lines 44-69, timerRef usage, and where the success timer is set)
    - .planning/phases/24-quality-gates-preflight-first-submit/24-PATTERNS.md (§"Render-time state reset" and the ReportFeedbackModal fix block — the exact target pattern)
  </read_first>
  <action>
    Remove the `if (visible) { setReason("wrong_answer"); setMessage(""); setState("idle"); setLastStatus(null); }` block from inside the `useEffect(..., [visible])`. Replace it with a render-body reset guarded by a previous-visible ref, added just after the `useState`/`useRef` declarations and before the JSX return:
    declare `const prevVisibleRef = useRef(visible);`; then `if (visible && !prevVisibleRef.current) { setReason("wrong_answer"); setMessage(""); setState("idle"); setLastStatus(null); }`; then `prevVisibleRef.current = visible;`.
    Reset values must be the current defaults: `reason` -> `"wrong_answer"`, `message` -> `""`, `state` -> `"idle"`, `lastStatus` -> `null`.
    Keep a `useEffect` whose ONLY job is clearing the pending success timer, with dependency array `[visible]` (NOT `[]`) — this preserves the current behavior of clearing the timer on every `visible` transition (not just unmount), which matters if the modal is closed and reopened while the 1.5s post-success auto-close timer is still pending. That effect must call `clearTimeout(timerRef.current)` and null `timerRef.current` both in its body (so a close mid-timer clears it immediately) and in its cleanup function (so unmount also clears it), and must NOT call any `setState`/`setReason`/`setMessage` anywhere in it. Do NOT introduce a `key`-based remount (rejected per D-01). Do not change the JSX, the submit handler, or the success-timer that sets `timerRef.current`.
  </action>
  <verify>
    <automated>npx eslint src/feedback/ReportFeedbackModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/feedback/ReportFeedbackModal.tsx` reports zero errors (the `react-hooks/set-state-in-effect` error at former line 54 is gone).
    - Source contains `const prevVisibleRef = useRef(visible)` and a render-body guard matching `if (visible && !prevVisibleRef.current)` that calls `setReason("wrong_answer")`, `setMessage("")`, `setState("idle")`, and `setLastStatus(null)`.
    - Source still contains a `useEffect` with `[visible]` dependency array that calls `clearTimeout(timerRef.current)` and sets `timerRef.current = null` in its body, and returns a cleanup doing the same.
    - No `setReason`/`setMessage`/`setState`/`setLastStatus` call remains inside any `useEffect` body.
    - No `key=` remount prop was added to the modal.
  </acceptance_criteria>
  <done>Report modal lints clean and still resets all four fields when it becomes visible; timer is cleared both on every visible transition and on unmount (matches prior behavior).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mirror the identical render-time ref reset in ProductFeedbackModal</name>
  <read_first>
    - src/productFeedback/ProductFeedbackModal.tsx (the file being modified — read the full component; the buggy useEffect is at lines 39-64, identical shape to the report modal but with `category`/`ProductFeedbackCategory` fields)
    - src/feedback/ReportFeedbackModal.tsx (the sibling just fixed in Task 1 — the fix transfers byte-for-byte except field names)
    - .planning/phases/24-quality-gates-preflight-first-submit/24-PATTERNS.md (§"Render-time state reset" and the ProductFeedbackModal fix block)
  </read_first>
  <action>
    Apply the exact same transformation as Task 1, substituting this modal's fields. Remove the `if (visible) { setCategory("bug"); setMessage(""); setState("idle"); setLastStatus(null); }` block from inside `useEffect(..., [visible])`. Add, after the `useState`/`useRef` declarations and before the JSX return: `const prevVisibleRef = useRef(visible);`; then `if (visible && !prevVisibleRef.current) { setCategory("bug"); setMessage(""); setState("idle"); setLastStatus(null); }`; then `prevVisibleRef.current = visible;`.
    Reset defaults: `category` -> `"bug"`, `message` -> `""`, `state` -> `"idle"`, `lastStatus` -> `null`.
    Keep the timer-clearing `useEffect` with `[visible]` deps (NOT `[]`) — same rationale as Task 1: clears `timerRef.current` both in the effect body (on every `visible` change) and in the cleanup (on unmount), so reopening the modal mid-timer still clears the stale timer. No `setState` inside it. No `key`-based remount. Do not change JSX or submit logic.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0 with zero errors across the whole project (both former errors resolved).
    - `src/productFeedback/ProductFeedbackModal.tsx` contains `const prevVisibleRef = useRef(visible)` and a render-body guard `if (visible && !prevVisibleRef.current)` that calls `setCategory("bug")`, `setMessage("")`, `setState("idle")`, and `setLastStatus(null)`.
    - Source still contains a `useEffect` with `[visible]` deps that clears `timerRef.current` in its body and its cleanup.
    - `npm run typecheck` exits 0 (no new type errors introduced in either modal).
    - No `setState`-family call remains inside any `useEffect` body in this file; no `key=` remount added.
  </acceptance_criteria>
  <done>Both modals lint clean, `npm run lint` and `npm run typecheck` both pass, feedback behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only refactors client-side reset logic; no new input, network, or persistence boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | Feedback-modal reset refactor | accept | Behavior-preserving refactor; existing `__tests__` for feedback modules plus `npm run lint`/`typecheck` gates catch regressions. No user input handling changes. |
</threat_model>

<verification>
- `npm run lint` exits 0.
- `npm run typecheck` exits 0.
- `npm test` still passes (no behavior change expected).
</verification>

<success_criteria>
SHIP-01 satisfied: `npm run lint` passes with zero errors, both previously-known
`react-hooks/set-state-in-effect` failures gone, feedback modals still reset all
fields on becoming visible and still clear the pending timer on unmount.
</success_criteria>

<output>
Create `.planning/phases/24-quality-gates-preflight-first-submit/24-01-SUMMARY.md` when done.
</output>
