---
phase: 20-native-build-risk-front-loading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements: [BUILD-01, BUILD-02]
user_setup: []

must_haves:
  truths:
    - "npx expo-doctor reports zero failing checks (0 checks failed)"
    - "npx expo install --check reports no version mismatches"
    - "eas-cli is a pinned devDependency and runs via npm run eas at >= 21.0.3"
  artifacts:
    - path: "package.json"
      provides: "SDK-aligned dependency versions, pinned eas-cli devDependency, eas npm script"
      contains: "eas-cli"
    - path: "package-lock.json"
      provides: "Regenerated lockfile reflecting the version bumps and eas-cli install"
  key_links:
    - from: "package.json scripts.eas"
      to: "node_modules/.bin/eas"
      via: "npm run eas -- <subcommand>"
      pattern: "\"eas\"\\s*:\\s*\"eas\""
---

<objective>
Establish a clean, SDK-aligned dependency baseline and pin the EAS CLI tooling
before any native build is attempted. This closes BUILD-01 (expo-doctor and
expo install --check must run clean) and lays the tooling foundation
(pinned eas-cli + npm script) that the Wave 2 native build depends on.

Purpose: A first-ever native cloud build compiles against the real native
dependency graph. Version drift that is invisible in Expo Go / dev-client must
be resolved now, or it surfaces as an opaque cloud-build failure later.

Output: Updated package.json / package-lock.json with 7 patch/minor version
bumps applied by `expo install --fix`, a pinned `eas-cli@^21.0.3` devDependency,
and an `eas` npm script that guarantees the pinned local binary runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-native-build-risk-front-loading/20-CONTEXT.md
@.planning/phases/20-native-build-risk-front-loading/20-RESEARCH.md
@.planning/phases/20-native-build-risk-front-loading/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Expo SDK dependency version drift</name>
  <read_first>
    - package.json (current dependency versions — the edit target)
    - .planning/phases/20-native-build-risk-front-loading/20-RESEARCH.md (Code Examples: live expo-doctor output; Pitfall 3: Minor vs Patch labels both block a clean pass)
    - .planning/phases/20-native-build-risk-front-loading/20-PATTERNS.md (package.json dependencies pinning convention: tilde ranges for expo-* packages)
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
    Run `npx expo-doctor` first to capture the current baseline. Then run
    `npx expo install --fix` to let the tool rewrite the drifted package
    versions in place — do NOT hand-edit version strings. Per the research
    live-capture the expected targets are: expo `~57.0.8`, expo-constants
    `~57.0.7`, expo-linking `~57.0.4`, expo-router `~57.0.8`, expo-splash-screen
    `~57.0.5`, expo-web-browser `~57.0.2`, and react-native-screens moves from
    the exact pin `4.25.2` to tilde `~4.26.0`. The flagged package set may have
    shifted if a newer SDK 57 patch shipped since 2026-07-23, so trust the live
    `expo-doctor` / `expo install --fix` output over that captured table. Fix
    EVERY flagged package (both the "Minor" and "Patch" tables) — expo-doctor
    exits non-zero if either category has entries (Pitfall 3). Do not touch any
    non-flagged dependency. After the fix, re-run `npx expo-doctor` and confirm
    it reports "0 checks failed".
  </action>
  <verify>
    <automated>npx expo-doctor && npx expo install --check</automated>
  </verify>
  <acceptance_criteria>
    - `npx expo-doctor` exits 0 and its output includes "0 checks failed" (no "checks failed" count above 0)
    - `npx expo install --check` exits 0 and reports no packages out of date
    - package.json expo-* dependencies use tilde (`~`) ranges (no exact pins reintroduced) and react-native-screens is `~4.26.0` or the current SDK-expected range
    - `git diff package-lock.json` reflects only the intended version bumps, no unrelated lockfile churn
  </acceptance_criteria>
  <done>expo-doctor reports 0 failing checks and expo install --check is clean; package.json/package-lock.json updated by the tool, not by hand.</done>
</task>

<task type="auto">
  <name>Task 2: Pin eas-cli devDependency and add eas npm script</name>
  <read_first>
    - package.json (devDependencies + scripts blocks — the edit target)
    - .planning/phases/20-native-build-risk-front-loading/20-CONTEXT.md (D-04: pin eas-cli ^21.0.3 as devDependency)
    - .planning/phases/20-native-build-risk-front-loading/20-RESEARCH.md (Pitfall 1: stale global eas-cli@20.0.0 shadows npx; mitigation is an npm script resolving node_modules/.bin/eas)
    - .planning/phases/20-native-build-risk-front-loading/20-PATTERNS.md (devDependencies pinning: caret `^` for standalone non-SDK tools; scripts pattern: one flat verb-named script)
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
    Run `npm install -D eas-cli@^21.0.3` so package.json's devDependencies gains
    `"eas-cli": "^21.0.3"` (caret range — eas-cli is a standalone tool not paired
    to the Expo SDK cadence, per D-04 and the PATTERNS pinning convention). Add a
    script entry `"eas": "eas"` to package.json scripts, keeping the existing flat
    verb-named script style. This exists because a stale global `eas-cli@20.0.0`
    on this machine shadows bare `npx eas-cli` (Pitfall 1); all EAS commands in
    this and later phases must be invoked as `npm run eas -- <subcommand>` so npm
    resolves the pinned local `node_modules/.bin/eas`. Do not modify any other
    devDependency or script.
  </action>
  <verify>
    <automated>npm run eas -- --version</automated>
  </verify>
  <acceptance_criteria>
    - `npm run eas -- --version` prints a version at or above `21.0.3` (proves the pinned local binary runs, not the global 20.0.0)
    - package.json devDependencies contains `"eas-cli": "^21.0.3"`
    - package.json scripts contains `"eas": "eas"`
    - `grep -c '"eas-cli"' package.json` returns at least 1
  </acceptance_criteria>
  <done>eas-cli is pinned as a caret devDependency, the eas npm script exists, and `npm run eas -- --version` reports >= 21.0.3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev machine → npm registry | package installs pull code that will execute during build tooling |
| local dev machine → EAS cloud API | eas-cli acts with the authenticated developer's account privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | Global eas-cli@20.0.0 shadowing the pinned ^21.0.3 | mitigate | Invoke EAS via `npm run eas -- <subcommand>` (resolves node_modules/.bin first); acceptance criterion asserts version >= 21.0.3 |
| T-20-SC | Tampering | npm install of eas-cli | accept | eas-cli is an official Expo-org package, slopcheck [OK], approved in RESEARCH.md Package Legitimacy Audit — no [ASSUMED]/[SUS] packages, no blocking human checkpoint required |
</threat_model>

<verification>
- `npx expo-doctor` → "0 checks failed"
- `npx expo install --check` → no packages out of date
- `npm run eas -- --version` → >= 21.0.3
- `git diff package.json package-lock.json` contains only intended version bumps + eas-cli addition + eas script
</verification>

<success_criteria>
BUILD-01 is satisfied: expo-doctor and expo install --check both run clean. The
EAS CLI is pinned and reachable via `npm run eas`, unblocking the Wave 2 build.
</success_criteria>

<output>
Create `.planning/phases/20-native-build-risk-front-loading/20-01-SUMMARY.md` when done
</output>
