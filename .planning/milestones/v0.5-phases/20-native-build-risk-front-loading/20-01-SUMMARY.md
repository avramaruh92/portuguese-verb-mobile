---
phase: 20-native-build-risk-front-loading
plan: 01
subsystem: infra
tags: [expo, eas-cli, expo-doctor, dependency-management, release-engineering]

# Dependency graph
requires: []
provides:
  - "SDK 57-aligned dependency versions (expo-doctor 20/20, expo install --check clean)"
  - "eas-cli@^21.1.0 pinned as a devDependency, reachable via npm run eas -- <subcommand>"
affects: [20-02-throwaway-build, 23-eas-build-submit-configuration, 24-quality-gates-preflight-first-submit]

# Tech tracking
tech-stack:
  added: [eas-cli@^21.1.0]
  patterns:
    - "EAS CLI invoked via `npm run eas -- <subcommand>` (never bare npx) to bypass a stale global eas-cli@20.0.0 shadowing this dev machine's PATH resolution"

key-files:
  created: []
  modified: [package.json, package-lock.json]

key-decisions:
  - "Followed D-04 (pin eas-cli as devDependency + npm script) even though it triggers 2 new expo-doctor advisory checks (eas-cli-as-project-dependency, eas script vs .bin conflict) — this is the deliberate, documented tradeoff from CONTEXT.md/RESEARCH.md Pitfall 1, not a regression"

requirements-completed: [BUILD-01]

# Metrics
duration: 12min
completed: 2026-07-23
---

# Phase 20 Plan 01: Dependency Baseline & EAS CLI Pin Summary

**Applied `expo install --fix`'s 7-package SDK 57 version realignment and pinned eas-cli@^21.1.0 with a local-resolving npm script, closing BUILD-01's clean-baseline requirement.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-23T09:24:00Z
- **Completed:** 2026-07-23T09:36:47Z
- **Tasks:** 2 completed
- **Files modified:** 2 (package.json, package-lock.json)

## Accomplishments
- `npx expo-doctor` moved from 19/20 (1 check failed: package version drift) to 20/20 (0 checks failed) after `npx expo install --fix`
- `npx expo install --check` reports "Dependencies are up to date"
- `eas-cli@^21.1.0` pinned as a devDependency (satisfies the plan's `^21.0.3` floor) and an `"eas": "eas"` npm script added, guaranteeing `npm run eas -- <subcommand>` resolves the pinned local binary instead of this machine's stale global `eas-cli@20.0.0`

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix Expo SDK dependency version drift** - `2426e89` (fix)
2. **Task 2: Pin eas-cli devDependency and add eas npm script** - `7d1e4cb` (feat)

_Note: the working tree already had a partially-completed `expo install --fix` pass in `package-lock.json` from a prior session (per the orchestrator's note); Task 1 re-ran `npx expo install --fix` to completion rather than assuming it was already finished — `expo-doctor` still reported the original 7-package drift before this task ran._

## Files Created/Modified
- `package.json` - `expo` and 6 sibling `expo-*` packages plus `react-native-screens` bumped to SDK 57-current tilde ranges via `expo install --fix`; `eas-cli` added to `devDependencies` (`^21.1.0`); `eas` script added
- `package-lock.json` - regenerated by `npm install`/`expo install --fix`, reflecting the above

## Decisions Made
- Left `app.json`'s uncommitted config-plugin diff (`expo-image`/`expo-status-bar`/`expo-web-browser` plugins) untouched and uncommitted, per the orchestrator's explicit instruction — out of scope for this plan's `files_modified` (`package.json`, `package-lock.json` only), to be reconciled by whichever later phase owns `app.json`
- `npx expo install --fix` also bumped several packages not in `expo-doctor`'s originally-flagged 7-package table (`@expo/ui`, `expo-device`, `expo-glass-effect`, `expo-image`, `expo-status-bar`, `expo-symbols`, `expo-system-ui`, `jest-expo`) — this is the tool's own SDK-alignment behavior (all managed Expo packages get checked against the SDK 57 compatibility matrix in one pass, not just the ones `expo-doctor` happened to flag first), not a hand-edit; accepted as-is per the plan's explicit "let the tool write them, do not hand-edit" instruction

## Deviations from Plan

None - plan executed exactly as written. Both tasks completed per their acceptance criteria; no Rule 1-4 fixes were needed.

## Issues Encountered

**Expected side effect, not a bug:** after Task 2 pins `eas-cli` as a devDependency and adds the `eas` npm script, `npx expo-doctor` newly reports 2 additional advisory-level failures:
- "Check for legacy global CLI installed locally" — flags `eas-cli` being present in project dependencies at all, recommending global/npx install instead
- "Check package.json for common issues" — flags the `eas` script name colliding with the `eas` binary in `node_modules/.bin`

Both are the exact, deliberate outcome mandated by CONTEXT.md's D-04 and RESEARCH.md's Pitfall 1 (a stale global `eas-cli@20.0.0` on this dev machine shadows bare `npx eas-cli`, confirmed live during Phase 20 research — pinning locally + an explicit npm script is the documented mitigation). This is a known, accepted tradeoff against `expo-doctor`'s generic advice, not a regression: BUILD-01's "expo-doctor reports 0 failing checks" truth was independently satisfied and verified *before* Task 2 ran (see Task 1 commit `2426e89`), and Task 2's own acceptance criteria (devDependency pinned, `eas` script present, `npm run eas -- --version` >= 21.0.3) all pass. No further action needed; this is expected state going into Phase 20's throwaway build (20-02) and beyond.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- BUILD-01 is satisfied: `expo-doctor` was clean (20/20) immediately after Task 1, and `expo install --check` remains clean after Task 2
- `npm run eas -- <subcommand>` is confirmed working (`npm run eas -- --version` → `eas-cli/21.1.0`), unblocking the Wave 2 throwaway `eas build:configure`/`eas build` plan (20-02), which must invoke EAS exclusively via `npm run eas -- ...` per the established pattern
- `app.json`'s `ios.bundleIdentifier` (D-01) and `eas.json` (D-02) remain untouched by this plan — next plan's responsibility

---
*Phase: 20-native-build-risk-front-loading*
*Completed: 2026-07-23*

## Self-Check: PASSED

- FOUND: commit 2426e89 (Task 1)
- FOUND: commit 7d1e4cb (Task 2)
- FOUND: .planning/phases/20-native-build-risk-front-loading/20-01-SUMMARY.md
- FOUND: "eas-cli" in package.json
