---
phase: 21-release-identity-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app.json]
autonomous: true
requirements: [IDENT-01, IDENT-02, IDENT-03, IDENT-04]

must_haves:
  truths:
    - "app.json ios.bundleIdentifier reads exactly com.avram.aruh.lafa"
    - "app.json slug and scheme both read lafa"
    - "app.json ios.buildNumber is the string 1 and version is unchanged at 1.0.0"
    - "D-01: extra.eas.projectId is unchanged and still resolves to the existing EAS project by ID (kept, not re-registered)"
    - "D-02: the expected server-slug vs. local-slug mismatch is verified read-only and documented as a carry-forward finding for Phase 24, not silently forced"
    - "D-03: no eas build/submit was run this phase — verification is config-only"
  artifacts:
    - path: "app.json"
      provides: "Locked release identity fields (slug, scheme, ios.buildNumber, ios.bundleIdentifier)"
      contains: "\"slug\": \"lafa\""
  key_links:
    - from: "app.json extra.eas.projectId"
      to: "EAS project 88aa092c-033c-4bcc-bf53-450c721977e8"
      via: "eas project:info --json --non-interactive (read-only lookup by ID)"
      pattern: "88aa092c-033c-4bcc-bf53-450c721977e8"
---

<objective>
Lock the app's release identity in `app.json` — change `slug` and `scheme` to
`lafa`, add `ios.buildNumber` `"1"`, and confirm `ios.bundleIdentifier`
(`com.avram.aruh.lafa`, set in Phase 20) and `version` (`1.0.0`) are unchanged.
Then run a read-only EAS project check to confirm the existing `projectId`
still resolves and to explicitly document the expected server-slug vs.
local-slug mismatch as a carry-forward finding for Phase 24.

Purpose: The release identity must be final and internally consistent before
any "real" (non-throwaway) build or App Store Connect record is created
against it (Phase 24). This is the roadmap's cheap window to lock it.
Output: Edited `app.json` (four fields) and a documented IDENT-04 finding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-release-identity-lock/21-CONTEXT.md
@.planning/phases/21-release-identity-lock/21-RESEARCH.md
@.planning/phases/21-release-identity-lock/21-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit app.json release identity fields</name>
  <files>app.json</files>
  <read_first>
    - app.json (current state — see current slug "portuguese-verb-mobile", scheme "portugueseverbmobile", version "1.0.0", ios.bundleIdentifier already "com.avram.aruh.lafa", no ios.buildNumber key yet)
    - .planning/phases/21-release-identity-lock/21-PATTERNS.md (the exact target diff for this file and the "one field-change = one atomic commit, feat prefix" precedent)
    - .planning/phases/21-release-identity-lock/21-CONTEXT.md (integration points confirming target values)
  </read_first>
  <action>
    In app.json's `expo` object, make exactly these edits:
    1. Change `slug` from `"portuguese-verb-mobile"` to `"lafa"` (IDENT-02).
    2. Change `scheme` from `"portugueseverbmobile"` to `"lafa"` (IDENT-02).
    3. Add a new key `buildNumber` inside the `ios` object with the STRING
       value `"1"` — not the number 1 (IDENT-03).
    4. Confirm `ios.bundleIdentifier` already equals `"com.avram.aruh.lafa"`
       and leave it untouched (IDENT-01, set in Phase 20 — confirm only).
    5. Confirm `version` stays `"1.0.0"` — do NOT modify it (IDENT-03).
    Do NOT touch `extra.eas.projectId`, `owner`, or any other field. These are
    single-field JSON edits with no sequencing risk. Follow the repo convention
    of committing CLI-generated config as-is; here all edits are hand-authored
    field changes matching the target diff in 21-PATTERNS.md.
  </action>
  <verify>
    <automated>node -e "const e=require('./app.json').expo; if(e.slug!=='lafa'||e.scheme!=='lafa')process.exit(1)" && node -e "const e=require('./app.json').expo; if(e.ios.buildNumber!=='1'||e.version!=='1.0.0')process.exit(1)" && node -e "if(require('./app.json').expo.ios.bundleIdentifier!=='com.avram.aruh.lafa')process.exit(1)" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `require('./app.json').expo.slug === 'lafa'`
    - `require('./app.json').expo.scheme === 'lafa'`
    - `require('./app.json').expo.ios.buildNumber === '1'` (string, not number)
    - `require('./app.json').expo.ios.bundleIdentifier === 'com.avram.aruh.lafa'`
    - `require('./app.json').expo.version === '1.0.0'` (unchanged)
    - `require('./app.json').expo.extra.eas.projectId === '88aa092c-033c-4bcc-bf53-450c721977e8'` (unchanged)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>slug and scheme read `lafa`, ios.buildNumber is `"1"`, bundleIdentifier and version confirmed unchanged, projectId untouched, typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify EAS project resolution and log the expected slug mismatch</name>
  <files>app.json</files>
  <read_first>
    - .planning/phases/21-release-identity-lock/21-RESEARCH.md (Code Examples: live `eas project:info` output; Common Pitfalls #1 and #2; Anti-Patterns section — which commands are forbidden this phase)
    - app.json (to read the new local slug `lafa` and the projectId to compare against)
  </read_first>
  <action>
    Run `npx eas-cli project:info --json --non-interactive`. From the returned
    JSON, read `fullName` (expected `@avram.aruh/portuguese-verb-mobile`) and
    extract its slug segment (the portion after the `/`). Compare that server
    slug segment against the new local `slug` value `lafa` in app.json. Confirm
    the returned `id` equals `88aa092c-033c-4bcc-bf53-450c721977e8` — this proves
    the projectId still resolves cleanly to the existing project (D-01/D-02).
    Expect the server slug (`portuguese-verb-mobile`) to DIFFER from the local
    slug (`lafa`) because nothing in this phase updates the server side — this
    is the correct, expected post-edit state, NOT a bug. Record this mismatch
    explicitly in this plan's SUMMARY as a known carry-forward finding for
    Phase 24 (its first real build must reconcile the server-side slug via the
    dashboard, handled by Plan 02's checkpoint). Do NOT run `eas init`,
    `eas init --force`, `eas build`, `eas submit`, `eas build:list`, or
    `eas update` — per RESEARCH Pitfall 2 and Anti-Patterns, these invoke the
    slug-consistency check and can throw or silently revert the local slug back
    to the server value, undoing IDENT-02. Do NOT attempt to "fix" the mismatch.
  </action>
  <verify>
    <automated>npx eas-cli project:info --json --non-interactive | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);if(j.id!=='88aa092c-033c-4bcc-bf53-450c721977e8')process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `eas project:info --json --non-interactive` exits 0 and returns `id` === `88aa092c-033c-4bcc-bf53-450c721977e8`
    - The returned `fullName` slug segment is compared against local slug `lafa` and the divergence (`portuguese-verb-mobile` vs `lafa`) is written into the plan SUMMARY as a Phase 24 carry-forward finding
    - `git diff app.json` shows NO change caused by the command (no CLI-side mutation of app.json)
    - No `eas init` / `eas build` / `eas submit` / `eas build:list` / `eas update` command was run
  </acceptance_criteria>
  <done>eas project:info returned the existing projectId, and the expected server-vs-local slug mismatch is documented as a Phase 24 carry-forward finding without any attempt to reconcile it via forbidden commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None applicable this phase. Only static, developer-edited build-time identity
fields in `app.json` are touched — no runtime input, no network-facing surface,
no auth/session/crypto/data-handling introduced.

## STRIDE Threat Register

Per 21-RESEARCH.md Security Domain: this phase has no applicable ASVS L1
category (V2/V3/V4/V5/V6 all N/A — no auth, session, access control, input
validation, or cryptography surface; `app.json` is static config, not
runtime-parsed external input). The one operational risk (EAS project/slug
reconciliation) is a release-tooling correctness concern, not a security
vulnerability class.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | N/A | app.json static identity fields | accept | No runtime attack surface; config-only edit, no ASVS category applies (RESEARCH Security Domain) |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (config-only edit; eas-cli already pinned in Phase 20) — nothing to audit |
</threat_model>

<verification>
- All three `node -e` config assertions pass (slug/scheme, buildNumber/version, bundleIdentifier)
- `npm run typecheck` exits 0
- `npm test` full suite unaffected (~251 tests still green — no `src/`/`app/` files touched)
- `npx expo-doctor` still reports zero issues (Phase 20 baseline; identity fields are in its scope)
- `eas project:info --json --non-interactive` returns the existing projectId; slug mismatch documented, not fixed
</verification>

<success_criteria>
- `app.json` `ios.bundleIdentifier` reads exactly `com.avram.aruh.lafa`
- `app.json` `slug` and `scheme` both read `lafa`
- `app.json` `ios.buildNumber` is `"1"` and `version` is confirmed unchanged at `1.0.0`
- `extra.eas.projectId` unchanged; `eas project:info` confirms it still resolves by ID
- The expected server-slug vs. local-slug mismatch is logged as a Phase 24 carry-forward finding
</success_criteria>

<output>
Create `.planning/phases/21-release-identity-lock/21-01-SUMMARY.md` when done.
Include the `eas project:info` output and the explicit slug-mismatch finding
for Phase 24 to inherit.
</output>
