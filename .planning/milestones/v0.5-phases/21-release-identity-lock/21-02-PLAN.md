---
phase: 21-release-identity-lock
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified: []
autonomous: false
requirements: [IDENT-04]

must_haves:
  truths:
    - "The operator has checked the expo.dev dashboard for a project-rename / slug-change option for project 88aa092c-033c-4bcc-bf53-450c721977e8"
    - "The outcome (renamed to lafa, OR no rename option exists) is logged as IDENT-04 closing evidence"
    - "If no rename option exists, the server-vs-local slug mismatch is explicitly handed to Phase 24 as a known pre-existing condition"
    - "D-01: no new EAS project is created — the existing projectId 88aa092c-033c-4bcc-bf53-450c721977e8 is kept regardless of dashboard-rename outcome"
    - "D-03: this checkpoint runs no eas build/submit — reconciliation is a dashboard/human action only, build verification stays deferred to Phase 24"
  artifacts:
    - path: ".planning/phases/21-release-identity-lock/21-02-SUMMARY.md"
      provides: "IDENT-04 closing evidence: dashboard-rename outcome + Phase 24 carry-forward disposition"
      contains: "IDENT-04"
  key_links:
    - from: "expo.dev dashboard project settings"
      to: "EAS project 88aa092c-033c-4bcc-bf53-450c721977e8"
      via: "operator manual check for a slug/rename affordance"
      pattern: "88aa092c-033c-4bcc-bf53-450c721977e8"
---

<objective>
Close IDENT-04 by having the operator check the expo.dev dashboard for a
project-rename / slug-change option (the only supported way to reconcile the
server-side slug to `lafa` — no CLI/API mutation exists per research). Log the
outcome as IDENT-04's closing evidence. This checkpoint depends on Plan 01
having landed the `app.json` slug edit and run `eas project:info`, which
surfaced the expected server-vs-local slug mismatch.

Purpose: `eas project:info` succeeding alone is NOT sufficient closure for
IDENT-04 (it skips the slug-consistency check). The reconciliation question —
can the server slug be renamed to match `lafa` while keeping the same
projectId? — is a dashboard-only, human-judgment operation that must be
resolved or explicitly deferred to Phase 24.
Output: Logged dashboard-rename outcome and Phase 24 disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-release-identity-lock/21-CONTEXT.md
@.planning/phases/21-release-identity-lock/21-RESEARCH.md
@.planning/phases/21-release-identity-lock/21-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator checks expo.dev dashboard for a project-rename option</name>
  <files>.planning/phases/21-release-identity-lock/21-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/21-release-identity-lock/21-01-SUMMARY.md (the logged `eas project:info` output and the recorded server-vs-local slug mismatch finding)
    - .planning/phases/21-release-identity-lock/21-RESEARCH.md (Open Questions #1: the unresolved dashboard-rename affordance; Architectural Responsibility Map: EAS slug reconciliation is dashboard/human-action tier)
  </read_first>
  <action>
    Pause for the operator to check the expo.dev dashboard for a project-rename
    / slug-change affordance on project `88aa092c-033c-4bcc-bf53-450c721977e8`,
    then record the outcome in 21-02-SUMMARY.md as IDENT-04 closing evidence.
    See `<what-built>`, `<how-to-verify>`, and `<resume-signal>` for the exact
    operator steps and the two acceptable outcomes. Claude does not automate
    this — no CLI/API can rename a registered EAS project's server-side slug
    (RESEARCH Architectural Responsibility Map); it is a dashboard-only human
    action. After the operator reports back, log their outcome verbatim.
  </action>
  <what-built>
    Plan 01 changed `app.json` `slug`/`scheme` to `lafa` and ran
    `eas project:info --json --non-interactive`, which confirmed the existing
    projectId `88aa092c-033c-4bcc-bf53-450c721977e8` still resolves but showed
    the server-side `fullName` is still `@avram.aruh/portuguese-verb-mobile`
    (server slug NOT yet reconciled to `lafa`). No CLI/API command can rename
    the server-side slug — only the expo.dev web dashboard can (if at all).
  </what-built>
  <how-to-verify>
    1. Log in to https://expo.dev as `avram.aruh`.
    2. Open the project with ID `88aa092c-033c-4bcc-bf53-450c721977e8`
       (currently shown as `@avram.aruh/portuguese-verb-mobile`).
    3. Open the project's Settings and look for a rename / slug-change option.
    4a. IF a rename option exists: rename the project slug to `lafa`, then
        re-run `npx eas-cli project:info --json --non-interactive` and confirm
        the returned `fullName` slug segment is now `lafa` while `id` is still
        `88aa092c-033c-4bcc-bf53-450c721977e8`. Report "renamed to lafa,
        fullName now matches, projectId unchanged".
    4b. IF NO rename option exists: report "no dashboard rename option found".
        This means the server-vs-local slug mismatch is a known pre-existing
        condition Phase 24 must inherit (its first real `eas build`/`eas submit`
        may throw on the slug-consistency check — see RESEARCH Open Question 2).
    Do NOT run `eas init`, `eas init --force`, or create a new EAS project —
    D-01 keeps the existing projectId; a new project would orphan Phase 20's
    proof build and re-provision Apple credentials for no benefit.
  </how-to-verify>
  <verify>
    <automated>MISSING — dashboard slug-rename has no CLI/API; if the operator reports "renamed to lafa", re-run `npx eas-cli project:info --json --non-interactive` and confirm the returned fullName slug segment is `lafa` and id is `88aa092c-033c-4bcc-bf53-450c721977e8`; if "no rename option", closing evidence is the logged finding in 21-02-SUMMARY.md</automated>
    <human-check>Operator reports one of the two outcomes and it is recorded in 21-02-SUMMARY.md</human-check>
  </verify>
  <acceptance_criteria>
    - The operator has reported one of two outcomes: (a) renamed to `lafa` with
      `eas project:info` fullName now matching and projectId unchanged, or
      (b) no rename option exists.
    - The chosen outcome is written into 21-02-SUMMARY.md as IDENT-04 closing
      evidence.
    - If outcome (b), the SUMMARY explicitly flags the slug mismatch as a
      Phase 24 carry-forward blocker/condition (not a regression Phase 24
      introduces).
    - No new EAS project was created; projectId remains `88aa092c-033c-4bcc-bf53-450c721977e8`.
  </acceptance_criteria>
  <done>Operator's dashboard-rename outcome is logged in 21-02-SUMMARY.md as IDENT-04 closing evidence, with a clear Phase 24 disposition, and projectId is unchanged.</done>
  <resume-signal>Report the outcome: "renamed to lafa" (with re-run confirmation) OR "no rename option exists" — then type "approved" to continue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None applicable — this plan performs no code or config mutation (a human
dashboard check plus a read-only CLI re-run at most). No runtime surface.

## STRIDE Threat Register

Per 21-RESEARCH.md Security Domain: no applicable ASVS L1 category. The EAS
slug reconciliation is a release-tooling correctness concern, not a security
vulnerability class.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | N/A | expo.dev dashboard project settings | accept | Operator-only read/rename action against an authenticated Expo account; no app runtime surface (RESEARCH Security Domain) |
</threat_model>

<verification>
- Operator has logged into expo.dev and inspected project settings for the target projectId
- Outcome (renamed vs. no-option) is recorded in 21-02-SUMMARY.md as IDENT-04 closing evidence
- If renamed: `eas project:info --json --non-interactive` fullName slug segment now reads `lafa`, id unchanged
- If not renamed: mismatch explicitly handed to Phase 24
</verification>

<success_criteria>
- `extra.eas.projectId` has been checked for a pre-existing value under the old slug and reconciled OR its mismatch documented (no stale/duplicate project id remains; projectId unchanged)
- IDENT-04 closing evidence captured with a clear Phase 24 disposition
</success_criteria>

<output>
Create `.planning/phases/21-release-identity-lock/21-02-SUMMARY.md` when done,
recording the dashboard-rename outcome as IDENT-04 closing evidence and the
Phase 24 carry-forward disposition.
</output>
