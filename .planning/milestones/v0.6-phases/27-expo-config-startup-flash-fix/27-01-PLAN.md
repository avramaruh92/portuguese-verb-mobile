---
phase: 27-expo-config-startup-flash-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
  - app/_layout.tsx
  - assets/images/android-icon-background.png
autonomous: false
requirements: [CONFIG-01, CONFIG-02, CONFIG-03, CONFIG-04]

must_haves:
  truths:
    - "Cold launch shows the warm Lafa background (#FFF9F6) on the splash screen, never Expo blue (#208AEF)"
    - "After the splash screen dismisses, no white/default-colored flash appears before the first screen paints"
    - "The Android adaptive icon renders the app mark on a solid warm background with no separate background image layer"
    - "The app renders in light appearance regardless of the device's system dark-mode setting"
    - "Every screen's native header uses the warm background with dark ink title/back button, and dark status bar content"
  artifacts:
    - path: "app.json"
      provides: "Splash, adaptive icon, and appearance config using brand values"
      contains: "#FFF9F6"
    - path: "app/_layout.tsx"
      provides: "Root Stack theming, dark status bar, and runtime root background"
      contains: "SystemUI"
  key_links:
    - from: "app/_layout.tsx"
      to: "src/theme/tokens.ts"
      via: "named import of colors"
      pattern: "from \"../src/theme/tokens\""
    - from: "app/_layout.tsx"
      to: "expo-system-ui"
      via: "setBackgroundColorAsync in a mount effect"
      pattern: "SystemUI\\.setBackgroundColorAsync"
    - from: "app.json"
      to: "assets/images/splash-icon.png"
      via: "expo-splash-screen plugin image key"
      pattern: "splash-icon\\.png"
---

<objective>
Eliminate the Expo-blue splash and the unbranded post-splash flash on cold launch by
applying the locked Lafa brand values to `app.json` and wiring the root layout
(`app/_layout.tsx`) to the brand tokens.

Purpose: The first thing a user sees must be Lafa, not Expo's default template chrome.
This is the last config gap between the shipped brand assets (Phase 25) / tokens
(Phase 26) and a branded cold-start experience.
Output: Updated `app.json`, updated `app/_layout.tsx`, deleted orphaned
`assets/images/android-icon-background.png`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-expo-config-startup-flash-fix/27-CONTEXT.md
@.planning/phases/27-expo-config-startup-flash-fix/27-PATTERNS.md
@src/theme/tokens.ts
@app/_layout.tsx
@app.json

<interfaces>
<!-- Contracts the executor needs. No codebase exploration required. -->

From src/theme/tokens.ts (Phase 26, already shipped — do NOT modify):
  colors.background = "#FFF9F6"   // warm Lafa canvas
  colors.text       = "#24201E"   // ink
  colors.surface    = "#F1EFED"
  (also: primary #F2643E, primarySoft #FDE7DF, pressed #C94A2D, info #36799A,
   success #1F7F66, error #D64545, textSecondary #746D69)

From expo-status-bar (installed, zero prior usage in this repo):
  <StatusBar style="dark" />

From expo-system-ui (installed, zero prior usage in this repo):
  SystemUI.setBackgroundColorAsync(color: string): Promise<void>

From expo-router:
  <Stack screenOptions={{ headerShown, headerStyle, headerTintColor, headerShadowVisible, contentStyle }} />

Per-screen non-interference (confirmed in 27-PATTERNS.md): none of `app/index.tsx`,
`app/quiz.tsx`, `app/results.tsx` set `headerStyle`, `headerTintColor`,
`headerShadowVisible`, or `contentStyle` in their `Stack.Screen` options — root
`screenOptions` propagates to all three without editing them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply brand values to app.json and delete the orphaned Android background asset</name>
  <files>app.json, assets/images/android-icon-background.png</files>
  <read_first>
    - app.json (full file — the exact current shape of the `plugins` array, `android.adaptiveIcon`, and top-level `userInterfaceStyle`)
    - .planning/REQUIREMENTS.md (CONFIG-01, CONFIG-02, CONFIG-03 — locked target values)
    - .planning/phases/27-expo-config-startup-flash-fix/27-CONTEXT.md (D-01)
    - .planning/phases/27-expo-config-startup-flash-fix/27-PATTERNS.md (`app.json` section — current-state JSON excerpt and editing instructions)
    - src/theme/tokens.ts (confirm `colors.background` is `#FFF9F6`)
  </read_first>
  <action>
    Edit `app.json` in place (do not reformat unrelated keys, do not reorder the file):

    1. CONFIG-01 — in the `expo-splash-screen` plugin tuple inside `expo.plugins`, change
       `backgroundColor` from `"#208AEF"` to `"#FFF9F6"` and change `imageWidth` from `76`
       to `160`. Leave `image` as `"./assets/images/splash-icon.png"` (Phase 25 already
       regenerated that asset — do not repoint or regenerate it).
    2. CONFIG-02 — in `expo.android.adaptiveIcon`, change `backgroundColor` from
       `"#E6F4FE"` to `"#FFF9F6"`, and delete the `backgroundImage` key entirely. Do not
       set it to `null` or `""` — remove the key. Leave `foregroundImage` and
       `monochromeImage` untouched.
    3. CONFIG-03 — change top-level `expo.userInterfaceStyle` from `"automatic"` to
       `"light"`.
    4. D-01 — delete `assets/images/android-icon-background.png` from the repo using
       `git rm assets/images/android-icon-background.png` (it is now unreferenced; this
       matches the Phase 25 asset-cleanup precedent). Before deleting, run
       `grep -rn "android-icon-background" --include=* . --exclude-dir=node_modules --exclude-dir=.git`
       and confirm the only remaining hits are inside `.planning/` docs (historical
       references are fine); if any source/config file still references it, stop and
       report rather than deleting.

    Leave `expo-status-bar` as a bare plugin string in `expo.plugins` — CONFIG-04's
    dark-content choice is handled declaratively in `app/_layout.tsx` (D-04), not via
    plugin options. Do not add a `splash` key at the `ios`/`android` level; the
    `expo-splash-screen` plugin block is the only splash config surface for this project.
  </action>
  <acceptance_criteria>
    - `app.json` parses as valid JSON.
    - The `expo-splash-screen` plugin options object equals exactly:
      `backgroundColor: "#FFF9F6"`, `image: "./assets/images/splash-icon.png"`, `imageWidth: 160`.
    - `expo.android.adaptiveIcon.backgroundColor === "#FFF9F6"` and
      `"backgroundImage" in expo.android.adaptiveIcon === false`.
    - `expo.userInterfaceStyle === "light"`.
    - The string `#208AEF` does not appear anywhere in `app.json`; neither does `#E6F4FE`.
    - `assets/images/android-icon-background.png` no longer exists on disk and is staged
      as deleted in git.
    - `assets/images/splash-icon.png`, `android-icon-foreground.png`, and
      `android-icon-monochrome.png` still exist and are unmodified.
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const a=require("./app.json").expo;const p=a.plugins.find(x=>Array.isArray(x)&&x[0]==="expo-splash-screen")[1];const ai=a.android.adaptiveIcon;const fs=require("fs");const raw=fs.readFileSync("app.json","utf8");const ok=p.backgroundColor==="#FFF9F6"&&p.imageWidth===160&&p.image==="./assets/images/splash-icon.png"&&ai.backgroundColor==="#FFF9F6"&&!("backgroundImage" in ai)&&a.userInterfaceStyle==="light"&&!raw.includes("#208AEF")&&!raw.includes("#E6F4FE")&&!fs.existsSync("assets/images/android-icon-background.png")&&fs.existsSync("assets/images/splash-icon.png");if(!ok){console.error("CONFIG-01/02/03 assertions failed");process.exit(1)}console.log("app.json OK")'</automated>
  </verify>
  <done>All three CONFIG-01/02/03 assertions pass via the node check above, and the orphaned Android background PNG is deleted and staged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire root layout theming, dark status bar, and runtime root background</name>
  <files>app/_layout.tsx</files>
  <read_first>
    - app/_layout.tsx (full 16-line current file)
    - src/theme/tokens.ts (exact `colors.background` / `colors.text` values)
    - .planning/REQUIREMENTS.md (CONFIG-04 — names all four Stack options explicitly)
    - .planning/phases/27-expo-config-startup-flash-fix/27-CONTEXT.md (D-02, D-03, D-04, D-05)
    - .planning/phases/27-expo-config-startup-flash-fix/27-PATTERNS.md (`app/_layout.tsx` section — import order, effect-per-concern, StatusBar placement)
    - .planning/codebase/CONVENTIONS.md (import ordering, relative-path imports, no path aliases)
  </read_first>
  <action>
    Extend `app/_layout.tsx` in place — it stays a single default-exported `RootLayout`
    component. Do not touch `app/index.tsx`, `app/quiz.tsx`, or `app/results.tsx`.

    1. Add imports, preserving the file's existing external-then-relative ordering:
       `StatusBar` from `"expo-status-bar"`, namespace import `SystemUI` from
       `"expo-system-ui"`, and named `colors` from `"../src/theme/tokens"`. Reference
       tokens (`colors.background`, `colors.text`) — never inline the hex literals in
       this file, per the project token convention.
    2. CONFIG-04 / D-02 / D-03 — extend the existing `Stack` `screenOptions` object
       (keep `headerShown: true`, add keys; do not replace the object wholesale):
       `headerStyle: { backgroundColor: colors.background }`,
       `headerTintColor: colors.text`,
       `headerShadowVisible: false`,
       `contentStyle: { backgroundColor: colors.background }`.
       `contentStyle` is what removes the white screen-body flash between splash dismissal
       and first paint; `headerShadowVisible: false` removes the default hairline so the
       header reads as one continuous warm surface. Both are named in CONFIG-04 and are
       required even though CONTEXT.md's D-02/D-03 only discussed the two color keys.
    3. CONFIG-04 / D-04 — render `<StatusBar style="dark" />` exactly once, as a sibling
       of `<Stack />` inside `SafeAreaProvider`, placed before `<Stack />`.
    4. CONFIG-04 / D-05 — add a second, separate `useEffect(..., [])` (do NOT merge into
       the existing `prefetch()` effect) that calls
       `SystemUI.setBackgroundColorAsync(colors.background)`. Append a `.catch(() => {})`
       to the returned promise so a native failure can never surface as an unhandled
       rejection — this mirrors the codebase's existing fire-and-forget posture
       (`prefetch()` in `src/dataset/source.ts` swallows failures and falls back silently)
       and CONTEXT.md leaves this choice to the executor. Root background color is
       cosmetic; failing loudly would be worse than failing silently.

    Keep the existing `prefetch()` effect first and unchanged.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `npm run lint` reports no new errors for `app/_layout.tsx`.
    - `npm test` passes (251+ existing tests, no regressions).
    - `app/_layout.tsx` contains `import { StatusBar } from "expo-status-bar"`,
      `import * as SystemUI from "expo-system-ui"`, and
      `import { colors } from "../src/theme/tokens"`.
    - `app/_layout.tsx` contains exactly two `useEffect(` call sites, the first still
      calling `prefetch()`, the second calling `SystemUI.setBackgroundColorAsync(colors.background)`.
    - `app/_layout.tsx` contains exactly one `<StatusBar style="dark"` occurrence, inside
      `SafeAreaProvider`.
    - The `Stack` `screenOptions` object contains all five keys: `headerShown`,
      `headerStyle`, `headerTintColor`, `headerShadowVisible`, `contentStyle`.
    - Zero raw hex literals (`#`) appear in `app/_layout.tsx`.
    - `src/theme/tokens.ts`, `app/index.tsx`, `app/quiz.tsx`, `app/results.tsx` are unmodified.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm test && node -e 'const s=require("fs").readFileSync("app/_layout.tsx","utf8");const need=["expo-status-bar","expo-system-ui","../src/theme/tokens","headerShadowVisible","contentStyle","headerTintColor: colors.text","colors.background","SystemUI.setBackgroundColorAsync"];const missing=need.filter(t=>!s.includes(t));const effects=(s.match(/useEffect\(/g)||[]).length;const sb=(s.match(/<StatusBar style="dark"/g)||[]).length;if(missing.length||effects!==2||sb!==1||s.includes("#")){console.error({missing,effects,sb,hexLiteral:s.includes("#")});process.exit(1)}console.log("_layout OK")'</automated>
  </verify>
  <done>Typecheck, lint, and the full Jest suite pass; the source assertions above hold; CONFIG-04's four Stack options plus StatusBar and expo-system-ui wiring are all present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm branded launch and header chrome on device</name>
  <what-built>
    `app.json` now declares a `#FFF9F6` splash background with a 160pt splash mark and a
    warm solid Android adaptive-icon background (no background image layer), and forces
    light appearance. `app/_layout.tsx` now themes every native header with the warm
    background + dark ink, hides the header hairline, paints the screen body warm via
    `contentStyle`, renders a dark-content status bar, and sets the runtime root
    background through `expo-system-ui`.
  </what-built>
  <how-to-verify>
    1. Run `npm start` and open the app (iOS simulator or device).
    2. Confirm the status bar shows DARK icons/text (not white) on all three screens.
    3. Confirm each screen's native header is warm off-white (`#FFF9F6`) with a dark
       title and dark back arrow, and has NO grey hairline/shadow under it.
    4. Confirm no white band or flash appears between the header and the screen body,
       and that a screen body area with no content is warm off-white, not pure white.
    5. Toggle the device to system Dark Mode and relaunch — the app must still render in
       light appearance (warm background, dark text), unchanged.
    6. NOTE — the splash screen itself (`#FFF9F6` + 160pt mark) will NOT render from
       `npm start` in Expo Go; splash config only takes effect in a prebuild/EAS build.
       Confirming the actual splash color end-to-end is Phase 29's manual release-build
       check. Do not treat an Expo-blue splash in Expo Go as a failure of this phase —
       verify the `app.json` values are correct instead (Task 1's automated check covers
       this).
  </how-to-verify>
  <acceptance_criteria>
    - Status bar content is dark on all three screens.
    - Header background is warm off-white with dark title/back arrow and no hairline.
    - No white flash or white body area is visible after launch.
    - Forcing system Dark Mode does not change the app's appearance.
  </acceptance_criteria>
  <files>none (verification only — no files modified)</files>
  <action>Pause execution and present the verification steps above to the developer. Do not modify any files in this task. If the developer reports a problem, capture the exact screen and observed appearance and fix it in `app/_layout.tsx` (header/status-bar/content styling) or `app.json` (appearance/splash config) before re-requesting approval.</action>
  <verify>
    <human-check>Developer confirms dark status bar, warm hairline-free headers, no white flash, and unchanged appearance under system Dark Mode.</human-check>
  </verify>
  <done>Developer replies "approved", or reported issues are fixed and re-approved.</done>
  <resume-signal>Type "approved" or describe what looked wrong (screen + what you saw)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build config → native binary | `app.json` values are compiled into the shipped app; no runtime input crosses here |
| app → OS UI surfaces | `expo-system-ui` / `expo-status-bar` set OS-level presentation attributes with hardcoded, in-repo constants |

No network, auth, persistence, or user-input boundary is touched by this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | `app.json` build config | mitigate | All values are literal constants committed to git and asserted by Task 1's automated node check; no dynamic/env-sourced config is introduced |
| T-27-02 | Denial of Service | `SystemUI.setBackgroundColorAsync` at mount | mitigate | Call is fire-and-forget with `.catch(() => {})` and runs in its own effect — it cannot reject unhandled, block the `prefetch()` effect, or prevent first paint |
| T-27-03 | Information Disclosure | deleted `android-icon-background.png` | accept | Asset is a decorative brand PNG containing no secrets; git history retention is acceptable |
| T-27-04 | Elevation of Privilege | `userInterfaceStyle: "light"` | accept | Appearance lock grants no capability and touches no permission surface |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — `expo-status-bar` and `expo-system-ui` are already-present dependencies (verify with `grep -n "expo-status-bar\|expo-system-ui" package.json` before starting; if either is absent, STOP and escalate rather than installing) |
</threat_model>

<verification>
1. `node -e` app.json assertion from Task 1 passes (CONFIG-01, CONFIG-02, CONFIG-03).
2. `npm run typecheck` exits 0.
3. `npm run lint` reports no new errors.
4. `npm test` passes with no regressions.
5. `grep -rn "android-icon-background" --include=*.json --include=*.ts --include=*.tsx . --exclude-dir=node_modules` returns no hits.
6. `git status` shows exactly three changed paths: `app.json` (modified), `app/_layout.tsx` (modified), `assets/images/android-icon-background.png` (deleted).
7. Human verification checkpoint approved (Task 3).
</verification>

<success_criteria>
- `app.json`'s `expo-splash-screen` plugin: `backgroundColor "#FFF9F6"`, `image "./assets/images/splash-icon.png"`, `imageWidth 160` (CONFIG-01).
- `app.json`'s `android.adaptiveIcon`: `backgroundColor "#FFF9F6"`, no `backgroundImage` key; the orphaned PNG is deleted from the repo (CONFIG-02, D-01).
- `app.json`'s `userInterfaceStyle` is `"light"` (CONFIG-03).
- `app/_layout.tsx` sets Stack `contentStyle`, `headerStyle`, `headerTintColor`, and `headerShadowVisible` from `src/theme/tokens.ts`, renders `<StatusBar style="dark" />`, and calls `SystemUI.setBackgroundColorAsync(colors.background)` in a dedicated mount effect (CONFIG-04, D-02..D-05).
- No hardcoded hex literals introduced in `app/_layout.tsx`; no old-palette hex (`#208AEF`, `#E6F4FE`) remains in `app.json`.
- Full test suite and typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/27-expo-config-startup-flash-fix/27-01-SUMMARY.md` when done
</output>
