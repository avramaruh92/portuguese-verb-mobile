---
phase: 25-brand-asset-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/brand/lafa-icon.svg
  - assets/brand/lafa-logo.svg
  - assets/brand/lafa-logo-v2.svg
  - assets/brand/lafa-logo-concept.png
  - assets/brand/lafa-logo-v2-concept.png
  - scripts/generate-brand-assets.ts
  - assets/images/icon.png
  - assets/images/favicon.png
  - assets/images/splash-icon.png
  - assets/images/android-icon-foreground.png
  - assets/images/android-icon-monochrome.png
autonomous: false
requirements: [BRAND-01, BRAND-02, BRAND-03]

must_haves:
  truths:
    - "D-01: The only brand SVG in assets/brand/ is lafa-icon.svg (renamed from Lafa_final_logo.svg)"
    - "D-02: scripts/generate-brand-assets.ts's extraction logic is rewritten around the new two-shape (background rect + mark paths) structure, not patched onto the old lafa-logo-v2.svg-shaped logic"
    - "D-04: No live code or config file references lafa-logo.svg, lafa-logo-v2.svg, or either concept PNG (all four git rm'd outright)"
    - "Running `npm run generate-assets` succeeds and rewrites all five PNG outputs from lafa-icon.svg"
    - "icon.png is 1024x1024 with no alpha channel and square (unrounded) corners"
    - "D-03: icon.png's background is flattened to a plain #FFF9F6-filled square (the SVG's baked-in rx=\"230\" rounding is not preserved)"
    - "favicon.png is 48x48 with no alpha channel"
    - "splash-icon.png contains the orange mark on a transparent background with no background rectangle"
    - "D-05: android-icon-foreground.png is 1024x1024, transparent, with the mark centred and sized inside the ~66% Android safe zone"
    - "D-06: android-icon-monochrome.png is 1024x1024, transparent, and the mark is a solid white silhouette (no orange) per Android 13+ themed-icon convention"
    - "D-07: No separate brand guide document is created — the SVG's own colors already match the locked v0.6 palette, so this decision requires no artifact"
  artifacts:
    - path: "assets/brand/lafa-icon.svg"
      provides: "Sole brand source SVG consumed by the generator"
      contains: 'fill="#F2643E"'
    - path: "scripts/generate-brand-assets.ts"
      provides: "Five-output brand asset generator driven by lafa-icon.svg"
      contains: 'assets/brand/lafa-icon.svg'
      min_lines: 120
    - path: "assets/images/icon.png"
      provides: "1024x1024 opaque app icon"
    - path: "assets/images/favicon.png"
      provides: "48x48 opaque web favicon"
    - path: "assets/images/splash-icon.png"
      provides: "Transparent mark-only splash image"
    - path: "assets/images/android-icon-foreground.png"
      provides: "Transparent adaptive-icon foreground, safe-zone centred"
    - path: "assets/images/android-icon-monochrome.png"
      provides: "Transparent white silhouette for Android 13+ themed icons"
  key_links:
    - from: "scripts/generate-brand-assets.ts"
      to: "assets/brand/lafa-icon.svg"
      via: "SOURCE_SVG_PATH constant read via readFileSync"
      pattern: 'SOURCE_SVG_PATH\s*=\s*"assets/brand/lafa-icon\.svg"'
    - from: "scripts/generate-brand-assets.ts"
      to: "assets/images/*.png"
      via: "writeFileSync of five rendered buffers"
      pattern: 'writeFileSync\('
    - from: "package.json"
      to: "scripts/generate-brand-assets.ts"
      via: "npm run generate-assets"
      pattern: 'node scripts/generate-brand-assets\.ts'
---

<objective>
Make `assets/brand/lafa-icon.svg` the sole brand source, delete the old AI-generated
brand sources/concepts, and rewrite `scripts/generate-brand-assets.ts` so a single
`npm run generate-assets` run produces all five app assets to spec.

Purpose: closes BRAND-01/02/03 and unblocks Phase 27 (`app.json` splash + adaptive
icon config) and Phase 28 (Setup-screen icon usage), which both consume the
regenerated PNGs.
Output: renamed source SVG, four deleted legacy assets, rewritten generator script,
five regenerated PNGs in `assets/images/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-brand-asset-pipeline/25-CONTEXT.md
@.planning/phases/25-brand-asset-pipeline/25-RESEARCH.md
@.planning/phases/25-brand-asset-pipeline/25-PATTERNS.md
@.planning/phases/25-brand-asset-pipeline/25-VALIDATION.md
@scripts/generate-brand-assets.ts

<interfaces>
<!-- Contracts the executor needs. No codebase exploration required. -->

Source SVG structure (verified against the real file this session):
- Root element: `<svg width="1024" height="1024" viewBox="0 0 1024 1024" fill="none" xmlns="http://www.w3.org/2000/svg">`
- One `<g clip-path="url(#clip0_2_4)">` containing, in order: the background rect,
  then exactly two `<path ... fill="#F2643E"/>` mark elements
- Background rect literal (exact, one occurrence): `<rect width="1024" height="1024" rx="230" fill="#FFF9F6"/>`
- A trailing `<defs><clipPath id="clip0_2_4"><rect width="1024" height="1024" fill="white"/></clipPath></defs>`
  block — the `clip-path="url(#clip0_2_4)"` reference on the `<g>` resolves against it,
  so `<defs>` MUST be preserved in every derived document or the mark will not render.
- `fill="#F2643E"` occurs exactly 2 times in the whole file.

Verified mark bounding box (background rect stripped, 1024-unit coordinate space):
`{ x: 156.57, y: 185.0, width: 693.43, height: 709.0 }` — do not hardcode these numbers;
compute them at runtime with `Resvg.getBBox()`.

Libraries already installed (no new dependencies):
- `@resvg/resvg-js` 2.6.2 — `new Resvg(svgString, { fitTo: { mode: "width" | "height", value: number }, background?: string })`,
  `.render()` -> `{ width, height, asPng(): Buffer }`, `.getBBox()` -> `{ x, y, width, height } | undefined`
- `sharp` 0.35.3 — `sharp(buffer).removeAlpha().png().toBuffer()`,
  `sharp(buffer).resize(w, h, { kernel: sharp.kernel.lanczos3 })`,
  `sharp({ create: { width, height, channels: 4, background: { r, g, b, alpha: 0 } } }).composite([{ input, gravity: "center" }]).png().toBuffer()`

Invocation contract (unchanged): `package.json` -> `"generate-assets": "node scripts/generate-brand-assets.ts"`.
Node v25 runs the `.ts` file directly via native type-stripping — do NOT add `ts-node`/`tsx`
or any build step.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the source SVG to lafa-icon.svg and delete the legacy brand assets</name>
  <files>assets/brand/lafa-icon.svg, assets/brand/lafa-logo.svg, assets/brand/lafa-logo-v2.svg, assets/brand/lafa-logo-concept.png, assets/brand/lafa-logo-v2-concept.png</files>
  <read_first>
    - `.planning/phases/25-brand-asset-pipeline/25-CONTEXT.md` (D-01 and D-04 — the exact rename target and the outright-delete decision)
    - Run `git status --porcelain assets/brand` and `git ls-files assets/brand` before touching anything, to confirm which files are tracked
  </read_first>
  <action>
    Per D-01, rename `assets/brand/Lafa_final_logo.svg` to `assets/brand/lafa-icon.svg`.
    IMPORTANT: `Lafa_final_logo.svg` is currently UNTRACKED in git (confirmed via
    `git ls-files assets/brand`, which lists only the four legacy files), so `git mv`
    will fail with "not under version control". Use a plain `mv` followed by
    `git add assets/brand/lafa-icon.svg`.

    Do not modify the SVG's contents in any way — byte-for-byte identical, rename only.

    Per D-04, delete the four legacy AI-generated brand assets outright with
    `git rm` (they ARE tracked): `assets/brand/lafa-logo.svg`,
    `assets/brand/lafa-logo-v2.svg`, `assets/brand/lafa-logo-concept.png`,
    `assets/brand/lafa-logo-v2-concept.png`. Do not merely decouple references —
    git history preserves them.

    Scope note per RESEARCH.md Pitfall 4: BRAND-02's "no longer referenced anywhere"
    is scoped to live code/config (`src/`, `app/`, `scripts/`, `app.json`,
    `__tests__/`). Do NOT edit `.planning/` documents — their mentions of the old
    filenames are historical records. The single live-code reference is
    `scripts/generate-brand-assets.ts` line 6 (`SOURCE_SVG_PATH`), which Task 2
    replaces; leaving it stale after Task 1 is expected and acceptable within this task.

    Do NOT touch `assets/images/android-icon-background.png` — it is Phase 27's concern.
  </action>
  <verify>
    <automated>test -f assets/brand/lafa-icon.svg &amp;&amp; test ! -e assets/brand/Lafa_final_logo.svg &amp;&amp; test $(ls assets/brand | wc -l | tr -d ' ') -eq 1 &amp;&amp; grep -c 'fill="#F2643E"' assets/brand/lafa-icon.svg | grep -qx 2 &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `assets/brand/` contains exactly one file: `lafa-icon.svg`
    - `assets/brand/lafa-icon.svg` contains the literal `<rect width="1024" height="1024" rx="230" fill="#FFF9F6"/>` exactly once and `fill="#F2643E"` exactly twice (proves the rename preserved contents)
    - `git status --porcelain assets/brand` shows `A  assets/brand/lafa-icon.svg` plus four `D ` deletions for `lafa-logo.svg`, `lafa-logo-v2.svg`, `lafa-logo-concept.png`, `lafa-logo-v2-concept.png`
    - `assets/images/android-icon-background.png` still exists and is unmodified
    - No file under `.planning/` was edited by this task
  </acceptance_criteria>
  <done>`assets/brand/lafa-icon.svg` is the only file in `assets/brand/`, staged in git, with the four legacy assets staged as deletions.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite generate-brand-assets.ts to produce all five outputs from lafa-icon.svg</name>
  <files>scripts/generate-brand-assets.ts</files>
  <read_first>
    - `scripts/generate-brand-assets.ts` (current version — the self-analog; read it in full before rewriting so the constant-naming, defensive-throw, dimension-assertion and alpha-comment conventions carry forward)
    - `assets/brand/lafa-icon.svg` (the new source — confirm the exact background-rect and `<defs>`/`clipPath` markup before writing any string matcher)
    - `.planning/phases/25-brand-asset-pipeline/25-RESEARCH.md` (Patterns 1-5, Pitfalls 1-3, and the "Full pipeline skeleton" code example — authoritative for the bbox/safe-zone mechanics that have no codebase analog)
    - `.planning/phases/25-brand-asset-pipeline/25-PATTERNS.md` (which idioms from the current script to preserve verbatim)
    - `scripts/preflight.ts` (sibling script — confirms the plain-TS, `node scripts/*.ts`, no-transpile invocation convention)
  </read_first>
  <action>
    Full rewrite of `scripts/generate-brand-assets.ts` (same path, same npm script name,
    same direct-`node` invocation). The old extraction logic is retired entirely: D-02
    confirms the new source has no `<g id="icon">` wrapper, no green accent dot, and no
    stroke-based paths, so `ICON_GROUP_OPEN_TAG`, `ICON_GROUP_CLOSE_TAG`,
    `GREEN_ACCENT_DOT`, `CROP_VIEWBOX`, `SWOOP_PATH_UNCLOSED_TAIL`,
    `SWOOP_PATH_FILL_NONE_TAIL`, `normalizeSwoopFill`, `extractIconGroup`,
    `wrapInSvgRoot`, `buildIconDoc` and `buildSplashDoc` must all be removed.

    Keep the existing imports unchanged (`readFileSync`, `writeFileSync` from `node:fs`;
    `Resvg` from `@resvg/resvg-js`; default `sharp`). Add no new dependency.

    Module-top named constants (no inline magic values anywhere inside functions):
    `SOURCE_SVG_PATH = "assets/brand/lafa-icon.svg"`,
    `ICON_OUTPUT_PATH = "assets/images/icon.png"`,
    `FAVICON_OUTPUT_PATH = "assets/images/favicon.png"`,
    `SPLASH_OUTPUT_PATH = "assets/images/splash-icon.png"`,
    `ANDROID_FOREGROUND_OUTPUT_PATH = "assets/images/android-icon-foreground.png"`,
    `ANDROID_MONOCHROME_OUTPUT_PATH = "assets/images/android-icon-monochrome.png"`,
    `CANVAS_PX = 1024`, `SPLASH_WIDTH_PX = 1024`, `FAVICON_PX = 48`,
    `SAFE_ZONE_RATIO = 0.66`, `SAFE_ZONE_PX = Math.round(CANVAS_PX * SAFE_ZONE_RATIO)` (= 676),
    `ICON_BACKGROUND_HEX = "#FFF9F6"`, `TRANSPARENT_BACKGROUND = "rgba(0,0,0,0)"`,
    `MARK_FILL = 'fill="#F2643E"'`, `MONOCHROME_FILL = 'fill="#FFFFFF"'`,
    `EXPECTED_MARK_PATH_COUNT = 2`,
    `BACKGROUND_RECT = '<rect width="1024" height="1024" rx="230" fill="#FFF9F6"/>'`,
    `FLAT_BACKGROUND_RECT = '<rect width="1024" height="1024" fill="#FFF9F6"/>'` (note: NO `rx`,
    per D-03 — the flattened square is deliberate so the OS mask does not double-round).

    Preserve the codebase's defensive idiom: every extraction/transform guard throws
    `new Error(\`${SOURCE_SVG_PATH} ... — source may have changed shape\`)` rather than
    rendering something wrong silently.

    Functions to implement:

    1. `extractMarkOnly(svg: string): string` — returns the source's INNER markup with
       the background rect removed, re-wrapped later by the callers. Steps: throw if
       `BACKGROUND_RECT` is absent; throw if the count of `MARK_FILL` occurrences is not
       `EXPECTED_MARK_PATH_COUNT` (use `svg.split(MARK_FILL).length - 1` for the count —
       avoids regex escaping of the `"` and `#` characters); slice the inner markup from
       the first `>` after the root `<svg` open tag through the final `</svg>`; then
       `.replace(BACKGROUND_RECT, "")`. CRITICAL: the returned markup must retain the
       trailing `<defs>...<clipPath id="clip0_2_4">...</clipPath></defs>` block, because
       the surviving `<g clip-path="url(#clip0_2_4)">` references it — dropping `<defs>`
       makes resvg render an empty image. Add a guard that throws if the returned markup
       is empty or no longer contains `clip-path="url(`.

    2. `wrapMarkDoc(markMarkup: string, viewBox: string): string` — wraps inner markup in
       a fresh `<svg xmlns="http://www.w3.org/2000/svg" viewBox="...">` root and closes it.

    3. `buildFullIconDoc(markMarkup: string): string` — wraps `FLAT_BACKGROUND_RECT`
       followed by `markMarkup` in a `viewBox="0 0 1024 1024"` root (Pattern 2 / D-03).

    4. `getMarkBBox(markOnlyDoc: string): { x: number; y: number; width: number; height: number }`
       — constructs a `Resvg` over the FULL-canvas mark-only doc (viewBox `0 0 1024 1024`,
       background already stripped) with `fitTo: { mode: "width", value: CANVAS_PX }` and
       returns `.getBBox()`. Throw if the result is undefined, or if `width`/`height` is 0,
       or if `width >= CANVAS_PX` (that last case is RESEARCH.md Pitfall 2 — a full-canvas
       bbox means the background rect was not actually stripped and the safe-zone maths
       would silently produce a mis-scaled mark). Expected real value is roughly
       `{ x: 156.6, y: 185, width: 693.4, height: 709 }`.

    5. `renderCenteredOnSafeZone(croppedMarkDoc: string, bbox): Promise<Buffer>` —
       Pattern 3. Compute `scale = SAFE_ZONE_PX / Math.max(bbox.width, bbox.height)`;
       derive `fitTo` mode `"width"` when `bbox.width >= bbox.height` else `"height"`,
       with value `Math.round(bbox.width * scale)` or `Math.round(bbox.height * scale)`
       respectively; render with `background: TRANSPARENT_BACKGROUND`; then composite the
       rendered PNG onto a fresh transparent `CANVAS_PX` x `CANVAS_PX` canvas via
       `sharp({ create: {...} }).composite([{ input: rendered, gravity: "center" }]).png().toBuffer()`.
       Do not hand-compute left/top offsets — `gravity: "center"` owns the centring.

    6. `toMonochromeSilhouette(markDoc: string): string` — `.replaceAll(MARK_FILL, MONOCHROME_FILL)`
       (D-06). Then throw if the result still contains `MARK_FILL` or the substring
       `#F2643E`, so a partial recolour can never ship as a themed icon.

    7. `main(): Promise<void>` — single unconditional run, no CLI flags (matches current
       behaviour and BRAND-03's "running `npm run generate-assets` produces" framing):
       - read the source, `extractMarkOnly`, build the full-canvas mark doc
         (`wrapMarkDoc(mark, "0 0 1024 1024")`) and the bbox-cropped mark doc
         (`wrapMarkDoc(mark, \`${bbox.x} ${bbox.y} ${bbox.width} ${bbox.height}\`)`)
       - `icon.png`: render `buildFullIconDoc(mark)` at `fitTo width CANVAS_PX` with
         `background: ICON_BACKGROUND_HEX`; keep the existing dimension assertion that
         throws when `render.width`/`render.height` !== `CANVAS_PX`; pipe through
         `sharp(...).removeAlpha().png().toBuffer()` and carry the existing explanatory
         comment about resvg always emitting RGBA verbatim (RESEARCH.md Pitfall 1 — this
         is a previously-fixed bug, do not regress it)
       - `favicon.png`: `sharp(iconPngBuffer).resize(FAVICON_PX, FAVICON_PX, { kernel: sharp.kernel.lanczos3 }).removeAlpha().png().toBuffer()`,
         downscaled from the already-rendered icon buffer (Pattern 5) so it is pixel-consistent with `icon.png`
       - `splash-icon.png`: render the FULL-canvas mark doc (not the cropped one — this
         preserves the mark's composition within the icon artwork) at
         `fitTo width SPLASH_WIDTH_PX` with `background: TRANSPARENT_BACKGROUND`, write
         `render.asPng()` directly with NO `removeAlpha()` (the transparency is required)
       - `android-icon-foreground.png`: `renderCenteredOnSafeZone(croppedMarkDoc, bbox)`
       - `android-icon-monochrome.png`: `renderCenteredOnSafeZone(toMonochromeSilhouette(croppedMarkDoc), bbox)`
       - call `main()` at module scope exactly as the current file does

    Do NOT bake the Task 1 file deletions into this script — asset removal is a one-time
    git operation, not regenerable build logic.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; grep -q 'assets/brand/lafa-icon.svg' scripts/generate-brand-assets.ts &amp;&amp; ! grep -qE 'lafa-logo|GREEN_ACCENT_DOT|ICON_GROUP_OPEN_TAG|CROP_VIEWBOX|normalizeSwoopFill' scripts/generate-brand-assets.ts &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'SOURCE_SVG_PATH' scripts/generate-brand-assets.ts` shows it assigned to `"assets/brand/lafa-icon.svg"` and nothing else
    - `grep -cE 'lafa-logo|GREEN_ACCENT_DOT|ICON_GROUP_OPEN_TAG|ICON_GROUP_CLOSE_TAG|CROP_VIEWBOX|SWOOP_PATH|normalizeSwoopFill|extractIconGroup' scripts/generate-brand-assets.ts` returns 0
    - The file defines all five output-path constants and contains exactly five `writeFileSync(` calls
    - `FLAT_BACKGROUND_RECT` contains no `rx=` attribute
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
    - No new entry was added to `package.json` `dependencies`/`devDependencies` (`git diff package.json` is empty)
    - `package.json`'s `"generate-assets"` script is still exactly `node scripts/generate-brand-assets.ts`
  </acceptance_criteria>
  <done>The rewritten script typechecks and lints clean, reads only `lafa-icon.svg`, and contains write paths for all five outputs with zero references to the retired source structure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Regenerate all five assets and verify specs plus visual correctness</name>
  <files>assets/images/icon.png, assets/images/favicon.png, assets/images/splash-icon.png, assets/images/android-icon-foreground.png, assets/images/android-icon-monochrome.png</files>
  <read_first>
    - `.planning/phases/25-brand-asset-pipeline/25-VALIDATION.md` (the binding requirement -> command map for this phase)
    - `.planning/ROADMAP.md` §"Phase 25" success criterion 2 (the exact per-output spec)
  </read_first>
  <what-built>
    Ran `npm run generate-assets`, which rewrote all five PNGs in `assets/images/` from
    `assets/brand/lafa-icon.svg`, and machine-verified every dimension/alpha spec plus
    the Android safe-zone fit before pausing for visual sign-off.
  </what-built>
  <action>
    Run `npm run generate-assets` (it must exit 0 with no thrown "source may have changed
    shape" error). Then run the automated spec checks below and confirm every value
    matches BRAND-03 before requesting human sign-off. If any check fails, fix the script
    in Task 2 and re-run — do not hand-edit the PNGs.

    Expected spec, per output:
    - `icon.png` — 1024 x 1024, `hasAlpha: no`
    - `favicon.png` — 48 x 48, `hasAlpha: no`
    - `splash-icon.png` — 1024 wide, `hasAlpha: yes`
    - `android-icon-foreground.png` — 1024 x 1024, `hasAlpha: yes`, trimmed content
      bounding box longest side within 1 px of 676 and both sides <= 676 (the 66% safe zone)
    - `android-icon-monochrome.png` — 1024 x 1024, `hasAlpha: yes`, same trimmed
      bounding box as the foreground

    Also confirm `git status --porcelain assets/images` shows the five expected
    modifications and that `android-icon-background.png` is untouched.
  </action>
  <how-to-verify>
    1. Run `npm run generate-assets` — expect exit code 0, no errors printed.
    2. Run `sips -g pixelWidth -g pixelHeight -g hasAlpha assets/images/icon.png assets/images/favicon.png assets/images/splash-icon.png assets/images/android-icon-foreground.png assets/images/android-icon-monochrome.png` and check each against the spec table above.
    3. Run `grep -rln "lafa-logo" --include="*.ts" --include="*.tsx" --include="*.json" . | grep -v .planning | grep -v node_modules` — expect NO output (BRAND-02).
    4. Open all five PNGs in Preview/Finder and confirm:
       - `icon.png`: warm `#FFF9F6` background fills the whole square with SQUARE corners (no rounded-corner artifact), orange mark centred and uncropped
       - `favicon.png`: same artwork as `icon.png`, legible at small size
       - `splash-icon.png`: orange mark only on a checkerboard/transparent background, NO background rectangle
       - `android-icon-foreground.png`: orange mark on transparent, comfortably inside a centred circle covering ~2/3 of the canvas (nothing near the edges)
       - `android-icon-monochrome.png`: mark is SOLID WHITE (not orange) on transparent, same size/position as the foreground
    5. Confirm `assets/brand/` contains only `lafa-icon.svg`.
  </how-to-verify>
  <verify>
    <automated>npm run generate-assets &amp;&amp; sips -g pixelWidth -g pixelHeight -g hasAlpha assets/images/icon.png assets/images/favicon.png assets/images/splash-icon.png assets/images/android-icon-foreground.png assets/images/android-icon-monochrome.png &amp;&amp; node -e 'const sharp=require("sharp");(async()=>{for(const f of ["android-icon-foreground","android-icon-monochrome"]){const {info}=await sharp(`assets/images/${f}.png`).trim({threshold:1}).toBuffer({resolveWithObject:true});const m=Math.max(info.width,info.height);if(m>677||m<675)throw new Error(`${f} safe-zone longest side ${m}, expected ~676`);console.log(f,info.width,info.height);}})()' &amp;&amp; test -z "$(grep -rln 'lafa-logo' --include='*.ts' --include='*.tsx' --include='*.json' . | grep -v .planning | grep -v node_modules)" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npm run generate-assets` exits 0
    - `sips` reports `pixelWidth: 1024`, `pixelHeight: 1024`, `hasAlpha: no` for `icon.png`
    - `sips` reports `pixelWidth: 48`, `pixelHeight: 48`, `hasAlpha: no` for `favicon.png`
    - `sips` reports `pixelWidth: 1024` and `hasAlpha: yes` for `splash-icon.png`
    - `sips` reports `pixelWidth: 1024`, `pixelHeight: 1024`, `hasAlpha: yes` for both `android-icon-foreground.png` and `android-icon-monochrome.png`
    - The sharp `trim()` check prints a longest content side of 676 (+/-1) for both Android outputs, and the two outputs report identical trimmed dimensions
    - `grep -rln "lafa-logo" --include="*.ts" --include="*.tsx" --include="*.json" .` (excluding `.planning/` and `node_modules/`) returns empty
    - `git status --porcelain assets/images` lists exactly the five regenerated PNGs; `android-icon-background.png` is absent from the diff
    - Human confirms all five visual checks in step 4 above
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what looks wrong in any of the five images</resume-signal>
  <done>All five PNGs regenerated, machine-verified against BRAND-03's spec, visually confirmed by the user, and zero live-code references to the old brand filenames remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This phase adds no trust boundary. `scripts/generate-brand-assets.ts` is a build-time-only Node script executed manually by the developer on a local checkout. It reads one repo-local SVG, transforms it in memory, and writes PNGs into the same repo. There is no network call, no user input, no auth, no external/untrusted data source, and no app-runtime code path — the app bundle consumes only the resulting static PNGs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | `assets/brand/lafa-icon.svg` (source input) | accept | The only input is a repo-tracked file already under code review and git history; a malicious substitution is equivalent to any other malicious commit and is covered by normal review, not by an ASVS control. The script's shape guards (throw on missing background rect / wrong mark-path count / empty bbox) additionally fail the build loudly if the source is altered unexpectedly. |
| T-25-02 | Tampering | npm/pip/cargo installs | accept | No packages are installed by this phase — `@resvg/resvg-js` 2.6.2 and `sharp` 0.35.3 are pre-existing dependencies whose legitimacy was established in Phase 22. RESEARCH.md's Package Legitimacy Audit records "not applicable, no new packages"; `git diff package.json` must stay empty (Task 2 acceptance criterion). |

**ASVS L1 applicability:** none. Per RESEARCH.md §"Security Domain", no ASVS category
applies to a local, offline, build-time image-generation script with no network, input,
auth, or data-handling surface. This block is recorded explicitly (rather than omitted)
so the block-on-high gate has an auditable rationale. No high-severity threats identified.
</threat_model>

<verification>
Phase-level checks after all tasks complete:

1. `npm run generate-assets` exits 0 and is idempotent (a second consecutive run leaves `git status assets/images` unchanged relative to the first).
2. `sips -g pixelWidth -g pixelHeight -g hasAlpha` on all five outputs matches BRAND-03's spec exactly.
3. `grep -rln "lafa-logo" --include="*.ts" --include="*.tsx" --include="*.json" . | grep -v .planning | grep -v node_modules` returns empty (BRAND-02).
4. `ls assets/brand` returns exactly `lafa-icon.svg` (BRAND-01).
5. `npm test`, `npm run typecheck`, and `npm run lint` all pass (no `src/`/`app/` runtime code changed, so `npm test` must be unaffected).
6. `assets/images/android-icon-background.png` and `app.json` are unmodified (Phase 27 owns those).
</verification>

<success_criteria>
- `assets/brand/` contains only `lafa-icon.svg`; the four legacy AI-generated assets are deleted in git.
- `scripts/generate-brand-assets.ts` reads only `assets/brand/lafa-icon.svg` and writes exactly five PNGs.
- `icon.png` = 1024x1024, no alpha, square-cornered flat `#FFF9F6` background.
- `favicon.png` = 48x48, no alpha, visually identical artwork to `icon.png`.
- `splash-icon.png` = transparent, mark only, no background rectangle.
- `android-icon-foreground.png` = 1024x1024 transparent, mark centred within the 676px (66%) safe zone.
- `android-icon-monochrome.png` = 1024x1024 transparent, solid-white silhouette at the same size/position as the foreground.
- `npm test`, `npm run typecheck`, `npm run lint` all pass.
</success_criteria>

<output>
Create `.planning/phases/25-brand-asset-pipeline/25-01-SUMMARY.md` when done
</output>
