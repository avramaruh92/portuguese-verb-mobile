# Requirements Archive: v0.6 Lafa Branding + Expo Splash Cleanup

**Archived:** 2026-08-16
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: Lafa — Portuguese Verb Conjugation App (Mobile)

**Defined:** 2026-08-13
**Milestone:** v0.6 Lafa Branding + Expo Splash Cleanup
**Core Value:** A learner can open the app, pick what to practice, complete a 10-question
conjugation quiz entirely offline, and see an accurate score.

## v0.6 Requirements

Requirements for this milestone. Each maps to roadmap phases.

### Brand Asset Source

- [x] **BRAND-01**: `assets/brand/lafa-icon.svg` (the user-supplied SVG) is the sole
      source `scripts/generate-brand-assets.ts` consumes to produce app assets.
- [x] **BRAND-02**: Old AI-generated brand assets (`lafa-logo.svg`, `lafa-logo-v2.svg`,
      concept PNGs) are removed or no longer referenced anywhere in the repo.
- [x] **BRAND-03**: The generator produces `icon.png` (1024x1024, opaque RGB, no alpha
      channel), `favicon.png` (48x48), `splash-icon.png` (transparent, mark only, no
      background rectangle), `android-icon-foreground.png` (1024x1024, transparent,
      mark centered in the Android safe zone), and `android-icon-monochrome.png`
      (1024x1024, transparent monochrome mask).

### Theme Palette

- [x] **THEME-01**: `src/theme/tokens.ts` carries the new guideline palette (primary
      orange `#F2643E`, deep orange `#C94A2D`, soft peach `#FDE7DF`, teal `#36799A`,
      green `#1F7F66`, ink `#24201E`, stone `#746D69`, canvas `#F1EFED`, warm
      background `#FFF9F6`), with existing semantic token names preserved where
      possible and new aliases added only for pressed/info/background states.
- [x] **THEME-02**: `src/theme/tokens.test.ts` is updated to assert the new palette
      values.

### Expo Config + Startup Flash

- [x] **CONFIG-01**: `app.json`'s `expo-splash-screen` plugin config uses the warm
      background (`#FFF9F6`) instead of Expo blue (`#208AEF`), keeps `image` pointed
      at the regenerated `splash-icon.png`, and sets `imageWidth` to `160`.
- [x] **CONFIG-02**: `app.json`'s `android.adaptiveIcon.backgroundColor` uses the warm
      background instead of the old Expo blue-tinted value, and `backgroundImage` is
      removed in favor of the solid background color.
- [x] **CONFIG-03**: `app.json`'s `userInterfaceStyle` is set to `"light"`.
- [x] **CONFIG-04**: `app/_layout.tsx` sets Stack `contentStyle`/`headerStyle`/
      `headerTintColor`/`headerShadowVisible` from brand tokens, configures
      `StatusBar` for dark content on the warm background, and sets the runtime root
      background via `expo-system-ui` to `#FFF9F6` so no white/default flash appears
      after the splash screen.

### UI Application

- [x] **UI-01**: The Setup screen's `Lafa` heading uses either the generated icon
      mark plus token-styled text, or a token-styled text heading if no vector
      wordmark is available (no wordmark is invented).
- [x] **UI-02**: Setup, Quiz, Results, both feedback modals, `OfflinePill`, and
      `ExplanationPanel` all consume the updated tokens — zero hardcoded hex values
      remain that depend on the old palette (`#208AEF`, `#E6F4FE`, `#E8663D`,
      `#FCE4DA`, `#2FA84F`).

### Validation

- [x] **VALID-01**: A brand validation script/check confirms: `app.json` has no Expo
      blue splash/adaptive background, generated PNG dimensions match expected
      sizes, `icon.png` has no alpha channel, and the generator no longer references
      old AI SVG source paths.
- [x] **VALID-02**: `npm test -- src/theme/tokens.test.ts`, `npm run typecheck`, and
      `npm run lint` all pass after the palette/config changes.
- [x] **VALID-03**: Manually verified on an EAS release/preview build (not Expo
      Go/dev client): cold launch shows the warm Lafa splash and never Expo blue,
      the iOS app icon uses the supplied SVG design, the Android adaptive icon is
      centered and not clipped by common masks, and Setup/Quiz/Results/modals all
      show the new palette consistently.

## v2 Requirements

None identified this milestone — see Out of Scope below for explicitly deferred
items surfaced during scoping.

## Out of Scope

Explicitly excluded. Documented to prevent scope creep.

| Feature | Reason |
|---------|--------|
| Hand-drawn/vector wordmark | No precise vector wordmark supplied yet; typography stays system-based per the plan's assumptions. Do not invent one. |
| Full dark mode / dual theme system | `userInterfaceStyle` is locked to `"light"` this milestone until a full dark brand system exists. |
| Re-tracing the raster branding guideline into a new logo | The raster guideline image is a visual/color reference only, not a source to trace. |
| Redesigning icon/screen layout or IA | Out of scope — this milestone is a brand-asset and token substitution pass, not a redesign. |

## Traceability

Which phases cover which requirements. Updated during roadmap creation.

| Requirement | Phase | Status |
|-------------|-------|--------|
| BRAND-01 | Phase 25 | Complete |
| BRAND-02 | Phase 25 | Complete |
| BRAND-03 | Phase 25 | Complete |
| THEME-01 | Phase 26 | Complete |
| THEME-02 | Phase 26 | Complete |
| CONFIG-01 | Phase 27 | Complete |
| CONFIG-02 | Phase 27 | Complete |
| CONFIG-03 | Phase 27 | Complete |
| CONFIG-04 | Phase 27 | Complete |
| UI-01 | Phase 28 | Complete |
| UI-02 | Phase 28 | Complete |
| VALID-01 | Phase 29 | Complete |
| VALID-02 | Phase 29 | Complete |
| VALID-03 | Phase 29 | Complete |
