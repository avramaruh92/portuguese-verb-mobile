---
phase: 11-lafa-design-tokens-brand-identity
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - app/index.tsx
  - app/quiz.tsx
  - app/results.tsx
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03]

must_haves:
  truths:
    - "Setup screen heading reads 'Lafa' instead of 'Portuguese Verb Quiz' (D-07)"
    - "All three screens reference only Lafa token keys (primary/surface/...), never colors.accent or colors.secondary"
    - "Answer-choice correct/wrong states use colors.success/colors.error with white (colors.background) text; selection behavior unchanged (D-10)"
  artifacts:
    - path: "app/index.tsx"
      provides: "Setup screen on Lafa tokens + Lafa heading"
      contains: ">Lafa<"
    - path: "app/quiz.tsx"
      provides: "Quiz screen + answer-choice states on Lafa tokens"
    - path: "app/results.tsx"
      provides: "Results screen on Lafa tokens"
  key_links:
    - from: "app/quiz.tsx"
      to: "src/theme/tokens.ts"
      via: "colors.success / colors.error for answer-choice states"
      pattern: "colors\\.(success|error)"
---

<objective>
Migrate the three Expo Router screens (Setup, Quiz, Results) off the removed
`colors.accent`/`colors.secondary` keys onto the Lafa tokens from 11-01, change
the Setup heading to "Lafa" (BRAND-01, D-07), and restyle answer-choice states
with `success`/`error` tokens keeping existing behavior (BRAND-03, D-10).

Purpose: These screens currently reference token keys deleted in 11-01, so they
must be updated to the new names. This is a mechanical key rename plus one
heading string plus the answer-choice color swap — no structural/layout changes.
Output: Three screens rendering on the Lafa palette.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-lafa-design-tokens-brand-identity/11-CONTEXT.md
@.planning/phases/11-lafa-design-tokens-brand-identity/11-UI-SPEC.md
@.planning/phases/11-lafa-design-tokens-brand-identity/11-PATTERNS.md
@.planning/phases/11-lafa-design-tokens-brand-identity/11-01-SUMMARY.md

<interfaces>
Token keys available after 11-01 (from src/theme/tokens.ts):
- colors: primary, primarySoft, success, error, background, text, textSecondary, surface
- radius: control (12), pill (999)
- spacing: sm, md, lg, xl2, xl3, choiceGap (unchanged)
- typography: caption, body, bodyStrong, heading, display (unchanged)

Rename map (apply to every call site in these three files):
- colors.accent    → colors.primary
- colors.secondary → colors.surface
- colors.error / colors.success / colors.text / colors.textSecondary / colors.background
  → same key name (new hex flows automatically from tokens.ts; no call-site edit)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup screen — rename tokens and change heading to Lafa</name>
  <files>app/index.tsx</files>
  <read_first>
    - app/index.tsx (current version)
    - src/theme/tokens.ts (post-11-01 key names)
    - .planning/phases/11-lafa-design-tokens-brand-identity/11-UI-SPEC.md §App Identity Copy + §Color rename map
  </read_first>
  <action>
    Replace the heading `<Text style={styles.heading}>Portuguese Verb Quiz</Text>`
    with `<Text style={styles.heading}>Lafa</Text>` (D-07). In the StyleSheet
    block, replace every `colors.accent` with `colors.primary` and every
    `colors.secondary` with `colors.surface`. Leave `colors.error` and all other
    token references as-is (new hex flows from tokens.ts). No structural, layout,
    or logic changes — token-key and one string edit only.
  </action>
  <verify>
    <automated>! grep -Eq 'colors\.(accent|secondary)' app/index.tsx && grep -q '>Lafa<' app/index.tsx && ! grep -q 'Portuguese Verb Quiz' app/index.tsx && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq 'colors\.(accent|secondary)' app/index.tsx` returns non-zero (no old keys)
    - File contains `>Lafa<` and no longer contains the string `Portuguese Verb Quiz`
    - `colors.primary` and `colors.surface` are present at the former accent/secondary call sites
  </acceptance_criteria>
  <done>Setup screen shows "Lafa" heading and uses only Lafa token keys.</done>
</task>

<task type="auto">
  <name>Task 2: Quiz screen — rename tokens and restyle answer-choice states</name>
  <files>app/quiz.tsx</files>
  <read_first>
    - app/quiz.tsx (current version — note choiceStyle() logic must NOT change)
    - src/theme/tokens.ts (post-11-01 key names)
    - .planning/phases/11-lafa-design-tokens-brand-identity/11-UI-SPEC.md §Answer-choice states (BRAND-03, D-10)
  </read_first>
  <action>
    In the StyleSheet block replace every `colors.accent` with `colors.primary`
    and every `colors.secondary` with `colors.surface` (there are multiple
    occurrences including choiceDefault/choice backgrounds, progress track/fill,
    exit/next/report button styles). For answer-choice states: `choiceCorrect`
    background stays `colors.success`, `choiceWrong` background stays
    `colors.error`, `choiceTextDefault` stays `colors.text`, `choiceTextOnColor`
    stays `colors.background` (white on colored choices — do not introduce a
    literal white). New hex values flow from tokens.ts automatically. Do NOT
    modify `choiceStyle()` selection logic or any component behavior (D-10) — no
    new interaction states, animation, or haptics.
  </action>
  <verify>
    <automated>! grep -Eq 'colors\.(accent|secondary)' app/quiz.tsx && grep -q 'colors\.success' app/quiz.tsx && grep -q 'colors\.error' app/quiz.tsx && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq 'colors\.(accent|secondary)' app/quiz.tsx` returns non-zero (no old keys)
    - choiceCorrect uses `colors.success`, choiceWrong uses `colors.error`, on-color text uses `colors.background`
    - `choiceStyle()` function body is byte-identical to the pre-edit version (selection behavior unchanged)
  </acceptance_criteria>
  <done>Quiz screen uses only Lafa token keys; answer-choice states styled with success/error tokens, behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Results screen — rename tokens</name>
  <files>app/results.tsx</files>
  <read_first>
    - app/results.tsx (current version)
    - src/theme/tokens.ts (post-11-01 key names)
    - .planning/phases/11-lafa-design-tokens-brand-identity/11-UI-SPEC.md §Color rename map
  </read_first>
  <action>
    In the StyleSheet block replace every `colors.accent` with `colors.primary`
    (shareButton, tryAgainButton, backButtonText) and every `colors.secondary`
    with `colors.surface` (backButton). Leave `colors.error`, `colors.text`,
    `colors.textSecondary`, `colors.background` references as-is (new hex flows
    from tokens.ts). No structural or logic changes.
  </action>
  <verify>
    <automated>! grep -Eq 'colors\.(accent|secondary)' app/results.tsx && grep -q 'colors\.primary' app/results.tsx && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq 'colors\.(accent|secondary)' app/results.tsx` returns non-zero (no old keys)
    - `colors.primary` and `colors.surface` present at the former accent/secondary call sites
  </acceptance_criteria>
  <done>Results screen uses only Lafa token keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Presentational token-key and copy edits only. No new input, network, or storage path introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering | app/*.tsx screen styles | accept | Style/copy-only changes; quiz behavior (choiceStyle) explicitly untouched and asserted in acceptance criteria |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `grep -Eq 'colors\.(accent|secondary)' app/index.tsx app/quiz.tsx app/results.tsx` returns non-zero (no old keys in any screen)
- After this plan AND 11-03 both complete, `npm run typecheck` passes and `npm test` is green. (Typecheck may still fail until 11-03 lands, because src/components/OfflinePill.tsx still references the removed colors.secondary key.)
</verification>

<success_criteria>
- All three screens compile against the Lafa palette with no accent/secondary references
- Setup heading reads "Lafa"; answer-choice states use success/error tokens
</success_criteria>

<output>
Create `.planning/phases/11-lafa-design-tokens-brand-identity/11-02-SUMMARY.md` when done
</output>
