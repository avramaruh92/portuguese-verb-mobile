---
phase: 07-dataset-seam-fetch-fallback-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quiz/engine.ts
  - src/dataset/verbs.ts
  - __tests__/quiz-engine.test.ts
autonomous: true
requirements: [FETCH-01, FETCH-03]

must_haves:
  truths:
    - "generate() accepts an injected verb list and no longer hardcodes the bundled dataset"
    - "Every existing call site (store, tests) keeps compiling and passing without modification"
    - "The local fallback dataset's querer.isIrregular matches the authoritative remote value (true)"
    - "The full existing test suite passes unchanged after the signature change and dataset edit"
  artifacts:
    - path: "src/quiz/engine.ts"
      provides: "generate() with an optional trailing verbs parameter defaulting to the bundled dataset"
      contains: "verbs: Verb[] = localVerbs"
    - path: "src/dataset/verbs.ts"
      provides: "local fallback dataset with querer.isIrregular corrected to true"
      contains: "isIrregular: true"
    - path: "__tests__/quiz-engine.test.ts"
      provides: "new test proving an injected verbs param overrides the bundled default"
  key_links:
    - from: "src/store/useQuizStore.ts"
      to: "src/quiz/engine.ts"
      via: "generate(options) call site stays byte-for-byte unchanged"
      pattern: "generate\\(options\\)"
---

<objective>
Open the injection seam in the quiz engine and reconcile the local fallback
dataset with the now-authoritative remote contract.

Purpose: Delivers the "quiz engine no longer hardcodes which dataset it uses"
half of this phase (supporting FETCH-01) via a non-breaking signature widening,
plus the D-02 dataset reconciliation, while keeping the full 122-test suite
green (Success Criterion 1, FETCH-03 regression safety).
Output: modified `src/quiz/engine.ts`, corrected `src/dataset/verbs.ts`, and a
new engine test case proving the seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dataset-seam-fetch-fallback-pipeline/07-CONTEXT.md
@.planning/phases/07-dataset-seam-fetch-fallback-pipeline/07-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — use directly. -->

Current generate() signature (src/quiz/engine.ts lines 11-14):
  export function generate(options: GenerateOptions, random: () => number = Math.random): QuizSession
  // line 3 currently: import { verbs } from "../dataset/verbs";
  // line 15 currently: const eligibleVerbs = verbs.filter(...)

Call sites that MUST stay unmodified (verify, do not touch):
  - src/store/useQuizStore.ts:40 → const session = generate(options);
  - __tests__/quiz-engine.test.ts → every call is generate({...}, Math.random) or generate({...})
  - __tests__/useQuizStore.test.ts → jest.spyOn(engine, "generate") (arity-agnostic; tolerates a new optional param)

From src/dataset/types.ts:
  export interface Verb { verb: string; translation: string; isIrregular: boolean; conjugations: Record<Tense, Record<Subject, string>>; }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen generate() with an optional trailing verbs parameter</name>
  <files>src/quiz/engine.ts, __tests__/quiz-engine.test.ts</files>
  <read_first>
    - src/quiz/engine.ts (full current file — the exact seam to modify)
    - src/store/useQuizStore.ts (line 40 call site that MUST remain unchanged)
    - __tests__/quiz-engine.test.ts (existing calls that MUST remain unchanged; where to add the new case)
    - src/dataset/types.ts (the Verb type)
    - .planning/phases/07-dataset-seam-fetch-fallback-pipeline/07-PATTERNS.md (engine.ts pattern assignment — the exact edit)
  </read_first>
  <behavior>
    - generate(options, random, customVerbs) draws its questions exclusively from customVerbs, not the bundled dataset
    - generate(options) and generate(options, Math.random) behave byte-for-byte as before (default = bundled dataset)
  </behavior>
  <action>
    Make exactly two edits to src/quiz/engine.ts, nothing else:
    (1) Rename the import on line 3 from `import { verbs } from "../dataset/verbs";` to
        `import { verbs as localVerbs } from "../dataset/verbs";` (avoids shadowing the new parameter).
    (2) Add a third parameter to generate: `verbs: Verb[] = localVerbs` AFTER `random`, so the signature
        becomes `generate(options: GenerateOptions, random: () => number = Math.random, verbs: Verb[] = localVerbs): QuizSession`.
    The body already reads `verbs.filter(...)` on line 15 — it now reads the parameter instead of the
    module binding; leave line 15 and all other lines unchanged. Do NOT add `verbs` as a required leading
    parameter (that shape breaks the store call site and every existing test — research Pitfall 1). Do NOT
    modify sampleTriples, buildQuestion, or pickDistractors. Do NOT touch src/store/useQuizStore.ts.
    In __tests__/quiz-engine.test.ts add a new test case (do not alter existing cases): build a small
    custom Verb[] fixture of 3+ verbs with distinct verb names, call generate(options, Math.random, customVerbs)
    with includeIrregular set so all fixtures are eligible, and assert every produced question's `verb` is one
    of the custom fixture's verb names (proving the bundled default was overridden).
  </action>
  <verify>
    <automated>npx jest __tests__/quiz-engine.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `src/quiz/engine.ts` line 3 imports `verbs as localVerbs` (grep: `import \{ verbs as localVerbs \}`)
    - generate signature contains `verbs: Verb[] = localVerbs` (grep: `verbs: Verb\[\] = localVerbs`)
    - The diff to src/quiz/engine.ts touches only the import line and the signature line (`git diff --stat src/quiz/engine.ts` shows a minimal change; no edits to sampleTriples/buildQuestion/pickDistractors bodies)
    - `git status` shows src/store/useQuizStore.ts unmodified
    - `npx jest __tests__/quiz-engine.test.ts -x` passes including the new injected-verbs test case
  </acceptance_criteria>
  <done>generate() accepts an injected verbs list, defaults to the bundled dataset, all existing engine tests plus the new injection test pass.</done>
</task>

<task type="auto">
  <name>Task 2: Correct querer.isIrregular and run full regression</name>
  <files>src/dataset/verbs.ts</files>
  <read_first>
    - src/dataset/verbs.ts (querer entry at lines 1682-1685 — the single field to change)
    - __tests__/dataset.test.ts (the shape-validation test — confirm it validates shape not content, so it stays green)
  </read_first>
  <action>
    In src/dataset/verbs.ts, change the querer entry's `isIrregular: false` (line 1684) to `isIrregular: true`
    per D-02 — reconciling the local fallback with the authoritative remote dataset. Change ONLY this one boolean
    literal; make no structural change and no other field/verb edits. Then run the full test suite to confirm the
    signature change from Task 1 and this content edit leave all existing suites green (Success Criterion 1: the
    122-test / 11-suite baseline must pass unchanged, plus the new tests from this phase).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/dataset/verbs.ts` line ~1684 (the querer entry) reads `isIrregular: true` (grep the querer block confirms true)
    - `git diff src/dataset/verbs.ts` shows exactly one changed line (the querer isIrregular boolean)
    - `npm test` passes with the full suite green (all pre-existing suites plus this phase's new suites; zero failures)
  </acceptance_criteria>
  <done>querer.isIrregular is true, and the entire test suite passes with no pre-existing test modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a pure-logic signature change plus a static dataset content edit; no untrusted input crosses any boundary here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering (regression risk) | src/quiz/engine.ts signature change | mitigate | Add `verbs` as an optional trailing parameter defaulting to the bundled dataset so all existing call sites and tests compile and pass unchanged; enforce via full `npm test` regression gate (research Pitfall 1). |
| T-07-SC | Tampering (supply chain) | npm installs | accept | No package installs this plan — no `npm install`/`npx expo install`. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `npm test` — full suite green (Success Criterion 1)
- `git status` confirms src/store/useQuizStore.ts and app/*.tsx untouched
- `git diff --stat` confirms only src/quiz/engine.ts, src/dataset/verbs.ts, and __tests__/quiz-engine.test.ts changed
</verification>

<success_criteria>
- FETCH-01 (seam half): `generate()` accepts an injected verb list and no longer hardcodes the bundled dataset at module scope
- FETCH-03 (regression): the full existing test suite passes unchanged after the signature change and the querer edit
- D-02: querer.isIrregular is true in the local fallback dataset
</success_criteria>

<output>
Create `.planning/phases/07-dataset-seam-fetch-fallback-pipeline/07-02-SUMMARY.md` when done
</output>
