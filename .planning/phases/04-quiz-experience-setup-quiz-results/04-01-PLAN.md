---
phase: 04-quiz-experience-setup-quiz-results
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quiz/labels.ts
  - src/quiz/share.ts
  - src/store/useQuizStore.ts
  - __tests__/quiz-labels.test.ts
  - __tests__/quiz-share.test.ts
  - __tests__/useQuizStore.test.ts
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-03, QUIZ-01, QUIZ-03, RSLT-02]

must_haves:
  truths:
    - "subjectLabels and tenseLabels cover all 6 Subject and all 4 Tense values with display-appropriate Portuguese/English labels"
    - "buildShareMessage(correct, total) produces the exact D-10 share text for any score"
    - "Store default state has includeIrregular: false, empty tenses, status idle"
    - "startQuiz(validOptions) transitions to in-progress status and populates session from generate()"
    - "startQuiz(insufficientVerbs) catches InsufficientVerbsError, sets error status with message, never sets session"
    - "selectAnswer() locks the choice on first call; subsequent calls are no-ops"
    - "advance() appends lockedChoice to answers array, increments currentIndex, resets lockedChoice to null"
    - "advance() on the 10th question transitions status to completed"
    - "reset() returns all state to initial idle values"
    - "All existing Phase 2/3 tests still pass (npm test green)"
  artifacts:
    - path: src/quiz/labels.ts
      provides: Subject/Tense display label lookups
      exports: ["subjectLabels", "tenseLabels"]
      covers_all_subjects: true
      covers_all_tenses: true
    - path: src/quiz/share.ts
      provides: Share message builder
      exports: ["buildShareMessage"]
    - path: src/store/useQuizStore.ts
      provides: Full quiz session store with state machine
      exports: ["useQuizStore"]
    - path: __tests__/quiz-labels.test.ts
      provides: Exhaustive label coverage tests
    - path: __tests__/quiz-share.test.ts
      provides: Share message format tests
    - path: __tests__/useQuizStore.test.ts
      provides: Store state machine tests
  key_links:
    - from: src/store/useQuizStore.ts
      to: src/quiz/engine.ts
      via: "import { generate } from '../quiz/engine'"
      pattern: "generate("
    - from: src/store/useQuizStore.ts
      to: src/quiz/types.ts
      via: "import { GenerateOptions, QuizSession, InsufficientVerbsError }"
    - from: src/store/useQuizStore.ts
      to: src/quiz/scoring.ts
      via: "import { score } from '../quiz/scoring'"
      pattern: "score("
---

<objective>
Create the pure-logic layer that the three screens will depend on: display-label lookups for tense/subject pronouns, a pure share-message builder, and the Zustand state machine that owns quiz session state across all three screens.

Purpose: Every piece of business logic in this phase is tested via plain Jest unit tests (no React Native rendering), exactly matching the pure-logic-first posture from Phases 2-3. The store becomes the single source of truth for cross-screen state, so the screens (Plan 02) stay thin renderers.

Output: `src/quiz/labels.ts`, `src/quiz/share.ts`, `src/store/useQuizStore.ts`, with companion tests in `__tests__/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quiz-experience-setup-quiz-results/04-CONTEXT.md
@.planning/phases/04-quiz-experience-setup-quiz-results/04-RESEARCH.md
@.planning/phases/04-quiz-experience-setup-quiz-results/04-UI-SPEC.md
@.planning/phases/04-quiz-experience-setup-quiz-results/04-VALIDATION.md

<interfaces>
From src/dataset/types.ts:
  - export type Tense = "present_indicative" | "preterite" | "imperfect" | "future"
  - export type Subject = "eu" | "tu" | "ele_ela" | "nos" | "voces" | "eles_elas"
  - export const TENSES: readonly Tense[]
  - export const SUBJECTS: readonly Subject[]

From src/quiz/types.ts:
  - export interface GenerateOptions { tenses: Tense[]; includeIrregular: boolean }
  - export interface QuizSession { questions: Question[] }
  - export interface Question extends Triple { choices: string[]; correctAnswer: string }
  - export class InsufficientVerbsError extends Error { readonly eligibleCount: number; readonly required: number }

From src/quiz/engine.ts:
  - export function generate(options: GenerateOptions, random?: () => number): QuizSession

From src/quiz/scoring.ts:
  - export function score(session: QuizSession, answers: readonly (string | null)[]): { correct: number; total: number }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create labels module, share message helper, and their complete tests</name>
  <files>
    src/quiz/labels.ts (NEW)
    src/quiz/share.ts (NEW)
    __tests__/quiz-labels.test.ts (NEW)
    __tests__/quiz-share.test.ts (NEW)
  </files>

  <read_first>
    @src/dataset/types.ts — Subject and Tense type definitions that the labels module maps
    @__tests__/dataset.test.ts — Existing test patterns (describe/it, Object.keys iteration, import patterns)
    @.planning/phases/04-quiz-experience-setup-quiz-results/04-UI-SPEC.md — Copywriting contract section for D-10 share message exact text
  </read_first>

  <action>
    Create `src/quiz/labels.ts` with two exported const record objects:

    1. `subjectLabels: Record<Subject, string>` mapping all 6 Subject enum literals to their learner-friendly Portuguese display labels (accents included):
       - `eu`            → `"eu"`
       - `tu`            → `"tu"`
       - `ele_ela`       → `"ele/ela"`
       - `nos`           → `"nós"`
       - `voces`         → `"vocês"`
       - `eles_elas`     → `"eles/elas"`

    2. `tenseLabels: Record<Tense, string>` mapping all 4 Tense enum literals to their English display labels:
       - `present_indicative` → `"Present"`
       - `preterite`          → `"Preterite"`
       - `imperfect`          → `"Imperfect"`
       - `future`             → `"Future"`

    Both records are exhaustive — every member of the respective union type has a key. Import `Subject` and `Tense` types from `../dataset/types`. No `as` type casting, no partial coverage.

    Create `src/quiz/share.ts` with one exported pure function:
    `export function buildShareMessage(correct: number, total: number): string`
    Returns: `` `I scored ${correct}/${total} on Portuguese Verb Quiz!` `` (backtick template literal).
    Uses the `total` parameter for the denominator (not a hardcoded `10`) so the message stays correct even if `QUESTIONS_PER_SESSION` changes later. No React Native imports, no side effects.

    Create `__tests__/quiz-labels.test.ts`:

    - `describe("subjectLabels")`: Iterate over `SUBJECTS` array from `src/dataset/types` and assert every key has a corresponding non-empty string value in `subjectLabels`. Also assert the exact value for at least one key (e.g. `ele_ela` → `"ele/ela"`) to catch accidental changes.

    - `describe("tenseLabels")`: Same exhaustive pattern — iterate over `TENSES`, assert all keys present with non-empty string values. Assert exact value for at least one key (e.g. `present_indicative` → `"Present"`).

    Create `__tests__/quiz-share.test.ts`:

    - `describe("buildShareMessage")`: Three test cases covering the full range:
      a) `buildShareMessage(8, 10)` returns `"I scored 8/10 on Portuguese Verb Quiz!"`
      b) `buildShareMessage(0, 10)` returns `"I scored 0/10 on Portuguese Verb Quiz!"`
      c) `buildShareMessage(10, 10)` returns `"I scored 10/10 on Portuguese Verb Quiz!"`
  </action>

  <verify>
    <automated>npx jest __tests__/quiz-labels.test.ts && npx jest __tests__/quiz-share.test.ts && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    1. `src/quiz/labels.ts` exports `subjectLabels` with all 6 Subject keys and `tenseLabels` with all 4 Tense keys
    2. `src/quiz/share.ts` exports `buildShareMessage(correct: number, total: number): string`
    3. `npx jest __tests__/quiz-labels.test.ts` exits 0 with at least 2 test cases (subject and tense coverage)
    4. `npx jest __tests__/quiz-share.test.ts` exits 0 with at least 3 test cases covering score range endpoints
    5. `npx tsc --noEmit` produces zero errors
  </acceptance_criteria>

  <done>
    All four new files exist and pass `npm test` for their respective tests. TypeScript compiles clean. Labels module covers every Subject and Tense value exhaustively. Share message builder returns the exact D-10 string.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace Zustand store with full state machine and extend store tests</name>
  <files>
    src/store/useQuizStore.ts (MODIFY)
    __tests__/useQuizStore.test.ts (MODIFY)
  </files>

  <read_first>
    @src/store/useQuizStore.ts — Current placeholder (status: "idle" only)
    @src/quiz/types.ts — QuizSession, GenerateOptions, InsufficientVerbsError
    @src/quiz/engine.ts — generate() signature and behavior
    @src/quiz/scoring.ts — score() signature and behavior
    @.planning/phases/04-quiz-experience-setup-quiz-results/04-CONTEXT.md — D-04, D-05, D-06, D-07, D-11 decisions
    @.planning/phases/04-quiz-experience-setup-quiz-results/04-RESEARCH.md — Pattern 1 store shape example and store-owns-interaction rationale
    @__tests__/useQuizStore.test.ts — Current placeholder test (one assertion on idle status)
  </read_first>

  <behavior>
    Before writing any implemention code in the store:

    Test 1: Initial state has status "idle", session null, filters null, currentIndex 0, answers empty array, lockedChoice null, errorMessage null
    Test 2: startQuiz with valid options sets status "in-progress", populates session with 10 questions, sets filters to the passed options, resets currentIndex to 0, resets answers to []
    Test 3: startQuiz with insufficient verbs (filters so narrow generate() throws InsufficientVerbsError) catches the error, sets status "error", sets errorMessage to the D-04 text, leaves session null
    Test 4: startQuiz with an unexpected error re-throws (not swallowed)
    Test 5: selectAnswer on unlocked state sets lockedChoice to the passed choice
    Test 6: selectAnswer when lockedChoice is already set is a no-op (lockedChoice unchanged)
    Test 7: advance appends lockedChoice to answers array, increments currentIndex by 1, resets lockedChoice to null
    Test 8: advance on the 10th question (currentIndex 9) sets status "completed", session.questions has 10 items, answers array has all 10 entries
    Test 9: advance when there are remaining questions keeps status "in-progress"
    Test 10: startQuiz called again (e.g. "Try Again") produces a fresh session — the session object is a new reference (not the same array), verified before navigation (stub: just confirm session !== oldSession)
    Test 11: reset returns all state to initial values (status idle, session null, etc.)
  </behavior>

  <action>
    Step 1 (RED): Extend `__tests__/useQuizStore.test.ts` with all the test cases from the `<behavior>` block above. Import `useQuizStore`, `generate`, `InsufficientVerbsError`, and `verbs` as needed. Use `useQuizStore.getState()` to read and `useQuizStore.getState().startQuiz(), selectAnswer(), advance(), reset()` to act. Mock `InsufficientVerbsError` by passing filters that would trigger the exception (e.g. call `generate` with zero/empty tenses selected — note the engine throws `InsufficientVerbsError` when the filtered pool has fewer than `QUESTIONS_PER_SESSION` triples). For the "Try Again" freshness test, call `startQuiz` twice with the same options and assert `getState().session` yields a different object reference each time (sampling — a deterministic-collision risk is negligible across repeated calls).

    Import approach: Use `jest.mock` to make `generate()` throw `InsufficientVerbsError` for the error-path test, or rely on the real `generate()` with guaranteed-insufficient filters (e.g. a fictional tense value — though that would fail type check). The cleaner approach is a tested pattern: pass empty tenses to `startQuiz({ tenses: [], includeIrregular: false })` which the real engine will reject since `pool.length < count`. This works because the engine does NOT filter by the Tense type at runtime beyond array membership.

    Run the new tests (they will fail RED since the store is still the placeholder).

    Step 2 (GREEN): Replace the entire contents of `src/store/useQuizStore.ts` with the full Zustand store. The interface `QuizStoreState` must include:

    - `status`: `"idle" | "error" | "in-progress" | "completed"` (initial: `"idle"`)
    - `filters`: `GenerateOptions | null` (initial: `null`)
    - `session`: `QuizSession | null` (initial: `null`)
    - `currentIndex`: `number` (initial: `0`)
    - `answers`: `(string | null)[]` (initial: `[]`)
    - `lockedChoice`: `string | null` (initial: `null`)
    - `errorMessage`: `string | null` (initial: `null`)

    Actions:

    - `startQuiz(options: GenerateOptions)`: Calls `generate(options)` (with default `Math.random` since this is production, not test). On success, sets all state fields: status `"in-progress"`, filters, session, currentIndex 0, answers [], lockedChoice null, errorMessage null. Catches `InsufficientVerbsError` specifically and sets status `"error"` with errorMessage content matching D-04 exactly: `"Not enough verbs for that combination — try selecting more tenses or including irregulars."`. Re-throws any non-InsufficientVerbsError.

    - `selectAnswer(choice: string)`: If `lockedChoice !== null`, return immediately (no-op per D-06). Otherwise set `lockedChoice` to `choice`.

    - `advance()`: Reads current state (session, currentIndex, answers, lockedChoice). If no session, return. Appends `lockedChoice` to a new `answers` array. If `currentIndex + 1 >= session.questions.length`, set status to `"completed"`. Otherwise increment `currentIndex` by 1 and reset `lockedChoice` to null.

    - `reset()`: Returns all state to initial values matching the initial state.

    Export `useQuizStore` via `export const useQuizStore = create<QuizStoreState>(...)`.

    Use `zustand`'s default create function, no middleware (no persist, no devtools — this project has no persistence and is small enough to not need devtools middleware).

    Run tests GREEN — confirm all pass.

    Step 3 (REFACTOR, optional): Clean up any duplication in the test file. Run full suite `npm test` to confirm no regression in Phase 2/3 tests.
  </action>

  <verify>
    <automated>npx jest __tests__/useQuizStore.test.ts && npm test</automated>
  </verify>

  <acceptance_criteria>
    1. `__tests__/useQuizStore.test.ts` has all 11 test cases from the behavior block
    2. `npx jest __tests__/useQuizStore.test.ts` exits 0 with all tests passing
    3. `npm test` exits 0 (no regression in existing Phase 2/3 tests or Phase 4 labels/share tests)
    4. `npx tsc --noEmit` produces zero errors
    5. `src/store/useQuizStore.ts` uses the exact state shape specified above (status union, filters, session, currentIndex, answers, lockedChoice, errorMessage)
    6. Store actions produce correct state transitions per the behavior block
  </acceptance_criteria>

  <done>
    Full TDD cycle complete (RED->GREEN). All store tests pass. Full suite green. TypeScript compiles clean. The store is ready for Plan 02's screens to import.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store actions (internal) | No external input crosses any trust boundary in this plan. All inputs are controlled UI selections (tenses from a fixed array, boolean toggle, choice from a pre-generated array). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `startQuiz()` catching `InsufficientVerbsError` | mitigate | Error catch is scoped to `InsufficientVerbsError` only; unexpected errors are re-thrown, not silently swallowed |
| T-04-02 | Information Disclosure | `errorMessage` content | mitigate | Error message is a fixed string (D-04 copy), not derived from the thrown error's internals — never leaks internals to the user |
| T-04-SC | Tampering | npm installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `npx jest __tests__/quiz-labels.test.ts` passes
2. `npx jest __tests__/quiz-share.test.ts` passes
3. `npx jest __tests__/useQuizStore.test.ts` passes with all 11 test cases
4. `npm test` full suite passes (no regression)
5. `npx tsc --noEmit` produces zero errors
</verification>

<success_criteria>
- Labels module provides learner-friendly display labels for all 6 subjects and all 4 tenses
- Share message builder returns exact D-10 text for any score value
- Zustand store correctly manages the full quiz state machine (idle -> in-progress -> completed, with error path)
- Every state transition is covered by automated tests
- Zero new dependencies added
- All existing tests remain green
</success_criteria>

<output>
Create `.planning/phases/04-quiz-experience-setup-quiz-results/04-01-SUMMARY.md` when done
</output>
