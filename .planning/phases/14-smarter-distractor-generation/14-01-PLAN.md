---
phase: 14-smarter-distractor-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quiz/engine.ts
  - __tests__/quiz-engine.test.ts
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, TEST-04]

must_haves:
  truths:
    - "When same-verb wrong-subject forms exist, they are used as distractors first (tier 1, unchanged)"
    - "When tier 1 does not fill 3 slots, same-verb wrong-tense forms fill next; when the question tense is preterite or imperfect, the paired tense (the other of preterite/imperfect) is the top-priority candidate"
    - "When same-verb tiers are exhausted, cross-verb same-subject/same-tense forms fill, preferring verbs of the same conjugation class (infinitive ending) before any other verb"
    - "Every question shows exactly 4 unique choices with exactly 1 correct answer under the new strategy across all tenses"
    - "A source verb whose infinitive class never matches (pôr → 'ôr') still produces 3 valid distractors"
  artifacts:
    - path: "src/quiz/engine.ts"
      provides: "3-tier pickDistractors with TENSE_PAIRS constant and same-conjugation-class tier-3 preference"
      contains: "TENSE_PAIRS"
    - path: "__tests__/quiz-engine.test.ts"
      provides: "Tier-2, tier-3 class-preference, and re-verified invariant unit tests"
      contains: "tier 2"
  key_links:
    - from: "src/quiz/engine.ts"
      to: "src/dataset/types.ts"
      via: "import TENSES value"
      pattern: "import \\{ SUBJECTS, TENSES \\}"
    - from: "src/quiz/engine.ts pickDistractors"
      to: "src/quiz/random.ts shuffle"
      via: "shuffle(candidates, random)"
      pattern: "shuffle\\("
---

<objective>
Replace `pickDistractors`' current 2-tier strategy (same-verb wrong-subject → cross-verb same-subject/tense) with a 3-tier priority strategy: tier 1 same-verb wrong-subject (unchanged) → tier 2 same-verb wrong-tense (preterite/imperfect pair prioritized) → tier 3 cross-verb fallback preferring same conjugation class. Wrong answers become pedagogically meaningful confusions instead of arbitrary wrong forms.

Purpose: Implements DIST-01 through DIST-04 and TEST-04 — the Phase 14 goal that wrong-choice options are meaningful (subject confusion, then tense confusion, then a plausible form from a same-pattern verb).
Output: Modified `src/quiz/engine.ts` (new tier-2 logic + TENSE_PAIRS constant + tier-3 class-preference pass) and extended `__tests__/quiz-engine.test.ts` with tier-2, tier-3-class-preference, and re-verified invariant tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-smarter-distractor-generation/14-CONTEXT.md
@.planning/phases/14-smarter-distractor-generation/14-RESEARCH.md
@.planning/phases/14-smarter-distractor-generation/14-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase this session. Do not explore further. -->

From src/dataset/types.ts:
```typescript
export type Tense = "present_indicative" | "preterite" | "imperfect" | "future";
export type Subject = "eu" | "tu" | "ele_ela" | "nos" | "voces" | "eles_elas";
export const TENSES: readonly Tense[];   // ["present_indicative","preterite","imperfect","future"]
export const SUBJECTS: readonly Subject[];
export interface Verb {
  verb: string;                          // plain infinitive, e.g. "falar" | "comer" | "partir" | "pôr"
  translation: string;
  isIrregular: boolean;
  conjugations: Record<Tense, Record<Subject, string>>;
}
```

From src/quiz/random.ts:
```typescript
export function shuffle<T>(items: readonly T[], random: () => number): T[]; // Fisher-Yates, empty/singleton-safe
```

Current signature to preserve UNCHANGED (locked by CONTEXT.md Established Patterns):
```typescript
export function pickDistractors(
  verb: Verb, tense: Tense, subject: Subject,
  allVerbs: readonly Verb[], random: () => number,
): string[];
```

Current import line in src/quiz/engine.ts (line 2) imports only `SUBJECTS`:
`import { SUBJECTS } from "../dataset/types";` — tier 2 needs `TENSES` added here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert tier-2 same-verb wrong-tense distractors with preterite/imperfect pair prioritization</name>
  <files>src/quiz/engine.ts, __tests__/quiz-engine.test.ts</files>
  <read_first>
    - src/quiz/engine.ts (current `pickDistractors` lines 57-89, tier-1 block lines 66-72, tier-3 loop lines 74-86, module constants lines 8-9, import line 2)
    - src/dataset/types.ts (Tense/Subject/TENSES/SUBJECTS/Verb definitions)
    - src/quiz/random.ts (shuffle signature and empty-array safety)
    - __tests__/quiz-engine.test.ts (mockRandom helper lines 7-10, `simpleVerbs` fixture lines 312-391, `collidingVerb` fixture lines 406-444, invariant assertions lines 393-400/453-456, deterministic-ordering test lines 458-476, `it()` naming style)
    - .planning/phases/14-smarter-distractor-generation/14-RESEARCH.md (Pattern 1 shape, D-01/D-02/D-03)
  </read_first>
  <behavior>
    - Test: when the correct-answer tense is `preterite`, tier 2 (forced by a fixture whose tier-1 same-verb same-subject-across-subjects candidates collapse below 3 unique forms) places the same-verb same-subject `imperfect` form ahead of `present_indicative`/`future` forms as the first tier-2 candidate consumed (D-01). Use a synthetic `collidingVerb`-style fixture where tier-1 yields fewer than 3 unique forms so tier 2 must run, and a `mockRandom` sequence so ordering is deterministic.
    - Test: symmetric case — when the correct-answer tense is `imperfect`, the same-verb same-subject `preterite` form is the first tier-2 candidate consumed (D-01).
    - Test: when the correct-answer tense is `present_indicative` (not in the pair), tier 2 draws from the other same-verb same-subject tense forms with no forced pair ordering (D-02) and still returns 3 unique distractors.
    - Test: tier-2 candidates are deduped against the correct answer and already-chosen tier-1 forms via the running exclude Set — no distractor equals the correct answer or a tier-1 pick (D-03).
  </behavior>
  <action>
    Add `TENSES` to the value import on line 2 so it reads `import { SUBJECTS, TENSES } from "../dataset/types";`. Add a module-level `TENSE_PAIRS` constant immediately after `DISTRACTOR_COUNT` (line 9), typed `Partial<Record<Tense, Tense>>`, mapping `preterite → "imperfect"` and `imperfect → "preterite"` only (present_indicative/future intentionally absent → `undefined`).

    Insert a NEW tier-2 block into `pickDistractors` BETWEEN the existing tier-1 `chosen` assignment (line 72) and the existing tier-3 `if (chosen.length < DISTRACTOR_COUNT)` block (line 74). Tier 2 must reuse the exact fill idiom used by tier 3: guard on `chosen.length < DISTRACTOR_COUNT`, build `const exclude = new Set([correctAnswer, ...chosen])`, gather candidates, then a `for` loop with `if (chosen.length >= DISTRACTOR_COUNT) break; if (exclude.has(form)) continue; chosen.push(form); exclude.add(form);`.

    Tier-2 candidate gathering (D-01/D-02/D-03): candidates are same-verb, same-subject, OTHER-tense forms — `verb.conjugations[t][subject]` for each `t` in `TENSES` where `t !== tense`. Ordering: let `pairedTense = TENSE_PAIRS[tense]`; if `pairedTense` is defined, order = `[pairedTense, ...shuffle(otherTenses without pairedTense, random)]`; else order = `shuffle(otherTenses, random)`. Map ordered tenses to forms and dedupe with `new Set(...)` before the fill loop (mirror tier-1's `[...new Set(...)]` collapse). Per Claude's Discretion (CONTEXT.md), either inline this block or extract a small named export `pickSameVerbWrongTenseCandidates(verb, tense, subject, random): string[]` matching the file's small-single-purpose-function convention (`sampleTriples`/`buildQuestion`). Do NOT change `pickDistractors`' signature or `buildQuestion`'s call site (line 52). Handle the `TENSE_PAIRS[tense]` possibly-undefined lookup with an `if (pairedTense)` branch (no non-null assertion needed since it is legitimately absent for present/future).

    Add the tier-2 test cases described in `<behavior>` to the `describe("buildQuestion / pickDistractors")` block, following the existing `collidingVerb` synthetic-fixture and `mockRandom` deterministic-ordering patterns. Name tests `"tier 2: <behavior>"`.
  </action>
  <verify>
    <automated>npx jest __tests__/quiz-engine.test.ts -t "tier 2" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/quiz/engine.ts` line 2 imports both `SUBJECTS` and `TENSES` from `../dataset/types`.
    - `src/quiz/engine.ts` contains a module-level `TENSE_PAIRS` constant mapping `preterite`↔`imperfect` and nothing else.
    - `pickDistractors` has a tier-2 block between tier 1 and tier 3 that gathers same-verb same-subject other-tense forms and routes them through a running `exclude` Set seeded with `correctAnswer` plus already-chosen forms.
    - `npx jest __tests__/quiz-engine.test.ts -t "tier 2"` passes with at least one test each for: preterite→imperfect-first, imperfect→preterite-first, present_indicative no-pair-ordering, and correct-answer/tier-1 dedupe.
    - `npm run typecheck` reports no errors (no non-null assertion on `TENSE_PAIRS[tense]`; the undefined case is branched).
    - `pickDistractors` and `buildQuestion` signatures are byte-for-byte unchanged.
  </acceptance_criteria>
  <done>Tier 2 (same-verb wrong-tense, pair-prioritized) fills distractor slots after tier 1 and before tier 3; new tier-2 tests pass; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tier-3 cross-verb fallback with same-conjugation-class preference</name>
  <files>src/quiz/engine.ts, __tests__/quiz-engine.test.ts</files>
  <read_first>
    - src/quiz/engine.ts (current tier-3 loop lines 74-86, after Task 1's tier-2 insertion)
    - __tests__/quiz-engine.test.ts (`simpleVerbs` fixture, `collidingVerb` fixture, invariant assertion shape)
    - .planning/phases/14-smarter-distractor-generation/14-RESEARCH.md (Pattern 2, D-04/D-05, Pitfall 1 the `pôr` edge case, Pitfall 4 empty same-class pass)
  </read_first>
  <behavior>
    - Test: when tiers 1+2 are exhausted (forced by a source verb whose same-verb forms all collide so only the cross-verb pool can supply distractors), tier 3 consumes forms from same-conjugation-class verbs (same infinitive last-two-letters) BEFORE forms from other-class verbs — assert via `mockRandom` and a fixture pool split into a known same-class group and a known other-class group with distinguishable literal forms (D-04/D-05).
    - Test: a source verb `pôr` (infinitive ending `ôr`, matches no `ar`/`er`/`ir` class) still returns exactly 3 valid distractors — the empty same-class pass is a silent no-op and the generic pass fills all slots (Pitfall 1/4).
    - Test: a small `irregular_only`-sized pool (about 13 verbs, or a synthetic 3-verb pool with mixed classes) still satisfies the 3-distractor invariant when forced into tier 3 (Pitfall 3).
  </behavior>
  <action>
    Replace the existing single-pass tier-3 candidate gathering (lines 74-86 pre-Task-1) with a TWO-PASS ordering that feeds the SAME loop body. Keep the `if (chosen.length < DISTRACTOR_COUNT)` guard and the `exclude`/`break`/`continue`/`push`/`add` loop shape unchanged. Compute `const ownClass = verb.verb.slice(-2)`. From `allVerbs.filter((v) => v.verb !== verb.verb)`, split into `sameClassVerbs` (`v.verb.slice(-2) === ownClass`) and `otherClassVerbs` (the rest). Build the ordered form list as `[...shuffle(sameClassVerbs, random), ...shuffle(otherClassVerbs, random)].map((v) => v.conjugations[tense][subject])` — shuffle the verb objects then map to forms (per RESEARCH Pattern 2, preserves per-verb identity through ordering). Iterate that ordered list through the existing exclude/dedupe/push loop. Do NOT add a `Verb` field for class — class is computed inline via `.slice(-2)` at call time only (D-04). Rely on `shuffle`'s existing empty-array safety for the `pôr` case where `sameClassVerbs` is empty — no defensive code.

    Add the tier-3 test cases from `<behavior>` to the same `describe` block, following the `simpleVerbs`/`collidingVerb` fixture style. For the `pôr` test, either reuse `pôr`'s real conjugations from `src/dataset/verbs.ts` or a synthetic verb with infinitive `"pôr"`. Name tests `"tier 3: <behavior>"`.
  </action>
  <verify>
    <automated>npx jest __tests__/quiz-engine.test.ts -t "tier 3" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Tier 3 in `pickDistractors` gathers cross-verb forms in two ordered passes: same-`slice(-2)`-class verbs first, then other-class verbs, both shuffled, feeding one shared exclude/dedupe loop.
    - No new field is added to the `Verb` type or dataset; conjugation class is only computed via `verb.verb.slice(-2)` inside `pickDistractors`.
    - `npx jest __tests__/quiz-engine.test.ts -t "tier 3"` passes, including: same-class-before-other-class ordering, `pôr` unmatched-class returns 3 valid distractors, and a small-pool (irregular-sized) 3-distractor invariant.
    - `npm run typecheck` reports no errors.
    - `pickDistractors` signature and `buildQuestion` call site remain unchanged.
  </acceptance_criteria>
  <done>Tier 3 prefers same-conjugation-class verbs before any other verb; the `pôr` unmatched-class and small-pool edge cases still yield 3 valid distractors; new tier-3 tests pass; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-verify 4-unique/1-correct invariant across all tiers, tenses, and modes; confirm tier-1 priority</name>
  <files>__tests__/quiz-engine.test.ts</files>
  <read_first>
    - __tests__/quiz-engine.test.ts (existing invariant tests lines 393-456, the `describe("buildQuestion / pickDistractors")` block)
    - src/quiz/engine.ts (final 3-tier `pickDistractors` after Tasks 1-2)
    - .planning/phases/14-smarter-distractor-generation/14-RESEARCH.md (DIST-01/DIST-04 rows, Phase Requirements → Test Map)
  </read_first>
  <behavior>
    - Test: DIST-01 tier-1 priority — when a source verb has 3+ unique same-verb wrong-subject forms, all 3 distractors come from that same verb's other subjects at the same tense (no tier-2/tier-3 forms leak in). Assert every returned distractor is a member of the source verb's `conjugations[tense]` values and none belongs to another verb.
    - Test: DIST-04 invariant across all four tenses — for each of `present_indicative`, `preterite`, `imperfect`, `future`, `buildQuestion` returns exactly 4 choices, all unique, exactly one equal to `correctAnswer` (loop or `it.each` over TENSES).
    - Test: DIST-04 invariant under `mixed` and `irregular_only` mode-shaped pools using the real `generate` path or a mode-filtered pool fixture, confirming the 3-distractor invariant holds when `allVerbs` is the smaller irregular pool.
  </behavior>
  <action>
    Add invariant and priority test cases to the `describe("buildQuestion / pickDistractors")` block. Reuse the existing assertion shape: `expect(question.choices).toHaveLength(4)`, `expect(new Set(question.choices).size).toBe(4)`, `expect(question.choices).toContain(question.correctAnswer)` and the `pickDistractors` `toHaveLength(3)` / `new Set(...).size === 3` shape. For the tier-1-priority test, assert distractor membership in `verb.conjugations[tense]` values. For the all-tenses test, iterate `TENSES`. For the mode test, either drive through `generate({ tenses, verbMode }, ...)` and assert every question satisfies the invariant, or call `pickDistractors` with an irregular-only-sized pool fixture. Name tests `"distractor: <behavior>"` / `"invariant: <behavior>"` matching the file's existing label style. Do NOT modify `src/quiz/engine.ts` in this task — it is test-only re-verification.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - A tier-1-priority test asserts all 3 distractors are same-verb, same-tense, other-subject forms when 3+ unique such forms exist (DIST-01).
    - An all-four-tenses test asserts the 4-unique-choices/1-correct invariant for `present_indicative`, `preterite`, `imperfect`, and `future` (DIST-04).
    - A mode-shaped-pool test asserts the invariant holds under an `irregular_only`/`mixed`-sized pool (DIST-04).
    - `npm test` passes the full suite (all pre-existing tests plus the new tier-2, tier-3, and invariant tests) with zero failures.
    - No change to `src/quiz/engine.ts` in this task's diff.
  </acceptance_criteria>
  <done>The 4-unique/1-correct invariant and tier-1 priority are re-verified across all tenses and mode-shaped pools; full `npm test` suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase is pure in-memory selection over already-bundled, already-Zod-validated verb data. No user input, network, auth, crypto, or external contract is touched. `pickDistractors`' signature and callers are unchanged; `POST /feedback` is untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `pickDistractors` output invariant | mitigate | Tests assert exactly 4 unique choices / 1 correct across all tiers, tenses, and mode pools — a regression that produces a duplicate or a leaked correct answer fails the suite (DIST-04/TEST-04). |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (zero new dependencies — language built-ins + existing `shuffle` only, confirmed by RESEARCH.md Package Legitimacy Audit "Not applicable"). No supply-chain surface introduced. |
</threat_model>

<verification>
- `npx jest __tests__/quiz-engine.test.ts` green (quick per-task loop)
- `npm test` green (full suite, phase gate)
- `npm run typecheck` clean (strict + noUncheckedIndexedAccess)
- `pickDistractors` and `buildQuestion` signatures unchanged; no caller (`app/quiz.tsx`, `src/store/useQuizStore.ts`) modified
</verification>

<success_criteria>
1. Tier 1 (same-verb wrong-subject) is preferred when 3+ such forms exist (DIST-01).
2. Tier 2 (same-verb wrong-tense) fills next, with the preterite/imperfect pair prioritized when the question tense is preterite or imperfect (DIST-02).
3. Tier 3 falls back to cross-verb same-subject/tense forms, preferring same conjugation class before any other verb (DIST-03).
4. Every question keeps exactly 4 unique choices with exactly 1 correct answer across all tenses and mode-shaped pools, including the `pôr` unmatched-class edge case (DIST-04).
5. Unit tests cover wrong-subject, wrong-tense (incl. preterite/imperfect pair), cross-verb class-preference, and the 4-unique/1-correct invariant; full suite green (TEST-04).
</success_criteria>

<output>
Create `.planning/phases/14-smarter-distractor-generation/14-01-SUMMARY.md` when done
</output>
