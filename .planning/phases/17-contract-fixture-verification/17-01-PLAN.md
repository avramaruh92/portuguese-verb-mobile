---
phase: 17-contract-fixture-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/fixtures/content-verbs-v0.4.sample.json
  - __tests__/contract-fixture.test.ts
autonomous: true
requirements: [CONTRACT-01, CONTRACT-02, CONTRACT-03]

must_haves:
  truths:
    - "The backend v0.4 sample fixture lives in the mobile repo's test tree, copied byte-for-byte, with no cross-repo import at test runtime"
    - "A test proves validateDataset(fixture.verbs) returns { valid: true, errors: [] }"
    - "A test proves LearningContentSchema.safeParse(fixture.learning) succeeds"
    - "A test proves the fixture parses through fetchRemoteVerbs (fetch mocked) and returns 50 verbs plus a defined learning block"
    - "A test proves accented (pôr/pôs) and tied (falam) forms survive parsing byte-for-byte unchanged"
  artifacts:
    - path: "__tests__/fixtures/content-verbs-v0.4.sample.json"
      provides: "Self-contained copy of backend's content-verbs-v0.4.sample.json (50 verbs + learning block)"
      contains: "pôr"
    - path: "__tests__/contract-fixture.test.ts"
      provides: "CONTRACT-01/02/03 proof test with five independent it() blocks"
      min_lines: 40
  key_links:
    - from: "__tests__/contract-fixture.test.ts"
      to: "src/dataset/validate.ts"
      via: "validateDataset(fixture.verbs)"
      pattern: "validateDataset"
    - from: "__tests__/contract-fixture.test.ts"
      to: "src/learning/schema.ts"
      via: "LearningContentSchema.safeParse(fixture.learning)"
      pattern: "LearningContentSchema"
    - from: "__tests__/contract-fixture.test.ts"
      to: "src/dataset/remote.ts"
      via: "fetchRemoteVerbs() with globalThis.fetch mocked to return the fixture"
      pattern: "fetchRemoteVerbs"
---

<objective>
Prove the mobile app's already-shipped runtime parsing paths (`validateDataset`,
`LearningContentSchema`, `fetchRemoteVerbs`) accept the real backend v0.4 sample
payload exactly as shipped, using a self-contained fixture with zero cross-repo
coupling at test time.

Purpose: Catch any drift between the backend's v0.4 content contract and mobile's
Zod schemas now, in a fast unit test, rather than at runtime against the live
backend. This is the gate that de-risks Phases 18/19, which build on this shape.
Output: One static JSON fixture in the test tree + one new Jest test file with five
independent proofs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-contract-fixture-verification/17-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the mobile codebase. Use directly — no exploration needed. -->

From src/dataset/validate.ts:
```typescript
export const VerbSchema; // includes formIndex: z.record(z.string(), z.array(FormMatchSchema)).optional() — DO NOT strip formIndex
export function validateDataset(verbs: unknown[]): { valid: boolean; errors: string[] };
```

From src/learning/schema.ts:
```typescript
export const FormMatchSchema; // z.object({...})
export const LearningContentSchema; // z.object({...}) — call .safeParse(fixture.learning)
```

From src/dataset/remote.ts:
```typescript
export async function fetchRemoteVerbs(): Promise<{ verbs: Verb[]; learning: LearningContent | undefined }>;
// Reads globalThis.fetch; validates payload.verbs via validateDataset; parses payload.learning via LearningContentSchema.safeParse
```

Established fetch-mock pattern (from __tests__/dataset-remote.test.ts):
```typescript
const originalFetch = globalThis.fetch;
afterEach(() => { globalThis.fetch = originalFetch; jest.clearAllMocks(); });
globalThis.fetch = jest.fn().mockResolvedValue({
  ok: true, status: 200, json: async () => fixture,
}) as unknown as typeof fetch;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the backend v0.4 sample fixture verbatim into the mobile test tree</name>
  <files>__tests__/fixtures/content-verbs-v0.4.sample.json</files>
  <read_first>
    - .planning/phases/17-contract-fixture-verification/17-RESEARCH.md (Summary, Anti-Patterns "Re-serializing/reformatting", Pitfall 4 "stale sibling-repo name")
  </read_first>
  <action>
    Resolve the sibling backend repo path at execution time by running `ls ~/portuguese-verb/`
    (do NOT hardcode `portuguese-verb-api` from CLAUDE.md prose — the correct local
    directory is `portuguese-verb-backend`). Create the `__tests__/fixtures/` directory
    (new — no fixtures directory exists in the mobile repo yet). Copy the source file
    `~/portuguese-verb/portuguese-verb-backend/contracts/content-verbs-v0.4.sample.json`
    to `__tests__/fixtures/content-verbs-v0.4.sample.json` using `cp` for a byte-for-byte
    copy. Do NOT open it in an editor, re-serialize it, run it through a JSON formatter,
    or `JSON.stringify` it — reformatting risks silently normalizing Unicode (breaking the
    NFC-encoded accented forms like `pôr`) or reordering keys, defeating the "exactly as
    shipped" intent of CONTRACT-01. The destination path must be inside the mobile repo so
    there is no cross-repo reference at test runtime.
  </action>
  <verify>
    <automated>test -f __tests__/fixtures/content-verbs-v0.4.sample.json && node -e "const f=require('./__tests__/fixtures/content-verbs-v0.4.sample.json'); if(f.verbs.length!==50) process.exit(1); if(f.learning.version!==1) process.exit(1); if(!f.verbs.find(v=>v.verb==='pôr')) process.exit(1); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/fixtures/content-verbs-v0.4.sample.json` exists (`test -f` succeeds)
    - Parsed JSON has `verbs.length === 50` and `learning.version === 1`
    - A verb with `verb === "pôr"` (NFC single codepoint U+00F4) is present, confirming the accented byte survived the copy
    - `git status --porcelain __tests__/fixtures/content-verbs-v0.4.sample.json` shows the file as a new untracked/added file, not a modification of an existing one
    - No source file references a path outside the mobile repo (the copy is a standalone asset)
  </acceptance_criteria>
  <done>The fixture exists in the mobile test tree, parses to 50 verbs + a version-1 learning block, and preserves the accented `pôr` verb.</done>
</task>

<task type="auto">
  <name>Task 2: Write contract-fixture.test.ts with five independent proofs</name>
  <files>__tests__/contract-fixture.test.ts</files>
  <read_first>
    - __tests__/fixtures/content-verbs-v0.4.sample.json (the fixture created in Task 1 — inspect the falar/pôr verb shapes)
    - __tests__/dataset-remote.test.ts (the established fetch-mock pattern to reuse)
    - src/dataset/validate.ts (validateDataset return shape and VerbSchema — formIndex is optional first-class, do NOT strip it)
    - src/dataset/remote.ts (fetchRemoteVerbs return shape)
    - src/learning/schema.ts (LearningContentSchema)
    - .planning/phases/17-contract-fixture-verification/17-RESEARCH.md (Pitfall 1 "don't strip formIndex", Pitfall 2 "prefer fs.readFileSync", Pitfall 3 "NFC", Code Examples section)
  </read_first>
  <action>
    Create `__tests__/contract-fixture.test.ts`. Load the fixture with
    `fs.readFileSync(path.join(__dirname, "fixtures/content-verbs-v0.4.sample.json"), "utf-8")`
    followed by `JSON.parse(...)` — NOT a bare `import fixture from "./fixtures/...json"`
    — to sidestep any `resolveJsonModule`/`noUncheckedIndexedAccess` uncertainty (research
    flagged this as unverified; the fs path avoids the question). Type the parsed result
    loosely (e.g. `as { verbs: any[]; learning: unknown }`) and let the Zod schemas do the
    real validation. Import `validateDataset` from `../src/dataset/validate`,
    `LearningContentSchema` from `../src/learning/schema`, and `fetchRemoteVerbs` from
    `../src/dataset/remote`.

    Write these as FIVE separate `it()` blocks (not one combined end-to-end test) so a
    regression in any layer fails independently and legibly:
    1. Sanity: `expect(fixture.verbs).toHaveLength(50)` and `expect(fixture.learning.version).toBe(1)` (cheap early-catch, not a substitute for the named proofs).
    2. `validateDataset` (CONTRACT-02): call `validateDataset(fixture.verbs)` — pass verbs AS-IS, do NOT strip `formIndex` (mobile's `VerbSchema` includes it as an optional field). Assert `valid === true` and `errors` deep-equals `[]`.
    3. `LearningContentSchema` (CONTRACT-02): call `LearningContentSchema.safeParse(fixture.learning)` and assert `result.success === true`.
    4. `fetchRemoteVerbs` (CONTRACT-02): reuse the mock pattern from `__tests__/dataset-remote.test.ts` — save `originalFetch`, restore it in `afterEach` alongside `jest.clearAllMocks()`, set `globalThis.fetch = jest.fn().mockResolvedValue({ ok: true, status: 200, json: async () => fixture }) as unknown as typeof fetch`, `await fetchRemoteVerbs()`, then assert the result has `verbs` length 50 and `learning` is defined.
    5. byte-for-byte (CONTRACT-03): find `falar` and `pôr` in `fixture.verbs`. Assert `falar.formIndex.falam` has length 2 (tie: "falam" is both voces and eles_elas in present_indicative); assert `falar.conjugations.present_indicative.voces === "falam"` and `...eles_elas === "falam"`; assert `por.conjugations.preterite.ele_ela === "pôs"` and `por.verb === "pôr"`. Write the accented literals normally in the editor (NFC) — no `.normalize()` call is needed since the fixture is verified NFC.

    Give the `it()` descriptions substrings that match the VALIDATION.md `-t` filters:
    include the literal tokens "validateDataset", "LearningContentSchema", "fetchRemoteVerbs",
    and "byte-for-byte" in the respective test names so per-task filtered runs resolve.
  </action>
  <verify>
    <automated>npm test -- __tests__/contract-fixture.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- __tests__/contract-fixture.test.ts` passes with 5 tests green
    - `npm test -- __tests__/contract-fixture.test.ts -t "validateDataset"` runs and passes (name substring matches)
    - `npm test -- __tests__/contract-fixture.test.ts -t "LearningContentSchema"` runs and passes
    - `npm test -- __tests__/contract-fixture.test.ts -t "fetchRemoteVerbs"` runs and passes
    - `npm test -- __tests__/contract-fixture.test.ts -t "byte-for-byte"` runs and passes
    - The fixture is loaded via `fs.readFileSync` + `JSON.parse`, not a bare `import ... from "*.json"` (grep the file: no `from "./fixtures` import statement)
    - The test does NOT strip `formIndex` before calling `validateDataset` (grep: no destructuring that removes formIndex)
    - `npm run typecheck` passes with the new test file present
  </acceptance_criteria>
  <done>All five proofs pass; the fixture flows through validateDataset (zero errors), LearningContentSchema.safeParse (success), and fetchRemoteVerbs (50 verbs + defined learning), and accented/tied forms are asserted byte-for-byte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend fixture → mobile test tree | A one-time, manual, developer-performed `cp` of a JSON asset from a trusted sibling repo under the same developer's control. Static, checked-in, test-only — never loaded at production runtime. No live boundary crosses at test time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | Copied fixture could be silently altered/normalized during copy (Unicode NFC→NFD, key reordering) | mitigate | Byte-for-byte `cp` (no reformat/re-serialize); Task 1 verify asserts `pôr` NFC codepoint and 50-verb count survive |
| T-17-02 | Information Disclosure | Fixture leaking secrets/PII | accept | Fixture is public verb-conjugation content — no credentials, no PII, no user data. Nothing sensitive to disclose. |
| T-17-SC | Tampering | npm/pip/cargo installs (supply chain) | accept | This phase installs ZERO new packages (RESEARCH.md "Package Legitimacy Audit: Not applicable"). No install task exists; no supply-chain surface added. |

**ASVS Level 1 note:** Only V5 (Input Validation) is marginally relevant, and it is
already fully covered by the existing `VerbSchema`/`LearningContentSchema` Zod schemas —
this phase adds no new validation surface, no new network-facing code, no new user-input
path, and no new external dependency. It only *proves* the existing validation accepts a
specific known-good real-world payload. No high-severity threat exists to block on.
</threat_model>

<verification>
- `npm test -- __tests__/contract-fixture.test.ts` → 5 tests green
- `npm test` → full suite green (no regression introduced by the new fixture/test)
- `npm run typecheck` → passes
- `git ls-files __tests__/fixtures/content-verbs-v0.4.sample.json` → fixture is tracked in the mobile repo
</verification>

<success_criteria>
1. `__tests__/fixtures/content-verbs-v0.4.sample.json` exists in the mobile repo test tree; the test file references it via `fs.readFileSync` (no cross-repo path). [CONTRACT-01]
2. A test proves `validateDataset(fixture.verbs)` returns `{ valid: true, errors: [] }`. [CONTRACT-02]
3. A test proves `LearningContentSchema.safeParse(fixture.learning).success === true`. [CONTRACT-02]
4. A test proves `fetchRemoteVerbs` (fetch mocked with the fixture) returns 50 verbs + a defined learning block. [CONTRACT-02]
5. A test asserts `pôr`/`pôs` accented forms and `falam` tied forms (formIndex length 2) survive parsing byte-for-byte. [CONTRACT-03]
</success_criteria>

<output>
Create `.planning/phases/17-contract-fixture-verification/17-01-SUMMARY.md` when done.
</output>
