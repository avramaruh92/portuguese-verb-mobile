---
phase: 06-polish-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/feedback/submit.ts
  - src/feedback/ReportFeedbackModal.tsx
autonomous: false   # requires a real 15+ min backend-idle wait + on-device human verification
requirements: []   # Addresses Phase 6 Success Criterion 2 (cold-start grace)
user_setup:
  - service: render-backend
    why: "Manual cold-start test requires the live Render free-tier API to be genuinely idle"
    dashboard_config:
      - task: "Leave the backend uncalled for 15+ minutes so Render's free tier spins it down before testing"
        location: "No dashboard action — just do not call https://portuguese-verb-api.onrender.com for 15+ min"

must_haves:
  truths:
    - "The feedback modal shows its in-flight submitting spinner during a genuine Render cold-start delay"
    - "The Quiz screen underneath stays fully interactive and loses no progress while feedback is in flight"
    - "The cold start resolves to either a success state or a graceful Retry-able error — never a stuck/frozen modal"
    - "The ~90s AbortController timeout is not hit under a normal cold start"
  artifacts:
    - path: "src/feedback/submit.ts"
      provides: "Cold-start-tolerant transport (only edited if the test reveals a real bug — D-04)"
      contains: "TIMEOUT_MS"
  key_links:
    - from: "src/feedback/ReportFeedbackModal.tsx"
      to: "src/feedback/submit.ts"
      via: "modal awaits submitFeedback and maps SubmitResult to submitting/success/error states"
      pattern: "submitFeedback"
---

<objective>
Manually confirm the feedback flow degrades gracefully against a genuinely
cold (spun-down) Render free-tier backend (D-02), and fix inline only if a
real bug surfaces (D-04). This is Phase 6 Success Criterion 2.

Purpose: Phase 5's live round-trip likely hit an already-warm instance. The
highest-risk real-world condition research flagged is Render's up-to-~1-minute
cold start blocking or corrupting quiz completion. This exercises the existing
90s-timeout / fire-and-forget UI against an actually-idle backend rather than
building anything new.

Output: A verified graceful cold-start experience (spinner, non-blocking quiz,
success-or-Retry). Code diffs only if a genuine defect is found.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-verification/06-CONTEXT.md
@.planning/phases/05-feedback-integration/05-CONTEXT.md

<interfaces>
From src/feedback/submit.ts:
- FEEDBACK_ENDPOINT = https://portuguese-verb-api.onrender.com/feedback
- TIMEOUT_MS = 90_000 (AbortController-backed)
- submitFeedback(payload) → SubmitResult: { success | validation-error | server-error | network-error }

From src/feedback/ReportFeedbackModal.tsx:
- ModalState = "idle" | "submitting" | "success" | "error"
- showRetry = state==="error" && (lastStatus==="server-error" || lastStatus==="network-error")
- ActivityIndicator renders during "submitting"
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Cold-start on-device verification</name>
  <read_first>
    - src/feedback/submit.ts (the 90s AbortController timeout + status branching under test)
    - src/feedback/ReportFeedbackModal.tsx (the submitting/success/error/Retry states under test)
    - .planning/phases/06-polish-verification/06-CONTEXT.md (D-02 exact cold-start expectations)
  </read_first>
  <what-built>
    Phase 5 shipped the Report-a-problem flow: a modal that calls
    submitFeedback() (native fetch, 90s AbortController timeout) and maps the
    result to submitting → success/error(+Retry) states, built to never block
    the quiz. This test exercises that existing code against a genuinely cold
    backend.
  </what-built>
  <how-to-verify>
    1. Ensure the backend has been idle 15+ minutes (do NOT call
       https://portuguese-verb-api.onrender.com in that window) so Render's free
       tier has spun it down.
    2. Start the app on a device/simulator: `npx expo start --ios`.
    3. Start a quiz, answer a question so the answer locks (the report trigger
       is gated on a locked choice), and open "Report a problem".
    4. Pick a reason, type a short message, and submit — this is the first call
       hitting the cold backend.
    5. Observe and confirm ALL of:
       - The in-flight spinner (ActivityIndicator) shows for the full
         cold-start delay (may be up to ~1 minute).
       - The 90s timeout is NOT hit under a normal cold start (it resolves
         before aborting to network-error).
       - While the request is in flight, the Quiz screen underneath stays
         fully interactive and the current question / progress is not lost.
       - It resolves to EITHER a success confirmation OR a graceful,
         Retry-able error — never a frozen/stuck modal you cannot dismiss.
    6. Report the observed outcome, including approximate cold-start duration
       and whether any of the four conditions failed.
  </how-to-verify>
  <acceptance_criteria>
    - User confirms the spinner displayed during the cold-start delay.
    - User confirms the quiz remained interactive with no lost progress.
    - User confirms a success or Retry-able error resolved (no frozen modal).
    - User confirms the 90s timeout was not hit under the normal cold start.
    - If ANY condition failed, the user described the specific failure for
      Task 2.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all four conditions held, or describe the specific failure observed</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Inline fix only if the cold-start test failed</name>
  <files>src/feedback/submit.ts, src/feedback/ReportFeedbackModal.tsx</files>
  <read_first>
    - src/feedback/submit.ts (current transport + timeout behavior)
    - src/feedback/ReportFeedbackModal.tsx (current state machine)
    - .planning/phases/05-feedback-integration/05-CONTEXT.md (D-05..D-09 — the intended success/error/retry/timeout behavior any fix must preserve)
    - .planning/phases/06-polish-verification/06-PATTERNS.md (route components stay presentation-only; typed errors caught at boundaries)
  </read_first>
  <action>
    If Task 1 reported all four conditions held, make NO code changes — record
    the clean pass in the summary and stop. If Task 1 reported a specific
    failure, fix it inline in the smallest possible change (D-04), preserving
    the Phase 5 design: submitFeedback stays fire-and-forget from the quiz's
    perspective (never a blocking await that freezes the Quiz screen), the modal
    keeps its idle/submitting/success/error state machine, and showRetry stays
    gated on server-error/network-error only. Do not add new dependencies, do
    not change FEEDBACK_ENDPOINT, and only adjust TIMEOUT_MS if the failure was
    specifically a premature-abort under a normal cold start. After any edit,
    run `npm test` and `npm run typecheck` — both must stay green.
  </action>
  <verify>
    <automated>npm test -- __tests__/feedback-submit.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - If no failure was reported: git diff is empty for both files and the
      summary records a clean cold-start pass.
    - If a failure was reported: the specific failure is addressed, `npm test`
      is green, `npm run typecheck` exits 0, and the fire-and-forget /
      non-blocking-quiz invariant is preserved (no blocking await added to the
      quiz path).
    - No new npm dependencies were added (package.json diff empty).
  </acceptance_criteria>
  <done>Cold-start behavior verified graceful; either no code change (clean pass) or a minimal inline fix with green suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → live backend (POST /feedback) | Existing, already-validated boundary from Phase 5; not modified here (only exercised). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-N/A | — | Existing POST /feedback flow (unchanged) | accept | No new endpoint, auth, or data flow — this exercises an already-shipped, Zod-validated boundary. No new attack surface; no package installs. Any inline fix is behavior-preserving. |
</threat_model>

<verification>
- Human checkpoint confirms spinner, non-blocking quiz, no lost progress,
  success-or-Retry resolution, no premature 90s abort.
- `npm test` green; `npm run typecheck` exits 0 (after any fix, or unchanged).
</verification>

<success_criteria>
A manual test against a genuinely cold Render backend confirms graceful
degradation (loading state, no crash, no lost quiz progress) — Phase 6
Success Criterion 2 satisfied.
</success_criteria>

<output>
Create `.planning/phases/06-polish-verification/06-03-SUMMARY.md` when done.
</output>
