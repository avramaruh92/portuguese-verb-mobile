---
phase: 06-polish-verification
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/dataset/verbs.ts
autonomous: false   # contains a blocking human-verify checkpoint
requirements: []   # Addresses Phase 6 Success Criterion 1 (dataset accuracy)
user_setup: []

must_haves:
  truths:
    - "The user has reviewed the discrepancy list and confirmed which cells (if any) are genuinely wrong"
    - "Every user-confirmed correction is applied to src/dataset/verbs.ts"
    - "The dataset still validates (zero shape/completeness errors) and the full test suite stays green after any edits"
    - "No outstanding discrepancies remain unresolved at phase close (Success Criterion 1)"
  artifacts:
    - path: "src/dataset/verbs.ts"
      provides: "Corrected 50-verb dataset (or unchanged if the review confirms it was already correct)"
      contains: "export const verbs"
  key_links:
    - from: "src/dataset/verbs.ts"
      to: "__tests__/dataset.test.ts"
      via: "validateDataset(verbs).errors stays empty and verbs.length === 50 after edits"
      pattern: "validateDataset"
---

<objective>
Have the user spot-check the discrepancy list from Plan 01, then apply every
confirmed conjugation correction inline to `src/dataset/verbs.ts` (D-01, D-04).
This is the human-review + fix half of Phase 6 Success Criterion 1.

Purpose: The dataset accuracy pass was always scoped as
assistant-drafts/user-reviews (Phase 2 D-04) — Claude's re-derivation is a
proposal, not authority. The user confirms real errors, then Claude corrects
them without disturbing dataset shape.

Output: `src/dataset/verbs.ts` with confirmed corrections applied (or
unchanged, with the review recorded as a clean pass), green test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-verification/06-CONTEXT.md
@.planning/phases/06-polish-verification/06-PATTERNS.md
@.planning/phases/06-polish-verification/06-DATASET-DISCREPANCIES.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: User spot-checks the discrepancy list</name>
  <read_first>
    - .planning/phases/06-polish-verification/06-DATASET-DISCREPANCIES.md (the findings the user is confirming)
  </read_first>
  <what-built>
    Plan 01 independently re-derived all 1,200 conjugation cells and produced
    a discrepancy list at
    `.planning/phases/06-polish-verification/06-DATASET-DISCREPANCIES.md`,
    including any low-confidence flags.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/06-polish-verification/06-DATASET-DISCREPANCIES.md`.
    2. Read the Summary section for total discrepancies and low-confidence flags.
    3. For each discrepancy row, confirm whether the "suspected correct value"
       is genuinely correct European Portuguese (approve), or whether the
       existing value in verbs.ts was actually right (reject), or defer if
       unsure.
    4. Reply with the disposition per row — e.g. "approve all", "approve rows
       1,3,5; reject 2,4", or "reject all, dataset is correct as-is".
  </how-to-verify>
  <acceptance_criteria>
    - The user has returned an explicit approve/reject disposition for every
      discrepancy row (or a blanket "approve all" / "reject all").
    - No discrepancy row is left without a disposition.
  </acceptance_criteria>
  <resume-signal>Provide per-row disposition (e.g. "approve all" or "approve rows X, reject rows Y")</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply confirmed corrections and re-validate</name>
  <files>src/dataset/verbs.ts</files>
  <read_first>
    - src/dataset/verbs.ts (the file being edited — read current state before touching)
    - .planning/phases/06-polish-verification/06-DATASET-DISCREPANCIES.md (which rows were approved)
    - .planning/phases/06-polish-verification/06-PATTERNS.md (verbs.ts edit rules: change only the specific tense.subject string, never reorder keys, never touch verb/translation/isIrregular)
    - src/dataset/validate.ts (validateDataset contract the edit must keep satisfied)
  </read_first>
  <action>
    For each discrepancy row the user APPROVED in Task 1, edit the single
    matching `conjugations[tense][subject]` string value in src/dataset/verbs.ts
    to the confirmed correct value. Change only those specific string values —
    do not reorder any keys, do not modify verb/translation/isIrregular fields,
    and do not touch cells for rejected or deferred rows. If the user rejected
    all rows (dataset already correct), make zero edits and record that in the
    summary. After edits, run `npm test` and `npm run typecheck`; both must
    pass, and dataset validation (via __tests__/dataset.test.ts) must still
    report zero errors with verbs.length === 50. If any approved correction
    would break shape/completeness, stop and surface it rather than forcing it.
  </action>
  <verify>
    <automated>npm test -- __tests__/dataset.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- __tests__/dataset.test.ts` passes (validateDataset errors
      empty, verbs.length === 50).
    - `npm run typecheck` exits 0.
    - `git diff src/dataset/verbs.ts` shows changes ONLY to approved
      `conjugations[tense][subject]` string values (no key reordering, no
      verb/translation/isIrregular changes) — or an empty diff if all rows were
      rejected.
    - Full `npm test` suite is green.
  </acceptance_criteria>
  <done>All user-approved corrections applied (or none, if rejected); dataset validates; test suite and typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. Editing a local static data file gated behind a human checkpoint.
No untrusted input, no network, no auth.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-N/A | — | Static dataset edit behind human sign-off | accept | No new attack surface. Correctness (not security) is the risk, mitigated by the human-verify checkpoint + dataset validation test. No package installs. |
</threat_model>

<verification>
- Human checkpoint returned explicit per-row dispositions.
- `npm test` green; `npm run typecheck` exits 0.
- verbs.ts diff limited to approved conjugation string values.
</verification>

<success_criteria>
No outstanding dataset discrepancies remain: every re-derived discrepancy was
either corrected (user-approved) or explicitly dismissed (user-rejected), with
the dataset still validating and the suite green — Phase 6 Success Criterion 1
satisfied.
</success_criteria>

<output>
Create `.planning/phases/06-polish-verification/06-02-SUMMARY.md` when done.
</output>
