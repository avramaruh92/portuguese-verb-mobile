---
phase: 06-polish-verification
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quiz/engine.ts
  - src/store/useQuizStore.ts
  - app/results.tsx
  - app/index.tsx
autonomous: false   # requires on-device human verification of three edge cases
requirements: []   # Addresses Phase 6 Success Criterion 3 (edge cases)
user_setup: []

must_haves:
  truths:
    - "A filter combination yielding fewer than 10 eligible triples shows the friendly InsufficientVerbsError message with no crash and a clear path to adjust filters and retry"
    - "Cancelling/dismissing the share sheet from Results leaves the screen interactive with no error surfaced"
    - "Toggling 'Include irregular verbs' after a quiz has started has no effect on the in-progress session's filters (snapshot boundary holds)"
  artifacts:
    - path: "src/quiz/engine.ts"
      provides: "InsufficientVerbsError guard (only edited if edge case #1 reveals a real bug — D-04)"
      contains: "InsufficientVerbsError"
  key_links:
    - from: "src/store/useQuizStore.ts"
      to: "src/quiz/engine.ts"
      via: "startQuiz catches InsufficientVerbsError and sets status:error + friendly errorMessage"
      pattern: "InsufficientVerbsError"
    - from: "app/index.tsx"
      to: "src/store/useQuizStore.ts"
      via: "includeIrregular is component-local useState read only at handleStartQuiz — never a live subscription into session filters"
      pattern: "startQuiz"
---

<objective>
Manually confirm the three research-flagged edge cases are handled without
crashes or dead ends (D-03), fixing inline only if a real defect surfaces
(D-04). This is Phase 6 Success Criterion 3.

Purpose: These are structurally untestable-by-automation UX conditions —
insufficient eligible verbs, share-sheet cancellation, and irregular-toggle
state during an active session. The existing code already handles all three
(06-PATTERNS.md); the goal is confirming that holds when actually exercised on
a device, not building new defensive code.

Output: Three verified edge-case behaviors. Code diffs only if a genuine
defect is found.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-verification/06-CONTEXT.md
@.planning/phases/06-polish-verification/06-PATTERNS.md
@.planning/phases/04-quiz-experience-setup-quiz-results/04-CONTEXT.md

<interfaces>
From src/quiz/engine.ts — sampleTriples(pool, count, random) throws
InsufficientVerbsError when pool.length < count (count = 10 session size).

From src/store/useQuizStore.ts — startQuiz(options) catches
InsufficientVerbsError → set({ status:"error", errorMessage:
INSUFFICIENT_VERBS_MESSAGE, session:null }); filters is written ONLY inside
startQuiz (snapshot at session start).

From app/results.tsx — handleShare() wraps Share.share() in try/catch that
silently swallows cancellation, keeping the screen interactive.

From app/index.tsx — includeIrregular is component-local useState (line ~16),
read only at handleStartQuiz call time; error message renders when
status==="error" && errorMessage.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device verification of the three edge cases</name>
  <read_first>
    - src/quiz/engine.ts (sampleTriples / InsufficientVerbsError guard — edge case #1)
    - src/store/useQuizStore.ts (startQuiz error translation + filters snapshot — edge cases #1 and #3)
    - app/results.tsx (handleShare silent-swallow try/catch — edge case #2)
    - app/index.tsx (irregular toggle local state + error display — edge cases #1 and #3)
    - .planning/phases/06-polish-verification/06-CONTEXT.md (D-03 exact edge-case expectations and the discretion note allowing a narrow real filter vs a temporary test-only override)
  </read_first>
  <what-built>
    All three code paths already exist (06-PATTERNS.md): the
    InsufficientVerbsError → friendly message chain, the share-cancel
    silent-swallow, and the filters-snapshot-at-startQuiz boundary. This test
    exercises the REAL code paths on-device (per D-03 discretion, a genuine
    narrow filter that yields <10 triples is preferred over a mocked stand-in;
    a temporary test-only filter widening/narrowing is acceptable as long as the
    real InsufficientVerbsError path is what fires).
  </what-built>
  <how-to-verify>
    Start the app: `npx expo start --ios`, then verify each edge case:

    EDGE CASE 1 — Insufficient eligible verbs:
    1. On the Setup screen, pick the narrowest filter combination that yields
       fewer than 10 eligible (verb × tense × subject) triples (e.g. a single
       tense with irregulars off if the regular pool for that tense is small,
       or whatever combination the real dataset makes scarce).
    2. Tap Start Quiz. Confirm: the friendly INSUFFICIENT_VERBS_MESSAGE shows
       on the Setup screen, the app does NOT crash, and you can adjust the
       filters and successfully start a valid quiz afterward.

    EDGE CASE 2 — Share-sheet cancellation:
    3. Complete (or fast-forward) a quiz to the Results screen.
    4. Tap Share, then cancel/dismiss the iOS share sheet without sharing.
    5. Confirm: the Results screen stays fully interactive (replay/share still
       work), and NO error message or crash surfaces.

    EDGE CASE 3 — Irregular-toggle mid-session:
    6. Start a quiz (note whether irregulars were included). Navigate back to
       Setup mid-quiz (or background and return), toggle "Include irregular
       verbs" to the opposite state, and return to the in-progress quiz.
    7. Confirm: the already-running session is UNCHANGED (same questions, same
       filters) — the toggle only affects the NEXT startQuiz. Then start a
       fresh quiz and confirm the new toggle state IS applied.

    Report the outcome per edge case, calling out any crash or dead-end.
  </how-to-verify>
  <acceptance_criteria>
    - Edge case 1: user confirms friendly message shown, no crash, retry path
      works.
    - Edge case 2: user confirms Results stays interactive, no error surfaced.
    - Edge case 3: user confirms in-progress session unaffected by the toggle,
      and a fresh quiz picks up the new toggle state.
    - Any crash or dead-end is described specifically for Task 2.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all three edge cases held, or describe which edge case failed and how</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Inline fix only if an edge case failed</name>
  <files>src/quiz/engine.ts, src/store/useQuizStore.ts, app/results.tsx, app/index.tsx</files>
  <read_first>
    - src/quiz/engine.ts (current InsufficientVerbsError guard style)
    - src/store/useQuizStore.ts (current error translation + filters snapshot invariant)
    - app/results.tsx (current handleShare try/catch)
    - app/index.tsx (current toggle local-state + error display)
    - .planning/phases/06-polish-verification/06-PATTERNS.md (invariants any fix must preserve: typed errors caught only at store boundary, routes stay presentation-only, filters snapshot boundary, share-cancel stays silently swallowed with no error UI)
  </read_first>
  <action>
    If Task 1 reported all three edge cases held, make NO code changes — record
    the clean pass in the summary and stop. If a specific edge case failed, fix
    it inline with the smallest change (D-04) while preserving the documented
    invariants: (1) for edge case 1, keep the typed InsufficientVerbsError
    thrown in src/quiz/engine.ts and caught only in useQuizStore.startQuiz —
    never surface a raw error in a route; (2) for edge case 2, keep the fix
    inside app/results.tsx's handleShare try/catch and do NOT add error-state UI
    (cancel must remain silent); (3) for edge case 3, do NOT add any live
    subscription from Setup's includeIrregular toggle into an in-progress
    session's store filters — preserve the snapshot-at-startQuiz boundary.
    Remove any temporary test-only filter override introduced during Task 1
    verification before finishing. After any edit, run `npm test` and
    `npm run typecheck` — both must stay green.
  </action>
  <verify>
    <automated>npm test && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - If no failure was reported: git diff is empty for all four files and the
      summary records a clean edge-case pass.
    - If a failure was reported: the specific edge case is fixed, `npm test` is
      green, `npm run typecheck` exits 0, and the relevant invariant is
      preserved (typed-error-at-store-boundary / silent share-cancel / filters
      snapshot boundary).
    - No temporary test-only filter override remains in the committed code.
    - No new npm dependencies were added (package.json diff empty).
  </acceptance_criteria>
  <done>All three edge cases verified handled; either no code change (clean pass) or a minimal invariant-preserving inline fix with green suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new. All three edge cases exercise existing local (offline) code paths —
quiz generation, native share sheet, and store state. No network beyond the
already-covered feedback flow, no auth, no untrusted input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-N/A | — | Existing quiz/store/share code paths (unchanged unless a bug is found) | accept | Verification-only; no new endpoints, auth, data flows, or dependencies. Any inline fix is invariant-preserving. No package installs. |
</threat_model>

<verification>
- Human checkpoint confirms all three edge cases handled without crash/dead-end.
- `npm test` green; `npm run typecheck` exits 0 (after any fix, or unchanged).
- No temporary test-only override left in committed code.
</verification>

<success_criteria>
Insufficient-verbs, share-sheet cancellation, and irregular-toggle-mid-session
edge cases are confirmed handled without crashes or dead ends — Phase 6
Success Criterion 3 satisfied.
</success_criteria>

<output>
Create `.planning/phases/06-polish-verification/06-04-SUMMARY.md` when done.
</output>
