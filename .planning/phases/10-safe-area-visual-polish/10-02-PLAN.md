---
phase: 10-safe-area-visual-polish
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/index.tsx
  - app/results.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "Setup and Results each show a titleless native header (top safe-area chrome) (D-01, D-02)"
    - "Setup and Results action buttons clear the iOS home indicator via bottom inset padding (D-01)"
    - "Setup's Start and Results' Try Again buttons show a native ActivityIndicator while loading, without changing button height (D-04)"
    - "The status==='error' and unexpectedError text on both screens is restyled with shared tokens (no bare inline red text) (D-05)"
    - "Both screens draw all colors/spacing/typography from the shared tokens module, not hex/number literals (D-03)"
  artifacts:
    - path: "app/index.tsx"
      provides: "Tokenized Setup screen with header, bottom inset, loading glyph, styled error"
      contains: "useSafeAreaInsets"
    - path: "app/results.tsx"
      provides: "Tokenized Results screen with header, bottom inset, loading glyph, styled error"
      contains: "useSafeAreaInsets"
  key_links:
    - from: "app/index.tsx"
      to: "src/theme/tokens"
      via: "StyleSheet.create references colors/spacing/typography"
      pattern: "from \"../src/theme/tokens\""
    - from: "app/results.tsx"
      to: "react-native-safe-area-context"
      via: "useSafeAreaInsets bottom padding"
      pattern: "useSafeAreaInsets"
---

<objective>
Apply the Wave-1 foundations to the two near-identical screens (Setup and
Results): add a titleless native header, tokenize all style literals, add
bottom safe-area inset padding, augment the "Starting…" button state with a
native `ActivityIndicator`, and restyle the existing error-text blocks with
shared tokens. Setup and Results share verbatim `starting`/`unexpectedError`
and error-text structure — kept in ONE plan so their treatments do not drift.

Purpose: UI-01 (nothing under status bar/home indicator), UI-02 (shared tokens),
UI-03 (styled loading + error) on the two form/results screens.
Output: updated `app/index.tsx`, `app/results.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-safe-area-visual-polish/10-CONTEXT.md
@.planning/phases/10-safe-area-visual-polish/10-PATTERNS.md
@.planning/phases/10-safe-area-visual-polish/10-UI-SPEC.md

<interfaces>
<!-- Tokens created in plan 10-01. Import and reference directly — no re-derivation. -->
From src/theme/tokens.ts:
```typescript
export const colors: { background; secondary; accent; error; success; text; textSecondary };
export const spacing: { sm: 8; md: 16; lg: 24; xl2: 48; xl3: 64; choiceGap: 12 };
export const radius: { control: 12 };
export const typography: { caption; body; bodyStrong; heading; display };
```

<!-- Canonical titleless-header shape to replicate (from app/quiz.tsx, minus headerLeft). -->
```typescript
<Stack.Screen options={{ headerShown: true, headerTitle: "" }} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish Setup screen (app/index.tsx)</name>
  <files>app/index.tsx</files>
  <read_first>
    - app/index.tsx (current full file — preserve all state logic and conditional rendering)
    - src/theme/tokens.ts (created in 10-01 — the exact token names to import)
    - app/quiz.tsx lines 89-99 (canonical titleless Stack.Screen header shape to copy, minus headerLeft)
    - .planning/phases/10-safe-area-visual-polish/10-PATTERNS.md (§"app/index.tsx (Setup)")
    - .planning/phases/10-safe-area-visual-polish/10-UI-SPEC.md (§Copywriting Contract, §Layout & Safe-Area Contract)
  </read_first>
  <action>
    Modify `app/index.tsx` (keep ALL existing state/handler logic and conditional
    rendering unchanged — this is styling + chrome only):
    (1) Import `Stack` from `expo-router` (currently only `useRouter` is imported).
    Render `<Stack.Screen options={{ headerShown: true, headerTitle: "" }} />` as
    the first child of a wrapping fragment — chrome-only, NO `headerLeft`/`headerRight`
    (per D-01/UI-SPEC "Setup defaults to chrome-only").
    (2) Import `useSafeAreaInsets` from `react-native-safe-area-context`; call
    `const insets = useSafeAreaInsets()` inside the component; apply
    `paddingBottom: insets.bottom` merged onto the outermost `container` View's style
    array so the Start button never sits under the home indicator.
    (3) Import `ActivityIndicator` from `react-native`; inside the Start `Pressable`,
    when `starting` is true, render `<ActivityIndicator size="small" color={colors.background} />`
    alongside the existing "Starting…" text (white glyph on the blue accent button).
    Do NOT change the button's `minHeight: 44` or its disabled logic.
    (4) Import `colors`, `spacing`, `typography` (and `radius` if used) from
    `../src/theme/tokens`; in `StyleSheet.create`, replace every hex literal and
    spacing/font number with the matching token reference (e.g. `"#007AFF"` →
    `colors.accent`, `"#F2F2F7"` → `colors.secondary`, `"#FF3B30"` → `colors.error`,
    `"#000000"` → `colors.text`, `16`→`spacing.md`, `24`→`spacing.lg`, `8`→`spacing.sm`,
    `borderRadius: 12`→`radius.control`, `gap: 8`→`spacing.sm`, and typography via
    spreads like `...typography.heading` / `...typography.body`). Keep each style key
    and structure intact — only the literal values become token refs.
    (5) Restyle `styles.errorText` using tokens (`color: colors.error`,
    `marginBottom: spacing.lg`, `...typography.caption`); per D-05 optionally wrap the
    two error `Text` blocks in a container `View` for light card/spacing treatment.
    Do NOT change the error copy or the `status === "error"`/`unexpectedError`
    conditional rendering.
  </action>
  <verify>
    <automated>npm run typecheck && test $(grep -Ec '#007AFF|#F2F2F7|#FF3B30|#34C759|#8E8E93|#FFFFFF|#000000' app/index.tsx) -eq 0 && grep -q 'useSafeAreaInsets' app/index.tsx && grep -q 'ActivityIndicator' app/index.tsx && grep -q 'headerTitle: ""' app/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec '#007AFF|#F2F2F7|#FF3B30|#34C759|#8E8E93|#FFFFFF|#000000' app/index.tsx` returns 0 (all hex literals replaced by tokens).
    - `grep -c 'from "../src/theme/tokens"' app/index.tsx` returns 1.
    - `grep -c 'headerTitle: ""' app/index.tsx` returns 1; `grep -c 'headerLeft' app/index.tsx` returns 0 (chrome-only).
    - `grep -c 'useSafeAreaInsets' app/index.tsx` returns >= 2 (import + call); `insets.bottom` applied to the container.
    - `grep -c 'ActivityIndicator' app/index.tsx` returns >= 2 (import + JSX); `minHeight: 44` still present on startButton.
    - `grep -c '"Starting…"' app/index.tsx` returns 1 (copy unchanged).
    - `npm run typecheck` exits 0; `npm test` passes unchanged.
  </acceptance_criteria>
  <done>Setup renders titleless header, tokenized styles, bottom inset, loading glyph, and styled error text — no logic or copy changes.</done>
</task>

<task type="auto">
  <name>Task 2: Polish Results screen (app/results.tsx)</name>
  <files>app/results.tsx</files>
  <read_first>
    - app/results.tsx (current full file — note BOTH the no-session fallback branch and the main render path)
    - app/index.tsx (Task 1 result — mirror its ActivityIndicator + error-block treatment for consistency; do not let the two drift)
    - src/theme/tokens.ts (token names to import)
    - .planning/phases/10-safe-area-visual-polish/10-PATTERNS.md (§"app/results.tsx (Results)", including the legacy paddingTop:64 re-evaluation)
    - .planning/phases/10-safe-area-visual-polish/10-UI-SPEC.md (§Layout & Safe-Area Contract — "Results' legacy paddingTop: 64" row)
  </read_first>
  <action>
    Modify `app/results.tsx` (keep ALL existing state/handler logic and both render
    branches unchanged — styling + chrome only):
    (1) Import `Stack` from `expo-router`; render
    `<Stack.Screen options={{ headerShown: true, headerTitle: "" }} />` as first child
    — chrome-only, no `headerLeft` (Results already has an in-body "Back to Setup"
    button; a header back affordance would be redundant per Claude's Discretion).
    (2) Import `useSafeAreaInsets`; apply `paddingBottom: insets.bottom` to the
    outermost `container` View style array so Share/Try Again/Back buttons clear the
    home indicator.
    (3) Re-evaluate `styles.container.paddingTop: 64` (currently `spacing.xl3` after
    tokenization): now that a native header supplies the top inset, reduce or remove
    this manual notch offset to avoid doubled top whitespace — replace the `64`
    literal with a smaller token (e.g. `spacing.lg`) OR remove `paddingTop` entirely.
    Do not leave `spacing.xl3` (64) stacked on top of the header. Flag the exact chosen
    value in the SUMMARY for the Wave-3 human-verify pass.
    (4) Add the SAME `ActivityIndicator size="small" color={colors.background}`
    treatment inside the Try Again `Pressable` when `starting` is true, matching
    Setup's Start button exactly (UI-SPEC calls for identical treatment). Keep
    `minHeight: 44`.
    (5) Import `colors`, `spacing`, `typography`, `radius` from `../src/theme/tokens`
    and replace every hex/number literal in `StyleSheet.create` with token refs
    (`"#007AFF"`→`colors.accent`, `"#F2F2F7"`→`colors.secondary`, `"#FF3B30"`→
    `colors.error`, `"#8E8E93"`→`colors.textSecondary`, `"#000000"`→`colors.text`,
    `"#FFFFFF"`→`colors.background`, `56/62`→`...typography.display`, `48`→`spacing.xl2`,
    `16`→`spacing.md`, `24`→`spacing.lg`, `8`→`spacing.sm`, `borderRadius: 12`→
    `radius.control`).
    (6) Restyle BOTH error `Text` blocks (in the no-session fallback branch, lines
    24-29) using the same token-based error treatment Setup landed on — keep the two
    call sites identical. Do not change error copy or conditional rendering.
  </action>
  <verify>
    <automated>npm run typecheck && test $(grep -Ec '#007AFF|#F2F2F7|#FF3B30|#34C759|#8E8E93|#FFFFFF|#000000' app/results.tsx) -eq 0 && grep -q 'useSafeAreaInsets' app/results.tsx && grep -q 'ActivityIndicator' app/results.tsx && grep -q 'headerTitle: ""' app/results.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec '#007AFF|#F2F2F7|#FF3B30|#34C759|#8E8E93|#FFFFFF|#000000' app/results.tsx` returns 0.
    - `grep -c 'from "../src/theme/tokens"' app/results.tsx` returns 1.
    - `grep -c 'headerTitle: ""' app/results.tsx` returns 1; `grep -c 'headerLeft' app/results.tsx` returns 0.
    - `grep -c 'useSafeAreaInsets' app/results.tsx` returns >= 2; `insets.bottom` applied to container.
    - `grep -c 'ActivityIndicator' app/results.tsx` returns >= 2; `minHeight: 44` still present on tryAgainButton.
    - `grep -c 'spacing.xl3' app/results.tsx` returns 0 (legacy 64px top offset reduced/removed after header lands).
    - `grep -c '"Starting…"' app/results.tsx` returns 1 (copy unchanged).
    - `npm run typecheck` exits 0; `npm test` passes unchanged.
  </acceptance_criteria>
  <done>Results renders titleless header, tokenized styles, bottom inset, matching loading glyph, styled errors, and de-doubled top padding — no logic or copy changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Styling/chrome changes only; no new input, network, storage, or auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | app/index.tsx, app/results.tsx styles | accept | Only literal values swapped for token refs; state/handler logic untouched; typecheck + full suite guard regressions |
| T-10-04 | Information Disclosure | error-text render | accept | Error copy preserved verbatim (InsufficientVerbsError / String(error)); no new data surfaced, only restyled |
| T-10-SC | Tampering | npm installs | mitigate | No package installs — all imports resolve to already-installed deps |

No new attack surface introduced by this plan.
</threat_model>

<verification>
- `npm test` full suite passes unchanged.
- `npm run typecheck` exits 0.
- No raw hex literals remain in either screen; both import from tokens.
- Both screens have titleless header, bottom inset, ActivityIndicator loading, styled error blocks.
</verification>

<success_criteria>
- UI-01: Setup and Results content clears status bar (native header) and home indicator (bottom inset).
- UI-02: Both screens draw all styling from shared tokens.
- UI-03: Loading (ActivityIndicator) and error states are styled, not bare.
</success_criteria>

<output>
Create `.planning/phases/10-safe-area-visual-polish/10-02-SUMMARY.md` when done. Record the chosen Results top-padding value for the human-verify pass.
</output>
