---
phase: 10-safe-area-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/tokens.ts
  - src/theme/tokens.test.ts
  - app/_layout.tsx
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "A single shared tokens module exports the app's exact existing colors, spacing, radius, and typography values"
    - "SafeAreaProvider wraps the root Stack so useSafeAreaInsets works on every screen"
    - "The root layout enables a native header app-wide (top safe-area chrome)"
  artifacts:
    - path: "src/theme/tokens.ts"
      provides: "Flat-export design tokens (colors, spacing, radius, typography)"
      exports: ["colors", "spacing", "radius", "typography"]
    - path: "src/theme/tokens.test.ts"
      provides: "Verbatim-value guard proving zero visible change (D-03 lock)"
    - path: "app/_layout.tsx"
      provides: "Root SafeAreaProvider wrap + headerShown default flip"
      contains: "SafeAreaProvider"
  key_links:
    - from: "app/_layout.tsx"
      to: "react-native-safe-area-context"
      via: "SafeAreaProvider import wrapping <Stack>"
      pattern: "SafeAreaProvider"
---

<objective>
Establish the two foundations every screen plan depends on: (1) a single shared
design-tokens module holding the app's exact existing palette/spacing/typography
values, and (2) the root-layout wiring that turns on native headers app-wide and
mounts `SafeAreaProvider` so per-screen inset hooks work.

Purpose: Screens in Wave 2 import tokens and call `useSafeAreaInsets()`; neither
works until this plan lands. This is a pure extraction + wiring pass — zero
user-visible color/spacing change (D-03 lock).
Output: `src/theme/tokens.ts`, `src/theme/tokens.test.ts`, updated `app/_layout.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-safe-area-visual-polish/10-CONTEXT.md
@.planning/phases/10-safe-area-visual-polish/10-PATTERNS.md
@.planning/phases/10-safe-area-visual-polish/10-UI-SPEC.md

<interfaces>
<!-- Analog module shape the executor must mirror (flat named const exports, no default export). -->
From src/quiz/labels.ts:
```typescript
export const subjectLabels: Record<Subject, string> = { /* ... */ };
export const tenseLabels: Record<Tense, string> = { /* ... */ };
```

Current app/_layout.tsx (full file — 11 lines):
```typescript
import { useEffect } from "react";
import { Stack } from "expo-router";
import { prefetch } from "../src/dataset/source";

export default function RootLayout() {
  useEffect(() => {
    prefetch();
  }, []);

  return <Stack screenOptions={{ headerShown: false }} />;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared design-tokens module + verbatim-value guard test</name>
  <files>src/theme/tokens.ts, src/theme/tokens.test.ts</files>
  <read_first>
    - src/quiz/labels.ts (flat named-const-export shape to mirror; no default export)
    - .planning/phases/10-safe-area-visual-polish/10-PATTERNS.md (§"src/theme/tokens.ts" — the authoritative verbatim value block)
    - .planning/phases/10-safe-area-visual-polish/10-UI-SPEC.md (§Color, §Spacing Scale, §Typography)
    - app/index.tsx, app/quiz.tsx, app/results.tsx (source of the literals being extracted — confirm values match before centralizing)
  </read_first>
  <behavior>
    - Test: `colors` exports exactly background="#FFFFFF", secondary="#F2F2F7", accent="#007AFF", error="#FF3B30", success="#34C759", text="#000000", textSecondary="#8E8E93".
    - Test: `spacing` exports exactly sm=8, md=16, lg=24, xl2=48, xl3=64, choiceGap=12.
    - Test: `radius` exports exactly control=12.
    - Test: `typography.display` has fontSize=56, fontWeight="600", lineHeight=62; typography.heading fontSize=20/weight"600"; typography.body fontSize=16/weight"400"; typography.bodyStrong fontSize=16/weight"600"; typography.caption fontSize=14/weight"400".
  </behavior>
  <action>
    Create `src/theme/tokens.ts` mirroring `src/quiz/labels.ts` shape: multiple flat
    `export const` objects, no default export. Export four objects with these EXACT
    verbatim values (pure extraction — introduce no new values, D-03 lock):
    `colors` = { background: "#FFFFFF", secondary: "#F2F2F7", accent: "#007AFF",
    error: "#FF3B30", success: "#34C759", text: "#000000", textSecondary: "#8E8E93" }.
    `spacing` = { sm: 8, md: 16, lg: 24, xl2: 48, xl3: 64, choiceGap: 12 } — keep
    choiceGap=12 as a named documented exception (not snapped to 8/16, per UI-SPEC).
    `radius` = { control: 12 } — separate named token from choiceGap even though the
    value coincides (radius vs spacing mean different things; do not alias).
    `typography` = { caption: { fontSize: 14, fontWeight: "400" as const },
    body: { fontSize: 16, fontWeight: "400" as const },
    bodyStrong: { fontSize: 16, fontWeight: "600" as const },
    heading: { fontSize: 20, fontWeight: "600" as const },
    display: { fontSize: 56, fontWeight: "600" as const, lineHeight: 62 } }.
    Use `as const` on each fontWeight so the literal type is assignable to RN
    `TextStyle.fontWeight`. Then create `src/theme/tokens.test.ts` (plain Jest, no
    RN/React imports) asserting each value above with `toBe`/`toEqual` per the
    behavior block — this is the guardrail proving the extraction changed nothing.
  </action>
  <verify>
    <automated>npm test -- src/theme/tokens.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/theme/tokens.ts` exports `colors`, `spacing`, `radius`, `typography` (no default export).
    - `grep -c 'export const' src/theme/tokens.ts` returns 4.
    - `npm test -- src/theme/tokens.test.ts` passes with all value assertions green.
    - `npm run typecheck` exits 0 (fontWeight `as const` literals accepted).
    - No value in tokens.ts differs from the literals currently in app/index.tsx, app/quiz.tsx, app/results.tsx.
  </acceptance_criteria>
  <done>Tokens module exists with verbatim values; guard test passes; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SafeAreaProvider + app-wide native header in root layout</name>
  <files>app/_layout.tsx</files>
  <read_first>
    - app/_layout.tsx (current 11-line file — preserve the prefetch useEffect exactly)
    - .planning/phases/10-safe-area-visual-polish/10-PATTERNS.md (§"app/_layout.tsx" required changes; §"SafeAreaProvider root wrap")
    - .planning/phases/10-safe-area-visual-polish/10-UI-SPEC.md (§Layout & Safe-Area Contract — "Root wrapping" and "Top safe area" rows)
    - package.json (confirm react-native-safe-area-context ~5.7.0 present; no install needed)
  </read_first>
  <action>
    Modify `app/_layout.tsx`: import `SafeAreaProvider` from
    `react-native-safe-area-context` (already a dependency — do NOT add a package).
    Wrap the returned `<Stack>` in `<SafeAreaProvider>...</SafeAreaProvider>` — this
    is the single, only mount point (no per-screen providers). Change the Stack's
    `screenOptions` from `{ headerShown: false }` to `{ headerShown: true }` so all
    3 screens get a native header (top safe-area chrome) by default; per-screen
    `Stack.Screen options` in Wave 2 will set `headerTitle: ""`. Keep the existing
    `prefetch()` `useEffect` byte-for-byte unchanged (Phase 7/8 logic, out of scope;
    D-04 confirms prefetch stays silent). Do not add StatusBar, headerTitle, or any
    other option here.
  </action>
  <verify>
    <automated>npm run typecheck && grep -c 'SafeAreaProvider' app/_layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SafeAreaProvider' app/_layout.tsx` returns >= 2 (import + JSX wrapper).
    - `grep -c 'headerShown: true' app/_layout.tsx` returns 1; `grep -c 'headerShown: false' app/_layout.tsx` returns 0.
    - `grep -c 'prefetch' app/_layout.tsx` returns >= 2 (import + useEffect call still present).
    - `npm run typecheck` exits 0.
    - `<Stack>` is nested inside `<SafeAreaProvider>` in the returned JSX.
  </acceptance_criteria>
  <done>Root layout mounts SafeAreaProvider and enables native headers app-wide; prefetch untouched; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure UI/styling phase — no new input, network, storage, or auth flows introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | src/theme/tokens.ts | accept | Static style constants, no runtime input; verbatim-value test guards accidental change |
| T-10-02 | (n/a) | app/_layout.tsx wiring | accept | No new data flow; SafeAreaProvider + header are inert chrome; prefetch logic unchanged from Phase 7/8 |
| T-10-SC | Tampering | npm installs | mitigate | No package installs in this phase — react-native-safe-area-context already a resolved dependency; nothing to audit |

No new attack surface introduced by this plan.
</threat_model>

<verification>
- `npm test` (full suite) passes unchanged.
- `npm run typecheck` exits 0.
- tokens module exports match verbatim literals in the 3 screens.
- SafeAreaProvider wraps root Stack; headerShown flipped to true.
</verification>

<success_criteria>
- Shared tokens module exists and is import-ready for Wave 2 screens.
- Root layout provides SafeAreaProvider context + native header default.
- Zero behavioral/visual change from this plan alone (foundation only).
</success_criteria>

<output>
Create `.planning/phases/10-safe-area-visual-polish/10-01-SUMMARY.md` when done.
</output>
