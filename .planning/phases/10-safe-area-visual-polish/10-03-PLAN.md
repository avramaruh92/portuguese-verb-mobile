---
phase: 10-safe-area-visual-polish
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/quiz.tsx
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "Quiz's Next/Report buttons clear the iOS home indicator via bottom inset padding on the scroll content (D-01)"
    - "Quiz draws all colors/spacing/typography from the shared tokens module, not hex/number literals (D-03)"
    - "Quiz keeps its existing titleless header with the headerLeft Exit control (Phase 9 behavior unchanged) (D-01, D-02)"
  artifacts:
    - path: "app/quiz.tsx"
      provides: "Tokenized Quiz screen with bottom safe-area inset, header/Exit preserved"
      contains: "useSafeAreaInsets"
  key_links:
    - from: "app/quiz.tsx"
      to: "src/theme/tokens"
      via: "StyleSheet.create references colors/spacing/typography/radius"
      pattern: "from \"../src/theme/tokens\""
---

<objective>
Bring the Quiz screen up to the shared visual language: tokenize its style
literals and add bottom safe-area inset padding to its ScrollView content.
Quiz already owns the canonical titleless header + `headerLeft` Exit control
(Phase 9) — that stays behaviorally unchanged; only tokenization and the bottom
inset are added here.

Purpose: UI-01 (Next/Report clear the home indicator), UI-02 (Quiz shares tokens).
Output: updated `app/quiz.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-safe-area-visual-polish/10-CONTEXT.md
@.planning/phases/10-safe-area-visual-polish/10-PATTERNS.md
@.planning/phases/10-safe-area-visual-polish/10-UI-SPEC.md

<interfaces>
<!-- Tokens created in plan 10-01. Import and reference directly. -->
From src/theme/tokens.ts:
```typescript
export const colors: { background; secondary; accent; error; success; text; textSecondary };
export const spacing: { sm: 8; md: 16; lg: 24; xl2: 48; xl3: 64; choiceGap: 12 };
export const radius: { control: 12 };
export const typography: { caption; body; bodyStrong; heading; display };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenize Quiz styles + add bottom safe-area inset</name>
  <files>app/quiz.tsx</files>
  <read_first>
    - app/quiz.tsx (current full file — the existing Stack.Screen header + headerLeft Exit is Phase 9 behavior, DO NOT change its logic)
    - src/theme/tokens.ts (created in 10-01 — token names to import)
    - .planning/phases/10-safe-area-visual-polish/10-PATTERNS.md (§"app/quiz.tsx (Quiz)")
    - .planning/phases/10-safe-area-visual-polish/10-UI-SPEC.md (§Layout & Safe-Area Contract — Header left/right row confirms Quiz keeps headerLeft)
  </read_first>
  <action>
    Modify `app/quiz.tsx` (styling only — do NOT touch the confirmExit/beforeRemove
    exit logic, the header's `headerLeft` Exit Pressable, or any quiz state/handlers):
    (1) Import `useSafeAreaInsets` from `react-native-safe-area-context`; call
    `const insets = useSafeAreaInsets()` inside the component; apply
    `paddingBottom: insets.bottom` to the ScrollView's `contentContainerStyle` (merge
    onto `styles.content`, currently `paddingVertical: 24`) so the Next/Report buttons
    clear the home-indicator gesture area. Use a style array on `contentContainerStyle`
    to merge the dynamic inset (do not mutate the StyleSheet object).
    (2) Import `colors`, `spacing`, `radius` (and `typography` if applying spreads)
    from `../src/theme/tokens`; in `StyleSheet.create`, replace every hex/number
    literal with the matching token: `"#007AFF"`→`colors.accent` (exitButtonText,
    progressFill, nextButton bg, reportButtonText), `"#F2F2F7"`→`colors.secondary`
    (progressTrack, choiceDefault, choice bg), `"#34C759"`→`colors.success`
    (choiceCorrect), `"#FF3B30"`→`colors.error` (choiceWrong), `"#000000"`→`colors.text`,
    `"#FFFFFF"`→`colors.background` (choiceTextOnColor, nextButtonText),
    `borderRadius: 12`→`radius.control`, `gap: 12` (choices)→`spacing.choiceGap`
    (documented 12px exception — do NOT snap to 8 or 16), `paddingHorizontal: 16`→
    `spacing.md`, `marginBottom: 24`→`spacing.lg`, `marginBottom: 8`→`spacing.sm`,
    `marginTop: 16`→`spacing.md`. Leave the progress-track height literals (6, 3) as-is
    — they are not part of the spacing scale and have no token (do not invent one).
    Keep every style key and structure intact; only literal values become token refs.
  </action>
  <verify>
    <automated>npm run typecheck && test $(grep -Ec '#007AFF|#F2F2F7|#FF3B30|#34C759|#8E8E93|#FFFFFF|#000000' app/quiz.tsx) -eq 0 && grep -q 'useSafeAreaInsets' app/quiz.tsx && grep -q 'spacing.choiceGap' app/quiz.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec '#007AFF|#F2F2F7|#FF3B30|#34C759|#8E8E93|#FFFFFF|#000000' app/quiz.tsx` returns 0.
    - `grep -c 'from "../src/theme/tokens"' app/quiz.tsx` returns 1.
    - `grep -c 'spacing.choiceGap' app/quiz.tsx` returns 1 (12px choice gap preserved as token, not snapped).
    - `grep -c 'useSafeAreaInsets' app/quiz.tsx` returns >= 2 (import + call); `insets.bottom` merged into contentContainerStyle.
    - `grep -c 'headerLeft' app/quiz.tsx` returns 1 (Exit control preserved); `grep -c 'beforeRemove' app/quiz.tsx` returns 1 (exit guard preserved).
    - `npm run typecheck` exits 0; `npm test` passes unchanged.
  </acceptance_criteria>
  <done>Quiz uses shared tokens and bottom inset padding; header/Exit/exit-guard logic untouched; typecheck and suite clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Styling/inset changes only; no new input, network, storage, or auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | app/quiz.tsx styles | accept | Only literal values swapped for token refs; exit/quiz logic untouched; typecheck + suite guard regressions |
| T-10-06 | Denial of Service | beforeRemove exit guard | accept | Guard logic not modified this plan; grep assertion confirms it remains present |
| T-10-SC | Tampering | npm installs | mitigate | No package installs — imports resolve to already-installed deps |

No new attack surface introduced by this plan.
</threat_model>

<verification>
- `npm test` full suite passes unchanged.
- `npm run typecheck` exits 0.
- No raw hex literals remain; Quiz imports from tokens.
- Bottom inset applied to ScrollView content; header/Exit/exit-guard preserved.
</verification>

<success_criteria>
- UI-01: Quiz Next/Report buttons clear the home indicator via bottom inset.
- UI-02: Quiz draws all styling from shared tokens, including the 12px choiceGap exception.
</success_criteria>

<output>
Create `.planning/phases/10-safe-area-visual-polish/10-03-SUMMARY.md` when done.
</output>
