---
phase: 10-safe-area-visual-polish
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified: []
autonomous: false
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "On Setup, Quiz, and Results, no content renders under the iOS status bar, notch, or home indicator"
    - "The three screens read as one consistent visual language (spacing, typography, color)"
    - "The Start / Try Again loading state shows a styled native ActivityIndicator, not a bare spinner"
    - "The error/fallback state renders with app styling, not raw unstyled red text"
  artifacts: []
  key_links: []
---

<objective>
Final on-device visual verification that the three screens render correctly
under iOS chrome and present a coherent, finished visual treatment. This is the
only success criterion (UI-01 safe-area on a real notch/home-indicator device)
that cannot be proven by an automated test — it requires human eyes on a
simulator or device.

Purpose: Close out the 4 ROADMAP success criteria for Phase 10 with a human sign-off.
Output: human-verify confirmation (no code changes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-safe-area-visual-polish/10-UI-SPEC.md
@.planning/phases/10-safe-area-visual-polish/10-02-SUMMARY.md
@.planning/phases/10-safe-area-visual-polish/10-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device visual sign-off of all 3 screens</name>
  <files>(none — verification only)</files>
  <what-built>
    Foundation (10-01): shared tokens module + SafeAreaProvider + app-wide native
    headers. Setup & Results (10-02): titleless headers, tokenized styles, bottom
    safe-area insets, ActivityIndicator loading glyph on Start/Try Again, restyled
    error text, and reduced Results top padding. Quiz (10-03): tokenized styles +
    bottom safe-area inset (header/Exit already from Phase 9).
  </what-built>
  <action>
    Launch the app in the iOS simulator on a notch/home-indicator device profile and
    walk all three screens plus the loading and error states, following the numbered
    steps in <how-to-verify>. No code is changed in this task — it is a visual
    confirmation gate. If any issue is found (content under chrome, doubled top
    whitespace on Results, unstyled spinner, or bare error text), report it so a
    gap-closure plan can address it before the phase is marked complete.
  </action>
  <how-to-verify>
    1. Run `npm run ios` (or `npx expo start` then press `i`) to launch the app in the
       iOS simulator — prefer a device profile WITH a notch/Dynamic Island and a home
       indicator (e.g. iPhone 15/16 Pro).
    2. Setup screen: confirm the header sits below the status bar/notch and NO heading,
       chip, toggle, or the Start button renders under the status bar or the bottom
       home indicator. Confirm the on-screen "Portuguese Verb Quiz" heading is still in
       the body (header itself is blank/titleless).
    3. Tap Start with tenses selected: confirm the "Starting…" state shows a native
       spinner (ActivityIndicator) inside the button, button height does not jump.
    4. Quiz screen: confirm the Exit control is in the header, and the Next/Report
       buttons at the bottom clear the home indicator (scroll if needed).
    5. Complete or exit to reach Results: confirm the score block, Share/Try Again/Back
       buttons clear both the header and the home indicator — check there is NO doubled
       top whitespace above the score (the legacy 64px padding was reduced). Tap Try
       Again to confirm the same styled ActivityIndicator loading state.
    6. Trigger the error state (on Setup, select a filter combo yielding <10 questions,
       e.g. irregular-off with a single tense that lacks enough verbs) and confirm the
       error message renders with app styling (proper spacing/token color), not bare
       inline red text.
    7. Cross-screen: confirm all 3 screens read as one consistent visual language —
       same blue accent, same button shape/spacing, same typography.
  </how-to-verify>
  <verify>
    <human-check>All 4 ROADMAP Phase 10 success criteria confirmed on a notched/home-indicator device; no content under chrome, styled loading + error, consistent visual language.</human-check>
  </verify>
  <done>Human confirms all 4 success criteria hold, or issues are logged for gap closure.</done>
  <resume-signal>Type "approved" or describe any safe-area, spacing, loading, or error-styling issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; no code changes, no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | (n/a) | human-verify checkpoint | accept | No code modified; visual confirmation only |
| T-10-SC | Tampering | npm installs | mitigate | No package installs in this plan |

No new attack surface introduced by this plan.
</threat_model>

<verification>
- Human confirms all 4 ROADMAP Phase 10 success criteria hold on a notched/home-indicator device.
</verification>

<success_criteria>
- UI-01: No content under status bar/notch/home indicator on Setup, Quiz, Results.
- UI-02: Three screens share a consistent visual language via shared tokens.
- UI-03: Loading (ActivityIndicator) and error states are styled.
</success_criteria>

<output>
Create `.planning/phases/10-safe-area-visual-polish/10-04-SUMMARY.md` recording the human-verify outcome and any issues found.
</output>
