---
phase: 24-quality-gates-preflight-first-submit
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified: []
autonomous: false
requirements: [SHIP-03, SHIP-04, SHIP-05]
must_haves:
  truths:
    - "The preflight script passes all 4 checks against a deliberately cold (>15 min idle) Render instance"
    - "eas build --profile production then eas submit --profile production completes and produces a Processing/Ready build in App Store Connect"
    - "Internal TestFlight testers (Apple Developer team members only) are added and confirm they can install the build"
  artifacts:
    - path: .planning/phases/24-quality-gates-preflight-first-submit/24-03-SUMMARY.md
      provides: "Operator-reported evidence for the cold preflight, build/submit, and tester-install steps"
  key_links:
    - from: "operator"
      to: "App Store Connect / TestFlight"
      via: "eas build + eas submit + manual tester invite"
      pattern: "Processing|Ready"
---

<objective>
Run the three human-only release steps that no agent can perform autonomously:
(1) a cold-instance (>15 min idle) run of the Plan 02 preflight script
(SHIP-03), (2) the first real `eas build --profile production` +
`eas submit --profile production` cycle (SHIP-04), and (3) adding internal
TestFlight testers and confirming they can install (SHIP-05).

Purpose: Land a signed, submitted iOS build in front of internal TestFlight
testers — the terminal deliverable of the v0.5 milestone.
Output: Operator-reported confirmations captured in the SUMMARY.

IMPORTANT — every task in this plan is operator-executed. Agents MUST NOT
run `eas build`/`eas submit`, MUST NOT sleep/idle 15+ minutes inline, and
MUST NOT add TestFlight testers. The agent's job is to present exact
commands/checklists and record the operator's reported results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-quality-gates-preflight-first-submit/24-CONTEXT.md
@.planning/phases/23-eas-build-submit-configuration/23-01-SUMMARY.md
@eas.json
@app.json

<preconditions>
Confirmed from prior phases — do NOT re-flag these as outstanding:
- `eas.json` `submit.production.ios.ascAppId` = "6794382182" (real value, filled in the Phase 23 follow-up — NOT a placeholder anymore).
- `eas.json`: `cli.appVersionSource: "remote"`, `build.production.autoIncrement: true`, EAS-managed iOS credentials.
- `app.json`: `ios.infoPlist.ITSAppUsesNonExemptEncryption` = false.
- EAS CLI already authenticated on this machine (`eas whoami` -> avram.aruh); no `eas login` needed. Only submit-time interactive Apple ID auth is required.
- Icon/splash assets already human-verified and shipped (Phase 22).
- Gate before running this plan: Plan 01 (lint clean) and Plan 02 (warm preflight passed) are complete.
</preconditions>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator runs the cold-instance preflight (SHIP-03)</name>
  <read_first>
    - scripts/preflight.ts (the script the operator runs — confirm it exists and `npm run preflight` is wired from Plan 02)
    - .planning/phases/24-quality-gates-preflight-first-submit/24-CONTEXT.md (§D-04 — cold check is a human-executed checkpoint, no inline sleep)
  </read_first>
  <what-built>
    Plan 02 delivered `npm run preflight`, verified against the warm backend. This checkpoint is the deliberately-cold repeat: SHIP-03 requires the same four checks to pass after the Render free-tier instance has spun down.
  </what-built>
  <how-to-verify>
    1. Leave the backend idle for more than 15 minutes so the Render free-tier instance spins down (do not hit any endpoint during this window). The very first request after idle will pay the cold-start latency — the 90s timeout in the script is sized for this.
    2. Run: `npm run preflight`
    3. Expect: all four checks report PASS — `/health` 200, `/content/verbs` 200, `/feedback` 201, `/product-feedback` 201 — and the process exits 0. (The first check may take many seconds while the instance wakes; that is expected and still a PASS as long as the status code is correct within the timeout.)
    4. Report back: the four status codes observed and whether the run exited 0.
  </how-to-verify>
  <resume-signal>Reply "cold preflight passed" (with the four status codes), or paste the failing output.</resume-signal>
  <verify>
    <human-check>Operator reports `npm run preflight` exited 0 with all four checks PASS after a >15 min idle window.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator confirms the instance was idle >15 minutes before the run (cold start, not warm).
    - Operator reports all four checks PASS: `/health` 200, `/content/verbs` 200, `/feedback` 201, `/product-feedback` 201, exit 0.
    - Observed status codes recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Cold-instance preflight confirmed passing by the operator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs the first real eas build + eas submit (SHIP-04)</name>
  <read_first>
    - eas.json (confirm `build.production` and `submit.production.ios` with real ascAppId 6794382182 — no edits needed)
    - app.json (confirm `ios.bundleIdentifier` com.avram.aruh.lafa, `ios.buildNumber` 1, export-compliance flag false)
    - .planning/phases/24-quality-gates-preflight-first-submit/24-CONTEXT.md (§D-05 — build/submit is operator-executed, not agent-run)
  </read_first>
  <what-built>
    All build/submit config is finalized (Phases 21-23). This checkpoint is the operator running the real cloud build and submit; interactive Apple ID authentication at submit time cannot be automated.
  </what-built>
  <how-to-verify>
    Pre-flight checklist (all must be true before starting — do not proceed otherwise):
    - [ ] `npm run lint` passes with zero errors (Plan 01).
    - [ ] `npm run preflight` passed warm (Plan 02) AND cold (Task 1 above).
    - [ ] `eas.json` `submit.production.ios.ascAppId` = "6794382182" (already done — do not edit).
    - [ ] `eas whoami` returns `avram.aruh` (already authenticated — no `eas login` needed).

    Steps:
    1. Run: `eas build --profile production --platform ios`
       - Wait for the EAS dashboard to report a successful build status. Note the build URL/ID.
    2. Run: `eas submit --profile production --platform ios`
       - Complete the interactive Apple ID authentication when prompted.
       - Wait for the upload to finish and confirm the build appears in App Store Connect.
    3. In App Store Connect (TestFlight tab), confirm the build shows `Processing` and then reaches `Ready to Submit` / `Ready to Test` (processing can take several minutes to ~an hour).
    4. Report back: the EAS build status/URL, the submit result, and the App Store Connect build state (Processing or Ready).
  </how-to-verify>
  <resume-signal>Reply "build submitted — <Processing|Ready>" with the build number/URL, or paste the failing output.</resume-signal>
  <verify>
    <human-check>Operator reports `eas build` succeeded and `eas submit` uploaded a build that reaches Processing/Ready in App Store Connect.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator confirms `eas build --profile production --platform ios` completed with a successful status (build URL/ID recorded).
    - Operator confirms `eas submit --profile production --platform ios` uploaded the build to App Store Connect.
    - Operator confirms the build shows `Processing` or `Ready` in App Store Connect TestFlight.
    - Build number/URL and final state recorded in the SUMMARY.
  </acceptance_criteria>
  <done>A signed production build is submitted and Processing/Ready in App Store Connect.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator adds internal TestFlight testers and confirms install (SHIP-05)</name>
  <read_first>
    - .planning/phases/24-quality-gates-preflight-first-submit/24-CONTEXT.md (§D-06 — testers handled as a human-executed checkpoint)
    - .planning/REQUIREMENTS.md (§Out of Scope — internal Apple Developer team members ONLY; no external/Beta App Review testers this milestone)
  </read_first>
  <what-built>
    Once Task 2's build reaches Ready in TestFlight, internal testers (Apple Developer team members only) can be invited. This is App Store Connect UI work the operator performs.
  </what-built>
  <how-to-verify>
    1. In App Store Connect → your app → TestFlight → Internal Testing.
    2. Create/confirm an Internal Testing group and add testers who are members of your Apple Developer team ONLY (internal testing needs no Beta App Review; do NOT add external testers this milestone — out of scope).
    3. Assign the build from Task 2 to that internal group.
    4. Have at least one tester open the TestFlight app on an iOS device, install the Lafa build, and launch it.
    5. Report back: which testers were added and that at least one confirmed a successful install + launch.
  </how-to-verify>
  <resume-signal>Reply "testers confirmed install" (naming the testers/count), or describe any install issue.</resume-signal>
  <verify>
    <human-check>Operator reports internal testers added and at least one confirms they installed and launched the build via TestFlight.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator confirms internal testers (Apple Developer team members only) were added to an Internal Testing group.
    - Operator confirms the Task 2 build was assigned to that group.
    - Operator confirms at least one tester installed and launched the build via TestFlight.
    - Tester count/names and install confirmation recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Internal TestFlight testers added and install confirmed — v0.5 terminal deliverable reached.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> Apple/App Store Connect | Interactive Apple ID auth and TestFlight distribution cross an external trust boundary; handled entirely by the operator through Apple's own auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Spoofing/Elevation | Apple ID auth at submit | accept | Auth handled interactively by the operator through Apple's official flow; no credentials stored in repo or handled by an agent. |
| T-24-05 | Info disclosure | TestFlight distribution scope | mitigate | Internal-only testers (Apple Developer team members); external distribution explicitly out of scope (REQUIREMENTS.md), avoiding Beta App Review exposure. |
</threat_model>

<verification>
- Task 1: operator reports cold preflight all-PASS, exit 0.
- Task 2: operator reports build success + submit + Processing/Ready in App Store Connect.
- Task 3: operator reports internal testers added and install confirmed.
</verification>

<success_criteria>
SHIP-03, SHIP-04, SHIP-05 satisfied: the preflight passes against a cold Render
instance; the first real production build is submitted and reaches
Processing/Ready in App Store Connect; internal TestFlight testers are added and
confirm they can install the build.
</success_criteria>

<output>
Create `.planning/phases/24-quality-gates-preflight-first-submit/24-03-SUMMARY.md` when done,
recording the operator-reported evidence (cold status codes, build number/URL + ASC state,
tester count + install confirmation).
</output>
