# Phase 25: Brand Asset Pipeline - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-13
**Phase:** 25-Brand Asset Pipeline
**Areas discussed:** SVG source provisioning, Icon background handling, Source file naming, Old asset removal, Android adaptive icon specifics

---

## SVG source provisioning (pre-discussion blocker)

`assets/brand/lafa-icon.svg` — the canonical source this phase depends on — did not exist in the repo when discussion started. Only the old AI-generated `lafa-logo.svg`/`lafa-logo-v2.svg` and concept PNGs were present.

| Option | Description | Selected |
|--------|-------------|----------|
| I'll provide it now | User pastes/places the SVG before continuing | ✓ |
| Use lafa-logo-v2.svg for now | Stand-in source, swap later | |
| It exists elsewhere, point you to it | User names a path/location | |

**User's choice:** Provided it — added `assets/brand/Lafa_final_logo.svg` mid-discussion.
**Notes:** User asked "where should i place them?" — confirmed `assets/brand/lafa-icon.svg` is the canonical path. User then asked if a separate brand guide document was also needed — answered no, since the SVG's own colors (`#FFF9F6`, `#F2643E`) already match the palette already locked in ROADMAP.md/PROJECT.md for Phase 26.

---

## Icon background handling

| Option | Description | Selected |
|--------|-------------|----------|
| Flatten to full square | Drop baked-in rx=230 rounding, render on plain full-bleed square, avoids double-rounding under OS mask | ✓ |
| Keep rounded corners as-authored | Render exactly as SVG specifies | |
| You decide after checking how it renders | Generate both, compare, default to flatten | |

**User's choice:** Flatten to full square (Recommended).
**Notes:** SVG bakes in `rect rx="230"` background; flattening avoids a rounded-square-within-rounded-square artifact once iOS/Android apply their own icon mask.

---

## Source file naming

| Option | Description | Selected |
|--------|-------------|----------|
| Rename to lafa-icon.svg | Matches BRAND-01 and every doc reference, no doc updates needed | ✓ |
| Keep Lafa_final_logo.svg | Update script constant + doc references instead | |

**User's choice:** Rename to lafa-icon.svg (Recommended).

---

## Old asset removal

| Option | Description | Selected |
|--------|-------------|----------|
| Delete outright | git rm the 4 old files once unreferenced, matches BRAND-02's "removed" phrasing | ✓ |
| Leave files, remove references only | Keep for historical reference, confirm zero references | |

**User's choice:** Delete outright (Recommended).

---

## Android adaptive icon specifics

| Option | Description | Selected |
|--------|-------------|----------|
| Standard convention (foreground) | Mark centered, ~66% safe-zone circle | ✓ |
| Match iOS icon's visual weight | Size for apparent-scale consistency instead | |

**User's choice:** Standard convention (Recommended).

| Option | Description | Selected |
|--------|-------------|----------|
| Solid white silhouette on transparent (monochrome) | Standard Android 13+ themed-icon convention, OS tints it | ✓ |
| Keep orange mark, no color conversion | Technically violates themed-icon spec | |

**User's choice:** Solid white silhouette on transparent (Recommended).

---

## Claude's Discretion

- Exact resvg/sharp rendering approach for new output types (`favicon.png`, `android-icon-foreground.png`, `android-icon-monochrome.png`) not previously generated by the script.
- Precise Android safe-zone pixel math (66% convention confirmed; exact centering/padding calculation left to implementation).
- Splash-icon generation (mark paths only, transparent background, background rect excluded) — not separately discussed, treated as low-ambiguity given BRAND-03's explicit spec and the SVG's simple two-shape structure.

## Deferred Ideas

None — discussion stayed within phase scope.
