---
phase: 11-lafa-design-tokens-brand-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/tokens.ts
  - src/theme/tokens.test.ts
autonomous: true
requirements: [BRAND-02, TEST-02]

must_haves:
  truths:
    - "src/theme/tokens.ts exports the Lafa palette (primary/primarySoft/success/error/background/text/textSecondary/surface) with no accent or secondary keys"
    - "radius export has both control (12) and pill (999)"
    - "The token-completeness test passes and fails if any required key is missing or if accent/secondary reappear"
  artifacts:
    - path: "src/theme/tokens.ts"
      provides: "Lafa design tokens (colors/spacing/radius/typography)"
      contains: "primarySoft"
    - path: "src/theme/tokens.test.ts"
      provides: "Token-completeness regression test (TEST-02)"
      contains: "toBeUndefined"
  key_links:
    - from: "src/theme/tokens.test.ts"
      to: "src/theme/tokens.ts"
      via: "import { colors, spacing, radius, typography }"
      pattern: "from \"./tokens\""
---

<objective>
Rewrite `src/theme/tokens.ts` in place with the locked Lafa palette (D-01/D-02),
add the new `radius.pill` token (D-04), and update the co-located
`src/theme/tokens.test.ts` into the token-completeness guard required by TEST-02.

Purpose: This is the foundation every screen and shared component consumes. All
downstream Wave 2 plans reference the new token keys (`primary`, `surface`,
`primarySoft`, `radius.pill`), so those keys must exist first.
Output: New token module + updated token test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-lafa-design-tokens-brand-identity/11-CONTEXT.md
@.planning/phases/11-lafa-design-tokens-brand-identity/11-UI-SPEC.md
@.planning/phases/11-lafa-design-tokens-brand-identity/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite tokens.ts with the Lafa palette and pill radius</name>
  <files>src/theme/tokens.ts</files>
  <read_first>
    - src/theme/tokens.ts (current version — preserve export shape and order)
    - .planning/phases/11-lafa-design-tokens-brand-identity/11-UI-SPEC.md §Color and §Radius (locked values)
    - .planning/phases/11-lafa-design-tokens-brand-identity/11-CONTEXT.md D-01/D-02/D-04/D-05/D-06
  </read_first>
  <action>
    Replace the entire `colors` export so its keys are exactly, in this order:
    `primary: "#E8663D"`, `primarySoft: "#FCE4DA"`, `success: "#2FA84F"`,
    `error: "#D64545"`, `background: "#FFFFFF"`, `text: "#1C1B1A"`,
    `textSecondary: "#6B6560"`, `surface: "#F2F2F1"`. Remove the old `accent`
    and `secondary` keys entirely (rename in place, no dead keys, no aliases —
    D-02). Add `pill: 999` to the `radius` export while keeping `control: 12`
    unchanged. Leave the `spacing` and `typography` exports byte-identical
    (D-05/D-06 — unchanged). Keep the existing flat `export const X = {...};`
    style; keep `as const` only on `typography`'s `fontWeight` fields, exactly
    as today. Do not touch any other file.
  </action>
  <verify>
    <automated>node -e "const t=require('./src/theme/tokens.ts');" 2>/dev/null; grep -c '"#E8663D"\|"#FCE4DA"\|"#2FA84F"\|"#D64545"\|"#1C1B1A"\|"#6B6560"\|"#F2F2F1"' src/theme/tokens.ts | grep -qx 7 && grep -q 'pill: 999' src/theme/tokens.ts && ! grep -Eq 'accent:|secondary:' src/theme/tokens.ts && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq 'accent:|secondary:' src/theme/tokens.ts` returns non-zero (keys removed)
    - `colors` object literal contains all 8 new keys with the exact hex strings from UI-SPEC §Color
    - `radius` export contains both `control: 12` and `pill: 999`
    - `spacing` and `typography` exports are unchanged from the pre-edit file (no key added/removed/renamed)
  </acceptance_criteria>
  <done>tokens.ts exports the full Lafa palette + radius.pill; no accent/secondary keys remain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update tokens.test.ts into the TEST-02 completeness guard</name>
  <files>src/theme/tokens.test.ts</files>
  <behavior>
    - `colors` deep-equals the full new palette object (8 keys, exact hex values)
    - `colors.accent` is `undefined` and `colors.secondary` is `undefined` (no dead keys — D-02)
    - `radius` deep-equals `{ control: 12, pill: 999 }`
    - `spacing` and `typography` assertions remain byte-identical to the current file (regression only)
  </behavior>
  <read_first>
    - src/theme/tokens.test.ts (current version — preserve describe/it structure and toEqual style)
    - src/theme/tokens.ts (post-Task-1 values to assert against)
    - .planning/phases/11-lafa-design-tokens-brand-identity/11-UI-SPEC.md §Test Contract (TEST-02)
  </read_first>
  <action>
    Update the `colors` `toEqual(...)` assertion to the new 8-key palette with
    the exact hex values from Task 1. Add two new assertions:
    `expect(colors.accent).toBeUndefined()` and
    `expect(colors.secondary).toBeUndefined()`. Update the `radius` assertion to
    `toEqual({ control: 12, pill: 999 })`. Leave the `spacing` and `typography`
    assertions unchanged. Rename the outer `describe` label away from
    "D-03 verbatim-value guard" (that D-03 predates this phase) to a
    phase-11-appropriate label such as "theme tokens (Lafa palette + token
    completeness)". Keep the `import { colors, spacing, radius, typography } from "./tokens"`
    line and the one-it-per-category structure.
  </action>
  <verify>
    <automated>npx jest src/theme/tokens.test.ts 2>&1 | grep -q 'PASS\|passed' && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/theme/tokens.test.ts` passes
    - Test file contains `expect(colors.accent).toBeUndefined()` and `expect(colors.secondary).toBeUndefined()`
    - Deliberately reverting one palette hex or re-adding `accent` in tokens.ts makes this test fail (guard is real, not vacuous)
  </acceptance_criteria>
  <done>tokens.test.ts asserts the full Lafa key set, absence of accent/secondary, and radius.pill; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure static token/config edit. No untrusted input crosses any boundary; no network, storage, or user input introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | src/theme/tokens.ts | accept | Static design constants, no runtime input; token-completeness test (Task 2) guards against silent key drift |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — no dependency added or upgraded |
</threat_model>

<verification>
- `npx jest src/theme/tokens.test.ts` passes
- `grep -Eq 'accent:|secondary:' src/theme/tokens.ts` returns non-zero (no old keys)
- Full `npm run typecheck` is expected to still FAIL after this plan alone, because screen/component consumers still reference the removed `colors.accent`/`colors.secondary` keys — those are fixed in Wave 2 (plans 11-02 and 11-03). Do not treat that failure as a defect of this plan.
</verification>

<success_criteria>
- tokens.ts exports 8-key Lafa palette + radius.pill, spacing/typography unchanged
- tokens.test.ts is the passing TEST-02 completeness guard
</success_criteria>

<output>
Create `.planning/phases/11-lafa-design-tokens-brand-identity/11-01-SUMMARY.md` when done
</output>
