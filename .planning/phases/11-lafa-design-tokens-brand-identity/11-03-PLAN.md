---
phase: 11-lafa-design-tokens-brand-identity
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/components/OfflinePill.tsx
  - src/feedback/ReportFeedbackModal.tsx
  - app.json
  - src/quiz/share.ts
  - __tests__/quiz-share.test.ts
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-04]

must_haves:
  truths:
    - "OfflinePill renders with primarySoft background, primary text, pill radius; copy 'Using saved content' unchanged"
    - "ReportFeedbackModal contains zero hardcoded hex values — every color/spacing/radius comes from tokens"
    - "app.json expo.name and the share message both read 'Lafa'; slug/scheme unchanged"
  artifacts:
    - path: "src/components/OfflinePill.tsx"
      provides: "OfflinePill on Lafa primarySoft/primary/pill tokens (BRAND-04)"
      contains: "primarySoft"
    - path: "src/feedback/ReportFeedbackModal.tsx"
      provides: "Feedback modal fully migrated to tokens (BRAND-02)"
      contains: "../theme/tokens"
    - path: "src/quiz/share.ts"
      provides: "Share message with Lafa brand name (D-09)"
      contains: "on Lafa!"
  key_links:
    - from: "src/feedback/ReportFeedbackModal.tsx"
      to: "src/theme/tokens.ts"
      via: "import { colors, radius, spacing, typography }"
      pattern: "from \"../theme/tokens\""
---

<objective>
Migrate the two shared components onto Lafa tokens (OfflinePill per BRAND-04/D-03/D-04,
ReportFeedbackModal full hardcoded-hex elimination per BRAND-02) and apply the
remaining brand-name copy changes (app.json expo.name, share message + its test)
per BRAND-01/D-09.

Purpose: OfflinePill still references the removed colors.secondary key, and
ReportFeedbackModal is the one documented file that bypasses tokens entirely
(100% hardcoded hex). This plan closes both, plus finishes the brand-name
substitutions in app.json and share.ts.
Output: Both shared components on tokens; app name and share text read "Lafa".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-lafa-design-tokens-brand-identity/11-CONTEXT.md
@.planning/phases/11-lafa-design-tokens-brand-identity/11-UI-SPEC.md
@.planning/phases/11-lafa-design-tokens-brand-identity/11-PATTERNS.md
@.planning/phases/11-lafa-design-tokens-brand-identity/11-01-SUMMARY.md

<interfaces>
Token keys available after 11-01 (from src/theme/tokens.ts):
- colors: primary (#E8663D), primarySoft (#FCE4DA), success (#2FA84F), error (#D64545),
  background (#FFFFFF), text (#1C1B1A), textSecondary (#6B6560), surface (#F2F2F1)
- radius: control (12), pill (999)
- spacing: sm (8), md (16), lg (24), xl2 (48), xl3 (64), choiceGap (12)
- typography: caption, body, bodyStrong, heading, display

Import path convention: files under src/<domain>/ use `../theme/tokens`
(OfflinePill.tsx and ReportFeedbackModal.tsx are both one level under src/).

ReportFeedbackModal hardcoded-hex → token map (D-03: neutral grays use surface, NOT primarySoft):
- #FFFFFF (container bg, submit/retry text, ActivityIndicator) → colors.background
- #000000 (title, reason text, input text)                     → colors.text
- #007AFF (selected reason bg, submit button bg)               → colors.primary
- #FF3B30 (error text, retry button bg)                        → colors.error
- #34C759 (success text)                                       → colors.success
- #8E8E93 (placeholderTextColor)                               → colors.textSecondary
- #F2F2F7 (reason-option default bg, text-input bg)            → colors.surface
- 16/24/12 px → spacing.md/spacing.lg/spacing.choiceGap or radius.control (match value)
- {fontSize:20,fontWeight:"600"} → typography.heading
- {fontSize:16,fontWeight:"400"|"600"} → typography.body / typography.bodyStrong
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OfflinePill — primarySoft background, primary text, pill radius</name>
  <files>src/components/OfflinePill.tsx</files>
  <read_first>
    - src/components/OfflinePill.tsx (current version — component logic must NOT change)
    - src/theme/tokens.ts (post-11-01 keys)
    - .planning/phases/11-lafa-design-tokens-brand-identity/11-UI-SPEC.md §OfflinePill (BRAND-04, D-03, D-04)
  </read_first>
  <action>
    In the StyleSheet: set `container.backgroundColor` to `colors.primarySoft`
    (was `colors.secondary`), set `container.borderRadius` to `radius.pill`
    (was `radius.control`), and set `text.color` to `colors.primary` (was
    `colors.textSecondary`). Leave the padding/margin (spacing.sm,
    spacing.sm / 2, spacing.md) and `...typography.caption` unchanged (D-05/D-06).
    Do not touch component logic, the `OFFLINE_PILL_TEXT` constant, the
    resolveVerbs polling, or JSX — StyleSheet-values-only edit. Copy stays
    exactly "Using saved content".
  </action>
  <verify>
    <automated>! grep -Eq 'colors\.(accent|secondary)' src/components/OfflinePill.tsx && grep -q 'colors\.primarySoft' src/components/OfflinePill.tsx && grep -q 'radius\.pill' src/components/OfflinePill.tsx && npx jest __tests__/offline-pill.test.ts 2>&1 | grep -q 'PASS\|passed' && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - container uses `colors.primarySoft` background and `radius.pill` border radius
    - `text.color` is `colors.primary`
    - `OFFLINE_PILL_TEXT` value is unchanged ("Using saved content") and `npx jest __tests__/offline-pill.test.ts` passes
    - `grep -Eq 'colors\.(accent|secondary)' src/components/OfflinePill.tsx` returns non-zero
  </acceptance_criteria>
  <done>OfflinePill styled with primarySoft/primary/pill; copy and logic unchanged; pill test green.</done>
</task>

<task type="auto">
  <name>Task 2: ReportFeedbackModal — full hardcoded-hex → token migration</name>
  <files>src/feedback/ReportFeedbackModal.tsx</files>
  <read_first>
    - src/feedback/ReportFeedbackModal.tsx (current version — state machine/JSX must NOT change)
    - src/components/OfflinePill.tsx (reference for correct token-import pattern)
    - src/theme/tokens.ts (post-11-01 keys)
    - .planning/phases/11-lafa-design-tokens-brand-identity/11-UI-SPEC.md §ReportFeedbackModal + hex→token map
  </read_first>
  <action>
    Add the import line `import { colors, radius, spacing, typography } from "../theme/tokens";`.
    Replace every hardcoded hex and literal numeric style value per the hex→token
    map in the interfaces block above. Critically: neutral grays (`#F2F2F7`
    reason-option default background and text-input background) map to
    `colors.surface`, NOT `colors.primarySoft` (D-03 — avoid coral tint on
    neutral surfaces). Also migrate the two inline JSX props: change
    `placeholderTextColor="#8E8E93"` to `placeholderTextColor={colors.textSecondary}`
    and `<ActivityIndicator color="#FFFFFF" />` to
    `<ActivityIndicator color={colors.background} />` — a StyleSheet-only sweep
    would miss these. Map literal spacing/radius numbers to their named token of
    the same value (borderRadius:12→radius.control, paddingHorizontal:16→spacing.md,
    marginBottom:24→spacing.lg, etc.) and literal font objects to
    typography.heading/body/bodyStrong. Do NOT change the ModalState machine,
    handleSubmit, the reset-on-visible effect, auto-close timer, or any JSX
    structure — color/token substitution only. After the edit there must be zero
    `#`-prefixed hex literals anywhere in the file.
  </action>
  <verify>
    <automated>! grep -Eiq '#[0-9a-f]{6}\|#[0-9a-f]{3}\b' src/feedback/ReportFeedbackModal.tsx && grep -q 'from "../theme/tokens"' src/feedback/ReportFeedbackModal.tsx && grep -q 'colors\.surface' src/feedback/ReportFeedbackModal.tsx && ! grep -q 'colors\.primarySoft' src/feedback/ReportFeedbackModal.tsx && npx jest __tests__/feedback-submit.test.ts 2>&1 | grep -q 'PASS\|passed' && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eiq '#[0-9a-f]{6}' src/feedback/ReportFeedbackModal.tsx` returns non-zero (zero hex literals remain, including inline props)
    - File imports from `"../theme/tokens"`; neutral surfaces use `colors.surface`; file does NOT reference `colors.primarySoft`
    - `placeholderTextColor` and `ActivityIndicator color` use `colors.textSecondary` / `colors.background`
    - ModalState machine, handleSubmit, effects, and JSX structure are behaviorally unchanged; feedback-submit test suite passes
  </acceptance_criteria>
  <done>ReportFeedbackModal fully on tokens, zero hardcoded hex, neutral surfaces use surface not primarySoft, behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Brand-name copy — app.json, share message, share test</name>
  <files>app.json, src/quiz/share.ts, __tests__/quiz-share.test.ts</files>
  <read_first>
    - app.json (current — only expo.name changes; slug/scheme/plugins unchanged per D-08)
    - src/quiz/share.ts (current single-function file)
    - __tests__/quiz-share.test.ts (current — three toBe assertions)
    - .planning/phases/11-lafa-design-tokens-brand-identity/11-UI-SPEC.md §App Identity Copy (BRAND-01, D-07/D-08/D-09)
  </read_first>
  <action>
    In `app.json` change `"name": "Portuguese Verb Quiz"` to `"name": "Lafa"`.
    Leave `slug` (`portuguese-verb-mobile`), `scheme` (`portugueseverbmobile`),
    the `expo-splash-screen` plugin `backgroundColor`, and everything else
    byte-identical (D-08 — do NOT touch splash/icon). In `src/quiz/share.ts`
    change the template literal from `on Portuguese Verb Quiz!` to `on Lafa!`
    (D-09). In `__tests__/quiz-share.test.ts` update all three `toBe`
    expectations to the `"I scored X/Y on Lafa!"` form matching the new message.
  </action>
  <verify>
    <automated>grep -q '"name": "Lafa"' app.json && ! grep -q 'Portuguese Verb Quiz' app.json src/quiz/share.ts __tests__/quiz-share.test.ts && grep -q 'on Lafa!' src/quiz/share.ts && npx jest __tests__/quiz-share.test.ts 2>&1 | grep -q 'PASS\|passed' && echo GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `app.json` `expo.name` is `"Lafa"`; `slug` and `scheme` unchanged
    - No file among app.json, share.ts, quiz-share.test.ts contains the string `Portuguese Verb Quiz`
    - `buildShareMessage` returns strings ending `on Lafa!`; `npx jest __tests__/quiz-share.test.ts` passes
  </acceptance_criteria>
  <done>App name, share message, and share test all read "Lafa"; slug/scheme/splash untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (POST /feedback) | Pre-existing boundary; this plan only restyles the feedback modal UI. The payload contract, submit path, and validation are NOT modified — no change to what crosses the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | ReportFeedbackModal styles | accept | Style/token substitution only; state machine, payload builder, and submit path explicitly unchanged and asserted in acceptance criteria |
| T-11-04 | Information Disclosure | app.json manifest | accept | Only expo.name changes; no secrets, endpoints, or credentials touched |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- OfflinePill uses primarySoft/primary/pill; `npx jest __tests__/offline-pill.test.ts` passes
- ReportFeedbackModal has zero hex literals and imports tokens; `npx jest __tests__/feedback-submit.test.ts` passes
- app.json/share.ts/quiz-share.test.ts read "Lafa"; `npx jest __tests__/quiz-share.test.ts` passes
- Phase-level (after 11-02 + 11-03): `npm run typecheck` passes and full `npm test` is green — no `colors.accent`/`colors.secondary` remain anywhere in app/ or src/
</verification>

<success_criteria>
- Both shared components render on Lafa tokens; ReportFeedbackModal has zero hardcoded hex
- App name and share message read "Lafa"; slug/scheme/splash unchanged
</success_criteria>

<output>
Create `.planning/phases/11-lafa-design-tokens-brand-identity/11-03-SUMMARY.md` when done
</output>
