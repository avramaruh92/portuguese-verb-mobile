---
phase: 12-tense-label-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quiz/labels.ts
  - app/quiz.tsx
  - __tests__/quiz-labels.test.ts
autonomous: true
requirements: [LABEL-01, LABEL-02, LABEL-03, TEST-01]

must_haves:
  truths:
    - "Quiz screen meta row shows 'Completed past' as the primary label for preterite questions"
    - "Quiz screen meta row shows 'Imperfect past' as the primary label for imperfect questions"
    - "'Present' (present_indicative) and 'Future' (future) primary labels are unchanged"
    - "Portuguese grammar names ('Pretérito perfeito' / 'Pretérito imperfeito') render inline-parenthesized after the tense label for preterite/imperfect only"
    - "present_indicative and future render with no parenthetical grammar name"
    - "'Perfect past' appears nowhere in the codebase"
    - "POST /feedback payload still sources tense from the Tense enum literal, not from tenseLabels — src/feedback/ has zero references to tenseLabels"
    - "__tests__/quiz-labels.test.ts passes with the new displayed-label assertions"
  artifacts:
    - path: "src/quiz/labels.ts"
      provides: "Updated tenseLabels values + new partial tenseGrammarNames export"
      contains: "tenseGrammarNames"
    - path: "app/quiz.tsx"
      provides: "Meta row rendering the inline parenthetical Portuguese grammar name"
      contains: "tenseGrammarNames"
    - path: "__tests__/quiz-labels.test.ts"
      provides: "Assertions for the new displayed labels and unchanged literals"
      contains: "Completed past"
  key_links:
    - from: "app/quiz.tsx"
      to: "src/quiz/labels.ts"
      via: "named import { subjectLabels, tenseLabels, tenseGrammarNames }"
      pattern: "tenseGrammarNames"
---

<objective>
Refresh the *displayed* tense labels to friendly English names and add
Portuguese grammar names as inline secondary text on the Quiz screen's meta
row, without touching any internal enum literal or the POST /feedback payload.

Purpose: Make preterite/imperfect labels clearer to A1-A2 learners
("Completed past" / "Imperfect past") while disambiguating with the exact
Portuguese grammar term, and explicitly avoiding the confusing "Perfect past"
wording (LABEL-01, LABEL-02).

Output: Updated `src/quiz/labels.ts` (two changed values + a new
`tenseGrammarNames` partial map), an updated `app/quiz.tsx` meta row, and
updated `__tests__/quiz-labels.test.ts` assertions. Display-only — zero
backend-contract impact (LABEL-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-tense-label-refresh/12-CONTEXT.md
@.planning/phases/12-tense-label-refresh/12-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

Tense union (from src/dataset/types.ts, DO NOT MODIFY):
  type Tense = "present_indicative" | "preterite" | "imperfect" | "future"
  const TENSES: readonly Tense[]  // iterated by quiz-labels.test.ts

Current src/quiz/labels.ts exports:
  export const subjectLabels: Record<Subject, string>   // unchanged this phase
  export const tenseLabels: Record<Tense, string> = {
    present_indicative: "Present",
    preterite: "Preterite",   // → "Completed past"
    imperfect: "Imperfect",   // → "Imperfect past"
    future: "Future",
  }

Available tokens (from src/theme/tokens.ts, already imported by app/quiz.tsx):
  colors.text            = "#..." (current metaRow color)
  colors.textSecondary   = "#6B6560"
  typography.body        = { fontSize: 16, fontWeight: "400" }
  typography.caption     = { fontSize: 14, fontWeight: "400" }

Current app/quiz.tsx meta row (lines 118-124):
  <View style={styles.questionBlock}>
    <Text style={styles.verbHeading}>{question.verb}</Text>
    <Text style={styles.metaRow}>
      {currentVerb?.translation ?? ""} · {tenseLabels[question.tense]} ·{" "}
      {subjectLabels[question.subject]}
    </Text>
  </View>

Current app/quiz.tsx metaRow style (lines 213-216):
  metaRow: { ...typography.body, color: colors.text }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tenseLabels values and add tenseGrammarNames partial map</name>
  <files>src/quiz/labels.ts</files>
  <read_first>
    - src/quiz/labels.ts (the file being modified — current full 17-line content)
    - src/dataset/types.ts (confirm the `Tense` union literals and `TENSES` const — DO NOT modify)
  </read_first>
  <action>
    In `src/quiz/labels.ts`, keep `tenseLabels` typed as the full
    `Record<Tense, string>` (per D-08 — do not narrow its type or shape).
    Change exactly two values: `preterite` from `"Preterite"` to
    `"Completed past"`, and `imperfect` from `"Imperfect"` to
    `"Imperfect past"`. Leave `present_indicative: "Present"` and
    `future: "Future"` untouched (D-01).

    Add a NEW named export `tenseGrammarNames` typed
    `Partial<Record<Tense, string>>` covering ONLY two keys:
    `preterite: "Pretérito perfeito"` and `imperfect: "Pretérito imperfeito"`
    (exact Portuguese strings locked by LABEL-02 / CONTEXT specifics). Do NOT
    add `present_indicative` or `future` keys (D-05). Do NOT overload or
    mutate `tenseLabels` (D-08) — this must be a separate export in the same
    file, mirroring the existing flat-object-literal style of
    `subjectLabels`/`tenseLabels`. Reuse the existing
    `import type { Subject, Tense } from "../dataset/types"` line for typing;
    add no new import statement. The string `"Perfect past"` must not appear
    anywhere in the file (D-02).
  </action>
  <verify>
    <automated>npm run typecheck && grep -q 'preterite: "Completed past"' src/quiz/labels.ts && grep -q 'imperfect: "Imperfect past"' src/quiz/labels.ts && grep -q 'tenseGrammarNames' src/quiz/labels.ts && ! grep -q "Perfect past" src/quiz/labels.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tenseLabels.preterite === "Completed past"` and `tenseLabels.imperfect === "Imperfect past"` in source
    - `tenseLabels.present_indicative === "Present"` and `tenseLabels.future === "Future"` unchanged in source
    - A new export `tenseGrammarNames` exists, typed `Partial<Record<Tense, string>>`, with exactly the keys `preterite` ("Pretérito perfeito") and `imperfect` ("Pretérito imperfeito")
    - `tenseGrammarNames` has NO `present_indicative` or `future` key
    - `tenseLabels` is still declared as `Record<Tense, string>` (full map, all four keys)
    - The substring `Perfect past` does not appear in the file (only "Completed past" / "Imperfect past" / "Pretérito perfeito" / "Pretérito imperfeito")
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    labels.ts has the two updated primary labels, a new partial
    tenseGrammarNames export for preterite/imperfect only, and typecheck passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render inline parenthetical Portuguese grammar name in the Quiz meta row</name>
  <files>app/quiz.tsx</files>
  <read_first>
    - app/quiz.tsx (the file being modified — read lines 1-11 imports, lines 116-127 meta row, lines 205-217 styles)
    - src/quiz/labels.ts (the just-updated source of `tenseLabels` and the new `tenseGrammarNames` export)
    - src/theme/tokens.ts (confirm `colors.text`, `colors.textSecondary`, `typography.body`, `typography.caption` exist for styling)
  </read_first>
  <action>
    Extend the existing named import on line 7 from
    `import { subjectLabels, tenseLabels } from "../src/quiz/labels";` to also
    import `tenseGrammarNames` (single import statement, do not add a second
    import line).

    In the `styles.metaRow` `<Text>` (lines 120-123), keep the single-line,
    `·`-separated template structure — do NOT split into a second row or a
    caption-sized secondary row (D-04 explicitly rejects that). Immediately
    after `{tenseLabels[question.tense]}` and before the ` · ` separator that
    precedes the subject, render an inline parenthetical grammar name ONLY when
    `tenseGrammarNames[question.tense]` is defined (i.e. preterite/imperfect).
    The rendered output for a preterite question must read exactly:
    `correr · Completed past (Pretérito perfeito) · eles/elas` — a single
    space before the `(`, the Portuguese term inside the parentheses, and the
    normal ` · ` separator continuing to the subject. For present_indicative
    and future, `tenseGrammarNames[question.tense]` is undefined, so no
    parenthetical and no extra spaces render (D-05) — output stays
    `translation · Present · subject`.

    Guard against `noUncheckedIndexedAccess`: `tenseGrammarNames[question.tense]`
    is `string | undefined`, so gate rendering on its truthiness (conditional
    expression returning the ` (…)` fragment or an empty string / null).

    For D-07 styling (implementer's discretion): either leave the parenthetical
    inline in the same `<Text>` inheriting `styles.metaRow` (`colors.text`), OR
    nest a `<Text>` for the parenthetical fragment using a token-based style.
    If nesting, add a sibling style key (e.g. `metaRowSecondary`) to the
    `StyleSheet.create` block following the existing spread pattern
    (`...typography.caption, color: colors.textSecondary`) — both tokens
    already exist in src/theme/tokens.ts. Do NOT hardcode any hex or pixel
    literal (ARCHITECTURE.md anti-pattern; ReportFeedbackModal.tsx is the
    negative example to avoid).

    Do NOT modify `app/index.tsx` — the Setup screen chips are out of scope
    (D-06), even though it also imports `tenseLabels`.
  </action>
  <verify>
    <automated>npm run typecheck && grep -q 'tenseGrammarNames' app/quiz.tsx && ! grep -q "Perfect past" app/quiz.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `app/quiz.tsx` imports `tenseGrammarNames` from `../src/quiz/labels` on the same import line as `subjectLabels`/`tenseLabels` (no second import statement)
    - The meta row remains a single `·`-separated line (no new second `<Text>` row added below metaRow, no caption row)
    - Rendering logic emits ` (…)` with the `tenseGrammarNames` value only when that value is defined (preterite/imperfect); no parenthetical for present_indicative/future
    - For a preterite question the produced meta-row string equals `<translation> · Completed past (Pretérito perfeito) · <subject>` (single space before `(`, ` · ` around segments)
    - No hardcoded hex color or pixel literal is introduced; any new style key uses `...typography.*` + `colors.*` token spreads
    - `app/index.tsx` is unmodified (git shows no changes to it)
    - The substring `Perfect past` does not appear in app/quiz.tsx
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    Quiz meta row renders the friendly English label plus an inline
    parenthesized Portuguese grammar name for preterite/imperfect only, using
    tokens (no hardcoded literals), and typecheck passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update quiz-labels.test.ts assertions and verify backend-contract isolation</name>
  <files>__tests__/quiz-labels.test.ts</files>
  <read_first>
    - __tests__/quiz-labels.test.ts (the file being modified — current full 29-line content)
    - src/quiz/labels.ts (the updated maps being asserted against)
  </read_first>
  <behavior>
    - tenseLabels.preterite asserts exactly "Completed past"
    - tenseLabels.imperfect asserts exactly "Imperfect past"
    - tenseLabels.present_indicative still asserts exactly "Present" (unchanged-value regression check)
    - The existing "covers every Tense value with a non-empty label" loop over TENSES still passes
    - (Discretionary) tenseGrammarNames has entries for preterite/imperfect and none for present_indicative/future
  </behavior>
  <action>
    In `__tests__/quiz-labels.test.ts`, preserve the existing structure exactly
    (one `describe` per exported map, a "covers every value with a non-empty
    string" loop test, plus explicit full-string-value assertions per D-10 —
    do not rewrite into a different test style, and do NOT add
    `@testing-library/react-native` or any component-rendering test; this
    codebase uses plain-function unit tests only).

    Keep the existing `"maps present_indicative to the exact expected label"`
    test asserting `"Present"` (untouched-value regression per D-01). Add two
    explicit assertions in the `tenseLabels` describe block:
    `expect(tenseLabels.preterite).toBe("Completed past")` and
    `expect(tenseLabels.imperfect).toBe("Imperfect past")`.

    (Claude's discretion, recommended) Add a new
    `describe("tenseGrammarNames", ...)` block: extend the import on line 1 to
    include `tenseGrammarNames`, assert `tenseGrammarNames.preterite ===
    "Pretérito perfeito"` and `tenseGrammarNames.imperfect === "Pretérito
    imperfeito"`, and assert `tenseGrammarNames.present_indicative` and
    `tenseGrammarNames.future` are both `undefined` (partial-map coverage per
    D-08). Iterate only `["preterite", "imperfect"]` for any loop over the
    partial map, not all of `TENSES`.

    After updating the test, verify the LABEL-03 backend-contract isolation
    invariant (D-09) still holds: `src/feedback/` must contain zero references
    to `tenseLabels` or `tenseGrammarNames` (the feedback payload sources
    `tense` from `question.tense`, the raw enum literal). This is a
    verification step, not a code change to src/feedback/.
  </action>
  <verify>
    <automated>npm test -- quiz-labels && grep -rL "tenseLabels\|tenseGrammarNames" src/feedback/ >/dev/null && ! grep -rq "tenseLabels\|tenseGrammarNames" src/feedback/</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/quiz-labels.test.ts` asserts `tenseLabels.preterite === "Completed past"` and `tenseLabels.imperfect === "Imperfect past"`
    - The `"maps present_indicative to the exact expected label"` test asserting `"Present"` is retained unchanged
    - The existing TENSES-loop non-empty-string test remains
    - No `@testing-library/react-native` import is added
    - `npm test -- quiz-labels` passes (all assertions green)
    - `grep -rq "tenseLabels\|tenseGrammarNames" src/feedback/` returns non-zero (no matches) — confirming LABEL-03 payload isolation
  </acceptance_criteria>
  <done>
    quiz-labels.test.ts asserts the new displayed labels plus the unchanged
    "Present" literal, the suite passes, and src/feedback/ is confirmed free of
    any label-map reference (LABEL-03 preserved).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase changes only in-app display strings and one test. No new trust boundary is introduced — no user input parsing, no network call, no serialization change, no package install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | POST /feedback payload (`tense` field) | mitigate | Label change is display-only; Task 3 verifies `src/feedback/` has zero references to `tenseLabels`/`tenseGrammarNames` so the locked enum literals (`present_indicative`/`preterite`/`imperfect`/`future`) still flow from `question.tense` unchanged (LABEL-03). |
| T-12-02 | Information Disclosure | Rendered UI copy | accept | Only friendly labels + Portuguese grammar names (public grammar terminology) are shown; no secrets, PII, or credentials involved. |

No package-manager installs occur in this phase, so no supply-chain (T-12-SC) checkpoint is required.
</threat_model>

<verification>
Phase-level checks after all three tasks:

1. `npm run typecheck` exits 0.
2. `npm test -- quiz-labels` passes.
3. `grep -rq "tenseLabels\|tenseGrammarNames" src/feedback/` returns no matches (LABEL-03).
4. `grep -rq "Perfect past" src/ app/` returns no matches (D-02 / LABEL-02).
5. `git diff --name-only` shows only `src/quiz/labels.ts`, `app/quiz.tsx`,
   `__tests__/quiz-labels.test.ts` changed — `app/index.tsx`,
   `src/dataset/types.ts`, and `src/feedback/*` untouched.
</verification>

<success_criteria>
- Displayed tense labels read "Completed past" (preterite) and "Imperfect past" (imperfect); "Present" and "Future" unchanged (LABEL-01).
- Portuguese grammar names appear only as inline secondary/parenthetical text on the Quiz meta row for preterite/imperfect, never as the primary label; "Perfect past" appears nowhere (LABEL-02).
- Internal enum literals and the POST /feedback payload are unchanged; src/feedback/ references no label map (LABEL-03).
- `__tests__/quiz-labels.test.ts` passes with new displayed-label assertions and the unchanged-literal regression check (TEST-01).
</success_criteria>

<output>
Create `.planning/phases/12-tense-label-refresh/12-01-SUMMARY.md` when done.
</output>
