---
phase: 26-theme-palette-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/tokens.test.ts
  - src/theme/tokens.ts
autonomous: true
requirements: [THEME-01, THEME-02]

must_haves:
  truths:
    - "Importing `colors` from src/theme/tokens.ts yields exactly the 10-key guideline palette (primary, primarySoft, pressed, info, success, error, background, text, textSecondary, surface)."
    - "`colors.pressed` (#C94A2D) and `colors.info` (#36799A) exist as new alias keys available to Phase 28 without any component change."
    - "`colors.error` still resolves to #D64545 — the one deliberate pre-rebrand carry-over (D-08)."
    - "`npm test -- src/theme/tokens.test.ts` passes with the new palette asserted via an exact toEqual snapshot."
    - "`spacing`, `radius`, and `typography` exports and their tests are byte-for-byte unchanged."
  artifacts:
    - path: "src/theme/tokens.ts"
      provides: "The single source-of-truth `colors` object with the new brand guideline palette"
      contains: "#F2643E"
    - path: "src/theme/tokens.test.ts"
      provides: "Exact toEqual assertion over the new 10-key colors palette"
      contains: "#FFF9F6"
  key_links:
    - from: "src/theme/tokens.test.ts"
      to: "src/theme/tokens.ts"
      via: "named import of colors"
      pattern: "from \"./tokens\""
---

<objective>
Repoint `src/theme/tokens.ts`'s `colors` export to the new Lafa brand guideline
palette and update `src/theme/tokens.test.ts` to assert it exactly.

Purpose: `tokens.ts` becomes the single source of truth for brand color, so
Phase 27 (`app.json` splash + `_layout.tsx` chrome) and Phase 28 (screen/component
token application) build on correct values instead of the old palette.

Output: Updated `colors` object (10 keys) and its exact-match test.

Explicitly out of scope: applying tokens to any screen or component, wiring
`pressed`/`info` into a `Pressable` or `OfflinePill`, and touching `spacing`,
`radius`, or `typography` (values or tests). Those belong to Phase 28.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-theme-palette-update/26-CONTEXT.md
@.planning/phases/26-theme-palette-update/26-PATTERNS.md
@src/theme/tokens.ts
@src/theme/tokens.test.ts

<interfaces>
<!-- Current contract the executor is modifying. Named exports, no default export. -->
<!-- src/theme/tokens.ts exports four plain untyped object literals: -->
<!--   colors  (8 keys today -> 10 keys after this plan) -->
<!--   spacing (sm, md, lg, xl2, xl3, choiceGap) — DO NOT TOUCH -->
<!--   radius  (control, pill) — DO NOT TOUCH -->
<!--   typography (caption, body, bodyStrong, heading, display) — DO NOT TOUCH -->
<!-- src/theme/tokens.test.ts imports: { colors, spacing, radius, typography } from "./tokens" -->
</interfaces>

**Target palette (authoritative, from 26-CONTEXT.md D-01..D-11):**

| key | new value | old value | decision |
|-----|-----------|-----------|----------|
| primary | `#F2643E` | `#E8663D` | D-01 |
| primarySoft | `#FDE7DF` | `#FCE4DA` | D-02 |
| pressed | `#C94A2D` | (new key) | D-09 |
| info | `#36799A` | (new key) | D-10 |
| success | `#1F7F66` | `#2FA84F` | D-03 |
| error | `#D64545` | `#D64545` (unchanged) | D-08 |
| background | `#FFF9F6` | `#FFFFFF` | D-06 |
| text | `#24201E` | `#1C1B1A` | D-04 |
| textSecondary | `#746D69` | `#6B6560` | D-05 |
| surface | `#F1EFED` | `#F2F2F1` | D-07 |

No other keys. No `accent`, no `secondary`, no `canvas`, no `warmBackground` (D-11).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update the colors assertion in tokens.test.ts to the new palette (RED)</name>
  <files>src/theme/tokens.test.ts</files>
  <read_first>
    - src/theme/tokens.test.ts (full file, 60 lines — the block being edited is the first `it`, lines 4-15)
    - .planning/phases/26-theme-palette-update/26-CONTEXT.md (D-01..D-11 — authoritative hex mapping)
    - .planning/phases/26-theme-palette-update/26-PATTERNS.md (`src/theme/tokens.test.ts` section — what must NOT change)
  </read_first>
  <behavior>
    - `colors` deep-equals exactly: primary #F2643E, primarySoft #FDE7DF, pressed #C94A2D, info #36799A, success #1F7F66, error #D64545, background #FFF9F6, text #24201E, textSecondary #746D69, surface #F1EFED — 10 keys, no more, no fewer.
    - The existing `accent`/`secondary` absence assertion continues to hold.
    - spacing/radius/typography assertions continue to pass untouched.
  </behavior>
  <action>
    In `src/theme/tokens.test.ts`, replace ONLY the expected object literal inside the
    first test, `it("colors export the exact Lafa palette", ...)`, with the 10-key
    palette above (per THEME-02). Keep `expect(colors).toEqual({...})` — do not switch
    to `toMatchObject`, do not split into per-key assertions, do not rename the
    `describe` title or the `it` title. Leave the second test
    (`it("colors no longer export the old accent/secondary keys", ...)`) and every
    `spacing`/`radius`/`typography` test block exactly as-is. Leave the import line
    unchanged. Expect this task to end with a FAILING test — `tokens.ts` still holds the
    old palette until Task 2; that failure is the intended RED signal, so record the
    observed failure output rather than "fixing" it here.
  </action>
  <verify>
    <automated>npm test -- src/theme/tokens.test.ts 2>&1 | grep -q "colors export the exact Lafa palette" &amp;&amp; grep -c '#F2643E' src/theme/tokens.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#F2643E\|#FDE7DF\|#C94A2D\|#36799A\|#1F7F66\|#D64545\|#FFF9F6\|#24201E\|#746D69\|#F1EFED' src/theme/tokens.test.ts` returns 10.
    - `grep -c '#E8663D\|#FCE4DA\|#2FA84F\|#FFFFFF\|#1C1B1A\|#6B6560\|#F2F2F1' src/theme/tokens.test.ts` returns 0.
    - `npm test -- src/theme/tokens.test.ts` fails on the "colors export the exact Lafa palette" test ONLY (all spacing/radius/typography tests still pass).
    - `git diff --stat src/theme/tokens.test.ts` shows only the first `it` block changed.
  </acceptance_criteria>
  <done>tokens.test.ts asserts the new 10-key palette via a single exact `toEqual`, and the suite fails only on that assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repoint the colors export in tokens.ts to the new palette (GREEN)</name>
  <files>src/theme/tokens.ts</files>
  <read_first>
    - src/theme/tokens.ts (full file, 33 lines — only lines 1-10 change)
    - .planning/phases/26-theme-palette-update/26-CONTEXT.md (D-01..D-11)
    - .planning/phases/26-theme-palette-update/26-PATTERNS.md (style conventions to preserve)
  </read_first>
  <behavior>
    - `npm test -- src/theme/tokens.test.ts` passes fully after this change.
    - `colors.pressed` and `colors.info` are resolvable at the type level by any consumer.
  </behavior>
  <action>
    In `src/theme/tokens.ts`, replace the body of the `colors` object literal with the
    10-key palette from the target table (primary #F2643E per D-01, primarySoft #FDE7DF
    per D-02, pressed #C94A2D per D-09, info #36799A per D-10, success #1F7F66 per D-03,
    error #D64545 unchanged per D-08, background #FFF9F6 per D-06, text #24201E per D-04,
    textSecondary #746D69 per D-05, surface #F1EFED per D-07). Preserve the existing
    style exactly: `export const colors = { ... };` as a plain untyped object literal —
    no `as const` on the object, no explicit type annotation, no default export, 2-space
    indent, double-quoted hex strings, trailing commas. Key ordering is your discretion
    (`toEqual` is order-independent); the table order is a reasonable default. Do not
    add, remove, reorder, or reformat `spacing`, `radius`, or `typography`, and do not
    add imports — this file has none and needs none. Do not modify any consumer file in
    `app/` or `src/components/`; they resolve the same token names to the new values
    automatically, and re-styling them is Phase 28's job.
  </action>
  <verify>
    <automated>npm test -- src/theme/tokens.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/theme/tokens.ts`'s `colors` export contains exactly 10 keys with values: primary=#F2643E, primarySoft=#FDE7DF, pressed=#C94A2D, info=#36799A, success=#1F7F66, error=#D64545, background=#FFF9F6, text=#24201E, textSecondary=#746D69, surface=#F1EFED.
    - `grep -c '#E8663D\|#FCE4DA\|#2FA84F\|#1C1B1A\|#6B6560\|#F2F2F1' src/theme/tokens.ts` returns 0.
    - `npm test -- src/theme/tokens.test.ts` exits 0 (all 9 tests pass).
    - `npm test` (full suite) exits 0 — no other test asserts old hex values.
    - `npm run typecheck` exits 0.
    - `npm run lint` reports no new errors for `src/theme/tokens.ts`.
    - `git diff src/theme/tokens.ts` touches only lines within the `colors` literal.
  </acceptance_criteria>
  <done>`colors` exports the new 10-key guideline palette; tokens.test.ts, the full Jest suite, and typecheck all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan changes only compile-time string constants in a design-token module. No untrusted input crosses any boundary: no network calls, no user input parsing, no persistence, no auth, no filesystem or process access are added or modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | `src/theme/tokens.ts` color constants | accept | Values are non-security constants (CSS hex strings) consumed only by React Native `StyleSheet`. Incorrect values cause a visual defect, not a security impact; the exact-match `toEqual` test in `tokens.test.ts` is the integrity control. |
| T-26-02 | Information Disclosure | `src/theme/tokens.ts` | accept | No secrets, credentials, endpoints, or PII are present or added — the file contains only color/spacing/typography literals. |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | No package installs occur in this phase; `package.json` and lockfile are not modified. Supply-chain legitimacy gate is not applicable. |

ASVS L1 assessment: no L1 control applies to a static color-constant change. No high-severity findings; nothing to block on.
</threat_model>

<verification>
1. `npm test` — full Jest suite exits 0.
2. `npm run typecheck` — exits 0.
3. `npm run lint` — no new errors.
4. `grep -c '#E8663D\|#FCE4DA\|#2FA84F\|#1C1B1A\|#6B6560\|#F2F2F1' src/theme/tokens.ts src/theme/tokens.test.ts` returns 0 for both files (no old palette values survive).
5. `git diff --stat` lists exactly two changed files: `src/theme/tokens.ts` and `src/theme/tokens.test.ts`.
6. `git diff src/theme/tokens.ts | grep -c '^[+-].*\(spacing\|radius\|typography\|fontSize\|choiceGap\)'` returns 0 (non-color tokens untouched).
</verification>

<success_criteria>
- THEME-01 satisfied: `src/theme/tokens.ts` carries the guideline palette under preserved semantic names, with `pressed` and `info` as the only new alias keys and `error` retained at `#D64545` per D-08.
- THEME-02 satisfied: `src/theme/tokens.test.ts` asserts the new palette exactly and passes.
- No screen or component file is modified — Phase 28's scope stays intact.
- Phase 27 can reference `colors.background` = `#FFF9F6` and `colors.text` = `#24201E` for splash/Stack/StatusBar config.
</success_criteria>

<output>
Create `.planning/phases/26-theme-palette-update/26-01-SUMMARY.md` when done.
</output>
