---
phase: 19-general-product-feedback
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/productFeedback/ProductFeedbackModal.tsx
autonomous: true
requirements: [PFDBK-03, PFDBK-05]

must_haves:
  truths:
    - "ProductFeedbackModal renders a category pill picker (Bug/Idea/Other), a required message input, and a Submit button disabled until the message is non-empty"
    - "On submit it builds a payload with only {category, message, screen, appVersion, platform} and calls submitProductFeedback, showing success/error/retry states"
    - "The modal takes only screen/appVersion/platform props — never any quiz-answer prop"
    - "All styling uses colors/spacing/radius/typography tokens — no hardcoded hex/px literals"
  artifacts:
    - path: "src/productFeedback/ProductFeedbackModal.tsx"
      provides: "The product feedback modal component (idle/submitting/success/error state machine)"
      exports: ["ProductFeedbackModal"]
      min_lines: 150
  key_links:
    - from: "src/productFeedback/ProductFeedbackModal.tsx"
      to: "src/productFeedback/payload.ts"
      via: "buildProductFeedbackPayload import + call"
      pattern: "buildProductFeedbackPayload"
    - from: "src/productFeedback/ProductFeedbackModal.tsx"
      to: "src/productFeedback/submit.ts"
      via: "submitProductFeedback import + call"
      pattern: "submitProductFeedback"
    - from: "src/productFeedback/ProductFeedbackModal.tsx"
      to: "src/theme/tokens.ts"
      via: "colors/spacing/radius/typography import"
      pattern: "from \"../theme/tokens\""
---

<objective>
Build `src/productFeedback/ProductFeedbackModal.tsx` — a structural/visual mirror of `src/feedback/ReportFeedbackModal.tsx` (D-07) with the product-feedback field set: a 3-pill category picker (D-05), a required message field (D-06), and the idle/submitting/success/error state machine. It consumes the domain module from Plan 01 and is the form the three screen entry points will open.

Purpose: Provides the actual feedback-capture UI. Enforces PFDBK-05 structurally (its props carry no quiz-answer context) and the D-06 required-message rule that diverges from the analog. Uses design tokens from the start (does not copy the hardcoded-literal anti-pattern flagged in ARCHITECTURE.md).
Output: One new component file under `src/productFeedback/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-general-product-feedback/19-CONTEXT.md
@.planning/phases/19-general-product-feedback/19-PATTERNS.md
@.planning/phases/19-general-product-feedback/19-UI-SPEC.md

<interfaces>
<!-- From src/productFeedback/types.ts (Plan 01): -->
<!--   ProductFeedbackScreen = "setup" | "quiz" | "results" -->
<!--   ProductFeedbackCategory = "bug" | "idea" | "other" -->
<!--   SubmitResult = { status: "success"; data } | { status: "validation-error" } | { status: "server-error" } | { status: "network-error" } -->
<!-- From src/productFeedback/categories.ts: CATEGORY_OPTIONS: { value; label }[] -->
<!-- From src/productFeedback/payload.ts: buildProductFeedbackPayload({category, message, screen, appVersion, platform}) -->
<!-- From src/productFeedback/submit.ts: submitProductFeedback(payload): Promise<SubmitResult> -->

<!-- Modal props contract (PATTERNS.md):
  ProductFeedbackModalProps = {
    visible: boolean;
    screen: ProductFeedbackScreen;
    appVersion: string;
    platform: "ios" | "android";
    onClose: () => void;
  }
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ProductFeedbackModal component</name>
  <files>src/productFeedback/ProductFeedbackModal.tsx</files>
  <read_first>
    - src/feedback/ReportFeedbackModal.tsx (analog — full file: imports, props, idle/submitting/success/error state machine, useEffect reset-on-visible, timerRef auto-close, pill-list picker, TextInput, submit handler, retry derivation, full StyleSheet with tokens)
    - src/productFeedback/types.ts (Plan 01 — prop types, SubmitResult)
    - src/productFeedback/categories.ts (Plan 01 — CATEGORY_OPTIONS)
    - src/productFeedback/payload.ts (Plan 01 — buildProductFeedbackPayload)
    - src/productFeedback/submit.ts (Plan 01 — submitProductFeedback)
    - src/theme/tokens.ts (colors/spacing/radius/typography — the token names to use)
    - .planning/phases/19-general-product-feedback/19-UI-SPEC.md (Component Inventory + Copywriting Contract — exact copy strings, pill/input/button visual contract)
    - .planning/phases/19-general-product-feedback/19-PATTERNS.md (ProductFeedbackModal section — line-referenced adaptation of every block)
  </read_first>
  <action>
    Create `src/productFeedback/ProductFeedbackModal.tsx` mirroring `ReportFeedbackModal.tsx`'s structure. Import `colors, radius, spacing, typography` from `../theme/tokens` (this import line in the analog is already correct — copy it; do NOT introduce hardcoded hex/px literals). Import `CATEGORY_OPTIONS` from `./categories`, `buildProductFeedbackPayload` from `./payload`, `submitProductFeedback` from `./submit`, and `ProductFeedbackScreen`/`ProductFeedbackCategory`/`SubmitResult` types from `./types`.

    Props: `ProductFeedbackModalProps = { visible: boolean; screen: ProductFeedbackScreen; appVersion: string; platform: "ios" | "android"; onClose: () => void }`. No verb/tense/subject/correctAnswer/selectedAnswer props (PFDBK-05).

    State: `category` (ProductFeedbackCategory, default `"bug"`), `message` (string, default `""`), `state` (ModalState `idle|submitting|success|error`), `lastStatus` (SubmitResult["status"] | null), and a `timerRef`. Copy the analog's `useEffect([visible])` reset block verbatim, resetting `category` to `"bug"`, `message` to `""`, `state` to `"idle"`, `lastStatus` to `null`, and clearing the timer.

    Modal shell: same `Modal` props as analog (`presentationStyle="pageSheet"`, `animationType="slide"`, `visible`, `onRequestClose={onClose}`). Title text "Send feedback".

    Category pill list: render `CATEGORY_OPTIONS.map(...)` as selectable `Pressable` pills using the same `reasonOption`/`reasonOptionSelected`/`reasonOptionText`/`reasonOptionTextSelected` style pattern (rename to `categoryOption*` or reuse the same names), selected pill filled `colors.primary`, unselected `colors.surface`, disabled while submitting.

    Message `TextInput`: placeholder "What's on your mind?", multiline, bound to `message`/`setMessage`, using the analog's text-input style.

    Submit button: label "Submit feedback" (idle) / `ActivityIndicator` while submitting. Disabled condition MUST be `isSubmitting || message.trim().length === 0` (D-06 required-message divergence — the analog disables only on `isSubmitting`), using the `submitButtonDisabled` opacity-0.6 treatment.

    Submit handler `handleSubmit`: set state `"submitting"`, call `buildProductFeedbackPayload({ category, message, screen, appVersion, platform })`, await `submitProductFeedback(payload)`; on `"success"` set state `"success"`, set `lastStatus`, and `timerRef.current = setTimeout(onClose, 1500)`; otherwise set state `"error"` and `lastStatus`. Copy the analog's try/catch (catch -> error + `"network-error"`).

    Success text: "✓ Feedback sent — thank you!" (colors.success). Error text: "Something went wrong. Please try again." (colors.error). Retry button "Retry submission" shown only when `state === "error" && (lastStatus === "server-error" || lastStatus === "network-error")` — same derivation as the analog; retry re-invokes `handleSubmit`.

    StyleSheet: copy the analog's block as the starting point (it already uses tokens), removing the reason-specific bits that no longer apply and keeping `minHeight: 44` on all tappable targets. All values via tokens.

    Named export `ProductFeedbackModal`, no default export.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` reports no errors
    - `grep -q "message.trim().length === 0" src/productFeedback/ProductFeedbackModal.tsx` succeeds (D-06 required-message gate)
    - `grep -q "from \"../theme/tokens\"" src/productFeedback/ProductFeedbackModal.tsx` succeeds
    - `grep -Ei "#[0-9a-f]{6}" src/productFeedback/ProductFeedbackModal.tsx` returns nothing (no hardcoded hex colors)
    - `grep -qE "verb|tense|subject|correctAnswer|selectedAnswer" src/productFeedback/ProductFeedbackModal.tsx` returns nothing (no quiz-answer props, PFDBK-05)
    - File contains the strings "Send feedback", "Submit feedback", "What's on your mind?", "✓ Feedback sent — thank you!"
    - `npm test` full suite still green (no regression)
  </acceptance_criteria>
  <done>ProductFeedbackModal renders category pills + required message + token-styled submit/success/error/retry, carrying no quiz-answer context; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (modal) → payload builder | Free-text message + category selection captured in the modal before submission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Information Disclosure | ProductFeedbackModal props (V5 boundary) | mitigate | Props are restricted to screen/appVersion/platform — the modal has no access to quiz-answer state, so PFDBK-05 is enforced structurally at the component boundary (grep-asserted) |
| T-19-06 | Tampering | required-message gate (client-side UX validation) | mitigate | Submit disabled until `message.trim().length > 0`; backend re-validates 1-2000 and returns 400 (surfaced via generic error text) |
| T-19-04 | Information Disclosure | user pastes sensitive text into message | accept | Same accepted risk as existing `/feedback` free-text field; no client-layer mitigation expected |
| T-19-SC | Tampering | npm installs | n/a | Zero new packages — no install task |
</threat_model>

<verification>
- `npm run typecheck` clean
- `npm test` full suite green (regression check — no unit test renders the modal per repo convention)
- Visual/interaction correctness deferred to the Plan 05 human-verify checkpoint
</verification>

<success_criteria>
- ProductFeedbackModal exists as a token-styled mirror of ReportFeedbackModal with category pills, required message, and the 4-state machine
- Props carry zero quiz-answer context (PFDBK-05)
- Submit is gated on a non-empty trimmed message (D-06)
</success_criteria>

<output>
Create `.planning/phases/19-general-product-feedback/19-02-SUMMARY.md` when done
</output>
