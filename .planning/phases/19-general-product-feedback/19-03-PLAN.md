---
phase: 19-general-product-feedback
plan: 03
type: execute
wave: 3
depends_on: [19-02]
files_modified:
  - app/index.tsx
  - app/results.tsx
autonomous: true
requirements: [PFDBK-01]

must_haves:
  truths:
    - "The Setup screen shows a low-visual-weight 'Help us improve' footer link below the Start Quiz button that opens ProductFeedbackModal with screen='setup'"
    - "The Results screen shows the same 'Help us improve' footer link below the Share/Try Again/Back-to-Setup action group that opens ProductFeedbackModal with screen='results'"
    - "Both screens compute appVersion and platform locally and pass them to the modal"
  artifacts:
    - path: "app/index.tsx"
      provides: "Setup footer-link entry point + ProductFeedbackModal wiring (D-01)"
      contains: "ProductFeedbackModal"
    - path: "app/results.tsx"
      provides: "Results footer-link entry point + ProductFeedbackModal wiring (D-02)"
      contains: "ProductFeedbackModal"
  key_links:
    - from: "app/index.tsx"
      to: "src/productFeedback/ProductFeedbackModal.tsx"
      via: "import + render with screen=\"setup\""
      pattern: "screen=\"setup\""
    - from: "app/results.tsx"
      to: "src/productFeedback/ProductFeedbackModal.tsx"
      via: "import + render with screen=\"results\""
      pattern: "screen=\"results\""
---

<objective>
Add the "Help us improve" footer-link entry point to the Setup (`app/index.tsx`, D-01) and Results (`app/results.tsx`, D-02) screens. Each screen gets local `productFeedbackVisible` state, a low-visual-weight footer link, and a rendered `<ProductFeedbackModal>` wired with a hardcoded literal `screen` prop plus locally-computed `appVersion`/`platform` (D-08 resolution: literal prop, not `usePathname()`).

Purpose: Delivers 2 of the 3 PFDBK-01 entry points. Setup and Results share the identical footer-link treatment (visual consistency per D-01/D-02), so they are grouped in one plan.
Output: Modifications to `app/index.tsx` and `app/results.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-general-product-feedback/19-CONTEXT.md
@.planning/phases/19-general-product-feedback/19-PATTERNS.md
@.planning/phases/19-general-product-feedback/19-UI-SPEC.md

<interfaces>
<!-- ProductFeedbackModal (Plan 02) props:
  { visible: boolean; screen: "setup"|"quiz"|"results"; appVersion: string; platform: "ios"|"android"; onClose: () => void } -->

<!-- appVersion/platform derivation (verbatim from app/quiz.tsx lines 3, 56-57):
  import Constants from "expo-constants";
  import { Platform } from "react-native";
  const appVersion = Constants.expoConfig?.version ?? "unknown";
  const platform: "ios" | "android" = Platform.OS === "android" ? "android" : "ios";
-->

<!-- Footer link visual contract (UI-SPEC Component Inventory):
  Pressable wrapping Text; no background/border/icon;
  Text: typography.caption + color: colors.primary, textAlign "center";
  Container: minHeight 44, justifyContent/alignItems center, marginTop: spacing.md;
  Copy: "Help us improve" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Setup screen footer link + modal (D-01)</name>
  <files>app/index.tsx</files>
  <read_first>
    - app/index.tsx (file being modified — current imports, startButton JSX at ~line 129, StyleSheet block; note it currently has NO expo-constants/Platform import)
    - app/quiz.tsx (reference — lines 3, 27, 56-57 for the Constants/Platform imports, reportVisible state pattern, and appVersion/platform derivation; lines 169-179 for ProductFeedbackModal-style wiring)
    - src/productFeedback/ProductFeedbackModal.tsx (Plan 02 — the component + prop contract)
    - src/theme/tokens.ts (typography.caption, colors.primary, spacing.md)
    - .planning/phases/19-general-product-feedback/19-UI-SPEC.md (footer-link visual contract, "Help us improve" copy)
  </read_first>
  <action>
    Modify `app/index.tsx`:
    1. Add `import Constants from "expo-constants";` and add `Platform` to the existing `react-native` import. Add `import { ProductFeedbackModal } from "../src/productFeedback/ProductFeedbackModal";`.
    2. Inside the component, compute `const appVersion = Constants.expoConfig?.version ?? "unknown";` and `const platform: "ios" | "android" = Platform.OS === "android" ? "android" : "ios";` (verbatim from quiz.tsx) — Pitfall 1: these do not currently exist on this screen.
    3. Add `const [productFeedbackVisible, setProductFeedbackVisible] = useState(false);` (mirrors quiz.tsx `reportVisible`).
    4. Render a footer-link `Pressable` wrapping a `Text` ("Help us improve") directly below the Start Quiz `Pressable` (~line 129), using a new `productFeedbackLink` container style (`minHeight: 44`, `justifyContent: "center"`, `alignItems: "center"`, `marginTop: spacing.md`) and `productFeedbackLinkText` style (`...typography.caption`, `color: colors.primary`, `textAlign: "center"`). `onPress` sets `productFeedbackVisible` true. No background/border/icon.
    5. Render `<ProductFeedbackModal visible={productFeedbackVisible} screen="setup" appVersion={appVersion} platform={platform} onClose={() => setProductFeedbackVisible(false)} />` at the bottom of the JSX tree (mirroring quiz.tsx's modal placement).
    Do not use `usePathname()` — pass the literal `screen="setup"` (D-08).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` clean
    - `grep -q 'screen="setup"' app/index.tsx` succeeds
    - `grep -q 'Help us improve' app/index.tsx` succeeds
    - `grep -q "Constants.expoConfig?.version" app/index.tsx` succeeds (appVersion computed locally)
    - `grep -q "usePathname" app/index.tsx` returns nothing (literal prop, D-08)
    - `grep -q "typography.caption" app/index.tsx` succeeds (low-weight footer style)
  </acceptance_criteria>
  <done>Setup screen has a caption-weight "Help us improve" link below Start Quiz that opens ProductFeedbackModal with screen="setup".</done>
</task>

<task type="auto">
  <name>Task 2: Wire Results screen footer link + modal (D-02)</name>
  <files>app/results.tsx</files>
  <read_first>
    - app/results.tsx (file being modified — current imports, the `styles.actions` block at ~line 96 containing Share Score/Try Again/Back to Setup, StyleSheet at ~line 150; note it currently has NO expo-constants/Platform import)
    - app/quiz.tsx (reference — lines 3, 56-57 Constants/Platform derivation; modal wiring pattern)
    - src/productFeedback/ProductFeedbackModal.tsx (Plan 02 — component + prop contract)
    - src/theme/tokens.ts (typography.caption, colors.primary, spacing.md)
    - .planning/phases/19-general-product-feedback/19-UI-SPEC.md (footer-link contract, D-02 placement below the actions group)
  </read_first>
  <action>
    Modify `app/results.tsx`, applying the same footer-link pattern as Task 1:
    1. Add `import Constants from "expo-constants";`, add `Platform` to the `react-native` import, add `import { ProductFeedbackModal } from "../src/productFeedback/ProductFeedbackModal";`.
    2. Compute `appVersion` and `platform` locally (verbatim from quiz.tsx) — Pitfall 1: absent on this screen today.
    3. Add `const [productFeedbackVisible, setProductFeedbackVisible] = useState(false);`.
    4. Render the "Help us improve" footer-link `Pressable`/`Text` BELOW the existing `<View style={styles.actions}>` group (after Share Score / Try Again / Back to Setup, per D-02), reusing the same `productFeedbackLink`/`productFeedbackLinkText` style shape as index.tsx (`minHeight: 44`, centered, `marginTop: spacing.md`; text `typography.caption` + `colors.primary`).
    5. Render `<ProductFeedbackModal visible={productFeedbackVisible} screen="results" appVersion={appVersion} platform={platform} onClose={() => setProductFeedbackVisible(false)} />` at the bottom of the JSX tree.
    Note: the footer link belongs on the primary completed-results branch (the one containing the `actions` group), not the no-session fallback branch. Pass literal `screen="results"` — no `usePathname()`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` clean
    - `grep -q 'screen="results"' app/results.tsx` succeeds
    - `grep -q 'Help us improve' app/results.tsx` succeeds
    - `grep -q "Constants.expoConfig?.version" app/results.tsx` succeeds
    - `grep -q "usePathname" app/results.tsx` returns nothing
    - `npm test` full suite green (no regression)
  </acceptance_criteria>
  <done>Results screen has a caption-weight "Help us improve" link below the actions group that opens ProductFeedbackModal with screen="results".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screen component → ProductFeedbackModal | Screen supplies only its own identity (literal `screen`) + app metadata; no quiz state is in scope on Setup/Results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Spoofing | `screen` field integrity | mitigate | Hardcoded compile-time-safe literal (`"setup"`/`"results"`) instead of runtime `usePathname()` mapping — no unmapped-path failure mode (D-08 resolution) |
| T-19-05 | Information Disclosure | modal props on Setup/Results | mitigate | Neither screen has quiz-answer state in scope; modal receives only screen/appVersion/platform (PFDBK-05 held structurally) |
| T-19-SC | Tampering | npm installs | n/a | Zero new packages — no install task |
</threat_model>

<verification>
- `npm run typecheck` clean
- `npm test` full suite green
- On-device entry-point visibility verified in Plan 05 checkpoint
</verification>

<success_criteria>
- Setup and Results each render a low-visual-weight "Help us improve" footer link that opens ProductFeedbackModal with the correct literal `screen` value
- Both screens compute appVersion/platform locally (Pitfall 1 avoided)
- No `usePathname()` usage (D-08)
</success_criteria>

<output>
Create `.planning/phases/19-general-product-feedback/19-03-SUMMARY.md` when done
</output>
