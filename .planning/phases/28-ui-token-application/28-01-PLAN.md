---
phase: 28-ui-token-application
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/tokens.ts
  - src/theme/tokens.test.ts
  - src/components/OfflinePill.tsx
autonomous: true
requirements: [UI-02]

must_haves:
  truths:
    - "The theme exposes an `infoSoft` color token paired with the existing `info` token"
    - "The OfflinePill renders with a teal tint background and teal text instead of the orange primary pairing"
    - "The exhaustive tokens test still passes against the new colors shape"
  artifacts:
    - path: "src/theme/tokens.ts"
      provides: "colors.infoSoft token"
      contains: "infoSoft: \"#DCEBF0\""
    - path: "src/theme/tokens.test.ts"
      provides: "Updated exhaustive colors toEqual assertion"
      contains: "infoSoft: \"#DCEBF0\""
    - path: "src/components/OfflinePill.tsx"
      provides: "Info-palette pill styling"
      contains: "colors.infoSoft"
  key_links:
    - from: "src/components/OfflinePill.tsx"
      to: "src/theme/tokens.ts"
      via: "named token import"
      pattern: "colors\\.(infoSoft|info)"
---

<objective>
Add the `colors.infoSoft` token that `colors.info` currently lacks, and switch
`OfflinePill` from the orange `primarySoft`/`primary` pairing to the teal
`infoSoft`/`info` pairing.

Purpose: Phase 26 added `info` explicitly for `OfflinePill` (D-10) but never
wired it up, and never shipped the soft companion tint. This closes that gap
(D-05, D-06) and frees `primarySoft` to read as a pure brand accent.
Output: One new token, one updated test assertion, one restyled component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-ui-token-application/28-CONTEXT.md
@.planning/phases/28-ui-token-application/28-UI-SPEC.md
@.planning/phases/28-ui-token-application/28-PATTERNS.md
</context>

<interfaces>
<!-- Current shape of the token module the executor is extending. -->

From src/theme/tokens.ts (current, before this plan):
`export const colors` is a plain object literal with keys in this exact order:
`primary`, `primarySoft`, `pressed`, `info`, `success`, `error`, `background`,
`text`, `textSecondary`, `surface`. `spacing`, `radius`, and `typography` are
separate named exports in the same file and are NOT changed by this plan.

From src/components/OfflinePill.tsx (current):
`export const OFFLINE_PILL_TEXT = "Using saved content"`,
`export function isLocalSource(source: VerbSource | null): boolean`,
`export function OfflinePill()`. Styles live in a single
`StyleSheet.create({ container, text })` block at the bottom of the file.
`colors, radius, spacing, typography` are already imported by name from
`../theme/tokens` — no import-list change is needed.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the infoSoft color token and update the exhaustive tokens test</name>
  <files>src/theme/tokens.ts, src/theme/tokens.test.ts</files>
  <read_first>
    - src/theme/tokens.ts (full file — confirm current key order)
    - src/theme/tokens.test.ts (full file — confirm the single-block `toEqual()` style)
    - .planning/phases/28-ui-token-application/28-UI-SPEC.md §"New token: `colors.infoSoft`" (locks hex + placement)
    - .planning/phases/28-ui-token-application/28-PATTERNS.md §`src/theme/tokens.ts` and §`src/theme/tokens.test.ts`
  </read_first>
  <behavior>
    - `colors.infoSoft` equals the string `#DCEBF0`
    - `expect(colors).toEqual({...})` in the existing "colors export the exact Lafa palette" test still passes with `infoSoft` included
    - No other token group (`spacing`, `radius`, `typography`) changes — their existing assertions still pass unmodified
  </behavior>
  <action>
    In `src/theme/tokens.ts`, insert the key `infoSoft` with the exact value
    `"#DCEBF0"` into the `colors` object literal, positioned immediately after
    the existing `info: "#36799A",` line and immediately before `success:
    "#1F7F66",`. This mirrors the existing `primary` → `primarySoft` adjacency
    convention (D-06). Do not change any other key, value, or export in the
    file — `spacing`, `radius`, and `typography` are untouched.

    In `src/theme/tokens.test.ts`, add the line `infoSoft: "#DCEBF0",` inside
    the existing exhaustive `expect(colors).toEqual({ ... })` block in the
    `it("colors export the exact Lafa palette")` test, in the same position
    (directly after `info: "#36799A",`). Keep the single-block `toEqual()`
    structure — do NOT restructure into per-key `toBe()` assertions and do NOT
    add a new `it()` block. Leave the "colors no longer export the old
    accent/secondary keys" test and all `spacing`/`radius`/`typography` tests
    exactly as-is.
  </action>
  <acceptance_criteria>
    - `grep -n 'infoSoft: "#DCEBF0",' src/theme/tokens.ts` returns exactly one match
    - In `src/theme/tokens.ts` the line number of the `infoSoft` key is exactly one greater than the line number of the `info: "#36799A",` key
    - `grep -n 'infoSoft: "#DCEBF0",' src/theme/tokens.test.ts` returns exactly one match
    - `npx jest src/theme/tokens.test.ts` exits 0 with all 9 existing tests passing
    - `grep -c "it(" src/theme/tokens.test.ts` returns 9 (no new test blocks added)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx jest src/theme/tokens.test.ts && npm run typecheck</automated>
  </verify>
  <done>`colors.infoSoft === "#DCEBF0"` is exported, sits directly after `info`, and the exhaustive tokens test passes unchanged in structure.</done>
</task>

<task type="auto">
  <name>Task 2: Switch OfflinePill to the info/infoSoft teal pairing</name>
  <files>src/components/OfflinePill.tsx</files>
  <read_first>
    - src/components/OfflinePill.tsx (full file — the `StyleSheet.create` block at the bottom)
    - src/theme/tokens.ts (post-Task-1 state — confirm `infoSoft` exists)
    - .planning/phases/28-ui-token-application/28-UI-SPEC.md §"OfflinePill Contract (D-05, D-06)" (the before/after property table)
  </read_first>
  <action>
    In `src/components/OfflinePill.tsx`'s `StyleSheet.create` block, change
    `styles.container.backgroundColor` from `colors.primarySoft` to
    `colors.infoSoft`, and change `styles.text.color` from `colors.primary` to
    `colors.info` (per D-05/D-06). Change nothing else: `alignSelf`,
    `borderRadius: radius.pill`, `paddingHorizontal: spacing.sm`,
    `paddingVertical: spacing.sm / 2`, `marginBottom: spacing.md`, and the
    `...typography.caption` spread all stay verbatim. The
    `OFFLINE_PILL_TEXT` constant ("Using saved content"), the `isLocalSource`
    helper, and the component's `resolveVerbs()` effect are untouched. The
    import line already pulls in `colors` — no import edits needed.
  </action>
  <acceptance_criteria>
    - `grep -c "colors.primarySoft\|colors.primary\b" src/components/OfflinePill.tsx` returns 0
    - `grep -c "colors.infoSoft" src/components/OfflinePill.tsx` returns 1
    - `grep -c "colors.info\b" src/components/OfflinePill.tsx` returns 1
    - `grep -c "radius.pill" src/components/OfflinePill.tsx` returns 1 (unchanged structural styling)
    - `grep -c 'OFFLINE_PILL_TEXT = "Using saved content"' src/components/OfflinePill.tsx` returns 1 (copy unchanged)
    - `npx jest __tests__/offline-pill.test.ts` exits 0
    - `npm run typecheck` exits 0 and `npm run lint` reports no new errors
  </acceptance_criteria>
  <verify>
    <automated>npx jest __tests__/offline-pill.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <done>OfflinePill's background resolves to `colors.infoSoft` and its text to `colors.info`; no other styling or copy changed; existing tests pass.</done>
</task>

</tasks>

<threat_model>
No new threat surface — styling-only change. This plan adds a static string
constant to a first-party design-token module and swaps two token references in
one presentational component. No new inputs, no new data flows, no network,
auth, storage, or dependency changes. No packages are installed, so the package
legitimacy gate does not apply.
</threat_model>

<verification>
- `npx jest src/theme/tokens.test.ts __tests__/offline-pill.test.ts` exits 0
- `npm run typecheck` exits 0
- `npm run lint` reports no new errors
- `grep -rEn "#[0-9A-Fa-f]{6}" src/components/ app/` returns zero matches (no literal hex leaked outside `src/theme/`)
</verification>

<success_criteria>
- `colors.infoSoft` is exported with value `#DCEBF0`, positioned directly after `info`
- `src/theme/tokens.test.ts`'s exhaustive `toEqual()` includes `infoSoft` and passes
- `OfflinePill` uses `colors.infoSoft` (background) and `colors.info` (text) with zero references to `colors.primarySoft`/`colors.primary`
- Full test suite (`npm test`) still passes
</success_criteria>

<output>
Create `.planning/phases/28-ui-token-application/28-01-SUMMARY.md` when done
</output>
