---
phase: 28-ui-token-application
plan: 03
type: execute
wave: 2
depends_on: ["28-01", "28-02"]
files_modified:
  - src/feedback/ReportFeedbackModal.tsx
  - src/productFeedback/ProductFeedbackModal.tsx
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Pressing the submit button in either feedback modal shows a deep-orange pressed background"
    - "Pressing the error-state retry button in either feedback modal shows a deep-orange pressed background"
    - "The product-feedback submit button remains disabled while the message is empty"
    - "No file under app/ or src/ references any old-palette hex value"
    - "ExplanationPanel remains fully token-based with no hex literals"
  artifacts:
    - path: "src/feedback/ReportFeedbackModal.tsx"
      provides: "Submit + retry pressed states"
      contains: "({ pressed })"
    - path: "src/productFeedback/ProductFeedbackModal.tsx"
      provides: "Submit + retry pressed states, disabled condition preserved"
      contains: "({ pressed })"
  key_links:
    - from: "src/productFeedback/ProductFeedbackModal.tsx"
      to: "src/theme/tokens.ts"
      via: "inline pressed background override"
      pattern: "pressed && \\{ backgroundColor: colors\\.pressed \\}"
---

<objective>
Wire `colors.pressed` into the submit and retry buttons of both feedback modals,
then run the phase-wide regression gate proving zero old-palette hex values
remain anywhere in `app/`/`src/` (UI-02's final acceptance criterion).

Purpose: Completes D-03's in-scope button list and closes out the phase with a
repo-wide assertion covering the work from plans 01 and 02.
Output: Two restyled modals plus a documented regression verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-ui-token-application/28-CONTEXT.md
@.planning/phases/28-ui-token-application/28-UI-SPEC.md
@.planning/phases/28-ui-token-application/28-PATTERNS.md
@.planning/phases/28-ui-token-application/28-01-SUMMARY.md
@.planning/phases/28-ui-token-application/28-02-SUMMARY.md
</context>

<interfaces>
<!-- Mechanism identical to plan 02; the two modals are structural twins. -->

Mechanism (apply verbatim, D-04): convert an existing `style` prop on a
`Pressable` into `style={({ pressed }) => [...]}` returning the same style
entries in the same order, with `pressed && { backgroundColor: colors.pressed }`
appended LAST. Never use `onPressIn`/`onPressOut`/`useState` press tracking.

`colors.pressed === "#C94A2D"` already exists in `src/theme/tokens.ts`.
`colors` is already imported by name in both modal files.

Key structural difference between the twins:
- `src/feedback/ReportFeedbackModal.tsx` submit button gates on `isSubmitting`
  alone (`disabled={isSubmitting}`, array entry
  `isSubmitting && styles.submitButtonDisabled`).
- `src/productFeedback/ProductFeedbackModal.tsx` submit button gates on a
  compound local `const isSubmitDisabled = isSubmitting || message.trim().length === 0`
  (`disabled={isSubmitDisabled}`, array entry
  `isSubmitDisabled && styles.submitButtonDisabled`). This condition MUST be
  preserved exactly.
Both retry buttons are `{showRetry ? <Pressable onPress={handleSubmit} style={styles.retryButton}>…</Pressable> : null}`
with a `colors.error`-based base background.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire pressed state into ReportFeedbackModal's submit and retry buttons</name>
  <files>src/feedback/ReportFeedbackModal.tsx</files>
  <read_first>
    - src/feedback/ReportFeedbackModal.tsx (full file — the submit `Pressable` around line 172, the `showRetry` retry `Pressable` around line 184, and the `styles.submitButton`/`submitButtonDisabled`/`retryButton` blocks)
    - .planning/phases/28-ui-token-application/28-UI-SPEC.md §"Interaction States — Pressed State"
    - .planning/phases/28-ui-token-application/28-PATTERNS.md §`src/feedback/ReportFeedbackModal.tsx`
  </read_first>
  <action>
    Submit button: convert `style={[styles.submitButton, isSubmitting && styles.submitButtonDisabled]}`
    into `({ pressed }) => [styles.submitButton, isSubmitting && styles.submitButtonDisabled, pressed && { backgroundColor: colors.pressed }]`.
    Preserve `onPress={handleSubmit}`, `disabled={isSubmitting}`, and the
    conditional `ActivityIndicator` / "Submit feedback" children verbatim.

    Retry button: convert the bare `style={styles.retryButton}` into
    `({ pressed }) => [styles.retryButton, pressed && { backgroundColor: colors.pressed }]`.
    The retry button's base background is `colors.error`, not `colors.primary` —
    the pressed override is still `colors.pressed` (single pressed color for all
    in-scope buttons per D-03/D-04, retry included). Preserve the
    `{showRetry ? … : null}` wrapper, `onPress={handleSubmit}`, and the
    "Retry submission" text verbatim.

    Out of scope (D-03) — leave the `reasonOption` chips and any text-link
    triggers untouched.

    Note: this file historically hardcoded hex literals (an anti-pattern noted
    in ARCHITECTURE.md). If any literal hex remains in its `StyleSheet.create`
    block, that is a pre-existing condition — do NOT refactor it as part of this
    task, but DO report it in the summary so Task 3's regression grep result is
    interpretable.
  </action>
  <acceptance_criteria>
    - `grep -c "({ pressed })" src/feedback/ReportFeedbackModal.tsx` returns 2
    - `grep -c "pressed && { backgroundColor: colors.pressed }" src/feedback/ReportFeedbackModal.tsx` returns 2
    - `grep -c "onPressIn\|onPressOut" src/feedback/ReportFeedbackModal.tsx` returns 0
    - `grep -c "isSubmitting && styles.submitButtonDisabled" src/feedback/ReportFeedbackModal.tsx` returns 1 (existing disabled entry preserved)
    - `grep -c "disabled={isSubmitting}" src/feedback/ReportFeedbackModal.tsx` returns 1
    - `grep -c "reasonOption" src/feedback/ReportFeedbackModal.tsx` is unchanged from before the edit (chips untouched)
    - `npm run typecheck` exits 0; `npx jest` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint && npx jest</automated>
  </verify>
  <done>Both the submit and retry buttons use the function-form `style` prop with `colors.pressed` appended last; disabled logic and chips are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pressed state into ProductFeedbackModal's submit and retry buttons</name>
  <files>src/productFeedback/ProductFeedbackModal.tsx</files>
  <read_first>
    - src/productFeedback/ProductFeedbackModal.tsx (full file — the `isSubmitDisabled` derivation, the submit `Pressable` around line 164, the `showRetry` retry `Pressable` around line 179)
    - src/feedback/ReportFeedbackModal.tsx (post-Task-1 state — the twin implementation to mirror)
    - .planning/phases/28-ui-token-application/28-PATTERNS.md §`src/productFeedback/ProductFeedbackModal.tsx`
  </read_first>
  <action>
    Submit button: convert `style={[styles.submitButton, isSubmitDisabled && styles.submitButtonDisabled]}`
    into `({ pressed }) => [styles.submitButton, isSubmitDisabled && styles.submitButtonDisabled, pressed && { backgroundColor: colors.pressed }]`.
    The existing compound condition `const isSubmitDisabled = isSubmitting || message.trim().length === 0`
    and the `disabled={isSubmitDisabled}` prop MUST be preserved exactly — do
    not simplify to `isSubmitting`, and do not remove the empty-message guard.

    Retry button: convert the bare `style={styles.retryButton}` into
    `({ pressed }) => [styles.retryButton, pressed && { backgroundColor: colors.pressed }]`,
    preserving the `{showRetry ? … : null}` wrapper, `onPress={handleSubmit}`,
    and the "Retry submission" text verbatim.

    Out of scope (D-03) — leave the `categoryOption` chips untouched.
  </action>
  <acceptance_criteria>
    - `grep -c "({ pressed })" src/productFeedback/ProductFeedbackModal.tsx` returns 2
    - `grep -c "pressed && { backgroundColor: colors.pressed }" src/productFeedback/ProductFeedbackModal.tsx` returns 2
    - `grep -c "onPressIn\|onPressOut" src/productFeedback/ProductFeedbackModal.tsx` returns 0
    - `grep -c "message.trim().length === 0" src/productFeedback/ProductFeedbackModal.tsx` returns 1 (empty-message guard preserved)
    - `grep -c "disabled={isSubmitDisabled}" src/productFeedback/ProductFeedbackModal.tsx` returns 1
    - `grep -c "isSubmitDisabled && styles.submitButtonDisabled" src/productFeedback/ProductFeedbackModal.tsx` returns 1
    - `grep -c "categoryOption" src/productFeedback/ProductFeedbackModal.tsx` is unchanged from before the edit
    - `npm run typecheck` exits 0; `npx jest` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint && npx jest</automated>
  </verify>
  <done>Both buttons use the function-form `style` prop with `colors.pressed` appended last; the compound `isSubmitDisabled` guard is intact; chips are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-wide UI-02 regression gate</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - src/components/ExplanationPanel.tsx (confirm it is already fully token-based and needs no change, per CONTEXT.md)
    - .planning/ROADMAP.md §"Phase 28: UI Token Application" Success Criteria (the exact old-palette hex list)
  </read_first>
  <action>
    Run the phase's final acceptance checks and record the exact command output
    in the plan summary. Do NOT modify any source file in this task — it is a
    read-only gate. If any check fails, stop and report rather than
    opportunistically fixing files outside this phase's declared scope.

    Checks to run, in order:
    1. Old-palette regression: search `app/` and `src/` for the five retired hex
       values `#208AEF`, `#E6F4FE`, `#E8663D`, `#FCE4DA`, `#2FA84F`. Expect zero
       matches.
    2. Hex-literal containment: search `app/` and `src/` for any six-digit hex
       pattern, excluding `src/theme/` (the token module is the only sanctioned
       home for literal hex). Report any match found — a pre-existing literal in
       `ReportFeedbackModal.tsx` would surface here and should be reported, not
       silently fixed.
    3. ExplanationPanel confirmation: verify `src/components/ExplanationPanel.tsx`
       references only `colors.*` token names and contains no hex literal, and
       confirm it has NOT been modified during this phase.
    4. Mechanism compliance: verify no file under `app/` or `src/` introduced
       `onPressIn`/`onPressOut` press-state tracking (D-04).
    5. Full gates: `npm run typecheck`, `npm run lint`, `npm test`.
  </action>
  <acceptance_criteria>
    - `grep -rEn "#208AEF|#E6F4FE|#E8663D|#FCE4DA|#2FA84F" app/ src/` returns no matches (exit status 1)
    - `grep -rEn "#[0-9A-Fa-f]{6}" app/ src/ --exclude-dir=theme` returns no matches, OR every match is reported verbatim in the summary as pre-existing and outside this phase's declared file list
    - `grep -rc "colors\." src/components/ExplanationPanel.tsx` is greater than 0 and `grep -cE "#[0-9A-Fa-f]{6}" src/components/ExplanationPanel.tsx` returns 0
    - `git diff --name-only` for this phase does NOT include `src/components/ExplanationPanel.tsx`
    - `grep -rc "onPressIn\|onPressOut" app/ src/` returns 0 for every file
    - `grep -rc "({ pressed })" app/ src/` totals 9 across exactly 5 files: `app/index.tsx` (1), `app/quiz.tsx` (2), `app/results.tsx` (2), `src/feedback/ReportFeedbackModal.tsx` (2), `src/productFeedback/ProductFeedbackModal.tsx` (2)
    - `npm run typecheck` exits 0, `npm run lint` reports no new errors, `npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>! grep -rEq "#208AEF|#E6F4FE|#E8663D|#FCE4DA|#2FA84F" app/ src/ && npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <done>All five old-palette hex values are provably absent from `app/`/`src/`; ExplanationPanel is confirmed token-based and untouched; the pressed-state mechanism appears in exactly the five in-scope files with the expected call-site counts; typecheck, lint, and the full test suite pass.</done>
</task>

</tasks>

<threat_model>
No new threat surface — styling-only change. Tasks 1 and 2 convert existing
`style` props on already-existing `Pressable` components; Task 3 is a read-only
verification gate that modifies nothing. The `disabled` guards controlling
feedback submission (including the empty-message guard) are explicitly preserved
rather than altered, so the existing `POST /feedback` submission path is
unchanged. No new inputs, data flows, network calls, or dependencies. No
packages are installed, so the package legitimacy gate does not apply.
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm run lint` reports no new errors
- `npm test` exits 0 (full suite, including `__tests__/feedback-*.test.ts` and `src/theme/tokens.test.ts`)
- `grep -rEn "#208AEF|#E6F4FE|#E8663D|#FCE4DA|#2FA84F" app/ src/` returns no matches
- `grep -rc "onPressIn\|onPressOut" app/ src/` returns 0 everywhere
</verification>

<success_criteria>
- Both feedback modals' submit and retry buttons use `Pressable`'s `style={({ pressed }) => [...]}` form with `colors.pressed` appended last
- `ProductFeedbackModal`'s `isSubmitDisabled` compound condition (including `message.trim().length === 0`) is preserved verbatim
- Reason/category chips in both modals are untouched
- Zero occurrences of the five old-palette hex values anywhere in `app/`/`src/` (UI-02 final acceptance)
- `ExplanationPanel.tsx` confirmed token-based and unmodified by this phase
</success_criteria>

<output>
Create `.planning/phases/28-ui-token-application/28-03-SUMMARY.md` when done
</output>
