---
phase: 23-eas-build-submit-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [eas.json, app.json]
autonomous: true
requirements: [EASCFG-01, EASCFG-02, EASCFG-03]

must_haves:
  truths:
    - "eas.json build.production uses remote appVersionSource + autoIncrement (EASCFG-01)"
    - "eas.json submit.production.ios has an ascAppId placeholder ready to fill in Phase 24 (EASCFG-02)"
    - "app.json declares ITSAppUsesNonExemptEncryption false for export compliance (EASCFG-03)"
  artifacts:
    - path: "eas.json"
      provides: "Build + submit production profiles with ascAppId placeholder"
      contains: "REPLACE_WITH_ASC_APP_ID"
    - path: "app.json"
      provides: "iOS export-compliance flag"
      contains: "ITSAppUsesNonExemptEncryption"
  key_links:
    - from: "eas.json submit.production.ios"
      to: "App Store Connect app record (Phase 24)"
      via: "ascAppId placeholder replaced at first real submit"
      pattern: "ascAppId"
---

<objective>
Declare a reproducible submit profile in `eas.json` and confirm export-compliance
config in `app.json`, so Phase 24's first real build/submit cycle has nothing left
to configure. The only file edit is adding an `ascAppId` placeholder under
`submit.production.ios`; the two other requirement fields are already present from
Phase 20's `eas build:configure` bootstrap and must be confirmed unchanged.

Purpose: Front-load all EAS build/submit config so the first real submit in Phase 24
is a one-shot with no config surprises.
Output: `eas.json` with a populated `submit.production.ios.ascAppId` placeholder;
verified `app.json` export-compliance flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-eas-build-submit-configuration/23-CONTEXT.md
@.planning/phases/23-eas-build-submit-configuration/23-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ascAppId placeholder to eas.json submit profile (EASCFG-01, EASCFG-02)</name>
  <files>eas.json</files>
  <read_first>
    - eas.json (current state: submit.production is an empty object `{}`; cli.appVersionSource is "remote"; build.production.autoIncrement is true)
    - .planning/phases/23-eas-build-submit-configuration/23-CONTEXT.md (D-01 placeholder decision, D-02 leave credentials/distribution implicit)
    - .planning/phases/23-eas-build-submit-configuration/23-PATTERNS.md ("Target edit for this phase" and "Config-file edit discipline")
  </read_first>
  <action>
    In eas.json, replace the empty `submit.production` object `{}` with a nested
    object so that `submit.production.ios.ascAppId` equals the string
    `REPLACE_WITH_ASC_APP_ID` (all-caps TODO-style placeholder per D-01 — no real
    numeric ASC id exists yet; Phase 24 fills it once the App Store Connect record
    is created). This is the ONLY key that changes. Per D-02, do NOT add
    `credentialsSource`, `distribution: "store"`, or any other field to
    `build.production` or `submit.production.ios` — leave EAS-managed iOS credential
    defaults implicit, matching Phase 20's "trust eas build:configure output as-is"
    convention. Do NOT regenerate or reorder the file; hand-edit the one section only.
    Leave `cli.version` (">= 21.1.0"), `cli.appVersionSource` ("remote"),
    `build.development`, `build.preview`, and `build.production.autoIncrement` (true)
    byte-for-byte unchanged. eas.json is strict JSON with no comment support, so the
    caveat that the placeholder must be replaced before Phase 24 lives here in the
    plan and in the commit message, not inline in the file.
  </action>
  <verify>
    <automated>node -e "const c=require('./eas.json'); if(c.submit.production.ios.ascAppId!=='REPLACE_WITH_ASC_APP_ID'){console.error('ascAppId placeholder missing');process.exit(1)} if(c.cli.appVersionSource!=='remote'){console.error('appVersionSource drifted');process.exit(1)} if(c.build.production.autoIncrement!==true){console.error('autoIncrement drifted');process.exit(1)} console.log('eas.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `eas.json` parses as valid JSON (node require succeeds without throwing)
    - `submit.production.ios.ascAppId` === `"REPLACE_WITH_ASC_APP_ID"` (EASCFG-02)
    - `cli.appVersionSource` === `"remote"` (EASCFG-01, confirmed unchanged)
    - `build.production.autoIncrement` === `true` (EASCFG-01, confirmed unchanged)
    - `build.production` contains no `credentialsSource` or `distribution` key (D-02)
    - `submit.production.ios` contains exactly one key, `ascAppId` (no extra fields per D-02)
  </acceptance_criteria>
  <done>eas.json has submit.production.ios.ascAppId placeholder; EASCFG-01 fields confirmed present and unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm app.json export-compliance flag survives unchanged (EASCFG-03)</name>
  <files>app.json</files>
  <read_first>
    - app.json (current state: expo.ios.infoPlist.ITSAppUsesNonExemptEncryption is already false, written by Phase 20's eas build run)
    - .planning/phases/23-eas-build-submit-configuration/23-CONTEXT.md (EASCFG-03 is confirm-only, "no edit expected")
    - .planning/phases/23-eas-build-submit-configuration/23-PATTERNS.md ("app.json ... confirm-only" section)
  </read_first>
  <action>
    This is a read-only verification, NOT an edit. Confirm that app.json's
    `expo.ios.infoPlist.ITSAppUsesNonExemptEncryption` is still exactly the boolean
    `false` (present since Phase 20's `eas build` answered Apple's encryption-compliance
    question, `915a3ea`). Do NOT modify app.json — if the flag is already `false` (it is),
    make no change to the file. If — and only if — the flag were somehow missing or not
    `false`, set `expo.ios.infoPlist.ITSAppUsesNonExemptEncryption` to boolean `false`
    without touching any sibling field (`bundleIdentifier` must remain
    `com.avram.aruh.lafa`, `buildNumber` must remain `"1"`, `slug`/`scheme` must remain
    `lafa`, `version` must remain `1.0.0`). Record in the SUMMARY that EASCFG-03 was
    confirm-only and no edit was required.
  </action>
  <verify>
    <automated>node -e "const c=require('./app.json'); const v=c.expo.ios.infoPlist.ITSAppUsesNonExemptEncryption; if(v!==false){console.error('ITSAppUsesNonExemptEncryption not false:',v);process.exit(1)} if(c.expo.ios.bundleIdentifier!=='com.avram.aruh.lafa'){console.error('bundleIdentifier drifted');process.exit(1)} if(c.expo.version!=='1.0.0'){console.error('version drifted');process.exit(1)} console.log('app.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app.json` parses as valid JSON (node require succeeds)
    - `expo.ios.infoPlist.ITSAppUsesNonExemptEncryption` === boolean `false` (EASCFG-03)
    - `expo.ios.bundleIdentifier` === `"com.avram.aruh.lafa"` (identity unchanged)
    - `expo.version` === `"1.0.0"` and `expo.ios.buildNumber` === `"1"` (identity unchanged)
    - `git diff --stat app.json` shows no changes (confirm-only, no edit made)
  </acceptance_criteria>
  <done>app.json export-compliance flag confirmed present as false; no edit made; identity fields intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo config → EAS cloud build/submit | Declarative `eas.json`/`app.json` values are consumed by EAS CLI to sign and submit a binary to Apple. No untrusted runtime input; risk is misdeclaration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | eas.json submit.production.ios.ascAppId | mitigate | Placeholder is a clearly-marked all-caps sentinel (`REPLACE_WITH_ASC_APP_ID`); Phase 24 must replace before first real submit — cannot silently submit to a wrong/real numeric ASC id |
| T-23-02 | Information disclosure | app.json ITSAppUsesNonExemptEncryption | accept | Declaring `false` is the standard HTTPS-only export-compliance exemption; app uses only standard TLS to the Render backend — accurate declaration, no secrets in config |
| T-23-03 | Repudiation | config edit provenance | accept | Every field change is git-committed with the requirement ID (EASCFG-01/02/03) and a stated list of fields confirmed unchanged — full audit trail per project convention |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — no eas-cli or dependency changes; eas-cli was pinned in Phase 20 |
</threat_model>

<verification>
- `node -e "require('./eas.json')"` and `node -e "require('./app.json')"` both parse without error (valid JSON).
- `eas.json` `submit.production.ios.ascAppId` === `"REPLACE_WITH_ASC_APP_ID"`.
- `eas.json` `cli.appVersionSource` === `"remote"` and `build.production.autoIncrement` === `true`.
- `app.json` `expo.ios.infoPlist.ITSAppUsesNonExemptEncryption` === `false`.
- `git diff --stat app.json` shows no change (confirm-only).
</verification>

<success_criteria>
1. `eas.json` `submit.production.ios.ascAppId` holds the placeholder `REPLACE_WITH_ASC_APP_ID` (EASCFG-02).
2. `eas.json` `cli.appVersionSource: "remote"` and `build.production.autoIncrement: true` confirmed present and unchanged (EASCFG-01).
3. `app.json` `ios.infoPlist.ITSAppUsesNonExemptEncryption: false` confirmed present, no edit made (EASCFG-03).
4. No other fields in either file changed; both remain valid JSON.
</success_criteria>

<output>
Create `.planning/phases/23-eas-build-submit-configuration/23-01-SUMMARY.md` when done.
Note in the SUMMARY: (a) EASCFG-02 was the only real edit; (b) EASCFG-01 and EASCFG-03
were confirm-only (already satisfied by Phase 20's bootstrap); (c) the ascAppId placeholder
must be replaced with the real numeric ASC App ID in Phase 24 before the first real submit.
</output>
