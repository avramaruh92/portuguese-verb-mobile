---
phase: 13-verb-mode-selection
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - app/index.tsx
autonomous: false
requirements: [MODE-01]

must_haves:
  truths:
    - "Setup screen shows a 3-chip verb-mode row (Regular only / Mixed / Irregular only) reusing the existing chip styles, below the tense row with a 'Verb mode' section label, where the Switch used to be (D-01, D-03, D-04, D-05, D-06)"
    - "Exactly one verb-mode chip is highlighted at a time (single-select, radio-style) (D-02)"
    - "Default selection is 'Regular only' on first render, leftmost in Regular -> Mixed -> Irregular order (D-05)"
    - "Starting a quiz passes the selected verbMode into startQuiz (D-08, D-09)"
    - "The old 'Include irregular verbs' Switch is gone (D-01)"
  artifacts:
    - path: "app/index.tsx"
      provides: "Verb-mode single-select chip row + verbMode state passed to startQuiz"
      contains: "verbMode"
  key_links:
    - from: "app/index.tsx"
      to: "src/quiz/types.ts"
      via: "VerbMode type import"
      pattern: "VerbMode"
    - from: "app/index.tsx"
      to: "src/store/useQuizStore.ts"
      via: "startQuiz({ tenses, verbMode })"
      pattern: "startQuiz\\(\\{ tenses"
---

<objective>
Replace the "Include irregular verbs" `Switch` on the Setup screen with a single-select
3-chip "Verb mode" row (Regular only / Mixed / Irregular only), defaulting to Regular only,
and pass the selected `verbMode` into `startQuiz`.

Purpose: MODE-01 — the user-facing control that lets a learner choose the verb-difficulty
scope. This consumes the `VerbMode` contract defined in Plan 01.
Output: Updated app/index.tsx with the new selector, visually verified on-device.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-verb-mode-selection/13-CONTEXT.md
@.planning/phases/13-verb-mode-selection/13-PATTERNS.md
@.planning/phases/13-verb-mode-selection/13-01-SUMMARY.md

<interfaces>
From src/quiz/types.ts (created in Plan 01):
  export type VerbMode = "regular_only" | "mixed" | "irregular_only";
  export interface GenerateOptions { tenses: Tense[]; verbMode: VerbMode; }

Existing reusable styles in app/index.tsx (StyleSheet, lines 122-199):
  section, sectionLabel, chipRow, chip, chipSelected, chipText, chipTextSelected
  (the tense chip row at lines 63-89 is the structural analog to copy, adapted to single-select)

startQuiz call site (app/index.tsx line 42): currently
  await startQuiz({ tenses: selectedTenses, includeIrregular });
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the irregular Switch with a single-select verb-mode chip row</name>
  <files>app/index.tsx</files>
  <read_first>
    - app/index.tsx (full file — Switch block lines 91-94, tense chip row lines 63-89, styles lines 122-199, startQuiz call line 42, includeIrregular state line 20, imports lines 1-9)
    - src/quiz/types.ts (VerbMode type from Plan 01)
    - .planning/phases/13-verb-mode-selection/13-PATTERNS.md (exact VERB_MODE_OPTIONS shape, chip row markup, and D-01..D-06 rules)
  </read_first>
  <action>
    Add `import type { VerbMode } from "../src/quiz/types";` (keep the existing
    `import type { Tense } from "../src/dataset/types";` — VerbMode lives in the quiz domain per
    D-08). Remove `Switch` from the `react-native` import on line 2 (nothing else uses it).

    Replace the `includeIrregular` boolean state (line 20) with
    `const [verbMode, setVerbMode] = useState<VerbMode>("regular_only");` (D-05 default = Regular
    only). Define a `VERB_MODE_OPTIONS` array of `{ value: VerbMode; label: string }` in this exact
    left-to-right order and verbatim copy (D-05, D-06): `{ value: "regular_only", label: "Regular
    only" }`, `{ value: "mixed", label: "Mixed" }`, `{ value: "irregular_only", label: "Irregular
    only" }`.

    Replace the Switch block (lines 91-94) with a `View style={styles.section}` containing a
    `sectionLabel` reading exactly "Verb mode" (D-04) and a `chipRow` mapping VERB_MODE_OPTIONS to
    Pressable chips. Each chip uses `selected = verbMode === opt.value`, applies
    `[styles.chip, selected && styles.chipSelected]` and `[styles.chipText, selected &&
    styles.chipTextSelected]`, renders `opt.label`, and calls `setVerbMode(opt.value)` directly on
    press (single-select radio behavior — D-02; no toggle/array logic and no "All"-equivalent chip,
    unlike the tense row). Position this section where the Switch was, below the tense chip row
    (D-03). Reuse the existing chip/section styles verbatim — add no new StyleSheet entries. Remove
    the now-unused `toggleRow` and `toggleLabel` style entries.

    Update the startQuiz call (line 42) to `await startQuiz({ tenses: selectedTenses, verbMode });`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' app/index.tsx | grep -c includeIrregular` returns 0
    - `grep -v '^#' app/index.tsx | grep -c Switch` returns 0
    - app/index.tsx contains `useState<VerbMode>("regular_only")`
    - app/index.tsx contains a sectionLabel with text "Verb mode" and the three labels "Regular only", "Mixed", "Irregular only" in that source order
    - app/index.tsx contains `startQuiz({ tenses: selectedTenses, verbMode })`
    - `npm run typecheck` reports no errors; `npm run lint` reports no new errors
  </acceptance_criteria>
  <done>Switch removed, single-select 3-chip verb-mode row rendered below tenses defaulting to Regular only, verbMode passed to startQuiz, typecheck and lint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the verb-mode selector on-device</name>
  <action>Pause for on-device verification per the how-to-verify steps below; no code changes in this task.</action>
  <what-built>The "Include irregular verbs" Switch on the Setup screen was replaced with a single-select "Verb mode" chip row (Regular only / Mixed / Irregular only), defaulting to Regular only, feeding the quiz pool filter shipped in Plan 01.</what-built>
  <how-to-verify>
    1. Run `npm run ios` (or `npm start` and open in Expo Go).
    2. On the Setup screen, confirm a "Verb mode" label with three chips — "Regular only", "Mixed", "Irregular only" — appears below the tense chips, where the toggle used to be.
    3. Confirm "Regular only" is highlighted by default and no "Include irregular verbs" switch remains.
    4. Tap each chip: exactly one chip is highlighted at a time (tapping one deselects the others).
    5. Select some tenses + "Irregular only" and Start Quiz — confirm the quiz starts (or shows the insufficient-verbs message for a too-small tense selection) without crashing.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/behavioral issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → store | Verb-mode selection is constrained to the three fixed chips; the value is a closed TS union, never free-form. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Tampering | Setup screen verbMode state | accept | Value can only be one of three compile-checked literals set by fixed chips; no untrusted input, network, or storage surface added. |
| T-13-SC | Tampering | npm installs | accept | No packages added or upgraded; no install tasks. |
</threat_model>

<verification>
- `npm run typecheck` clean
- `npm run lint` reports no new errors
- Human checkpoint approved (visual + interaction verification on-device)
- No `includeIrregular`/`Switch` references remain in app/index.tsx
</verification>

<success_criteria>
- Setup screen shows a 3-option single-select verb-mode selector defaulting to Regular only, replacing the Switch (MODE-01)
- Selected verbMode flows into startQuiz and drives Plan 01's pool filter
- On-device verification confirms correct rendering and single-select behavior
</success_criteria>

<output>
Create `.planning/phases/13-verb-mode-selection/13-02-SUMMARY.md` when done
</output>
