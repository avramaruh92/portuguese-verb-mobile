---
phase: 05-feedback-integration
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified: []
autonomous: false
requirements: [FDBK-02, FDBK-03]

must_haves:
  truths:
    - "A real feedback payload submitted to the live backend returns a 201 with the expected persisted-row shape"
    - "The client's Tense/Subject/platform enum literals match the live backend (no 400 on a well-formed payload)"
    - "While a feedback submission is in flight, the Quiz screen underneath stays fully interactive and loses no progress"
  artifacts: []
  key_links:
    - from: "src/feedback/submit.ts"
      to: "live https://portuguese-verb-api.onrender.com/feedback"
      via: "real POST round-trip during verification"
      pattern: "onrender.com/feedback"
---

<objective>
Perform the one verification that unit tests structurally cannot: a live round-trip against the real deployed backend, plus an on-device confirmation that a submission never interrupts the quiz. This is where the cross-repo enum-literal assumption (flagged since Phase 2, CLAUDE.md) finally gets exercised against the real API for the first time, and where FDBK-03's non-interruption guarantee is observed rather than inferred.

Purpose: FDBK-02 (real 201 / graceful handling against the live endpoint) and FDBK-03 (on-device non-interruption). Per STATE.md's Phase 5 blocker and RESEARCH Open Question #1, at least one live round-trip MUST happen in this phase — the final full cold-start manual test is deferred to Phase 6, but the reachability + payload-shape + non-interruption checks belong here.

Output: a verified live 201 (or documented failure to escalate) and a confirmed non-interruption observation, recorded in the plan summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-feedback-integration/05-CONTEXT.md
@.planning/phases/05-feedback-integration/05-VALIDATION.md

<interfaces>
<!-- What was built in Plans 01-03 -->
- src/feedback/submit.ts → submitFeedback(payload) POSTs to https://portuguese-verb-api.onrender.com/feedback
- app/quiz.tsx → "Report a problem" trigger (gated on lockedChoice) opens ReportFeedbackModal
- Backend contract (CLAUDE.md): 201 returns persisted row with generated id + createdAt;
  400 → {error:"ValidationError", fields:{...}}; 500 → {error:"InternalServerError"}
- Enum literals under test (must match backend exactly):
  tense: present_indicative|preterite|imperfect|future
  subject: eu|tu|ele_ela|nos|voces|eles_elas ; platform: ios|android
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scripted live round-trip against the deployed backend</name>
  <files></files>
  <read_first>
    - CLAUDE.md §"Key Domain Facts" (locked POST /feedback contract, exact fields + response shapes)
    - src/feedback/schema.ts (the payload shape to send)
    - .planning/phases/05-feedback-integration/05-RESEARCH.md §"Open Questions" #1 and §"Environment Availability" (live reachability is unverified by research — must be checked at execution)
  </read_first>
  <action>
    Send one well-formed feedback payload to the live endpoint to confirm reachability, the 201 path, and the persisted-row shape — proving the client enum literals match the backend (no 400 on a valid payload). Use `curl` against `https://portuguese-verb-api.onrender.com/feedback` with a JSON body containing every required field and valid enum literals (e.g. `tense: "present_indicative"`, `subject: "eu"`, `platform: "ios"`, a clearly-marked test `message` like "GSD phase-5 verification — please ignore", plus placeholder `verb`/`correctAnswer`/`selectedAnswer`/`appVersion`). Allow up to ~90s for a cold start (`--max-time 120`). Record the HTTP status and response body.

    Expected: `201` with a JSON body including a generated `id` and `createdAt`. If instead a `400` returns, the enum literals or field names do NOT match the backend — capture the `fields` object and escalate as a blocking cross-repo contract mismatch (do NOT silently proceed; this is exactly the risk CLAUDE.md flags). If a `500` or timeout returns, note whether it was a cold start and retry once. Do not add any code — this is a verification action only.
  </action>
  <verify>
    <automated>curl -sS -o /tmp/gsd-feedback-resp.json -w "%{http_code}" --max-time 120 -X POST https://portuguese-verb-api.onrender.com/feedback -H "Content-Type: application/json" -d '{"message":"GSD phase-5 verification — please ignore","verb":"falar","tense":"present_indicative","subject":"eu","correctAnswer":"falo","selectedAnswer":"falas","appVersion":"0.0.0","platform":"ios"}'</automated>
  </verify>
  <acceptance_criteria>
    - The curl command returns HTTP `201`
    - `/tmp/gsd-feedback-resp.json` contains an `id` and a `createdAt` field (persisted-row shape confirmed)
    - No `400 ValidationError` — confirming client enum literals match the live backend
    - If a non-201 occurs, it is documented in the summary with the response body and escalated (cross-repo mismatch = blocker)
  </acceptance_criteria>
  <done>A live 201 with a persisted-row body confirms reachability, payload-shape correctness, and enum-literal parity — or a non-201 is captured and escalated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full report-a-problem flow: a "Report a problem" trigger on the Quiz screen (enabled once you've answered a question) that opens a modal with a reason picker, optional details field, and Submit — handling success, error, and slow/cold-start responses without interrupting the quiz.</what-built>
  <how-to-verify>
    1. Run `npx expo start --ios` and start a quiz from the Setup screen.
    2. Answer the current question (tap a choice so it locks). Confirm the "Report a problem" button now appears beneath the choices (it should be hidden/unusable before you answer — D-02).
    3. Tap "Report a problem". Pick a reason, optionally type details, tap "Submit feedback". Confirm an inline spinner shows inside the modal.
    4. On success: confirm "✓ Feedback sent — thank you!" appears and the modal auto-dismisses after ~1.5s, returning you to the still-in-progress quiz at the same question.
    5. Non-interruption (FDBK-03): while a submission is in flight (use a cold backend, or throttle the network), confirm the Quiz screen underneath is untouched — your locked answer, progress (e.g. "3 / 10"), and current question are all preserved, and you can still interact with the quiz. Dismiss the modal mid-request and confirm the quiz continues normally.
    6. Error path: if you can trigger a failure (airplane mode → network error), confirm the generic "Something went wrong. Please try again." message and a "Retry submission" button appear, with your reason + text preserved.
  </how-to-verify>
  <resume-signal>Type "approved" if the flow works and the quiz is never interrupted, or describe what went wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile client → live external API | The real HTTPS `POST /feedback` round-trip, exercised for the first time against the deployed Render backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | Live 500 response | mitigate | Verified on-device that a failure shows only generic copy, never response internals (D-07). Backend also returns only `{error:"InternalServerError"}`. |
| T-05-05 | Denial of Service | Cold-start non-interruption | mitigate | On-device check confirms the quiz stays interactive and loses no progress during an in-flight/cold-start request (FDBK-03). Final full cold-start test deferred to Phase 6 per STATE.md. |
| T-05-06 | Spoofing/Repudiation | Cross-repo enum-literal mismatch causing silent legitimate-feedback 400s | mitigate | Live round-trip proves a well-formed payload returns 201, not 400 — the definitive verification of the Phase-2 enum assumption. A 400 here is escalated as a blocker, not absorbed. |
| T-05-SC | Tampering | npm installs | accept | No packages installed — verification-only plan. |
</threat_model>

<verification>
- Live `POST /feedback` returns 201 with a persisted-row body (id + createdAt)
- Human-verify checkpoint approved: report flow works and the quiz is never interrupted, error/success/Retry states behave per UI-SPEC
- Any non-201 or interruption is documented and escalated, not silently accepted
</verification>

<success_criteria>
- FDBK-02: graceful, correct handling confirmed against the real deployed backend (201 path + enum parity)
- FDBK-03: on-device confirmation that a submission (including a slow/cold-start one) never blocks or corrupts quiz completion
</success_criteria>

<output>
Create `.planning/phases/05-feedback-integration/05-04-SUMMARY.md` when done, recording the live response status/body and the checkpoint outcome
</output>
