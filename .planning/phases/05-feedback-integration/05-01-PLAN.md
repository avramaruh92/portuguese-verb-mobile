---
phase: 05-feedback-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/feedback/types.ts
  - src/feedback/schema.ts
  - src/feedback/reasons.ts
  - src/feedback/payload.ts
  - __tests__/feedback-schema.test.ts
  - __tests__/feedback-payload.test.ts
autonomous: true
requirements: [FDBK-01, FDBK-04]

must_haves:
  truths:
    - "The feedback payload Zod schema accepts every valid tense/subject/platform literal and rejects invalid ones"
    - "buildFeedbackPayload maps quiz-question context + form input to a correct FeedbackPayload for every reason/free-text combination"
    - "The FeedbackPayload type is the single source of truth (z.infer), not a hand-duplicated interface"
  artifacts:
    - path: "src/feedback/schema.ts"
      provides: "feedbackPayloadSchema Zod schema mirroring the locked backend contract"
      exports: ["feedbackPayloadSchema"]
      contains: "z.enum"
    - path: "src/feedback/types.ts"
      provides: "FeedbackReason union, FeedbackPayload (z.infer), SubmitResult discriminated union"
      exports: ["FeedbackReason", "FeedbackPayload", "SubmitResult"]
    - path: "src/feedback/reasons.ts"
      provides: "FEEDBACK_REASONS ordered list + reasonLabels lookup"
      exports: ["FEEDBACK_REASONS", "reasonLabels"]
    - path: "src/feedback/payload.ts"
      provides: "buildFeedbackPayload pure mapping function"
      exports: ["buildFeedbackPayload"]
  key_links:
    - from: "src/feedback/schema.ts"
      to: "src/dataset/types.ts"
      via: "TENSES / SUBJECTS enum literal reuse"
      pattern: "from \"\\.\\./dataset/types\""
    - from: "src/feedback/types.ts"
      to: "src/feedback/schema.ts"
      via: "z.infer<typeof feedbackPayloadSchema>"
      pattern: "z\\.infer"
---

<objective>
Establish the feedback module's contract layer and the pure payload-mapping logic: the Zod schema mirroring the backend's locked `POST /feedback` contract, the domain types (`FeedbackReason`, `FeedbackPayload`, `SubmitResult`), the preset-reason lookup tables, and the pure `buildFeedbackPayload()` function that maps quiz-question state + form input into a backend-shaped payload.

Purpose: This is the foundation the network layer (Plan 02) and the report modal (Plan 03) both consume. Getting the enum literals and message composition provably correct here removes the cross-repo `400`-mismatch risk before any UI or network code touches it (FDBK-04). All code is pure, RN-free, and unit-testable with plain Jest.

Output: `src/feedback/{types,schema,reasons,payload}.ts` plus green `__tests__/feedback-schema.test.ts` and `__tests__/feedback-payload.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-feedback-integration/05-CONTEXT.md
@.planning/phases/05-feedback-integration/05-RESEARCH.md
@.planning/phases/05-feedback-integration/05-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor MUST build against. Extracted from codebase — no exploration needed. -->

From src/dataset/types.ts (SOURCE OF TRUTH for enum literals — reuse, do not retype):
```typescript
export type Tense = "present_indicative" | "preterite" | "imperfect" | "future";
export type Subject = "eu" | "tu" | "ele_ela" | "nos" | "voces" | "eles_elas";
export const TENSES: readonly Tense[] = ["present_indicative", "preterite", "imperfect", "future"];
export const SUBJECTS: readonly Subject[] = ["eu", "tu", "ele_ela", "nos", "voces", "eles_elas"];
```

Backend `POST /feedback` locked contract (CLAUDE.md §Key Domain Facts) — fields and enums:
message, verb, tense, subject, correctAnswer, selectedAnswer, appVersion, platform
- tense enum: present_indicative | preterite | imperfect | future
- subject enum: eu | tu | ele_ela | nos | voces | eles_elas
- platform enum: ios | android

Analog for the schema style — src/dataset/validate.ts uses `z.object({...})` with `.min(1)` on every required string, `export const <Thing>Schema`.
Analog for lookup tables — src/quiz/labels.ts uses `Record<Key, string>` objects.
Analog for pure builder — src/quiz/share.ts is a one-function pure string builder, no RN imports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Feedback contract — schema, types, reasons</name>
  <files>src/feedback/schema.ts, src/feedback/types.ts, src/feedback/reasons.ts, __tests__/feedback-schema.test.ts</files>
  <read_first>
    - src/dataset/types.ts (source of truth for TENSES/SUBJECTS enum literals — import and reuse, do not retype)
    - src/dataset/validate.ts (Zod schema style: `export const VerbSchema = z.object({...})`, `.min(1)` on every string)
    - src/quiz/types.ts (project convention for flat interface/type exports and typed Error subclass style)
    - src/quiz/labels.ts (Record<Key,string> lookup-table convention to mirror for reasonLabels)
    - __tests__/dataset.test.ts (test style: nested forEach over TENSES/SUBJECTS, `.safeParse().success` assertions, negative-case mutation test)
    - .planning/phases/05-feedback-integration/05-UI-SPEC.md §Copywriting Contract (exact reason label strings)
  </read_first>
  <behavior>
    - feedbackPayloadSchema.safeParse(validPayload).success === true for all 4×6×2 = 48 valid tense/subject/platform combinations
    - feedbackPayloadSchema rejects an invalid tense literal (e.g. "present"), an invalid subject literal, and an invalid platform literal (.success === false)
    - feedbackPayloadSchema rejects an empty-string message/verb/correctAnswer/selectedAnswer/appVersion (.min(1) enforced)
    - FEEDBACK_REASONS order is exactly: wrong_answer, typo, confusing, other
    - reasonLabels maps each reason to exactly: "Wrong answer", "Typo or spelling", "Confusing wording", "Other"
  </behavior>
  <action>
    Create `src/feedback/schema.ts`: export `feedbackPayloadSchema = z.object({...})` mirroring the backend contract. Fields: `message`, `verb`, `correctAnswer`, `selectedAnswer`, `appVersion` all `z.string().min(1)`; `tense: z.enum([...TENSES])`, `subject: z.enum([...SUBJECTS])` (spread the imported readonly arrays from `../dataset/types` so literals never drift), `platform: z.enum(["ios", "android"])`. Import `z` from "zod". Do NOT re-declare the tense/subject literal arrays inline.

    Create `src/feedback/types.ts`: `export type FeedbackReason = "wrong_answer" | "typo" | "confusing" | "other";`. Export `FeedbackPayload` as `z.infer<typeof feedbackPayloadSchema>` re-exported from schema (import the schema, `export type FeedbackPayload = z.infer<...>`) — do NOT hand-duplicate the interface. Export the `SubmitResult` discriminated union: `{ status: "success"; data: unknown } | { status: "validation-error" } | { status: "server-error" } | { status: "network-error" }` (per RESEARCH Pattern 1 — note the `validation-error` variant carries NO fields payload per D-06).

    Create `src/feedback/reasons.ts`: import `FeedbackReason` from "./types". Export `reasonLabels: Record<FeedbackReason, string>` mapping to the exact UI-SPEC strings. Export `FEEDBACK_REASONS: { value: FeedbackReason; label: string }[]` as an ordered array in the D-03 order (wrong_answer → typo → confusing → other), derived from reasonLabels.

    Use relative imports only (no path aliases — none exist in tsconfig). Write `__tests__/feedback-schema.test.ts` FIRST (RED): nested `TENSES.forEach`/`SUBJECTS.forEach` × `["ios","android"].forEach` asserting `.safeParse().success === true`, plus negative cases mutating each enum to an invalid literal and an empty-string field asserting `.success === false`, plus an assertion that FEEDBACK_REASONS maps to the exact label strings. Then implement until green.
  </action>
  <verify>
    <automated>npm test -- __tests__/feedback-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- __tests__/feedback-schema.test.ts` exits 0
    - `grep -n "z.enum" src/feedback/schema.ts` shows tense/subject/platform enums
    - `grep -c "z.infer" src/feedback/types.ts` returns >= 1 (FeedbackPayload derived, not duplicated)
    - `grep -n "present_indicative" src/feedback/schema.ts` returns nothing (literals reused via TENSES spread, not retyped)
    - reasonLabels values are exactly "Wrong answer", "Typo or spelling", "Confusing wording", "Other"
    - `npx tsc --noEmit` reports zero errors
  </acceptance_criteria>
  <done>Schema/types/reasons compile under strict TS, feedback-schema test green covering all 48 valid combos + negative cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildFeedbackPayload pure mapping function</name>
  <files>src/feedback/payload.ts, __tests__/feedback-payload.test.ts</files>
  <read_first>
    - src/feedback/schema.ts (created in Task 1 — FeedbackPayload shape to return)
    - src/feedback/types.ts (created in Task 1 — FeedbackReason, FeedbackPayload)
    - src/feedback/reasons.ts (created in Task 1 — reasonLabels for message prefix)
    - src/quiz/share.ts (pure-builder style: one exported function, no side effects, no RN imports)
    - __tests__/quiz-share.test.ts (test style: exact input→string-output assertions, no mocking)
    - .planning/phases/05-feedback-integration/05-CONTEXT.md D-03 (message composition: "<label>: <free text>")
  </read_first>
  <behavior>
    - buildFeedbackPayload with a non-empty freeText returns message === "<reasonLabel>: <trimmed freeText>" (e.g. "Wrong answer: verb is misspelled")
    - buildFeedbackPayload with empty/whitespace-only freeText returns message === "<reasonLabel>" (no trailing colon)
    - All non-message fields (verb, tense, subject, correctAnswer, selectedAnswer, appVersion, platform) pass through unchanged
    - The returned object passes feedbackPayloadSchema.safeParse (round-trip valid)
  </behavior>
  <action>
    Create `src/feedback/payload.ts` exporting `buildFeedbackPayload(params: { verb: string; tense: Tense; subject: Subject; correctAnswer: string; selectedAnswer: string; reason: FeedbackReason; freeText: string; appVersion: string; platform: "ios" | "android" }): FeedbackPayload`. Compose `message` as `reasonLabels[reason]` when `freeText.trim()` is empty, else `` `${reasonLabels[reason]}: ${freeText.trim()}` ``. Return the full payload object with every other field passed through verbatim. Import `Tense`/`Subject` from "../dataset/types", `FeedbackReason`/`FeedbackPayload` from "./types", `reasonLabels` from "./reasons". No RN imports, no fenced network code — pure function only.

    Write `__tests__/feedback-payload.test.ts` FIRST (RED): assert exact message strings for each of the 4 reasons with free text and without, assert pass-through of all context fields, and assert the built payload satisfies `feedbackPayloadSchema.safeParse(...).success === true`. Then implement until green.
  </action>
  <verify>
    <automated>npm test -- __tests__/feedback-payload.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- __tests__/feedback-payload.test.ts` exits 0
    - Test asserts both message forms (with-freetext colon form and label-only form)
    - Test asserts the built payload round-trips through feedbackPayloadSchema.safeParse
    - `grep -c "react-native" src/feedback/payload.ts` returns 0 (pure, no RN imports)
    - `npx tsc --noEmit` reports zero errors
  </acceptance_criteria>
  <done>buildFeedbackPayload produces schema-valid payloads with correct message composition for all reasons; feedback-payload test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quiz UI state → outbound feedback payload | Quiz-question values + user free text are marshalled into a payload destined for an external API. This plan defines the shape/validation; no network crossing happens here yet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Free-text `message` field | accept | Client passes user text through unmodified (D-06 rationale). No client-side sanitization — backend re-validates and owns injection defense (out of this repo per RESEARCH Security Domain). Attempting to escape/strip here would corrupt legitimate user text. |
| T-05-V5 | Tampering | Outbound payload shape (`feedbackPayloadSchema`) | mitigate | Zod schema validates every field client-side (enum literals + `.min(1)` non-empty) as defense-in-depth before any network call; catches app-side mapping bugs that would otherwise cause a legitimate-looking `400`. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced this phase — `zod`/`expo-constants`/RN core all pre-installed and vetted (RESEARCH Package Legitimacy Audit = no-op). No install task exists. |
</threat_model>

<verification>
- `npm test -- __tests__/feedback-schema.test.ts __tests__/feedback-payload.test.ts` fully green
- `npx tsc --noEmit` zero errors
- `npm test` full suite green (no regression in existing quiz/dataset tests)
</verification>

<success_criteria>
- FDBK-04: schema round-trips + payload mapping proven correct for every tense/subject/platform literal via automated tests
- FDBK-01 (logic half): buildFeedbackPayload produces a backend-shaped, schema-valid payload
- Enum literals reused from src/dataset/types.ts (zero drift risk), FeedbackPayload derived via z.infer
</success_criteria>

<output>
Create `.planning/phases/05-feedback-integration/05-01-SUMMARY.md` when done
</output>
