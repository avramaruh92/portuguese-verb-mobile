---
phase: 22-icon-splash-asset-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
  - assets/expo.icon
autonomous: true
requirements: [ICON-02]
user_setup: []

must_haves:
  truths:
    - "app.json has no `icon` key inside the `ios` block"
    - "The `assets/expo.icon/` Icon Composer bundle no longer exists"
    - "The top-level `expo.icon` PNG (./assets/images/icon.png) is the sole iOS app-icon source"
  artifacts:
    - path: "app.json"
      provides: "iOS icon config with no ios.icon override"
  key_links:
    - from: "app.json expo.icon"
      to: "assets/images/icon.png"
      via: "flat PNG path (unchanged, now the only icon source)"
      pattern: "assets/images/icon\\.png"
---

<objective>
Un-adopt the Icon Composer bundle: delete `assets/expo.icon/` and remove the `ios.icon` key
from `app.json` so the flat `expo.icon` PNG becomes the sole iOS app-icon source (ICON-02, D-07).

Purpose: This project deliberately reverts from the SDK 54+ Icon Composer `.icon` bundle back to
a single flat PNG (the operator's chosen path, already logged in PROJECT.md's Key Decisions) —
regenerating a proper multi-appearance bundle needs macOS-only Icon Composer tooling not used here.

Output: `app.json` with the `ios.icon` line removed; `assets/expo.icon/` deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-icon-splash-asset-pipeline/22-CONTEXT.md
@.planning/phases/22-icon-splash-asset-pipeline/22-RESEARCH.md
@app.json

<interfaces>
<!-- Current app.json ios block (read directly): -->
<!--   "ios": { "icon": "./assets/expo.icon", "bundleIdentifier": "com.avram.aruh.lafa", -->
<!--            "buildNumber": "1", "infoPlist": { "ITSAppUsesNonExemptEncryption": false } } -->
<!-- Remove ONLY the "icon": "./assets/expo.icon" line. Leave bundleIdentifier, buildNumber, -->
<!-- and infoPlist untouched (locked by Phase 21). -->
<!-- Top-level "icon": "./assets/images/icon.png" stays untouched — it becomes the sole source. -->
<!-- The expo-splash-screen plugin config (#208AEF / imageWidth 76) is NOT in scope here (D-05). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ios.icon key and delete the Icon Composer bundle</name>
  <files>app.json, assets/expo.icon</files>
  <read_first>
    - app.json (current `ios` block — remove only the `icon` line)
    - .planning/phases/22-icon-splash-asset-pipeline/22-CONTEXT.md (D-07 — delete bundle, remove key, do not regenerate)
    - .planning/phases/22-icon-splash-asset-pipeline/22-RESEARCH.md (Common Pitfalls 3 — stale local ios/ dir is gitignored and regenerated by EAS; no action required for correctness)
  </read_first>
  <action>
    In `app.json`, delete the line `"icon": "./assets/expo.icon",` from inside the `expo.ios` object.
    Leave every other key in the `ios` block exactly as-is: `bundleIdentifier` = `com.avram.aruh.lafa`,
    `buildNumber` = `1`, and the `infoPlist` object with `ITSAppUsesNonExemptEncryption: false`
    (all locked by Phase 21 — do NOT touch). Leave the top-level `"icon": "./assets/images/icon.png"`
    untouched — it becomes the sole iOS icon source. Do NOT touch the `expo-splash-screen` plugin
    config (D-05).

    Delete the entire `assets/expo.icon/` directory (contains `icon.json` and an `Assets/` subfolder).
    Do NOT attempt to regenerate or repair the Icon Composer bundle (explicitly out of scope, D-07).

    Note (no action required): a stale local `ios/PortugueseVerbQuiz/expo.icon` folder may exist on
    this machine from a prior prebuild — it is gitignored and regenerated fresh from app.json by EAS
    cloud builds, so it does not affect correctness (RESEARCH.md Pitfall 3).
  </action>
  <verify>
    <automated>test ! -d assets/expo.icon && node -e "const ios=require('./app.json').expo.ios||{}; if('icon' in ios) throw new Error('ios.icon still present'); if(require('./app.json').expo.icon!=='./assets/images/icon.png') throw new Error('top-level icon changed'); if(ios.bundleIdentifier!=='com.avram.aruh.lafa') throw new Error('bundleId changed'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `assets/expo.icon/` directory does not exist (`test ! -d assets/expo.icon`)
    - `app.json` `expo.ios` object has no `icon` key
    - `app.json` `expo.icon` still equals `./assets/images/icon.png`
    - `app.json` `expo.ios.bundleIdentifier` still equals `com.avram.aruh.lafa`, `buildNumber` still `1`, `infoPlist.ITSAppUsesNonExemptEncryption` still `false`
    - `app.json` is still valid JSON (`node -e "require('./app.json')"` exits 0)
    - The `expo-splash-screen` plugin config (backgroundColor #208AEF, imageWidth 76) is unchanged
  </acceptance_criteria>
  <done>ios.icon key removed, Icon Composer bundle deleted, all other identity/splash config preserved, app.json valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only deletes a local asset bundle and edits a declarative config key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering | `app.json` identity keys (bundleIdentifier/buildNumber) | accept | Edit is scoped to removing one `ios.icon` line; acceptance criteria assert the Phase-21-locked identity keys are unchanged after the edit. |

No auth/session/access-control/input-validation/crypto surface (V2-V6) is touched — config-only change.
</threat_model>

<verification>
- `test ! -d assets/expo.icon` -> success
- `node -e "require('./app.json')"` -> exits 0 (valid JSON)
- `jq '.expo.ios.icon' app.json` -> `null` (or use the node check in the task)
- `jq '.expo.icon' app.json` -> `"./assets/images/icon.png"`
</verification>

<success_criteria>
- ICON-02: `assets/expo.icon/` and the `app.json` `ios.icon` key both no longer exist; the flat `expo.icon` PNG governs the iOS app icon
</success_criteria>

<output>
Create `.planning/phases/22-icon-splash-asset-pipeline/22-02-SUMMARY.md` when done
</output>
